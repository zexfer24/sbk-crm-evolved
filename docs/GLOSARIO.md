# Glosario de archivos

Mapa módulo por módulo del proyecto. Su propósito es que orquestador y
subagentes ubiquen el código relevante sin releer el proyecto entero.

**Regla de mantenimiento:** todo cambio que cree, mueva o cambie el propósito
de un archivo actualiza su línea aquí, en el mismo commit.

Convención de tests: cada módulo lleva su `*.test.ts(x)` al lado, con el mismo
nombre. Los tests sin módulo propio (`data-*.test.ts`, `queue-limit`,
`queue-spacing`, `debounce`, `turn-correlation`, `dashboard-tickets`,
`conversation-quotes`, `ventana-24h-contrato`) prueban facetas del módulo
vecino que nombran.

---

## Raíz

| Archivo | Qué es |
|---|---|
| `next.config.ts` | Cabeceras de seguridad (antes en Caddyfile), `output: standalone`, `staleTimes` de navegación, orígenes de dev para túneles |
| `src/proxy.ts` | Middleware de Next: refresco de sesión + quién puede ver cada página (delega en `lib/supabase/middleware`) |
| `Dockerfile` / `docker-compose.yml` | Imagen standalone sin privilegios con HEALTHCHECK; stack app + caddy + cron (cada minuto desde el 7/9/2026, antes cada 5 min — ver "Rampa de los topes" en `docs/PRODUCCION.md`) |
| `docker-compose.dokploy.yml` | El mismo stack menos Caddy (Dokploy trae Traefik); mismo cambio de ritmo del cron que `docker-compose.yml` |
| `.env.local.example` / `.env.production.example` | Plantillas versionadas (las reales `.env.local`/`.env.production` no lo son). 7/9/2026 ("La respuesta llega en siete segundos"): el bloque de las cinco variables de ritmo de la IA (`AGENT_MAX_CONCURRENT_TURNS`, `AGENT_MAX_TURNS_PER_MINUTE`, `AGENT_QUEUE_MAX_PER_RUN`, `AI_MAX_CONCURRENT_REQUESTS`, `AI_MAX_REQUESTS_PER_MINUTE`) reescrito con los defaults destopados y la historia de por qué van siempre juntas (ver `queue.ts`/`rate-limit.ts`/"Rampa de los topes" en `docs/PRODUCCION.md`) |
| `Caddyfile` | TLS automático y cabeceras cuando el proxy es Caddy |
| `moto_catalog_schema.sql` / `moto_catalog_data.sql` | Fuente del catálogo de motos/repuestos (5.438 productos); de aquí salen la migración y el seed del catálogo |
| `vitest.config.ts` / `vitest.setup.ts` / `vitest.server-only-stub.ts` | Vitest con entorno `node` por defecto (los tests de UI declaran jsdom por docblock); `testTimeout` 15s contra la contención de workers; stub que anula `server-only` en pruebas; `vitest.setup.ts` sube el `asyncUtilTimeout` de los `waitFor`/`findBy` de Testing Library a 5 s (solo jsdom) para que la espera escale con la carga en vez del 1 s de fábrica; el plugin de React (`@vitejs/plugin-react`) solo transforma `.tsx`/`.jsx` (29/8/2026: su include de fábrica es `/\.[tj]sx?$/` y pasaba Babel sobre todo `.ts` del grafo, incluidos los tests de entorno node y src/lib, que no pueden tener JSX) |
| `liminalwork.md` | Metodología de trabajo (fuente de la skill `liminalwork`) |
| `AGENTS.md` | Aviso de `next dev`: esta versión de Next 16 difiere de los datos de entrenamiento; leer `node_modules/next/dist/docs/` |
| `CREDENCIALES.txt`, `.env.local*` | Solo en disco, ignorados por git; lo versionado son los `.example` |

## `src/app` — páginas (App Router)

| Ruta | Qué es |
|---|---|
| `page.tsx` | Tablero (Dashboard) en la raíz: monta `DashboardView` con agentes, conversaciones del tablero y actividad del día. B3 (5/9/2026): suma `fetchBusinessHours` (lectura liviana, nunca lanza) al mismo `Promise.all` y le pasa `businessHours` a `DashboardView` para que "Con asesor" mida en horario laboral (la línea anterior de este glosario decía "redirección de entrada", que es lo que hace `proxy.ts`, no esta página) |
| `layout.tsx`, `globals.css`, `theme.css` | Marco global y tokens de tema claro/oscuro |
| `inbox/` (+`[id]/`) | Bandeja compartida: la página arma los datos iniciales —desde el 30/8/2026 siembra "Pendientes" (`{activeOnly:true, awaitingReplyOnly:true, limit: INBOX_PAGE_SIZE}`), no "No leídas"— y monta `crm-shell` |
| `clientes/` | Directorio de clientes (ficha, notas, etiquetas, historial comercial) |
| `inventario/` | Inventario: lo que se ve aquí es lo que la IA cotiza (misma tabla `products`) |
| `ventas/` | Ventas cerradas: verificación y reversión (supervisor) |
| `agent-control/` | Panel de control de la IA |
| `login/` | Acceso |

## `src/app/api` — rutas de servidor

| Ruta | Qué es |
|---|---|
| `webhooks/whatsapp/route.ts` | Entrada de Meta: handshake, verificación de firma HMAC, mensajes entrantes (texto/multimedia/reply), estados de envío; escribe con service role y encola turnos. La bienvenida automática se reclama en la base (`claimWelcome` sella `conversations.welcome_sent_at` con un UPDATE condicional antes de enviar; se devuelve a `null` solo si Meta rechaza la plantilla), no en memoria de la invocación; el insert de la bienvenida lleva `is_auto_reply: true` (T0.1/T0.2, 5/9/2026) para que el trigger no la cuente como respuesta real. El SELECT de la conversación existente trae también `status`/`ai_enabled`/`assigned_agent_id` (T2.1, 5/9/2026; el tercero sumado en el anexo A2, 5/9/2026): si venía `closed`, la reabre (`status = 'open'`) ANTES de guardar el mensaje entrante, deja el evento "El cliente volvió a escribir" y decide el destino del `recordHandoff({reason: "reabierta_por_cliente", ...})` en tres pasos — `ai` si la IA seguía encendida; si no, `human` + su `assigned_agent_id` si el chat ya tenía asesor; solo sin ninguna de las dos, `unassigned` — sin esto el mensaje entraba igual pero la fila seguía invisible para "Pendientes" y demás píldoras que descuentan `status = 'closed'`, y antes de A2 un chat con dueño y la IA apagada (el estado normal tras una escalación o un cierre) quedaba en la bitácora como si no lo tuviera. `new-contact-race.test.ts` cubre la carrera de contacto nuevo y `welcome-race.test.ts` la carrera de la bienvenida (y ahí se afirma el `is_auto_reply: true` de la fila insertada) — los tres archivos comparten el espejo de mocks (incluido `@/lib/redis`) y la importación del route en `beforeAll`; `route.test.ts` suma el escenario de reapertura con su propio `conversationRow`/`conversationUpdates`/`handoffCalls` mutables. T3.4 (5/9/2026) atiende dentro del mismo `for` los otros tres `field` de la WABA además de `messages` -- `message_template_status_update`, `phone_number_quality_update` y `account_update` --, ninguno con `metadata.phone_number_id`: el canal se resuelve por coincidencia de dígitos de `display_phone_number`/`phone_number` contra `whatsapp_channels.phone_number` (`resolveHealthChannel`) o, si no hay coincidencia, por el único canal `connected`; guarda `quality_rating`/`messaging_limit`/`account_restrictions`/`health_updated_at` en el canal y `templates.status` por nombre e idioma (CHECK ampliado a `paused`/`disabled` en la migración 20260905060000), con `log.error` cuando la calidad baja, llega una restricción de cuenta, o una plantilla queda `paused`/`disabled`/`rejected`. T3.2 (5/9/2026, migración 20260905050000): dentro de la rama `field === "messages"`, `interactive.button_reply`/`list_reply` y `type: "button"` (botón de plantilla) guardan `message_type: "interactive"` con `content: "Respondió: {title}"`, `customerText` el título y `payload` con `{type, id}` o `{type: "button", template}`; `order` guarda `message_type: "order"` con el resumen en español de `describirPedido()` (ítems y total por moneda), `customerText = order.text` si vino, y `payload.productItems` crudo; el `else` final (antes una frase fija) pasa a `content: null`, `message_type: "unsupported"`, `payload: {type: message.type}` (F10); `context.referred_product` se funde en el `payload` del mensaje sea cual sea su tipo. `message.referral` (el anuncio "Click to WhatsApp" que originó el hilo) actualiza `conversations.referral` UNA sola vez por conversación (`existingConversation?.referral` corta la reentrega de Meta) y deja el evento de sistema `Llegó desde el anuncio "…"`. `value.errors` deja `log.error("webhook_error_meta")`; el `error` del `update` de `whatsapp_status` de los estados, antes ignorado, ahora deja `log.error("webhook_error_actualizar_estado")`. `whatsapp_status` admite `"played"` (nota de voz reproducida) igual que cualquier otro estado. S1 del plan "El cliente que cambió de número" (6/9/2026): `type: "system"` entra tras validar el teléfono viejo (`phoneNumberFromWaId(message.from)`) y antes del upsert de contacto — `handleSystemMessage()`. Con `system.type === "user_changed_number"` y un `wa_id`/`new_wa_id` válido: si el número nuevo no tiene contacto, mueve `contacts.phone_number` (D1 — conversación, historial, pines, pedido y bitácora cuelgan de `contact_id`, así que siguen solos); si ya tiene contacto (o el `UPDATE` choca con `23505`, otro webhook ganó la carrera), no fusiona nada (D2) y deja `log.error("webhook_cambio_numero_conflicto")`. Cualquier otro `system.type` (`customer_identity_changed`, uno nuevo, o un número que no resultó válido) deja un evento genérico con `payload.systemType`. Sin contacto o sin conversación para el número viejo, no crea nada (`log.info("webhook_system_sin_conversacion")`). Nunca reabre una conversación `closed` ni registra traspaso: eso sigue siendo exclusivo del mensaje normal del cliente. El `system_event` que deja es inerte para `handle_new_message()` (20260905010000): no mueve preview, `last_reply_at` ni `awaiting_reply`. Desde el 6/9/2026 (D3, misma corrida, S3a), un `type: "unsupported"` que llega SOLO —sin ninguna foto/video/audio/documento/sticker del mismo remitente en el lote— ya no se descarta: se guarda como `message_type: "unsupported"` con `content` null y el tipo real de Meta más el código de error en `payload` (`{type, code}`), visible en el chat y en la lista (el trigger mueve `last_message_at`/preview) pero —desde la migración 20260907010000, corrida "La ventana de 24 h dice la verdad", 7/9/2026— SIN mover `last_customer_message_at`/`unread_count`/`awaiting_reply`: Meta no lo cuenta para su ventana de 24 h y el CRM tampoco (antes sí lo hacía, y ese fue el bug de `aa75ef33…` del 6/9/2026: caja habilitada con la ventana cerrada del lado de Meta); tampoco encola turno de IA (`esUnsupportedSolo` lo deja fuera de `touchedByCustomer`; un texto de la misma conversación en el mismo lote sí la encola) y con `log.info("webhook_unsupported_guardado")`. El que llega junto a una galería se sigue descartando igual que antes. Deuda: la vista previa de la bandeja queda en "Unsupported" por el `initcap` del trigger, igual que cualquier multimedia sin texto |
| `messages/send/route.ts` | Envío del composer: canal `connected` → Cloud API real (con reintentos solo ante 5xx/red); si no, simulado. El token nunca toca el navegador. Plantillas con variables (T3.3, 5/9/2026): `kind: "template"` recibe `variables: string[]` (además de `content` con el cuerpo CRUDO, `{{n}}` sin sustituir); antes de guardar sustituye con `substituteTemplateVariables` (`whatsapp/template-variables.ts`) para que la fila guarde el mensaje real que le llegó al cliente, y arma los `components` posicionales con `buildTemplateBodyComponents` para pasárselos a `sendWhatsappTemplate`. T3a (9/9/2026): `mediaType` admite `"sticker"`; `content` se fuerza a `null` cuando `mediaType === "sticker"` aunque el llamador mande uno —Meta rechaza un sticker con caption, ver `meta-client.ts` |
| `conversations/[id]/read/route.ts`, `.../typing/route.ts` | T3.1 (4/9/2026): doble check azul y "escribiendo…" hacia Meta, un endpoint por señal. Mismo patrón de sesión que `.../close` (`@/lib/supabase/server` + `fetchCurrentAgent`, 401 sin ella); sin cliente admin, porque acá no hay ninguna RPC de `service_role` de por medio. Resuelven el `phone_number_id`/`status` del canal y el `whatsapp_message_id` del último mensaje ENTRANTE (Meta exige ese wamid como "message_id" para las dos señales); con canal no `connected`, sin `WHATSAPP_ACCESS_TOKEN` o sin ningún mensaje entrante, no llaman a Meta pero responden 200 igual — nunca rompen la bandeja. `crm-shell.tsx` llama a `.../read` (vía `sendReadReceipt`, `mutations.ts`) junto con `markConversationRead`, nunca en su lugar; `composer.tsx` llama a `.../typing` (vía `sendTypingSignal`) al primer carácter y cada 20 s mientras haya texto |
| `cron/process-queue/route.ts` | Red de seguridad de la cola de turnos (cada minuto desde el 7/9/2026, antes cada 5 min); exige `CRON_SECRET`, falla cerrado con 503. Resguardo en `route.test.ts`: los caminos cerrados afirman que ni la cola ni el reconciliador se llamaron. Desde el 30/8/2026 reconcilia ANTES de drenar, para que lo rescatado se atienda en la misma pasada y no espere otros 5 min |
| `agent/backlog/route.ts` | Repaso del atraso al encender la IA: encola lo que quedó esperando mientras estuvo apagada. Resguardo en `route.test.ts` (incluye el DEFECTO CONOCIDO D1: el lock queda tomado 30 min en los caminos que no encolan). 7/9/2026: los comentarios de cabecera y de `SWEEP_LOCK_SECONDS` dejan de asumir un ritmo fijo de drenado ("diez cada cinco minutos"); el freno de emergencia real sigue siendo `agent_can_run()` por turno, no la velocidad de la cola |
| `agent/stop/route.ts` | Freno de emergencia: apaga la IA y también lo que ya estaba en marcha. Resguardo en `route.test.ts`: orden interruptor→purga, degradación con Redis caído, línea de auditoría `ia_apagada` |
| `dev/simulate-message/route.ts` | Simulador del panel: mensaje entrante sin pasar por Meta, turno síncrono. Su test afirma que la guarda de producción corta ANTES de tocar dependencia alguna. C3 ("El reloj dice la verdad", 5/9/2026): el contacto y el canal de prueba fijos usan números E.164 puros (`+580000000001`/`+580000000000`, antes "+00 000 0000001" con espacios) porque `turn-target.ts` exige `isDeliverablePhoneNumber` y sin `conversationId` TODO turno simulado moría con "Identidad no verificable"; el `upsert` va por id fijo, así que la fila vieja se corrige sola en la próxima llamada. Su test cubre además que sin `conversationId` el turno llega a `runAgentTurn` (mockeado) |
| `media/[...path]/route.ts` | Sirve el bucket privado `whatsapp-media` con la sesión del CRM por delante. Resguardo en `route.test.ts`: 401 sin sesión sin llegar a firmar, y el path viaja sin sanear (DEFECTO CONOCIDO D5) |
| `health/route.ts` | 200 solo si alcanza la base y tiene sus variables; para el monitor externo. Su test afirma los CÓDIGOS HTTP (lo único que leen monitor y HEALTHCHECK), no solo el JSON. Desde el 30/8/2026 informa además `unassigned_waiting` (vía la RPC `unassigned_waiting_count`, expuesto SIN sesión por decisión del operador) y `redis_persistence`: ninguno de los dos cambia el código HTTP — un lead esperando no es una caída. T3.4 (5/9/2026) suma `channel_quality`/`channel_messaging_limit` (`checkChannelHealth()`, mismo criterio "connected primero, si no el primer canal que exista" que `fetchWhatsappChannelHealth` en `src/lib/data.ts`): tampoco cambian el código, un número degradado sigue entregando hasta que Meta lo restrinja de verdad |
| `workflows/` | (dentro de `dev/`) utilidades de desarrollo |

## `src/lib` — núcleo compartido

| Archivo | Qué es |
|---|---|
| `types.ts` | Tipos de dominio de toda la app (formas mínimas por vista, a propósito). `BoardConversation` gana `lastReplyAt`/`lastReplySender` (T0.2, 5/9/2026): la "última respuesta real" que separa T0.1, distinta de `lastMessageAt` (que sigue siendo "último mensaje visible") — es lo único que `awaitingReply()` (`dashboard.ts`) compara contra `lastCustomerMessageAt` desde esa migración. `InboxFilter` suma `"escalated"` (T1.5, mismo día): la píldora "Escaladas". `Message` gana `whatsappErrorCode` (T3.3, 5/9/2026, número crudo detrás de `whatsappError`): lo necesita la burbuja para saber si el motivo tiene, además, un botón que hacer algo (`failureAction` en `whatsapp/failure-reason.ts`). T3.2 (5/9/2026): `MessageType` suma `"interactive"`/`"order"`/`"unsupported"`, `WhatsappMessageStatus` suma `"played"`; `Message` gana `payload: MessagePayload | null` (id del botón/ítem respondido, payload de plantilla, ítems del pedido); `Conversation` (no `BoardConversation`/`ConversationSummary`: la bandeja no lo pinta) gana `referral: ConversationReferral | null` — `{headline, sourceUrl, receivedAt}`, lo mínimo que necesita el banner de la cabecera del chat contra la ventana de 72 h. B3 (5/9/2026): `AgentSettings.businessHours?: BusinessHours` — opcional a propósito (tres fixtures de test lo fabrican a mano sin el campo); `fetchAgentSettings` SIEMPRE lo llena y quien lo lea sin garantía cae a `DEFAULT_BUSINESS_HOURS`. T3a (9/9/2026, apéndice al final del archivo): `Sticker` — `{id, url, name, animated, createdBy, createdAt}`, `url` ya resuelta con `mediaUrlFor` igual que `Message.mediaUrl` |
| `data.ts` | Capa de LECTURA de la bandeja/chat contra Supabase (55K; mapea filas crudas a tipos de dominio). `fetchConversations` gana `unreadOnly` y `assignedTo` (`FetchConversationsOptions`) para resolver contra la base las píldoras "No leídas" y "Mías" — antes "Mías" filtraba en memoria sobre la ventana cargada y un chat asignado viejo no aparecía. `InboxCounts` gana `unread` (mismo predicado que `unreadOnly`); `pending`/`pendingStale` seguían ahí solo para el panel de inicio hasta el 28/8/2026 tarde — desde el 30/8/2026 `pending` volvió a tener píldora propia en la bandeja (además de seguir alimentando el panel de inicio), la única de las cuatro que cuenta contra toda la base con `awaiting_reply and status <> 'closed'`, no contra lo que la bandeja tenga cargado. Paginación por CURSOR (29/8/2026): `offset` salió de `FetchConversationsOptions`, entró `cursor?: ConversationCursor` (`inbox-paging.ts`) — un `offset` de posición se rompía apenas una fila subía al tope en medio de la bajada de la bandeja (confirmado en producción, ~3 reordenamientos/minuto, filas que no volvían nunca). `fetchConversationRows` ordena por `last_message_at desc nulls last, id desc` (el desempate por `id` es necesario: hay empates reales de `last_message_at`) y acumula sus disyunciones (`pendingWindow: "stale"`, `unreadOnly`, el predicado del cursor) en un arreglo para emitir un solo `.or()` con `orExpression` — dos `.or()` en la misma consulta no son fiables. El recorrido MULTIPÁGINA INTERNO (las llamadas sin `limit`: tablero y Control de IA vía `fetchBoardConversations`) también avanza por cursor desde el 29/8/2026: cada página interna pide `range(0, pageSize-1)` con el predicado armado desde la última fila de la página anterior, en vez del `range(rows.length,…)` posicional que perdía en silencio la fila del borde cuando algo salía del conjunto (un chat que se cierra con `activeOnly`) y duplicaba el borde cuando algo subía al tope; el cursor de ENTRADA (`options.cursor`) gobierna solo la primera página interna, después lo reemplaza el de continuación (`cursorFromRow`, gemelo crudo de `cursorAfterPage`), inyectado en el mismo acumulador `orGroups` para que la distribución del OR con `unreadOnly` siga saliendo en un solo `.or()`. `CONVERSATION_BOARD_COLUMNS`/`CONVERSATION_DETAIL_SELECT` ganan `last_reply_at, last_reply_sender` (T0.2, 5/9/2026), y los mappers (`mapBoardConversation`/`mapConversationSummary`/`mapConversation`) los pasan crudos, sin `new Date`. `FetchConversationsOptions.order?: "recent" | "oldest"` (T1.3, 5/9/2026, píldora "Más antiguas"): con `"oldest"` el orden base se da vuelta entero (`last_message_at asc nulls first, id asc`) y el predicado del cursor se arma espejado — la zona nula cambia de lugar (al principio en vez de al final), así que un cursor CON fecha ya no necesita el tercer término que cruza hacia ella (quedó atrás) y un cursor SIN fecha sí necesita uno nuevo (`last_message_at.not.is.null`) para cruzar HACIA la zona con fecha. Verificado contra PostgREST local el 5/9/2026, mismo método que el descendente el 29/8/2026. `escalatedOnly` (T1.5, mismo día, píldora "Escaladas"): `journey_stage = 'assigned' and ai_enabled = false and status <> 'closed'` por `.eq()`/`.neq()`, más `(last_reply_sender is distinct from 'agent' or awaiting_reply)` como un grupo más de `orGroups` — usa el índice `conversations_escalated_idx` de T0.1. `fetchInboxCounts` gana `escalated` con el mismo predicado (`data-inbox-counts.test.ts`). `fetchPinnedIds` (T2.2, 5/9/2026): `conversation_pins` filtrado por `agent_id` (explícito en el `.eq()`, aunque la RLS de la tabla ya filtre lo mismo — mismo motivo que `viewerId` en `fetchInboxCounts`: la condición explícita es la que un mock de test puede afirmar), devuelve un `Set<string>` de ids, no filas. `fetchWhatsappChannelHealth` (T3.4, 5/9/2026): calidad/límite/restricciones/fecha del canal que el webhook fue guardando; prefiere el canal `connected` y si no hay ninguno cae al primero que exista, null solo si no hay ningún canal creado. `mapMessage` gana `whatsappErrorCode: row.whatsapp_error_code` (T3.3, 5/9/2026): crudo, al lado de `whatsappError` (ya resuelto por `failureReason`) — la columna ya viajaba en `MESSAGE_SELECT`, faltaba mapearla al tipo de dominio. T3.2 (5/9/2026): `MESSAGE_SELECT` suma `payload` (`mapMessage` lo pasa crudo); `CONVERSATION_DETAIL_SELECT` suma `referral` (`mapConversation` lo pasa crudo) — ninguno de los dos entra a `CONVERSATION_BOARD_COLUMNS`/`CONVERSATION_LIST_SELECT`: la bandeja no pinta el banner de anuncio, solo el chat abierto. B3 (5/9/2026): `fetchAgentSettings` suma `business_hours` al select y devuelve `businessHours` vía `parseBusinessHours`; `fetchBusinessHours` es la lectura liviana (sin el RPC `agent_spend_today`) que nunca lanza —error o fila rota → default— para quien solo necesita el horario: `src/app/page.tsx` la usa para pasarle `businessHours` a `DashboardView`. Usa `console.error` y no `lib/log.ts` porque `data.ts` también se importa desde el cliente y `log.ts` es `server-only`. T7 (corrida "La IA ve lo que llega", 8/9/2026): `unansweredFreeWork` en `fetchBacklogConversationIds`/`fetchBacklogCounts` pide `"id, last_customer_message_at"` en vez de solo `"id"` y llama a `conversationsWrittenByHumans` (`human-handled.ts`) con `{id, lastCustomerMessageAt}` de cada fila y `{now}` — el diálogo de encendido de la IA deja de descontar para siempre un chat con una nota vieja de un asesor: solo lo descuenta si el asesor se adelantó al cliente o escribió hace menos de `AI_HUMAN_GRACE_MINUTES` |
| `mutations.ts` | Capa de ESCRITURA: enviar, asignar, etiquetar, cerrar venta, eventos de sistema en la burbuja. `closeConversation`/`reopenConversation` (T2.1, 5/9/2026) son la excepción a "toca Supabase directo": pasan por `fetch` a `api/conversations/[id]/close`/`.../reopen` porque `record_handoff()` solo la puede llamar `service_role`, mismo motivo por el que enviar mensajes pasa por `api/messages/send` en vez de tocar Meta desde el navegador. `pinConversation`/`unpinConversation` (T2.2, mismo día) SÍ tocan Supabase directo (`conversation_pins`, RLS `agent_id = auth.uid()` ya basta): el error del cuarto pin —el trigger `conversation_pins_limit_before_insert` de 20260905040000_conversation_pins.sql— sube tal cual, en español, listo para mostrarse. `sendReadReceipt`/`sendTypingSignal` (T3.1, 4/9/2026) son la misma excepción, un escalón más simple: `fetch` a `api/conversations/[id]/read`/`.../typing`, sin devolver nada y sin lanzar nunca (`try/catch` mudo) — un check o un typing que no salió no puede tumbar la bandeja ni bloquear a quien los llama. `sendTemplateMessage` (T3.3, 5/9/2026) gana `variables: string[]` (default `[]`): sigue mandando `content: template.bodyPreview` SIN sustituir —la sustitución la hace la ruta, una sola vez— y las variables viajan aparte para que `api/messages/send` arme los `components`. B5 (5/9/2026): `updateBusinessHours(supabase, agent, hours)` — mismo patrón que `setDailySpendCap` (update de `agent_settings.business_hours` + `updated_by`/`updated_at`, lanza si hay error; RLS ya limita a supervisor/admin); el panel valida el borrador antes de llamarla. Guarda de identidad (6/9/2026): `createPlaybook`/`updatePlaybook` corren `revealsIdentity(responseText)` (`identity-guard.ts`) ANTES de tocar la base y lanzan `PlaybookIdentityError` ("El texto describe a la tienda como automatizada: «…»" o "…como una persona: «…»") si el texto del escenario calza — es la única otra vía, aparte del modelo, por la que sale texto al cliente sin pasar por la guarda en caliente; `playbooks-panel.tsx` la muestra en el toast. Tests: `mutations-playbook-identity.test.ts` (rechazo antes de tocar la base, en los dos sentidos, y los dos negativos del catálogo/uso real que exige la trampa de `automátic*` suelto). T3a (9/9/2026, apéndice al final del archivo, biblioteca de stickers): `saveStickerFromMessage(supabase, message, agent)` copia el archivo de un mensaje ya recibido a `stickers/<uuid>.webp` con `storage.copy` (mismas dos políticas del bucket que ya bastan para el resto del multimedia) e inserta la fila con `source_message_id`; si `.copy` no está disponible cae a un respaldo (descargar por la ruta propia `/api/media/...`, que ya valida sesión, y subir). `createSticker(supabase, blob, name, agent)` sube un WebP armado desde cero (T3b, con canvas) e inserta. `deleteSticker(supabase, sticker)` borra la FILA primero —la RLS de `stickers` decide si este agente puede, y el error sube tal cual— y solo si eso funcionó borra el objeto del bucket, para no dejar un archivo huérfano sin fila si la RLS frena el borrado. `sendStickerMessage(conversationId, mediaUrl)` es el mismo camino que `sendMediaMessage` (`kind: "media"`, `api/messages/send`) pero con `mediaType: "sticker"` y sin `content`: Meta rechaza un sticker con caption |
| `data-tags.test.ts` | El filtro por etiqueta de la bandeja: `fetchTagsInUse` (solo las etiquetas realmente aplicadas) y `tagId` en `FetchConversationsOptions`. Su test clave es el que afirma que una conversación filtrada por una etiqueta sigue trayendo TODAS las del contacto: resolver el filtro con un embed `!inner` dentro del select de la lista recortaría los demás chips de color, y ese es el motivo de que `tagId` se resuelva antes, contra `contact_tags`, y viaje como `contactIds` |
| `customers.ts` / `customers-data.ts` | Sección Clientes: lógica pura (URL, paginación, gasto) / consultas alrededor de la persona |
| `inventory.ts` / `inventory-data.ts` / `inventory-freshness.ts` | Sección Inventario: lógica pura / consultas a `products` / cuán viejo es el inventario antes de que la IA lo cotice |
| `dashboard.ts` / `dashboard.test.ts` / `dashboard-tickets.test.ts` | Reclamos (contacto etiquetado "Reclamo*"), ventana de 24h (`withinFreeformWindow`), recorrido, e `isStalePending` — ventana de 24h sin respuesta, hoy solo del Dashboard y el `AgentHomePanel` (la reforma del 28/8/2026 tarde le quitó a `inbox-sections.ts` la píldora "Pendientes" entera y con ella el test de contrato que las mantenía de acuerdo). El contrato se repuso el 30/8/2026 en `ventana-24h-contrato.test.ts`, pero amarrando otras patas: `inbox-sections.ts` ya no corta por ventana de 24h (su "Pendientes" vuelto parte por LECTURA), así que hoy ata `dashboard.ts` (`isStalePending`/`withinFreeformWindow`) ↔ `fetchInboxCounts.pendingStale` ↔ `fetchConversations({pendingWindow})`, las tres patas que sí siguen midiendo la misma ventana. `awaitingReply()` (T0.2, 5/9/2026) compara `lastReplyAt` contra `lastCustomerMessageAt` — antes comparaba `lastMessageAt`, que cualquier mensaje visible movía; gemelo SQL de esta función: `supabase/tests/awaiting_reply.sql`. `dashboard.test.ts` (nuevo) prueba `awaitingReply` con los cuatro casos espejo de los pasos (2)(4)(5)(6) de ese `.sql`. A1 ("El reloj dice la verdad", 5/9/2026): `stageOf` deja de confiar en `journey_stage` a ciegas — escalera de cinco peldaños (asesor asignado → herramienta corriendo → clasificando → primer contacto → consulta), todas bajo `awaitingReply` salvo la de asesor asignado; `journey_stage = 'assigned'` sin asesor ya no cuenta como "Con asesor" (cae hasta "Consulta"), y un `classifying`/`tool_running` escrito sin `awaitingReply` es un resto congelado que tampoco se honra. Reloj nuevo `waitingMinutes(c, now, hours = DEFAULT_BUSINESS_HOURS)`: mide desde `lastCustomerMessageAt` (nunca `lastMessageAt`, que una nota o un evento adelantan sin que el cliente haya hecho nada), `null` si la conversación está cerrada o no espera respuesta; en "Con asesor" usa `businessMinutesBetween` (`business-hours.ts`, B2) — de noche o domingo un asesor no está atascado. `isStalled(c, now, hours)` es la fórmula única exportada para que la UI no la duplique. `STAGE_DEFINITIONS.stallMinutes` pasa a `number | null` (first_contact nunca se atasca; umbrales 15/5/3/60 laborales). `minutesInStage` YA NO es el reloj del tablero — queda solo para `ticketQueue`. `buildJourney` gana `hours` con default y reordena: primero las que esperan (mayor espera arriba), luego por `lastMessageAt` descendente. `stageDetail` gana "esperando su siguiente mensaje" (first_contact) y "sin respuesta del cliente" (inquiry sin `awaitingReply`). `dashboard.test.ts` suma los cinco peldaños, la tabla de casos del artefacto (Diana, Carlos, Laura, Ana ×2, "Cliente de prueba", Roberto) y que `buildJourney` cuenta 1 solo atascado sobre esa base |
| `ventana-24h-contrato.test.ts` | Repuesto el 30/8/2026 (el original se perdió el 28/8/2026 tarde cuando la reforma le quitó a `inbox-sections.ts` la píldora "Pendientes" y con ella el contrato que la ataba a `dashboard.ts`). Amarra tres patas que tienen que medir la ventana de 24h con la misma vara —`withinFreeformWindow`/`isStalePending` en memoria (`dashboard.ts`), el contador `fetchInboxCounts.pendingStale` y la consulta paginada `fetchConversations({pendingWindow})`, ambas en `data.ts`— con fakes de `.select`/`.or`/`.range` de PostgREST y una tabla de filas alrededor del borde exacto del corte; confirma en su cabecera, leyendo el código, que `inbox-sections.ts` YA NO es una pata de este contrato (su `now` no lo usa ninguna rama). Gana (T0.2, 5/9/2026) un caso que amarra `isWithin24hWindow` (`whatsapp-window.ts`) como cuarta pata: mismo criterio de ventana, otra implementación (la usa `claimWelcome` del webhook). Su fake de conteos gana (T1.5, mismo día) las tres columnas de "Escaladas" —ninguna fila de este archivo tiene `journeyStage: "assigned"`, así que ese conteo da 0 en todos sus tests; el punto es que la quinta consulta de `fetchInboxCounts` no reviente contra este fake, no medirla acá |
| `invariante-leads-contrato.test.ts` | El gemelo en TypeScript de `supabase/tests/invariante_leads.sql`: prueba `isUnassignedLead` (`inbox-filters.ts`) con los MISMOS casos, mismos nombres, mismo orden que el `.sql` — si una de las dos definiciones cambia sin la otra, uno de los dos archivos se pone rojo. Caso 7 (anexo A1, 5/9/2026): la despedida de la IA al escalar sin asesores lleva `is_auto_reply = true`, así que `awaitingReply` sigue en `true` y el traspaso más reciente (`unassigned`) SÍ cuenta como sin dueño. Caso 8 (anexo A2, 5/9/2026): cerrada, reabierta por el cliente y CON asesor asignado — el traspaso más reciente es `human`, no `unassigned` — NO cuenta. Caso 9 (anexo B2, 5/9/2026): un escenario que escaló sin asesores cuyo texto salió con `is_auto_reply = false` (todavía no se sabía si haría falta un asesor) y se marcó `true` DESPUÉS con un UPDATE aparte — el trigger de B1 recalculó `awaiting_reply` de vuelta a `true`, y el traspaso más reciente sigue siendo `unassigned`/`escalada_sin_asesor` — SÍ cuenta |
| `inbox-filters.ts` | Las seis píldoras de la bandeja (Pendientes/Sin dueño/Escaladas/No leídas/Mías/Todos), iguales para todos los roles; `DEFAULT_INBOX_FILTER = "pending"` desde la tercera reforma (30/8/2026, medida contra producción: "No leídas" es subconjunto ESTRICTO de "Pendientes" —282 filas contra 51— así que 231 chats leídos-y-sin-responder no tenían ninguna píldora que los alcanzara). `case "pending"` en `matchesFilter`: `status !== "closed" && awaitingReply(conversation)`, el mismo predicado que ya vivía en `dashboard.ts` — la píldora no reinventa el corte de 24h, solo reintroduce en la lista el que ya existía. `case "escalated"` (T1.5, 5/9/2026): `journeyStage === "assigned" && !aiEnabled && status !== "closed" && (lastReplySender !== "agent" || awaitingReply(c))`. Hasta el anexo A1 (5/9/2026) el `|| awaitingReply` rescataba la escalación sin asesores donde la IA se despedía con un mensaje de cortesía y ESE mensaje apagaba `awaitingReply` sin que ningún humano hubiera escrito nada; desde A1 esa despedida sale con `is_auto_reply = true` y ya no la apaga, así que la pata `lastReplySender !== "agent"` queda como la única necesaria para ese caso — SE CONSERVA de todos modos porque sigue cubriendo escalaciones anteriores a A1 y cualquier otra respuesta de la IA previa a la escalación. Exporta `isUnread` (LA definición de "sin leer": `unreadCount > 0 || manuallyUnread`, corte GLOBAL de equipo — cerrar un chat no es leerlo). `SERVER_FILTER_LIMIT`/`serverFilterTruncated` (tope mudo de 200 filas y su aviso de recorte) se retiraron el 29/8/2026: las píldoras que resuelven contra la base pasaron a paginar por cursor (`inbox-paging.ts`) con `INBOX_PAGE_SIZE`, igual que "Todos" — el recorte silencioso que el aviso vigilaba dejó de existir. La guardia anti-crecimiento vive en su test, subida de 3 a 4 píldoras el 30/8/2026 junto con el regreso de "Pendientes", de 5 a 6 el 5/9/2026 junto con "Escaladas". `applyInboxFilters` gana `pinnedIds` (T2.2, mismo día, hasta tres chats fijados por asesor — `conversation_pins`): NO es un filtro, es un segundo `sort` ESTABLE aplicado DESPUÉS del `sort` normal — sube los fijados como grupo sin desordenar entre ellos ni entre el resto; una conversación fijada que no calza con la píldora activa sigue sin aparecer. C1 ("El reloj dice la verdad", 5/9/2026): `InboxCriteria` gana `unassignedIds?: ReadonlySet<string> | null` y `case "unassigned"` deja de devolver `true` sin condición: pasa solo si el id está en el set que trajo `fetchUnassignedConversations` — bug visto en la verificación visual del 5/9/2026: el conteo ya salía bien, pero la LISTA pintaba toda la ventana local que `searchableConversations` mezcla en `inbox-sidebar.tsx`; con el set en null (consulta en vuelo) no pasa nada, y la sidebar ya muestra "Buscando…" |
| `inbox-sections.ts` | Partición pura de la lista de bandeja en secciones por píldora: `pending` partida en "Sin abrir"/"Leídas sin responder" (por LECTURA, con `isUnread` — no por ventana de 24h, que sigue siendo terreno de `dashboard.ts`), `unassigned` una sola sin encabezado, `escalated` partida en "Sin asesor"/"Con asesor" (T1.5, 5/9/2026 — por si YA hay un asesor a cargo, no por lectura), `unread` una sola sin encabezado, `mine` partida en Sin leer/Leídas (mismo `isUnread`), `all` una sola; no reordena, respeta el sort activo. El parámetro `now` de `buildInboxSections` no lo usa ninguna rama (se conserva en la firma porque las seis píldoras comparten una sola función) — la prueba de que el módulo no corta por tiempo, solo por lectura |
| `inbox-paging.ts` | Cursor de paginación de la bandeja (29/8/2026): `ConversationCursor` (`lastMessageAt`/`id`, orden del servidor) y `cursorAfterPage` (última fila de una página, o `null` si vino vacía). Cursor por VALOR, no por posición: reemplaza el `offset` que `crm-shell.tsx` usaba para "cargar más", que perdía filas cuando una conversación subía al tope en medio de la bajada de la lista. Gana `mergeById` (une dos tramos sin repetir, el primero manda), movida de `crm-shell.tsx` tras una revisión de código: vivía duplicada con el dedupe-append que `loadMoreServerRows` reimplementaba a mano en `inbox-sidebar.tsx`. Gana `reconcileHead` (29/8/2026): une una cabecera fresca con lo acumulado y suelta lo que salió del conjunto —en "Pendientes"/"No leídas"/"Mías" la pertenencia es un predicado y las filas se van, cosa que `mergeById` no puede saber—, por posición e id y nunca por fecha. El módulo se queda PURO a propósito: sus tests corren en entorno node y `data.ts` lo importa desde el servidor. `reconcileHead` vale igual con `order: "oldest"` (T1.3, 5/9/2026): nunca mira qué significa "más profundo" en términos de fecha, solo posición dentro de `accumulated` — dado vuelta el orden de la consulta, cambia cuál fila queda "más profunda" (la más nueva en vez de la más vieja), no el algoritmo |
| `message-search.ts` | Buscar conversaciones por lo que se dijo adentro (no solo nombre/número) |
| `message-grouping.ts` | Galerías de fotos/videos consecutivos y separadores de fecha |
| `outbox.ts` | Cola de envío del composer (pura, sin red): reintento, descarte, limpieza |
| `sale-cart.ts` | Carrito de la venta en curso: lo cotizado por la IA más lo agregado a mano |
| `playbook-price.ts` | Detecta escenarios con precio escrito a mano (que envejece sin que nadie lo toque) |
| `bcv-schedule.ts` | Cuándo volver a preguntar la tasa al BCV (no publica todos los días) |
| `chart-scale.ts` | Líneas de referencia de los gráficos |
| `format.ts` | Formatos de hora/moneda coherentes en toda la app |
| `time-zone.ts` | Una sola zona horaria: la del equipo (`NEXT_PUBLIC_CRM_TIME_ZONE`), no la del navegador. B2 (5/9/2026): gana `crmWeekday(instant, timeZone)` → 0..6 (0 = domingo) en la zona del equipo con `Intl`, nunca `getDay()` — misma técnica que `crmMinuteOfDay` |
| `business-hours.ts` | Único dueño del horario de atención (B2, "El reloj dice la verdad", 5/9/2026; puro: sin React ni Supabase). `parseBusinessHours` valida el jsonb de `agent_settings.business_hours` (forma, `HH:MM`, inicio < fin) y ante cualquier cosa rota devuelve `DEFAULT_BUSINESS_HOURS` (L–V 08:00–18:00) sin lanzar, porque lo llama un turno de IA. `DAY_BANDS`/`dayBand`/`greetingFor` son LA fuente de las franjas mañana/tarde/noche (`greeting-window.ts` las importa de acá). `businessStatus` dice si la tienda está abierta, a qué hora cierra y cuándo abre (`dayLabel` es "hoy" o "el lunes" — NUNCA "mañana": chocaba con la franja "mañana" en el mismo renglón del prompt, decisión del 5/9/2026), `describeSchedule` la frase "lunes a viernes de 8:00 am a 6:00 pm", `businessMinutesBetween` los minutos de horario laboral entre dos instantes (el reloj de "Con asesor" en el tablero, A1) y `turnClockLine` el renglón completo del `TURNO ACTUAL` (B3). Todo con instantes UTC y zona explícita, sin depender del `TZ` del proceso |
| `venezuela.ts` | Los 24 estados |
| `storage.ts` | La URL que se guarda en `messages.media_url`: relativa al CRM, no al bucket |
| `media-link.ts` | URL firmada de 10 min para que Meta descargue el adjunto saliente |
| `sticker-image.ts` | T3a ("Seis frentes del buzón", 9/9/2026). Módulo PURO (sin DOM, sin imports) con la geometría y los límites de los stickers de WhatsApp: `STICKER_SIDE` (512), `STICKER_STATIC_MAX_BYTES`/`STICKER_ANIMATED_MAX_BYTES` (100 KB / 500 KB, los límites de Meta), `fitInSquare(width, height, side?)` encaja con "contain" centrado y nunca agranda una imagen ya más chica que el lado, `qualityLadder()` los pasos de calidad WebP para reintentar la codificación (0.92 → 0.5 de a ~0.07) e `isWithinStickerLimit(bytes, animated?)`. El canvas que arma el archivo de verdad es de T3b, que consume estas funciones |
| `stickers-data.ts` | `fetchStickers(supabase)`: la biblioteca completa, más recientes primero, mapeando `storage_path` a la ruta propia del CRM con `mediaUrlFor` (T3a, 9/9/2026) |
| `whatsapp-window.ts` / `whatsapp-window.test.ts` | La regla de las 24h de Meta: `WINDOW_MS`, `isWithin24hWindow` y `hoursUntilWindowCloses` (las usan el webhook, `agent.ts` y la lista). T2 de la corrida "La ventana de 24 h dice la verdad" (7/9/2026) suma `windowClosedByMeta(messages)` —true si el saliente más reciente con `whatsappStatus === 'failed'` y `whatsappErrorCode === 131047` no tiene ningún entrante posterior que no sea `unsupported`; ordena por `createdAt` porque el array no viene garantizado en orden— e `isComposerWindowOpen(lcma, messages, now?)` = `isWithin24hWindow && !windowClosedByMeta`: espejo en memoria de los dos candados de la migración 20260907010000, red de seguridad del composer para el hueco entre el rechazo de Meta y el refresh por realtime. El test (nuevo, 7/9/2026) espeja por nombre los casos 1-5 y 7-9 de `supabase/tests/ventana_24h.sql` (el 6, backfill, es solo SQL) |
| `redis.ts` | Conexión compartida a Redis (una por proceso) |
| `log.ts` | Registro estructurado JSON (`{"level","event","ts",...}`), eventos en español, oculta valores sensibles. `errorText` es el ÚNICO traductor de errores a texto de log (8/9/2026): antes de mirar `instanceof Error` revisa si el objeto trae `message` + `code` (cubre `PostgrestError`, `AuthError`, `StorageError` — `PostgrestError` sí extiende `Error`, así que si `instanceof` ganara primero el código se perdía igual) para devolver `"código: mensaje"`; si no hay código usa solo el mensaje; strings tal cual; `undefined`/`null` literales; cualquier otro objeto cae a `JSON.stringify` acotado a 300 caracteres. Corrige `turno_lock_no_liberado` y `webhook_error_actualizar_estado`, que salían con `detail: "[object Object]"` pese a llamar ya a `errorText` |
| `permisos-funciones.test.ts` | Guardián estático: lee las migraciones de `supabase/migrations` y falla si una función `security definer` nueva no trae su `revoke execute ... from anon, authenticated` explícito por firma en la misma migración que la crea |
| `agent-tool-keys.ts` | Claves de `agent_tools` sin `server-only`, para que el panel las use |
| `use-clock.ts`, `use-debounced-callback.ts`, `use-element-width.ts`, `use-long-press.ts`, `use-theme.ts` | Hooks utilitarios (reloj cuantizado, agrupación de refrescos, ancho real, long-press, tema) |
| `read-on-arrival.ts` | T1.1 (4/9/2026): `decideReadOnArrival({visibilityState, hasFocus})` → `"mark" \| "defer"` y `shouldFlushDeferred(visibilityState)`. Antes, el INSERT de un mensaje entrante en `messages-{id}` (`crm-shell.tsx`) marcaba leído sin preguntar nada; con dos pestañas abiertas y el mismo chat de fondo en una, un mensaje nuevo apagaba "No leídas" ahí sin que nadie lo hubiera visto. Puro y sin DOM a propósito: lo prueba `read-on-arrival.test.ts` con una tabla de casos, y lo consume `crm-shell.tsx` pasando `document.hidden ? "hidden" : "visible"` (no `document.visibilityState` directo: en jsdom viajan desincronizados, y `use-live-refresh.ts` ya usa `document.hidden` como la señal de pestaña oculta) |
| `realtime-status.ts` | `nextRealtimeAction(previous, status)` (F9, 4/9/2026): módulo puro que decide qué hacer con el callback de `channel.subscribe((status, err) => …)` de `@supabase/realtime-js` — caer por primera vez (`CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED`) avisa (`log_down`), seguir caído no repite el aviso, y volver `SUBSCRIBED` tras una caída pide resincronizar (`resync`). Lo usan `use-live-conversations.ts` y los cinco canales angostos de `crm-shell.tsx` (`unassigned-handoffs`, `agent-settings-changes`, `quick-replies-changes`, `tags-changes`, `messages-{id}`); todos caen a `console.warn`, no a `log.ts` (`server-only`, no se puede importar desde el navegador) |
| `use-live-conversations.ts` / `use-live-refresh.ts` / `use-live-sales.ts` | Realtime: mantener bandeja/ventas al día aplicando eventos en memoria y agrupando refetches. El `patch` de `applyConversationRow` aplica `last_reply_at`/`last_reply_sender` (T0.2, 5/9/2026) igual que el resto de columnas propias de la fila. F9 (4/9/2026): `use-live-conversations.ts` resincroniza con `realtime-status.ts` al reconectar tras una caída del canal; `use-live-refresh.ts` guarda `lastRefreshAt` y, al volver la pestaña, refresca si hay algo pendiente O si pasaron más de `STALE_AFTER_MS` (2 min) sin un refresco real — cubre la caída que no anota nada porque el canal simplemente dejó de escuchar |
| `use-inbox-pager.ts` | El paginador por cursor de la bandeja, uno solo para las tres píldoras (29/8/2026): cursor + candado en vuelo + sesión + `reachedEnd` + estado de la primera página (loading/ready/error). Todo lo que decide se lee de un ref y no del render: dos eventos de scroll del mismo frame veían el mismo snapshot y disparaban dos consultas con el mismo cursor. Nació de una revisión que encontró la misma máquina escrita dos veces —`cursorRef`+flags en el shell, `serverRows`+`serverSessionRef`+`serverBusyRef` en el sidebar— endurecida a medias en cada copia. El shell lo usa sembrado (`seed`); el sidebar, uno por píldora (`sessionKey`). Un fallo NUNCA enciende `reachedEnd`: por eso existe `status: 'error'` con `retry()` |

## `src/lib/ai` — el agente vendedor

| Archivo | Qué es |
|---|---|
| `history-line.ts` | T2 (8/9/2026, "La IA ve lo que llega"). Módulo PURO (sin `server-only`, sin más import que tipos): convierte una fila de `messages` en la línea que ve el modelo. `historyLine(row)` mantiene las reglas ya vigentes (nota interna, `sender_type === "system"`, `message_type === "unsupported"` → `null`; texto/`location`/`contacts`/`interactive`/`order`/`template`/`system_event` → `content` tal cual) y agrega: foto/video/nota de voz/documento/sticker del CLIENTE sin `content` legible ya no se descartan — arman un marcador entre corchetes (`[El cliente envió una foto sin texto; no puedes verla]`, con pie si trae `content`: `[El cliente envió una foto. Pie: …]`); lo mismo saliente del ASESOR (`[El asesor envió una foto]`). `messages.content` en la base NO se toca — el marcador vive solo en memoria, lo que sale de `historyLine`. Sus textos exactos tienen que coincidir con la sección 7 (`MEDIA_RULES`) de `prompt.ts`. `isHistoryMarker(text)` reconoce esas líneas (`^\[(El cliente\|El asesor) envió `) para que `agent.ts` las trate distinto de texto real (fase 0, `lastCustomerMessage`, `alreadySentPlaybook`/`alreadyRedirected`) |
| `agent.ts` | Orquestador del turno: fase 0 (escenario) ‖ fase 1 (intención) en paralelo → tool loop (máx. 5 pasos); mide tiempos por tramo. Antes de hablarle al cliente, `deliver()` confirma que el lock de `conversation-lock.ts` sigue siendo suyo (renovación fenceada por token); si lo perdió, no envía y queda `turno_lock_perdido_sin_enviar`. `deliver()` es genérica desde T0.3 (5/9/2026): devuelve el `DeliveryOutcome` de `enviar()` (o `null` si una guarda lo frenó), y los TRES consumidores que hablan de verdad —el escenario de fase 0, la redirección de fuera de tema y la respuesta final del tool loop— lo pasan por `deliveryFailed()` (renombrada de `rejectedByMeta`, S6, 8/9/2026): si el envío falló (`whatsapp_status: "failed"`) bifurca por `entrega.origenDelFallo` -- "red" (un TypeError/DNS/timeout que nunca llegó a la Graph API) deja `log.error("turno_envio_fallo_de_red")` y `recordHandoff(unassigned, entrega_fallida)`; "meta" (o `null`, compatibilidad) es el camino de siempre: deja `turno_rechazado_por_meta` en el registro y `recordHandoff(unassigned, rechazado_por_meta)` -- ninguno de los dos reintenta — salvo que en el tool loop ya haya corrido una escalación (`outcome.escalated`, de la herramienta o de la red de seguridad de devolución/queja): esa ya dejó su propio traspaso más específico, y un segundo `rechazado_por_meta` lo pisaría en el conteo de "Sin dueño", así que solo queda el log con `traspaso_omitido: "escalada_previa"`. `fireTypingIndicator` (T3.1, 4/9/2026) dispara "escribiendo…" justo al arrancar el tool loop —sin esperarlo, `sendTypingIndicator` ya no lanza— solo con la ventana de 24h abierta (`isWithin24hWindow`), el canal `connected` y algún mensaje entrante con wamid (`lastInboundWamid`, consulta aparte de `loadHistory`: esa trae texto para el modelo, no wamids). `loadHistory` (T3.2, 5/9/2026) suma `message_type` al `select` y salta explícito `message_type === "unsupported"` (antes lo sacaba solo la nulidad de `content`, que sigue dándose pero ya no es de lo único de lo que depende); un `order` entra igual que cualquier texto, con el resumen en español que ya arma el webhook en `content`. Anexo A1 (5/9/2026): el envío final del tool loop pasa `sendAgentText(..., { isAutoReply: outcome.escalated && outcome.unassigned === true })` — cubre los dos caminos por los que la IA se despide sin asesor (el texto fijo de la red de seguridad de devolución/queja y el que redacta el modelo tras la herramienta `escalarAAsesor`), copiando `outcome.unassigned` de `forced.unassigned` en la red de seguridad igual que ya se copiaba `escalated`/`assignedAgentName`. Anexo A2 (5/9/2026), `runAgentTurn`: las dos guardas silenciosas de apertura se invirtieron de orden — se mira `assigned_agent_id` ANTES que `ai_enabled` — para que un chat con dueño y la IA apagada en él (el estado normal tras una escalación o un cierre manual) deje `asignada`/`human` en la bitácora en vez de `pausada`/`unassigned`; el efecto observable (return sin enviar nada) no cambia. Anexo B2 (5/9/2026), `runPlaybook`: un escenario con `afterSend: "escalate"` manda su texto ANTES de escalar (T0.3 exige ese orden), así que ese mensaje sale con `is_auto_reply = false` sin saber todavía si haría falta un asesor. Si `escalateConversation` acaba de descubrir que no hay NADIE (`result.unassigned`), un UPDATE marca `is_auto_reply = true` en todo lo que la IA mandó en este turno (`conversation_id` + `direction: outbound` + `sender_type: ai` + `not is_internal_note` + `created_at >` el `last_customer_message_at` de la conversación, que `runTurnPhases` le pasa a `runPlaybook` como parámetro nuevo) — el trigger que sumó B1 (20260905070000) recalcula `last_reply_at`/`awaiting_reply` al ver ese cambio. Si `last_customer_message_at` viniera nulo (el tipo lo permite; no pasa en un turno real porque `withinFreeformWindow` ya lo exige antes) no se marca nada y queda `turno_escenario_sin_fecha_cliente` en el registro, para no arriesgar marcar la despedida de un turno anterior. Si el UPDATE falla, `log.error("turno_escenario_despedida_no_marcada")` y el turno sigue igual — es observabilidad de `awaiting_reply`, no una barrera; si sale bien, `log.info("turno_escenario_sin_asesor_marcado")`. A3 ("El reloj dice la verdad", 5/9/2026): `deliveryFailed`, en la rama que registra `rechazado_por_meta`, también pone `journey_stage = null` y `active_tool = null` en un solo UPDATE — antes ese `return` salía antes del reseteo normal y el tablero veía "Clasificando"/"Herramienta" congelados para siempre; NO se toca en la rama `yaEscalada`, donde `escalateConversation` ya dejó `assigned` antes del envío. Si el UPDATE falla, `log.error("turno_etapa_no_reseteada_tras_rechazo")` y el turno sigue: la fila de traspaso ya quedó escrita. B3 (5/9/2026): `runAgentTurn` suma al `Promise.all` de arranque (`agent_can_run` + conversación) una lectura de `agent_settings.business_hours`, la pasa por `parseBusinessHours` (fila rota, sin fila o error → default y `log.warn("turno_horario_no_legible")`, el turno nunca se cae por el horario) y la enhebra a `runTurnPhases` → `matchPlaybook`, `buildInstructions` y el objeto `deps` de las herramientas (donde B4 la toma para la despedida al escalar). B4 (5/9/2026): `runPlaybook` recibe `businessHours` y lo enhebra a `escalateConversation` en el camino de escenario con `afterSend = "escalate"`; la despedida fija de la red de seguridad (devolución/queja sin escalar) sigue sin horario, a propósito. D2 (6/9/2026): la guarda (b) de `deliver()` —`stillEnabled`, que vuelve a consultar `agent_can_run`— deja `agente_no_puede_correr` en vez de `pausada`; esa razón queda reservada para el `ai_enabled` de ESTE chat, que solo mira la guarda de `openTurn`. Guarda de identidad (6/9/2026): `applyIdentityGuard(...)` es el único punto de aplicación, dentro de `runTurnPhases`, sobre el texto final del tool loop —después de la red de seguridad de devolución/queja, justo antes del `deliver(... sendAgentText ...)` de la redacción—. Tres pasos: (1) `revealsIdentity` detecta; (2) si calza, UNA reescritura con `generateText` (no `ToolLoopAgent`: las herramientas ya corrieron y volver a dárselas arriesga una segunda escalación), modelo `getAgentModel("low")`, `maxRetries: 0`, tokens sumados a `turnTokens`, y el resultado vuelve a pasar por `revealsIdentity`; (3) si sigue calzando —o si `generateText` falló— ese texto NO sale: se escala con `escalateConversation` motivo `seguimiento` ("La respuesta redactada se describía como automatizada y no pudo corregirse. Retomar el hilo.") y sale la despedida fija de siempre (`DESPEDIDA_SIN_ASESOR`/`DESPEDIDA_CON_ASESOR`, `is_auto_reply` con la regla de siempre); si `outcome.escalated` ya era `true` (el modelo escaló antes de redactar) no se escala dos veces, solo se reemplaza el texto. Rastro: `log.warn("identidad_reescrita", {conversationId, categoria, fragmento})` cuando el reescrito arregló, `log.error("identidad_bloqueada", …)` cuando no, y el `summary` de `agent_turns` lleva el prefijo `[identidad reescrita]`/`[identidad bloqueada]`. `persona` y `automatizacion` bloquean igual (decisión del operador, 6/9/2026). `OFF_TOPIC_REPLY`, las despedidas fijas y los escenarios de `ai_playbooks` NO pasan por la guarda en caliente —son constantes o texto del supervisor—: las constantes se cubren con un test estático en `agent.test.ts`, los escenarios con la validación al guardar (ver `mutations.ts`). De paso (6/9/2026) la red de seguridad de devolución/queja copia también `outcome.motivo = intent`: su fila de `agent_turns` decía «Motivo: undefined.». Sin migración. T2 (8/9/2026, "La IA ve lo que llega"): `loadHistory` deja de descartar filas sin `content` — pasa cada fila por `historyLine` (`history-line.ts`) y usa lo que devuelva; `lastCustomerMessage` devuelve `null` si la última línea de cliente es un marcador (`isHistoryMarker`); `alreadySentPlaybook`/`alreadyRedirected` saltan marcadores salientes al buscar "nuestra última respuesta". Nueva `lastUserLineIsMarker(history)`: si la última línea del cliente es un marcador, `runTurnPhases` NO llama a `matchPlaybook` (usa `{ playbook: null, usage: ZERO_USAGE }` de `playbooks.ts`) y deja `log.info("turno_ultimo_mensaje_sin_texto")`; `classifyIntent` sigue corriendo igual. T4 (8/9/2026, misma corrida, Bug 2): el `if (history.length === 0) return;` deja de ser mudo — deja `log.warn("turno_sin_contenido_legible")`, `recordHandoff(unassigned, sin_contenido_legible)` y limpia la etapa (caso `cea69118…`; red de seguridad desde T2, que hace que un historial vacío casi no se dé). Nueva `resetStage(supabase, conversationId, evento)`: el UPDATE `journey_stage/active_tool = null` con manejo de error (`log.error("turno_etapa_no_reseteada", { evento, detail: errorText(error) })`, mismo patrón de observabilidad que `deliveryFailed`, que también la reusa en la rama "red") que ahora comparten esa salida, el `return` de clasificación fallida y el `catch` del tool loop — las dos últimas SIN traspaso nuevo (`agent_turns` ya queda con `action: "error"` y el reconciliador la recoge sola). Ajuste de medición: la construcción de la promesa de `matchPlaybook`/`ZERO_USAGE` se movió DENTRO del callback de `medir(..., "clasificacionMs", ...)`, junto a `classifyIntent`, para que el tramo se mida desde que la llamada se dispara de verdad. S6 (corrida "La IA ve lo que llega", hallazgo 4, 8/9/2026): el corte de red de OpenRouter del 7/9 a las 11:57 UTC dejó envíos `failed` con `detalle: "fetch failed"` que el código de entonces confundía con un rechazo de Meta -- `deliveryFailed` los distingue por `entrega.origenDelFallo` (`send.ts`) y ya no escribe `rechazado_por_meta` para un corte de red. T7 (misma corrida, 8/9/2026): la guarda de apertura de `runAgentTurn` y `deliver()` pasan `lastCustomerMessageAt` a `humanHasWritten`/`humanWroteMeanwhile` — la regla dejó de ser "¿alguna vez escribió un asesor?" y pasó a mirar fechas (ver `human-handled.ts`); `deliver()` gana ese parámetro y los tres llamadores (`runPlaybook`, la redirección de fuera de tema, la redacción final) se lo pasan. T0 ("La respuesta llega en siete segundos", 7/9/2026): `TurnTiming` separa `esperaMs` en `debounceMs` (diseño: la ventana de silencio) y `colaMs` (atraso: del vencimiento al arranque) a partir de `options.vencioEn` —un único `Date.now()` en `newTurnTiming` garantiza `debounceMs + colaMs === esperaMs`; ambos `null` en turnos fuera de la cola, como `api/dev/simulate-message`— y suma `herramientas` (nombres de `result.steps[].toolCalls[].toolName` unidos por coma, `""` si el tool loop no usó ninguna, `null` si no llegó) para poder medir T4 sin tocar el prompt; `runAgentTurn(conversationId, options?: { vencioEn?: number })` |
| `queue.ts` | Cola de turnos: el webhook encola y sigue; debounce adaptativo (6s ráfaga / 2s pregunta cerrada, `CIERRA_LA_IDEA`); el turno que encuentra la conversación tomada se pospone 30s y vuelve a la cola (`deferred`), sin gastar intentos ni contar como atendido. 7/9/2026 ("La respuesta llega en siete segundos", S1): los tres frenos —`AGENT_MAX_CONCURRENT_TURNS` (default 3→8), `AGENT_MAX_TURNS_PER_MINUTE` (default 4→30) y el tope por pasada, ahora `maxPerRun()` leyendo `AGENT_QUEUE_MAX_PER_RUN` (antes `MAX_PER_RUN` fijo en diez; default 30)— salen todos del entorno con el mismo patrón (número finito y positivo o default, nunca `NaN`); medido contra producción que la cola, no el modelo, era la espera (hasta 90 min con 360 turnos a 4/min). T0 (7/9/2026): `atender()` reclama con `cola.claimDue()` (`{ conversationId, vencioEn }`) y pasa `vencioEn` a `runAgentTurn`; los cuatro re-encolados (ritmo, cupo, lock, error) usan `cola.defer(...)` en vez de `enqueue`, para no perder el vencimiento original en cada reintento. T3 (7/9/2026): cada `defer` de ritmo/cupo/lock (no el de error) anota su plazo; al terminar una pasada con diferidos y sin continuación pendiente, `registrarDiferidos` programa un `setTimeout` (3/20/30 s + margen, `.unref()`) que reintenta con `maxPerRun()` —nunca con el `limit` de quien la disparó— y puede encadenarse a sí mismo si vuelve a dejar diferidos; estado de módulo con tres valores (`inactiva`/`programada`/`corriendo`) para que una pasada ajena no duplique el timer mientras la continuación corre; `stopAgentQueue` la cancela; `resetContinuacionParaPruebas` solo para tests. Antes, un turno frenado por ritmo (20 s) solo lo reclamaba el próximo webhook ajeno o el cron. Una pasada que agota su tope SIN diferir nada no programa continuación: ese resto lo drena el cron (cada minuto, 30 por pasada = el mismo ritmo que `AGENT_MAX_TURNS_PER_MINUTE`) |
| `redis-queue.ts` | La cola en Redis con scripts Lua atómicos: cupos globales, ritmo, sweep lock. T0 ("La respuesta llega en siete segundos", 7/9/2026): `CLAIM_SCRIPT` devuelve `[conversationId, vencimiento]` —el guardado en `liminal:agent:vencimiento:{id}` por un reclamo anterior o, si no, el score crudo, que además siembra esa clave (TTL 1 h) para el próximo reclamo—; `claimDue()` devuelve `{ conversationId, vencioEn }`; nueva `defer(id, seconds)` reencola un reintento del SISTEMA conservando (NX) el vencimiento más viejo si la conversación sigue en la cola; `enqueue` borra esa clave (mensaje nuevo del cliente = ventana nueva) y `purge` la barre con SCAN |
| `conversation-lock.ts` | Un solo turno de IA por conversación, con lease de 90s en `ai_turn_lock_until` + dueño en `ai_turn_lock_token`, renovado cada 30s por el propio turno y soltado solo por quien lo tomó; un proceso que muere deja la conversación libre en ≤90s en vez de para siempre; encontrarlo tomado ya no es silencio, es `ConversationBusyError` |
| `turn-target.ts` | Identidad congelada del turno: a qué chat/cliente se le habla; cierra el riesgo de cruzar respuestas |
| `turn-delivery.ts` | Barrera contra el doble envío: un turno que ya respondió no se reintenta |
| `reconciler.ts` | La red de seguridad del lado de Postgres: si Redis perdió un turno, nadie lo reencola nunca porque el único que encola es el webhook. Busca en la base lo que sigue esperando, descarta lo que ya está en cola, con lock vigente o atendido por una persona AHORA, y reencola hasta 50 por pasada. Confirmado en T0.3 (5/9/2026): el predicado nunca lee `conversation_handoffs` (ni `to_kind` ni `reason`), solo columnas de `conversations` — una huérfana por `rechazado_por_meta` se rescata igual que cualquier otra, sin importar el motivo que la dejó así. T7 (corrida "La IA ve lo que llega", 8/9/2026): el `select` suma `last_customer_message_at`, y `conversationsWrittenByHumans` recibe `{id, lastCustomerMessageAt}` de cada candidata sin lock — una nota de un asesor de hace semanas ya no la saca del reconciliador para siempre; solo bloquea si el asesor se adelantó al cliente o escribió hace menos de `AI_HUMAN_GRACE_MINUTES` |
| `handoffs.ts` | La bitácora de traspasos: cada salida silenciosa (turno, cola, webhook) deja dicho a quién pasó la conversación y por qué. `recordHandoff` para quien ya tiene cliente; `recordHandoffAdmin` se fabrica el suyo. NUNCA lanzan: registrar el traspaso no puede tumbar un turno. `HandoffReason` suma (T0.3, 5/9/2026) `escalada`/`escalada_sin_asesor` (las usa `escalate.ts`) y `rechazado_por_meta` (la usa `agent.ts` cuando Meta rechaza un envío que ya pasó todas las guardas de `deliver()`); T2.1 (5/9/2026) suma `cerrada_por_asesor`/`reabierta_por_asesor` (las rutas `api/conversations/[id]/close` y `.../reopen`) y `reabierta_por_cliente` (el webhook, cuando reabre sola una conversación cerrada). T4 (8/9/2026, "La IA ve lo que llega") suma `sin_contenido_legible`: el turno arrancó sin nada legible para el modelo tras describir la media con `historyLine` (T2) — caso `cea69118…` — y `agent.ts` la usa en el `return` por historial vacío de `runTurnPhases` |
| `rate-limit.ts` | El único cuello hacia el proveedor del modelo: cuenta PETICIONES, no turnos. 7/9/2026: defaults subidos junto con `queue.ts` — `AI_MAX_CONCURRENT_REQUESTS` 3→12, `AI_MAX_REQUESTS_PER_MINUTE` 15→120; el techo de 20/min que justificaba el 15 era de la cuenta gratuita de OpenRouter (confirmado contra `/api/v1/key` de producción: `is_free_tier: false`, `limit: null`). Un turno gasta ≈3,4 peticiones, así que este tope y el de turnos se suben siempre juntos |
| `classify.ts` | Fase 1: clasificación barata de intención; decide qué herramientas recibe el modelo |
| `playbooks.ts` | Fase 0: reconocimiento de escenario (respuestas predeterminadas del supervisor, enviadas tal cual; no se repiten en 6h). B3 (5/9/2026): `buildPrompt` y `matchPlaybook` reciben `businessHours` (4.º parámetro, con default) y la línea de fecha del prompt de escenarios suma "— franja: tarde" más horario y estado calculados con `dayBand`/`describeSchedule`/`businessStatus`, el mismo criterio que el flujo genérico. `ZERO_USAGE` exportada (8/9/2026): `agent.ts` la reusa para el "sin escenario, sin costo" cuando salta `matchPlaybook` porque el último mensaje del cliente es un marcador de media |
| `prompt.ts` | Identidad y reglas del agente: un solo bloque idéntico en todos los turnos (cacheable). B3 ("El reloj dice la verdad", 5/9/2026): `TurnContext` gana `businessHours` (default `DEFAULT_BUSINESS_HOURS`) y el bloque `TURNO ACTUAL` ya no lleva la hora en texto sino `turnClockLine` (`business-hours.ts`): franja, saludo, horario de atención y si la tienda está abierta, ya calculados — la sección 6 del `SYSTEM_PROMPT` dice "usa la franja y el saludo tal cual" y la regla vieja "Saber la hora no es saber el horario" se reemplazó por "el horario te llega en TURNO ACTUAL, no inventes otro"; la sección 5.1 suma la regla del operador: fuera de horario se sigue vendiendo, y si el cliente quiere comprar con la tienda cerrada se le explica el paso a ventas y se nombra el horario regular. `SYSTEM_PROMPT` sigue siendo prefijo exacto; el umbral de "sufijo corto" del test subió de 120 a 150 tokens estimados. 8/9/2026 (S2, "La IA ve lo que llega"): nueva sección `7. LO QUE TE LLEGA SIN TEXTO`, exportada aparte como `MEDIA_RULES` e interpolada dentro de `SYSTEM_PROMPT` (sigue siendo prefijo cacheable, no sufijo) — le dice al modelo qué hacer con los marcadores entre corchetes que arma `history-line.ts` (foto/video sin adivinar, pedir el pie si trae, nota de voz por escrito, sticker sin comentar, documento a un asesor) sin explicar nunca por qué no puede ver ni escuchar. El test la pasa sola por `revealsIdentity` (`identity-guard.ts`) y confirma que da `null` |
| `tools.ts` | Herramientas del tool loop: catálogo, historial de pedidos, escalar; topes de resultados. `EscalationOutcome` gana `unassigned?: boolean` (anexo A1, 5/9/2026), que `buildEscalateTool` copia de `result.unassigned`: es lo que le dice a `agent.ts` si la despedida que va a mandar el modelo se quedó sin nadie detrás. B4 (5/9/2026): `ToolDeps` gana `businessHours?`/`now?` (los usa solo la herramienta de escalar) y `unassignedEscalationInstruction(status)` traduce el `businessStatus` que devuelve `escalateConversation` a la instrucción con la que el modelo redacta la despedida sin asesores — abierta → "en breve" sin prometer plazo; cerrada → "un asesor te escribe el lunes a partir de las 8:00 am"; todo cerrado → "apenas la tienda vuelva a abrir". Sigue siendo `is_auto_reply` (no apaga `awaiting_reply`); con asesor asignado la instrucción no cambia. D3 ("Lo pequeño que quedó atrás", 6/9/2026): si la consulta de catálogo o de historial devuelve `error`, `log.error` deja `herramienta_catalogo_fallo`/`herramienta_historial_fallo` con `conversationId` y `detail` antes de devolverle al modelo el "No se pudo consultar…" de siempre; antes ese error se tragaba en silencio |
| `agent-tools.ts` | Interruptores por herramienta, en la base, cambiables en vivo |
| `catalog-search.ts` / `knowledge-search.ts` | La parte que se equivoca en silencio: cómo se arma la búsqueda en catálogo/biblioteca |
| `knowledge.ts` | Consulta de la biblioteca (políticas, garantías, horarios): información para REDACTAR, no respuestas. D3 (6/9/2026): recibe `conversationId` y, si la consulta devuelve `error`, deja `herramienta_biblioteca_fallo` en el log antes de responder "No se pudo consultar la biblioteca…" |
| `escalate.ts` / `claim-agent.ts` | Escalamiento a humano; reclamo atómico del asesor menos recién asignado. Desde T0.3 (5/9/2026), tras el `update` de la conversación y la nota interna, `escalateConversation` deja su propio traspaso en `conversation_handoffs`: `human`+`toId` con razón `escalada` si hubo candidato, `unassigned` con razón `escalada_sin_asesor` si no. B4 ("El reloj dice la verdad", 5/9/2026): `escalateConversation` gana `businessHours`/`now` (defaults: horario por defecto y reloj real), calcula `businessStatus` UNA vez, sufija el evento de sistema sin asesores con "(en horario)"/"(fuera de horario)" y devuelve ese estado en el resultado (`unassigned`) para que `tools.ts` arme la despedida sin recalcularlo con otro reloj |
| `human-handled.ts` | La IA no entra donde una persona atiende el chat AHORA (sin `server-only` a propósito: lo usa `data.ts`). T7 (corrida "La IA ve lo que llega", 8/9/2026): dejó de preguntar "¿alguna vez escribió un asesor?" —47 de 48 conversaciones mudas medidas el 8/9 tenían un humano que había escrito ALGUNA VEZ, caso `3b654d2c…`, un "a" del 28/8— y pasó a `humanClaimsChat(lastHumanAt, lastCustomerMessageAt, now, graceMinutes)`, función PURA que comparten `humanHasWritten` (consulta individual, `select("created_at")...order().limit(1)`) y `conversationsWrittenByHumans` (lote, `select("conversation_id, created_at").in(ids).eq("agent").gt("created_at", umbral)` con `umbral = min(now − G, min lcma)`, UNA sola consulta): bloquea si el humano escribió DESPUÉS del último mensaje del cliente (`created_at > lastCustomerMessageAt`) o si escribió hace menos de `G` minutos (`AI_HUMAN_GRACE_MINUTES`, `humanGraceMinutes()`, default 30). `lastCustomerMessageAt` null → `false` sin consultar. Sigue fallando CERRADO |
| `identity-guard.ts` | Guarda de identidad (6/9/2026, módulo PURO, sin imports, sin `server-only` a propósito —comparte el rasgo con `human-handled.ts`: la reusa `mutations.ts` desde el navegador). `revealsIdentity(text)` normaliza a minúsculas sin diacríticos y devuelve `null` o `{categoria: "automatizacion" \| "persona", fragmento}`; ancla los patrones en la AUTORREFERENCIA ("soy un…", "asistente automatizado", "no soy una persona") y en términos sin otro uso en el negocio (`bot`, `chatbot`, `inteligencia artificial`, `IA` en mayúsculas sobre el texto ORIGINAL —así `guía`/`GUIA`/`AUTOASIA` no calzan—, `ChatGPT`, `OpenAI`, `GPT`, `Gemini`, `modelo de lenguaje`), nunca en `automátic*`/`digital`/`sistema` sueltos: en `products` hay 25 filas como `AUTOMATICO HORSE` o `TACOMETRO DIGITAL BERA SBR` que esos adjetivos sí nombran de verdad. `rewriteSuffix(fragmento)` arma el pedido de reescritura que se pega DESPUÉS de `SYSTEM_PROMPT` (prefijo exacto intacto, el caché no se rompe). Medido en `messages` antes de la guarda: 34/68 mensajes el 26/8/2026 y 26/151 el 27/8/2026 se describían como "asistente automatizado"; con el prompt nuevo, 0/702 el 28/8/2026 — la guarda es la red para cuando la regla del prompt falla igual |
| `greeting-window.ts` | El saludo lo decide el reloj de Barinas, no el mensaje (falla real del 27-08: "¡Buenos días!" a las 10 pm). Desde B2 (5/9/2026) sus bordes de franja vienen de `DAY_BANDS` (`business-hours.ts`), no son suyos: una sola definición de mañana/tarde/noche para escenarios y turno genérico |
| `send.ts` | Envío de las respuestas del turno (texto redactado o escenario con adjunto). `sendAgentText`/`sendAgentMedia`/`sendPlaybookReply` devuelven (T0.3, 5/9/2026) el `DeliveryOutcome` en vez de `Promise<void>` — `sendPlaybookReply` propaga el del TEXTO, no el del adjunto — para que `agent.ts` pueda mirar `whatsapp_status` y registrar el traspaso cuando Meta rechaza un envío. `sendAgentText` gana `opciones?: { isAutoReply?: boolean }` (anexo A1, 5/9/2026): con `isAutoReply: true` el insert lleva `is_auto_reply: true` (misma marca que la bienvenida automática, T0.1), así que el trigger `handle_new_message` no lo cuenta como respuesta real. Default `false` para los tres llamadores existentes (`agent.ts` ×2, `sendPlaybookReply`). S6 (corrida "La IA ve lo que llega", hallazgo 4, 8/9/2026): `DeliveryOutcome` suma `origenDelFallo: "meta" \| "red" \| null` — `entregar()` lo decide por el TIPO de excepción del `catch` (`MetaApiError` → `"meta"`, cualquier otra cosa como `TypeError: fetch failed` → `"red"`), no por si trae código numérico. `origenDelFallo` NO es columna de `messages`: el nuevo helper `columnasDeEntrega(entrega)` reemplaza el `...entrega` que esparcía el objeto entero en los dos inserts (`sendAgentText`/`sendAgentMedia`) por las cuatro columnas explícitas de siempre, así que sumarle campos a `DeliveryOutcome` no puede volver a colarse en la base por accidente |
| `precio.ts` | Formato de precios en código, no en el prompt: la aritmética no se le confía al modelo |
| `model.ts` | Selección de proveedor/modelo por `.env` (prod: OpenAI-compatible vía `OPENAI_BASE_URL`, hoy OpenRouter con `gpt-5.6-luna`; dev: Gemini); clasificador puede ir aparte. `AI_AGENT_REASONING` (S6, 8/9/2026, default `on`): `build()` solo adjunta `providerOptions.openai.reasoningEffort` si el proveedor es OpenAI Y la variable no es exactamente `"off"` — `gpt-5.6-luna` no razona (es un alias de OpenRouter) y mandarle el parámetro igual solo producía el warning del AI SDK `reasoningEffort is not supported` 3-4 veces por turno sin aplicar nada (medido 7/9/2026). Gobierna el proceso, no el nombre del modelo: es una decisión de `.env`, no una heurística sobre el id. T2 ("La respuesta llega en siete segundos", 7/9/2026): su test fija la trampa — un id de OpenRouter con prefijo de fabricante (`google/gemini-3.1-flash-lite`) NO empieza con `gemini`, así que sale por el proveedor `openai` (`OPENAI_BASE_URL`), que es lo que se quiere; si `resolveProvider` reconociera el prefijo `google/`, el caso se pone en rojo |
| `bcv.ts` / `bcv-fetch.ts` / `bcv-intermediate-ca.ts` | Tasa oficial del BCV leída de su web; con el certificado intermedio que su servidor no manda. Tests: `bcv.test.ts` (parseo contra el fixture real de `__fixtures__/bcv-home-2026-08-28.html`, con `it.todo` del defecto D1: punto decimal → tasa ×100), `bcv-cache.test.ts` (caché/refresco/respaldo `isStale`), `bcv-fetch.test.ts` (TLS/HTTP sin red) |
| `__fixtures__/` | Capturas literales de bcv.org.ve, sin editar; `.gitattributes` en la raíz las protege de la normalización CRLF/LF (las regex del parseo miden distancias en caracteres) |
| `pgrst.ts` | Entrecomillado seguro para filtros `.or()` de PostgREST (anti-inyección) |
| `fake-redis.ts` | Redis en memoria SOLO para pruebas que no dependen de la atomicidad Lua. T0 (7/9/2026): suma `zscore`, `get` y `scan` (para `defer`/`purge`) y el `CLAIM_SCRIPT` reconocido devuelve `[member, vencimiento]`, sembrando/leyendo la misma clave de vencimiento que el script real |
| `queue-continuation.test.ts` | Segundo archivo de test de `queue.ts` (T3, 7/9/2026): la continuación automática de la cola con `FakeRedis` + `vi.useFakeTimers()` — programa exactamente una, elige el menor plazo, no se solapa, `stopAgentQueue` la cancela, un error no la dispara, y la simulación de 100 conversaciones sin cron que documenta que una pasada que agota su tope sin diferir no la programa |

## `src/lib/supabase` y `src/lib/whatsapp`

| Archivo | Qué es |
|---|---|
| `supabase/admin.ts` | Cliente service_role (bypassa RLS); solo rutas de servidor sin sesión |
| `supabase/client.ts` / `server.ts` | Clientes anon para navegador / Server Components |
| `supabase/middleware.ts` | Sesión desde la cookie sin preguntarle a GoTrue en cada petición; redirecciones de acceso |
| `supabase/database.types.ts` | Tipos generados del schema (a mano cuando `supabase gen types` no está disponible; ver T0.2, 5/9/2026, que le agregó `conversations.last_reply_at`/`last_reply_sender` y `messages.is_auto_reply`; T1.7, mismo día, le agrega `contacts.search_text` solo en `Row` —generada, no se puede insertar/actualizar—, igual que ya está tipado `products.search_text`; T3.4, mismo día, le agrega a `whatsapp_channels` las cuatro columnas de salud: `quality_rating`, `messaging_limit`, `account_restrictions`, `health_updated_at`; T3.2, mismo día, le agrega `messages.payload` y `conversations.referral`, ambas `Json | null`). B3 (5/9/2026): `agent_settings.business_hours: Json` en Row/Insert/Update (migración 20260906010000). T3a (9/9/2026): tabla `stickers` (migración 20260909020000), insertada alfabéticamente entre `sinonimos_busqueda` y `tags` |
| `whatsapp/meta-client.ts` | Cliente server-only de la Graph API: texto, plantillas, multimedia, reply, descarga de media. `markWhatsappRead`/`sendTypingIndicator` (T3.1, 4/9/2026, doble check azul y "escribiendo…") NUNCA lanzan —a diferencia del resto del archivo, que sí lanza `MetaApiError`—: atrapan su propio fallo con `log.warn` y listo, porque un check o un typing que no salió no puede tumbar la bandeja ni el turno de la IA. Las dos comparten `notifyGraphApi` y el mismo "message_id" que exige Meta: el wamid del mensaje ENTRANTE al que se está respondiendo. `sendWhatsappTemplate` gana `components?` (T3.3, 5/9/2026, tipo `WhatsappTemplateComponent[]` importado de `whatsapp/template-variables.ts`): solo lo agrega al payload si viene con al menos un elemento —Meta rechaza `components: []` en una plantilla sin parámetros—. T3a (9/9/2026): `MediaKind` suma `"sticker"`; su payload es `{link}` SIN `caption` —Meta lo rechaza si lo lleva, probado contra la Cloud API—, mismo trato que ya tenía `audio` |
| `whatsapp/template-variables.ts` | T3.3 (5/9/2026): variables posicionales `{{1}}`, `{{2}}`... del cuerpo de una plantilla — puro y compartido a propósito entre el selector (cliente, para saber cuántos campos pedir y renderizar la vista previa) y `api/messages/send` (servidor, para armar `components` y sustituir el `content` que se guarda): `templateVariableCount`, `substituteTemplateVariables`, `buildTemplateBodyComponents` |
| `whatsapp/failure-reason.ts` | Del código de error de Meta a una frase accionable para el asesor. Tabla completa (T3.3, 5/9/2026): opt-out/bloqueo (131050), límite de ritmo por número (131056) y de cuenta/app (130429/80007/4), la plantilla misma (132012/132015/132016, además de 132000/132001 que ya estaban), y cuenta/número restringidos (131037/131031/368/133010) o con límite por mala calidad (131064). `failureAction(code)` es nuevo: la única acción con gesto propio hoy es 131047 (ventana vencida) → `"abrir_plantillas"`, que `message-bubble.tsx` usa para ofrecer el botón que abre el selector. D4 (6/9/2026): la frase de 131026 pasa de "confirma el número" a decirle al asesor qué mirar primero —el chat, por si hay un aviso de cambio de número— antes de asumir que el cliente desapareció; `failureAction(131026)` sigue en `null` porque el botón para abrir la conversación del número nuevo necesita que la burbuja conozca ese número (deuda, otra corrida) |
| `whatsapp/phone.ts` | Qué cuenta como número escribible (la falla del `+undefined`) |

## `src/components`

**Shell:** `crm-shell.tsx` (cliente raíz de la bandeja: estado, realtime, outbox, navegación — 26K, el componente más cargado; la paginación de "Todos" es `useInboxPager` sembrado con la primera página que resuelve el servidor — el shell ya no guarda `cursorRef` ni banderas propias, una ráfaga de scroll disparaba varias consultas con el mismo cursor; `livePulse` (A.T6b, 29/8/2026) es un contador que sube dentro de `fetchInboxHead` cada vez que el refresco en vivo trae una cabecera fresca de `conversations`, y viaja como prop a `InboxSidebar` — es el eco que le avisa a las píldoras que resuelven contra la base ("Pendientes" desde el 30/8/2026, "No leídas" y "Mías") que algo cambió sin que necesiten su propio canal de realtime; la prop de siembra es `initialPendingConversations`, que el shell pasa como `initialPendingRows` a `InboxSidebar`; T1.1, 4/9/2026: el canal `messages-{id}` consulta `read-on-arrival.ts` en cada INSERT entrante — si el chat abierto no está de verdad al frente y con foco, anota `pendingRead` en vez de marcar leído, y un oyente de `visibilitychange`/`focus` del mismo efecto lo suelta una sola vez al volver, siempre que el chat siga siendo el mismo — cambiar de conversación limpia el pendiente del chat viejo al desmontar sus oyentes; T3.1, mismo día: junto a `markConversationRead` —al abrir un chat apartado/con mensajes sin leer, y en `markReadNow()`— llama también a `sendReadReceipt` (`mutations.ts`), el doble check azul hacia Meta; NO se llama desde el apartado/desapartado a mano de `markUnread`/`markRead` del menú, que no prueban que el asesor haya mirado nada), `app-rail` (navegación entre secciones), `url-search-box` (buscador sincronizado con la URL), `sliding-pills` (píldoras de filtro), `context-menu`, `theme-toggle`, `section-skeleton`, `sbk-logo`, `crm.css` (49K, estilos del CRM).

**chat/:** `chat-panel` (historial + composer; reenvía `messages` al `composer` desde T2 "La ventana de 24 h dice la verdad", 7/9/2026; cabecera gana el botón "Cerrar"/"Reabrir" T2.1, 5/9/2026 — `closeConversation`/`reopenConversation` de `mutations.ts`, misma acción que el menú contextual de la bandeja; T3.3, 5/9/2026: guarda `templateModalSignal`, un contador que sube cada vez que una burbuja pide abrir el selector de plantillas —botón "Abrir plantillas" de `message-bubble` en un envío rechazado con 131047— y se lo pasa a `composer` como `openTemplateModalSignal`; T3.2, mismo día: pinta el banner "Llegó desde el anuncio…" bajo la cabecera con `isReferralFresh(conversation.referral)` —pasadas 72 h desde `referral.receivedAt` deja de mostrarse aunque la fila se conserve—, `.crm-referral-banner` en `crm.css`), `composer` (texto, adjuntos, quick replies, plantillas — 19K; T2 "La ventana de 24 h dice la verdad", 7/9/2026: recibe la prop `messages` y `withinWindow` pasa a `isComposerWindowOpen(lastCustomerMessageAt, messages)` en un `useMemo` —un saliente rechazado con 131047 sin entrante real posterior deshabilita la caja, muestra el placeholder de plantilla y esconde `WindowCountdown` aunque `lastCustomerMessageAt` diga que quedan horas [caso real `aa75ef33…`, 6/9/2026]; dos casos nuevos en `composer.test.tsx`; T3.1, 4/9/2026: "escribiendo…" hacia Meta vía `sendTypingSignal` —`mutations.ts`—, un intervalo fijo de 20 s mientras haya texto, arrancado y detenido por la PRESENCIA de texto y no por su contenido; no usa `use-debounced-callback.ts`, un debounce trailing-edge nunca dispararía mientras el asesor sigue tecleando sin pausa, que es justo lo contrario de lo que hace falta; T3.3, 5/9/2026: `openTemplateModalSignal?: number` — un pedido de abrir el selector que viene de AFUERA, de una burbuja fallida; se compara contra la referencia del valor anterior, no contra `> 0`, para tolerar cualquier valor inicial y para que dos pedidos seguidos abran el modal las dos veces; `handleSelectTemplate` pasa las `variables` a `sendTemplateMessage`), `message-bubble` (T3.3, 5/9/2026: con `whatsappStatus === "failed"` y `failureAction(whatsappErrorCode) === "abrir_plantillas"` —hoy solo 131047— pinta un botón "Abrir plantillas" que llama a la prop `onOpenTemplatePicker`, opcional: sin ella la burbuja se queda con el motivo en palabras, como antes; T3.2, mismo día: `messageType === "interactive"` pinta el chip "Respondió a: {título}" —recortado del `content` que ya arma el webhook, no duplica el texto crudo— y `messageType === "order"` pinta `OrderCard` con los ítems y el total de `payload.productItems`; en los dos casos se omite el `content` en prosa para no repetir la misma frase dos veces; S3b, "El cliente que cambió de número", 6/9/2026: `messageType === "unsupported"` —D3, los tipos que Meta no entrega por la API: encuesta, foto de ver una vez, evento…, guardados con `content` null— pinta un marcador fijo en vez de una burbuja vacía, y una segunda línea atenuada con `payload.type` cuando viene; sin lógica de datos nueva, `payload` ya viajaba desde T3.2; T3a, 9/9/2026: gana la prop opcional `agent?: Agent` —`chat-panel.tsx` le pasa `currentAgent`— que reenvía a `message-context-menu` para que pueda ofrecer "Guardar sticker" con dueño), `outbox-bubble` (en cola/fallido con reintento), `media-group` + `media-lightbox` (galerías), `formatted-text` (negritas estilo WhatsApp), `quoted-content` (cita), `message-context-menu` (T3a, 9/9/2026: con `messageType === "sticker"`, `mediaUrl` presente y un `agent` conocido —prop opcional, sin ella la opción no se ofrece— pinta "Guardar sticker", que llama a `saveStickerFromMessage` con un `createClient()` propio, no uno recibido por prop; toast "Sticker guardado"/"No se pudo guardar el sticker", mismo criterio que `copiarImagen`: el fallo se dice, no se cierra el menú como si hubiera funcionado), `template-picker-modal` (reabrir fuera de 24h; T3.3, 5/9/2026: detecta `{{n}}` en `body_preview` con `templateVariableCount` —`whatsapp/template-variables.ts`—; una plantilla sin variables se envía directo, con variables abre un paso intermedio con un campo por cada una y sugerencias que lo llenan de un clic —el nombre del contacto siempre; el último repuesto cotizado en el chat si `fetchConversationQuotes` devuelve alguno, la misma consulta que ya usa `sale-items-editor.tsx`, no una nueva— y una vista previa con `substituteTemplateVariables`; una plantilla `status <> 'approved'` queda deshabilitada con un `Tooltip` que explica por qué), `quick-replies-modal`, `window-countdown` (cuenta atrás de 24h; C2, "El reloj dice la verdad", 5/9/2026: el redondeo vive en `window-countdown-format.ts` —`formatWindowRemaining`—, que redondea los MINUTOS TOTALES una sola vez y de ahí deriva horas y minutos, para que el acarreo empuje a la hora siguiente en vez de mostrar "23h 60m"; tests `window-countdown-format.test.ts` [tabla de casos, sin jsdom] y `window-countdown.test.tsx` [el borde 23h 59m 40s renderizado]), `delivery-check` (palomitas; T3.2, 5/9/2026: `whatsappStatus === "played"` —nota de voz reproducida— lleva ícono y etiqueta "Reproducido" propios, distintos del doble check azul de "Leído"), `ai-status-banner`.

**inbox/:** `inbox-sidebar` (C1, "El reloj dice la verdad", 5/9/2026: `unassignedIds` es un `useMemo` con los ids de `resolvedRows` SOLO cuando la píldora activa es "Sin dueño" —null mientras esa consulta viaja— y viaja a `applyInboxFilters` para que la lista de "Sin dueño" sea la que resolvió la base y no la ventana local entera; seis píldoras Pendientes/Sin dueño/Escaladas/No leídas/Mías/Todos —"Escaladas" sube de cinco a seis el 5/9/2026, T1.5—, abre en "Pendientes" desde la tercera reforma del 30/8/2026 (medida contra producción: "No leídas" resultó subconjunto ESTRICTO de "Pendientes", 282 filas contra 51, dejando 231 chats leídos-y-sin-responder sin ninguna píldora que los alcanzara); secciones por píldora vía `inbox-sections` — "Pendientes" y "Mías" se parten por LECTURA, "No leídas"/"Todos" van en una sola sección, ninguna corta por ventana de 24h (eso sigue siendo terreno de `dashboard.ts`); "Pendientes"/"No leídas"/"Mías"/"Escaladas" se resuelven contra la base vía `pillQueryOptions` — función de módulo con `switch` exhaustivo sobre `InboxFilter` (T1.5, 5/9/2026: suma `case "escalated"` → `escalatedOnly`, mismo predicado que el `case "escalated"` de `matchesFilter` en `inbox-filters.ts`) (reforma del 30/8/2026: reemplazó al ternario de dos ramas que, al sumar `pending` al union, habría mandado la píldora nueva a la rama `assignedTo` en silencio —pidiendo la cola de un asesor en vez de la del equipo— sin que `tsc` avisara; un `switch` sin `default` sí lo caza: falta un `case` y la build no compila) que arma `activeOnly`+`awaitingReplyOnly` para "Pendientes", `unreadOnly` para "No leídas", `assignedTo` para "Mías"; gana el parámetro `sort` (T1.3, 5/9/2026, píldora "Más antiguas"): viaja en `page` como `order` para las cuatro ramas y entra también al `sessionKey` de `useInboxPager` (junto a la etiqueta activa) — con `sort === "oldest"` hasta "Todos" se resuelve en el servidor sin necesitar etiqueta, misma vía que ya usaba la etiqueta activa, porque la ventana cargada localmente solo tiene las conversaciones más recientes, nunca la más vieja de toda la base; `initialPendingRows` siembra la primera para que la bandeja abra con datos y no con "Buscando…"; contador de píldora desde la base (`counts.pending`/`counts.unread`); el buscador salta a "Todos"; paginación por cursor con `useInboxPager` (`src/lib/use-inbox-pager.ts`), el mismo paginador que usa "Todos" en el shell: `serverRows` guarda solo `{filter, rows}` y el cursor, el candado, la sesión y `reachedEnd` viven en el hook — las tres carreras del 29/8/2026 se cierran allá y se prueban allá; un fallo de la primera página (`pager.status === "error"`, A.T5) pinta "No se pudo traer la bandeja." con botón Reintentar en vez de festejar "Todo leído", sin borrar filas ya pintadas; un fallo de la página SIGUIENTE (`pager.lastPageFailed`, A.T4) cambia el botón "Cargar más conversaciones" del pie por un aviso angosto con Reintentar que llama `pager.loadMore` — no `retry`, que es solo para la primera página —, en las cuatro píldoras: "Todos" recibe la bandera como prop `lastPageFailed` desde `allPager.lastPageFailed` en `crm-shell.tsx`, mismo pager que ya presta `hasMore`/`onLoadMore`; `serverRows` era invisible para realtime (A.T6b, revisión de código del 29/8/2026): lo que hace ESTE asesor lo cubre `patchServerRows`, pero lo que hace OTRO —leer el chat, que se lo reasignen— llegaba por el canal de "Todos" en `crm-shell.tsx`, que no toca esta consulta aparte, así que una fila vieja sin leer podía quedarse pintada de más o de menos hasta reentrar a la píldora. El eco del pulso vivo del shell (prop `livePulse`) lo cierra: un efecto que, solo con la píldora de servidor activa y ya con filas cargadas, vuelve a pedir la cabecera de ESA píldora sin cursor y la reconcilia con `reconcileHead` (`inbox-paging.ts`) contra `current.rows` — con `freshIsComplete` SIEMPRE en `false`, porque esa cabecera nunca es el conjunto entero, solo el tamaño de una página, y tratarla como completa borraría las páginas profundas que el asesor bajó; descarta la respuesta si la píldora cambió mientras viajaba; no toca `serverPager`, es una consulta aparte de su candado/cursor/`reachedEnd`), `conversation-list-item` (gana T1.6, 5/9/2026: solo con `awaitingReply(conversation)` —`dashboard.ts`— pinta la píldora `.crm-thread-window` con `hoursUntilWindowCloses(lastCustomerMessageAt)` —`whatsapp-window.ts`—, "18 h" neutro / "3 h" ámbar bajo 4h [corte más generoso que los 2h de `WindowCountdown` del chat: acá se ve de reojo, sin abrir el hilo] / "cerrada" en rojo con la ventana ya vencida, con `useClock` al minuto y los colores semánticos de `crm.css` [`--lm-wait-ink`/`--lm-hot-ink`], nunca el acento; DEUDA: el `title` con `failureReason` —`whatsapp/failure-reason.ts`— que pedía la tarea para `DeliveryCheck` en mensajes `failed` queda pendiente porque la fila [`ConversationSummary`/`BoardConversation`] no trae ningún dato de motivo de fallo, ni código ni `whatsappError` —eso solo vive en `Message`—, y agregarlo es una columna nueva fuera de esta tarea), `filter-scroller`, `tag-filter-menu`, `conversation-context-menu`, `agent-home-panel` (panel de inicio: Pendientes / Esperando +24 h / Tuyas / Sin dueño / Esperando asesor —esta última T1.5, 5/9/2026, mismo número que la píldora "Escaladas"; sin tinte de alarma, a diferencia de "Sin dueño": una escalación recién hecha es esperable— la ventana de 24h que ya no vive en la bandeja sigue viva acá), `bcv-rate-chip`. T1.2 (4/9/2026, scroll infinito, F7): `handleListScroll` (umbral de scroll a 200px del fondo) se reemplaza por un sentinel `.crm-list-sentinel` al final de las filas, observado con `IntersectionObserver` (`root` = `.crm-list` vía `listRef`, `rootMargin: "0px 0px 150% 0px"`) que llama a `pager.loadMore()` con página y media de anticipación — mismo `pager` (local o `serverPager`) que ya usan las cuatro píldoras; el efecto se recrea entero cada vez que cambia `pager` (la primera observación de un `IntersectionObserver` siempre entrega una entrada inicial, así que recrearlo al terminar una carga reobserva el sentinel y encadena páginas solas si la lista quedó corta); el botón "Cargar más" queda de red de seguridad para navegadores sin soporte. `INBOX_PAGE_SIZE` (`src/lib/data.ts`) sube de 30 a 50. T2.2 (5/9/2026, hasta tres chats fijados por asesor — `conversation_pins`): `inbox-sidebar.tsx` fija/desfija con `pinConversation`/`unpinConversation` (mismo patrón optimista que `markUnread`/`markRead` de `crm-shell.tsx`: mueve el estado local, y si la escritura falla —el trigger del cuarto pin, típicamente— el catch relee `fetchPinnedIds` para devolver la verdad, sin abrir un canal de realtime nuevo); pasa `pinnedIds` a `applyInboxFilters` y a cada `ConversationListItem` (pinta un ícono junto al nombre, `.crm-thread-pin`); el menú gana "Fijar"/"Desfijar" en `conversation-context-menu`, deshabilitado con tooltip cuando ya hay tres fijados y esta conversación no es una de ellas. La preferencia de píldora y orden (`filter`/`sort`) se recuerda en `localStorage` bajo `sbk:inbox:{agentId}`: se lee UNA sola vez al montar (nunca en el render — el SSR de este "use client" no tiene `localStorage`) y se escribe en cada cambio, las dos veces con `try/catch` — un `didLoadPrefsRef` hace que el efecto de escritura se salte su primera pasada, para no clobbear el valor guardado con los defaults del primer render antes de que el efecto de lectura alcance a restaurarlo.

**context-panel/:** `context-panel` (ficha del contacto junto al chat), `close-sale-modal` (cierre de venta + datos de cédula/dirección), `sale-items-editor` (carrito), `manage-tags-modal`.

**agent-control/:** `agent-control-view` (42K — la vista más grande del proyecto: interruptores, métricas, simulador, tarifas; B5, 5/9/2026: pinta `BusinessHoursPanel` debajo de `SpendCapPanel` con el mismo `canEdit` y `saveBusinessHours` → `updateBusinessHours`), `playbooks-panel` (escenarios; 6/9/2026: al guardar, un `PlaybookIdentityError` de `mutations.ts` muestra su mensaje en el toast en vez del genérico "No se pudo guardar el escenario."; su test `playbooks-panel.test.tsx` abre el formulario de verdad y prueba las dos ramas del `catch`, la de la guarda de identidad y la de nombre duplicado), `knowledge-panel` (biblioteca), `agent-tools-panel`, `spend-cap-panel` (tope de gasto), `business-hours-panel` (B5, "El reloj dice la verdad", 5/9/2026: siete filas lunes–domingo, cada una "Cerrado" o hasta dos franjas con `<input type="time">`; `validateDraft` local con el mensaje de error por fila —"El cierre tiene que ser después de la apertura"— porque `parseBusinessHours` solo sabe caer al default sin decir qué falló; D4, 6/9/2026: `validateDraft` también rechaza la segunda franja si empieza antes de que termine la primera —mensaje "Las franjas se solapan.", contiguas tipo `12:00`–`12:00` sí valen, y desordenadas dan el mismo error sin reordenarse solas— porque `businessMinutesBetween` sumaría dos veces los minutos en común y el tablero marcaría "Con asesor" en rojo antes de tiempo; sigue siendo deuda solo de la puerta de entrada del formulario, no de `parseBusinessHours`; vista previa con `describeSchedule`/`businessStatus` sobre el borrador, solo cuando es válido entero; `canEdit` igual que el tope de gasto, los demás lo ven en solo lectura; guarda con `updateBusinessHours` de `mutations.ts`; estilos `ac-hours-*` en `agent-control.css`; su test fija el reloj con `vi.setSystemTime` SIN `useFakeTimers()` completo, que colgaba los clics de `userEvent` con HeroUI), `channel-health-panel` (T3.4, 5/9/2026 — tarjeta "Salud del número": calidad/límite/restricciones de `fetchWhatsappChannelHealth`, "sin datos" hasta el primer webhook de Meta, puramente informativa), `agent-roster-panel` (reparto entre asesores), `agent-metrics-row`, `token-usage-chart`.

**dashboard/:** `dashboard-view` (A4, "El reloj dice la verdad", 5/9/2026: prop opcional `businessHours` con default `DEFAULT_BUSINESS_HOURS`, enhebrada a `buildJourney` y a `JourneyBoard`; la página la conecta con `fetchAgentSettings` desde B3), `journey-board` (recorrido del cliente; A4: la tarjeta pinta "espera 23 min"/"espera 3 h"/"espera 1 día y 2 h" con `waitingMinutes` —`formatWait`, local al componente— o, si es null, el `stageDetail` en gris con la misma `.dash-card-meta`; el punto rojo usa `isStalled` y lleva `title` "Umbral de esta etapa: N min" o "60 min en horario de atención" en "Con asesor"; ya no importa `minutesInStage`; test `journey-board.test.tsx` con `ResizeObserver` stubeado como en `sliding-pills.test.tsx`), `activity-chart`, `ticket-queue`, `ticket-stats`.

**sales/:** `sales-view`, `sale-detail-modal`. **clientes/:** `clientes-view`, `cliente-ficha`, `cliente-datos-panel`, `cliente-notas`, `cliente-etiquetas`. **inventario/:** `inventario-view`, `producto-fila`. **auth/:** `login-form`.

## `supabase/`

| Qué | Dónde |
|---|---|
| Migraciones (62, timestampeadas; recontadas el 9/9/2026 con `ls supabase/migrations/*.sql \| wc -l` tras sumar `20260909020000_stickers.sql` — la fila venía diciendo 61 desde T1 de "La IA ve lo que llega") | `migrations/` — regla: commit propio con `[migración]` en el título. El número es el CONTEO, no una secuencia: los archivos van por fecha (`20260830040000_…`), y leerlo como "la próxima es la 51" ya hizo que un plan pidiera crear `051_…`, que se habría ordenado antes que todo 2026 |
| Seed de demo (3 usuarios, 5 conversaciones) | `seed.sql` — **no va a producción** |
| Catálogo y escenarios para producción | `seeds/moto_catalog_seed.sql`, `seeds/ai_playbooks.sql` |
| Config del stack local | `config.toml` |
| Test de permisos de funciones `security definer` | `tests/permisos_funciones.sql` — falla si alguna queda ejecutable por `anon` fuera de la lista blanca (`is_agent`, `is_supervisor_or_admin`). Agrupa por a quién debe quedarle el acceso: `service_role` (cerradas también a `authenticated`), `authenticated`, y desde el anexo B1 (5/9/2026) un grupo "funciones de trigger" (`handle_new_message`, `handle_message_status_change`) que solo afirma que `anon`/`authenticated` no pueden ejecutarlas —sin exigir nada de `service_role`: Postgres no comprueba `EXECUTE` al disparar un trigger, así que ningún rol necesita el grant— |
| La invariante "ningún lead invisible", verificada contra la base | `tests/invariante_leads.sql` — proxy de Etapa 1 de la invariante (ver CLAUDE.md): cuántas conversaciones siguen esperando y quedaron sin dueño, contra `unassigned_waiting_count()`. Gemelo en TypeScript: `src/lib/invariante-leads-contrato.test.ts` (mismos casos, mismos nombres). Caso 7 (anexo A1, 5/9/2026): escalada sin asesores con la despedida de la IA guardada `is_auto_reply = true` — inserta el entrante del cliente y el saliente de la IA por separado para que sea el trigger `handle_new_message`, no un valor escrito a mano, quien deje `awaiting_reply` en `true`. Caso 8 (anexo A2, 5/9/2026): cerrada, reabierta por el cliente y CON asesor asignado — inserta un agente mínimo en `auth.users` (mismo patrón que `pins.sql`, dispara `handle_new_agent()`) para satisfacer la FK de `conversations.assigned_agent_id` y afirma que el traspaso `reabierta_por_cliente` con destino `human` NO cuenta como sin dueño. Caso 9 (anexo B2, 5/9/2026): escenario que escaló sin asesores cuyo texto nació con `is_auto_reply = false` (como sale de verdad un escenario `afterSend: "escalate"`, antes de saber si haría falta un asesor) — un bloque `do $$` propio, después de los inserts y antes de los traspasos, afirma primero que `awaiting_reply` es `false` (el bug que corrige B2), corre el mismo `update ... set is_auto_reply = true` que hace `runPlaybook`, y afirma que `awaiting_reply` vuelve a `true` — es el trigger de B1 (20260905070000) quien recalcula, no un valor escrito a mano |
| Bitácora de traspasos | `migrations/20260830040000_conversation_handoffs.sql` — tabla `conversation_handoffs` + `record_handoff()`. La razón es `text` con CHECK (no enum) porque las Etapas 2 y 3 la amplían (20260905010000 sumó `escalada`/`escalada_sin_asesor`/`rechazado_por_meta`; 20260905030000 sumó `cerrada_por_asesor`/`reabierta_por_asesor`/`reabierta_por_cliente`, T2.1); `record_handoff` sigue concedida SOLO a `service_role` — T2.1 (5/9/2026) trajo los botones de cerrar/reabrir, pero corren como `authenticated` a través de una ruta que recién abre un cliente admin para esa RPC puntual (`api/conversations/[id]/close`, `.../reopen`), no con un `grant` nuevo |
| Cerrar/reabrir una conversación (T2.1) | `migrations/20260905030000_handoff_reasons_cierre.sql` — solo amplía el CHECK de `conversation_handoffs.reason` (arriba); no toca `to_kind` (ya admitía `'closed'` desde 20260830040000) ni permisos. Código: `api/conversations/[id]/close/route.ts` y `.../reopen/route.ts` — sesión con `@/lib/supabase/server`, cliente admin (`@/lib/supabase/admin`) solo para `recordHandoff`, mismo patrón que `api/media/[...path]/route.ts`. El webhook (`webhooks/whatsapp/route.ts`) reabre sola una conversación `closed` cuando el cliente vuelve a escribir, ANTES de guardar su mensaje: evento "El cliente volvió a escribir" + `recordHandoff({reason: "reabierta_por_cliente", ...})`, cuyo destino decide en tres pasos desde el anexo A2 (5/9/2026): `ai` si la IA seguía encendida; si no, `human` + `assigned_agent_id` si el chat ya tenía asesor; solo sin ninguna de las dos, `unassigned` — antes de A2 se miraba solo `ai_enabled` y un chat con dueño y la IA apagada quedaba `unassigned` en la bitácora sin serlo de verdad. `openTurn` (`agent.ts`) hace el mismo giro: mira `assigned_agent_id` ANTES que `ai_enabled`, para que ese mismo chat registre `asignada`/`human` en vez de `pausada`/`unassigned` la próxima vez que le toque un turno. `mutations.ts` expone `closeConversation`/`reopenConversation` (fetch a esas rutas, no tocan Supabase directo); los ofrecen `conversation-context-menu.tsx` ("Cerrar conversación"/"Reabrir conversación", según `isConversationClosed`) y la cabecera de `chat-panel.tsx` (botón "Cerrar"/"Reabrir"). `crm-shell.tsx` no necesita canal nuevo: el UPDATE de `status` ya viaja por el outcome "applied" de `use-live-conversations.ts` |
| Última respuesta real, separada del último mensaje visible | `migrations/20260905010000_conversations_last_reply.sql` — T0.1 del plan "La bandeja que no pierde" (5/9/2026): suma `messages.is_auto_reply` y `conversations.last_reply_at`/`last_reply_sender`, y `awaiting_reply` (columna generada) pasa a compararse contra `last_reply_at` en vez de `last_message_at` — antes una nota interna, un evento de sistema, la bienvenida automática o un envío de la IA rechazado por Meta apagaban "esperando respuesta" sin que el cliente recibiera nada. `handle_new_message()` separa "visible" (mueve `last_message_*`) de "respuesta real" (mueve `last_reply_*`); `handle_message_status_change()` recalcula `last_reply_at`/`last_reply_sender` si un envío que la había apagado resulta rechazado por Meta después de insertarse. Recrea `conversations_free_unanswered_idx`/`conversations_pending_idx` con el mismo predicado, y suma `conversations_escalated_idx` (preparado para T1.5). La migración `20260905070000_auto_reply_recalcula` (anexo B1, 5/9/2026) amplía el trigger `on_message_status_changed` para que dispare también por `is_auto_reply` (no solo por `whatsapp_status`): marcar `is_auto_reply = true` sobre un mensaje YA insertado ahora hace recalcular `last_reply_at`/`last_reply_sender`/`awaiting_reply` igual que ya hacía un rechazo de Meta, y trae un backfill que corrige escalaciones sin asesor anteriores al anexo A1. Test: `tests/awaiting_reply.sql` (los ocho casos de T0.1 más los pasos 10 y 11 de B1, transacción con rollback) |
| Búsqueda de contactos sin acentos (F13) | `migrations/20260905020000_contacts_search_unaccent.sql` — T1.7 del plan "La bandeja que no pierde" (5/9/2026): columna generada `contacts.search_text` (`display_name`+`profile_name`+`phone_number`, sin acentos y en minúsculas) con índice trigram, mismo patrón que `products.search_text` (20260822100000). A propósito NO crea extensión ni función nuevas: `unaccent`, `pg_trgm` y el wrapper `public.immutable_unaccent(text)` ya existían desde esa migración (los tres viven en el esquema `public`, no `extensions`) y los reutiliza tal cual — es la misma función que ya usa `messages.search_text` (20260822110000). `searchConversationSummaries` (`src/lib/data.ts`) filtra `search_text.ilike.%<normalizado>%` con `normalizeForSearch` (`message-search.ts`) en vez del `ilike` directo contra las tres columnas sueltas que traía antes (ese filtro seguía sin acentos solo para lo ya cargado en memoria de la bandeja). Test: `tests/contacts_search.sql` (inserta "José", encuentra "jose"; transacción con rollback) |
| Entrantes completos: botones, listas, pedidos, anuncios, "reproducido" (T3.2) | `migrations/20260905050000_messages_interactive_payload.sql` — T3.2 del plan "La bandeja que no pierde" (Etapa 3, 5/9/2026): `messages.message_type` admite `'interactive'` (respuesta a botón/lista de un mensaje interactivo, y también el `type: "button"` de una plantilla), `'order'` (pedido del catálogo) y `'unsupported'` (lo que el CRM no sabe representar: `content` queda null y el tipo real de Meta va en `payload.type`, ya no una frase fija). `whatsapp_status` admite `'played'` (nota de voz reproducida) en `messages` Y en `conversations.last_message_status` (20260822060000, que lo recibe propagado); `whatsapp_status_rank()` (`create or replace`, no es `security definer`, no hace falta revoke) rankea `played` por encima de `read` para que `keep_whatsapp_status_moving_forward()` no dependa un "leído" tardío. Columnas nuevas `messages.payload jsonb` y `conversations.referral jsonb`. Test: `tests/whatsapp_status_forward.sql` (sent→delivered→read→played avanza, un `read` tardío no pisa `played`, `failed` sí pisa cualquiera; transacción con rollback) |
| Salud del número y estado de plantillas (T3.4) | `migrations/20260905060000_whatsapp_channel_health.sql` — T3.4 del plan "La bandeja que no pierde" (Etapa 3, 5/9/2026): suma a `whatsapp_channels` las columnas `quality_rating text`, `messaging_limit text`, `account_restrictions jsonb` y `health_updated_at timestamptz` (todo null hasta el primer webhook de calidad/cuenta). Amplía el CHECK `templates_status_check` para admitir `'paused'`/`'disabled'` además de `'approved'`/`'pending'`/`'rejected'`: `message_template_status_update` pasa una plantilla por esos dos estados cuando Meta la pausa/deshabilita por quejas repetidas, y sin ampliar el CHECK ese UPDATE se habría rechazado. Sin función `security definer` nueva: son solo columnas y un CHECK, no hace falta revoke/grant. Código: `webhooks/whatsapp/route.ts` atiende `message_template_status_update` (`templates.status` por nombre+idioma, `log.error` en `paused`/`disabled`/`rejected`), `phone_number_quality_update` y `account_update` (guardan en el canal resuelto por `resolveHealthChannel`, `log.error` cuando la calidad baja o llega una restricción). `fetchWhatsappChannelHealth` (`src/lib/data.ts`) + `ChannelHealthPanel` (`src/components/agent-control/channel-health-panel.tsx`) exponen la tarjeta "Salud del número" en Control de IA |
| El trigger de estado también recalcula por `is_auto_reply` (B1) | `migrations/20260905070000_auto_reply_recalcula.sql` — anexo "Bandeja que no pierde" B1 (5/9/2026): `handle_message_status_change()` (`create or replace`, sin revokes/grants nuevos: conserva el ACL que ya tenía desde 20260830010000) recalcula `last_reply_at`/`last_reply_sender` no solo cuando `whatsapp_status` pasa a `'failed'`, también cuando `is_auto_reply` pasa de `false` a `true` sobre un mensaje ya insertado — el caso de un escenario que escala sin asesores (B2) y de escalaciones históricas anteriores al anexo A1. El trigger `on_message_status_changed` se recrea disparando por `whatsapp_status` O `is_auto_reply`. Backfill: marca `is_auto_reply = true` en la despedida de escalaciones sin asesor, `journey_stage='assigned'`, sin asesor, abiertas, que hoy parecen atendidas (`awaiting_reply=false`, `last_reply_sender='ai'`) — tras aplicarla reaparecen en "Pendientes"/"Sin dueño", ver `docs/PRODUCCION.md` §2. Test: `tests/awaiting_reply.sql` (pasos 10 y 11) y `tests/permisos_funciones.sql` (grupo "funciones de trigger") |
| Pines de conversación (T2.2) | `migrations/20260905040000_conversation_pins.sql` — T2.2 del plan "La bandeja que no pierde" (5/9/2026): tabla `conversation_pins` (`agent_id`, `conversation_id`, `created_at`, PK compuesta) para hasta tres chats fijados por asesor. RLS `for all` con `is_agent() and agent_id = auth.uid()` (mismo patrón que el resto del esquema): cada agente lee y escribe SOLO sus propios pines — a diferencia de `conversation_handoffs`, esto es personal, no bitácora compartida. El tope de tres lo impone el trigger `conversation_pins_limit_before_insert` → `enforce_conversation_pins_limit()`, `security invoker` A PROPÓSITO (no definer): quien inserta ya puede contar sus propios pines por la misma RLS que protege la tabla, así que no hace falta saltársela; si alguien intentara fijar a nombre de otro agente, el trigger (corriendo con los privilegios de quien llama) contaría cero filas ajenas y dejaría pasar la cuenta, pero el `with check` de la política igual rechaza la fila — el trigger nunca es la única barrera, lo verifica `pins.sql`. Aun sin ser definer, lleva los DOS revokes de higiene (`from public` y `from anon, authenticated`): el EXECUTE de fábrica a PUBLIC que Postgres concede a toda función nueva no depende de si es definer o invoker, y 20260830020000 solo cambió el default de anon/authenticated, no el de PUBLIC. Sin grant: Postgres ejecuta funciones de trigger sin comprobar EXECUTE del rol que dispara la operación. Test: `tests/pins.sql` (el cuarto pin falla con el mensaje del trigger; otro agente ve 0 filas propias y no puede insertar a nombre de otro — RLS con `set local role authenticated` + `set local "request.jwt.claim.sub"`, verificado contra la base que esa forma sí alimenta `auth.uid()`) |
| Horario de la tienda para la IA y el tablero (B1, "El reloj dice la verdad") | `migrations/20260906010000_agent_settings_business_hours.sql` — B1 del plan "El reloj dice la verdad" (5/9/2026): `agent_settings.business_hours jsonb not null` con llaves `mon..sun`, cada una una lista de franjas `[inicio, fin]` en `"HH:MM"` (lista vacía = cerrado; más de una franja = horario partido); horas LOCALES de `America/Caracas` (la zona vive en `src/lib/time-zone.ts`, no acá). Default L–V 08:00–18:00, fin de semana cerrado; columna con default y SIN backfill, aplicable en caliente. Check `agent_settings_business_hours_object` exige un objeto. RLS sin cambios: `agent_settings_update` (20260820050000) sigue exigiendo supervisor/admin, lectura abierta a cualquier agente. Sin funciones `security definer`. La IA lo lee en cada turno (B3) y el tablero mide con él el atasco de "Con asesor" (A1). Test: `tests/business_hours.sql` (default con los siete días; un `update` a `'[]'::jsonb` falla por el check), cableado en el job `migraciones` de CI |
| `conversations_pending_idx` también ordena por `id` (D1, "Lo pequeño que quedó atrás") | `migrations/20260906020000_conversations_pending_idx_id.sql` — D1 del plan "Lo pequeño que quedó atrás" (`docs/planes/2026-09-06-lo-pequeno-que-quedo-atras.md`, 6/9/2026): dropea y recrea `conversations_pending_idx` como `(last_message_at desc nulls last, id desc) where awaiting_reply and status <> 'closed'` — mismo predicado de siempre, ahora calza el orden completo que pide `fetchConversations` (`src/lib/data.ts:931-932`: `.order("last_message_at")` + `.order("id")`), el mismo desempate que 20260829010000 ya le había dado a los otros tres índices de la bandeja el 29/8/2026 pero que a este, creado un día antes (20260828020000) sin cursor todavía, nadie le había sumado. Verificado contra Supabase local: `explain` con `enable_seqscan = off` da `Index Only Scan`/`Index Only Scan Backward` sobre `conversations_pending_idx` para el orden de "Pendientes" y el reverso de "Más antiguas" (`asc nulls first, id asc`), sin `Sort` ni `Incremental Sort`. Sin backfill, sin código de app: es solo el índice |
| La ventana de 24 h dice la verdad (T1) | `migrations/20260907010000_ventana_24h_dice_la_verdad.sql` — T1 de la corrida "La ventana de 24 h dice la verdad" (7/9/2026): el caso que la destapó, `aa75ef33-38e8-4ff4-8422-7e7f49615795`, mostraba la caja de texto habilitada ("quedan 11 h") mientras Meta rechazaba todo con 131047 (ventana cerrada). Dos candados sobre `last_customer_message_at` (lcma), los dos en `handle_new_message()` (`create or replace`, camino INSERT — la IA inserta sus fallos ya con `whatsapp_status='failed'`, `src/lib/ai/send.ts`) y en `handle_message_status_change()` (`create or replace`, camino UPDATE — el asesor en `api/messages/send/route.ts` y el callback de Meta en `webhooks/whatsapp/route.ts`, ambos un solo UPDATE con `whatsapp_status`+`whatsapp_error_code`): candado A, un entrante `message_type='unsupported'` deja de mover lcma/`unread_count` (sigue siendo visible: `last_message_at`/preview no cambian) — Meta tampoco lo cuenta para su propia ventana; candado B, un saliente rechazado con 131047 empuja lcma hacia atrás, `least(lcma, created_at - 24h)`, solo si `lcma is not null` (`least(null, x)` devuelve `x`, no `null` — sin esta guarda un 131047 sobre un lead sin mensajes le inventaría fecha y encendería `awaiting_reply`) y solo si `created_at > lcma` (protege contra un callback tardío que no debe retroceder un reloj que un mensaje real posterior ya adelantó). Sin `security definer` nueva, sin revokes/grants. Backfill en tres pasos con `raise notice`: (a) reclasifica a `unsupported` el texto histórico anterior a T3.2 (`content like 'El cliente envió un mensaje que el CRM todavía no sabe mostrar%'`), (b) recalcula lcma en conversaciones donde apuntaba al `created_at` de un entrante ahora `unsupported` (null si no queda ningún inbound real; `unread_count` NO se recalcula, sin forma fiable), (c) aplica el candado B retroactivo sobre fallos 131047 ya registrados. Test: `tests/ventana_24h.sql`, nueve casos (candado A visible-pero-no-cuenta, candado B por INSERT y por UPDATE, otro código no toca nada, reapertura, backfill vía `\i` con el trigger `on_message_inserted` desactivado para sembrar el estado "de producción", sin mensaje del cliente, callback tardío), cableado en el job `migraciones` de CI |
| Un turno sin contenido legible deja rastro (T1, "La IA ve lo que llega") | `migrations/20260908010000_traspaso_sin_contenido_legible.sql` — T1 de la corrida "La IA ve lo que llega" (8/9/2026): el caso que la destapó, `cea69118-5d17-4f08-84c6-925755672b87`, un audio del cliente sin texto previo — `loadHistory` descartaba toda fila sin `content`, el turno salía por historial vacío SIN escribir traspaso (viola "ningún lead invisible") y el reconciliador la reencoló 30 veces hasta que un asesor contestó a mano. Amplía el CHECK de `conversation_handoffs.reason` con `sin_contenido_legible` (mismos 24 valores vigentes desde 20260905030000, más este al final): lo escribe el código de T4 con `record_handoff(..., to_kind => 'unassigned')` cuando el historial queda vacío tras describir la media (S1/S3 de la misma corrida). Sin `security definer` nueva, sin revokes/grants. Backfill único (no en tres pasos, a diferencia de 20260907010000): limpia `journey_stage`/`active_tool` a null en las conversaciones que ese bug dejó congeladas en `classifying`/`tool_running` SIN lock vigente (`ai_turn_lock_until is null or ai_turn_lock_until < now()`, la misma condición de "libre" que usa `ai_turn_lock_acquire()`) — 17 medidas en producción el 7/9/2026, la más vieja del 27/8/2026; una con lock vigente no se toca, podría ser un turno de verdad corriendo ahora. Idempotente: reaplicada con `\i` no encuentra ya nada que limpiar. Test: `tests/traspaso_sin_contenido_legible.sql`, cuatro casos (record_handoff acepta la razón nueva, rechaza una inventada, el backfill limpia classifying sin lock y tool_running con lock vencido, no toca un classifying con lock vigente), cableado en el job `migraciones` de CI |

## `scripts/` y `docs/`

| Archivo | Qué es |
|---|---|
| `scripts/deploy.sh` | Despliegue por SSH: valida antes de tocar el servidor, copia, levanta, verifica TLS |
| `scripts/preflight.sh` | Frena config rota antes de arrancar (claves cruzadas, URLs locales, secretos de juguete) |
| `scripts/backup.sh` / `restore.sh` | Respaldo/restauración probados de verdad (solo `public`; el bucket no entra) |
| `scripts/verificar-respaldo.sh` | Verifica un `.sql.gz` (gzip íntegro, tamaño, `CREATE TABLE`) sin borrar ni mover nada — aísla el `pipefail` para que un SIGPIPE del productor no se lea como corrupción (bug del 29/8/2026) |
| `scripts/generar-iconos.mjs` | Iconos de la app |
| `scripts/exportar-historiales-clasificador.sql` | Export de producción para el comparador grande×chico (T2, 7/9/2026): un solo JSON con hasta 200 conversaciones con turno de IA en 7 días (últimos 20 mensajes c/u, ascendentes), escenarios activos con tags y `agent_settings.business_hours`; se corre por SSH contra `supabase-db` (`psql -At`); su salida `scripts/historiales-clasificador.json` trae texto de clientes y está en `.gitignore` |
| `scripts/comparar-clasificador.test.ts` | Mide acuerdo de intención (`classifyIntent`) y escenarios perdidos (`matchPlaybook`) entre el modelo grande y un candidato chico (`AI_CLASSIFIER_MODEL`, default `google/gemini-3.1-flash-lite`, configurable con `COMPARAR_CLASIFICADOR_CHICO`). Es un archivo de vitest porque `classify.ts`/`playbooks.ts`/`model.ts` importan `server-only`; `describe.skipIf` sin `COMPARAR_CLASIFICADOR`. Dos pasadas (todo el grande, luego todo el chico: la variable es global al proceso) con pool de 5 conversaciones; corre contra el fixture sintético por default o el export real con `COMPARAR_CLASIFICADOR_FIXTURE`; escribe matriz de confusión, escenarios perdidos/ganados, tiempos, tokens y eventos de log en `comparar-clasificador.reporte.md` (gitignored). El veredicto (≥95 % y cero escenarios perdidos) se imprime, no se afirma: falla solo si ninguna llamada al proveedor completó |
| `scripts/comparar-clasificador.fixture-sintetico.json` | 5 conversaciones INVENTADAS (marcadas `_sintetico`) para probar la plomería del comparador sin tocar datos reales |
| `docs/PRODUCCION.md` | LA guía de puesta en producción, paso a paso con verificación. 7/9/2026: suma "Rampa de los topes" (los dos escalones que el operador carga a mano en Dokploy para destopar la cola, con las señales a vigilar) y el cron pasa de cada 5 min a cada minuto |
| `docs/superpowers/plans`, `specs/` | Planes y specs de diseño históricos (decisiones documentadas) |
| `docs/planes/` | Planes aprobados bajo la metodología `liminalwork`, fuente de verdad de cada corrida de implementación (`2026-09-04-bandeja-que-no-pierde.md`: las cuatro etapas de la reforma de la bandeja, con el prompt del orquestador al lado; `2026-09-05-anexo-bandeja-que-no-pierde.md`: los dos hallazgos de esa corrida que cambiaban el plan y el operador aprobó corregir —la despedida de la IA al escalar sin asesores como `is_auto_reply` [A1] y la reapertura por el cliente traspasando al asesor asignado [A2]— y la parte B, que cierra la última excepción: la base recalcula `awaiting_reply` cuando un mensaje se marca `is_auto_reply` después de insertado [B1, migración 20260905070000 con backfill de escalaciones viejas] y el escenario que escala sin asesores marca su texto [B2]; `2026-09-07-ventana-24h-dice-la-verdad.md`: los dos candados sobre `last_customer_message_at` [migración 20260907010000] y la red de seguridad del composer; `2026-09-08-la-ia-ve-lo-que-llega.md`: siete tareas —el historial describe media en vez de esconderla [`history-line.ts`], el bloque `MEDIA_RULES` del prompt, el traspaso `sin_contenido_legible` [migración 20260908010000] con la limpieza de las tres puertas que dejaban `journey_stage` congelado, `AI_AGENT_REASONING` y `origenDelFallo` [red vs. Meta], `errorText` entendiendo errores de Supabase, y la guarda "escribió un humano" con gracia de `AI_HUMAN_GRACE_MINUTES` en vez de vitalicia— con el brief completo del operador y el resultado real de la corrida; `2026-09-07-la-respuesta-llega-en-siete-segundos.md`: los tres frenos de la cola destopados a la vez [S1], el comparador que descarta mudar la clasificación a un modelo chico [S2, sin cambio de `.env`], `claimDue` separando `debounceMs` de `colaMs` con las herramientas usadas [S4] y la continuación propia de la cola cuando un turno queda frenado [S3] —sin migración—, con el brief completo del operador, la tabla del comparador y la rampa en producción pendiente como T5) |
