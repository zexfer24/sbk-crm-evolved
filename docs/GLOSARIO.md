# Glosario de archivos

Mapa módulo por módulo del proyecto. Su propósito es que orquestador y
subagentes ubiquen el código relevante sin releer el proyecto entero.

**Regla de mantenimiento:** todo cambio que cree, mueva o cambie el propósito
de un archivo actualiza su línea aquí, en el mismo commit.

Convención de tests: cada módulo lleva su `*.test.ts(x)` al lado, con el mismo
nombre. Los tests sin módulo propio (`data-*.test.ts`, `queue-limit`,
`queue-spacing`, `debounce`, `turn-correlation`, `dashboard-tickets`,
`conversation-quotes`, `ventana-24h-contrato`) prueban facetas del módulo
vecino que nombran.

---

## Raíz

| Archivo | Qué es |
|---|---|
| `next.config.ts` | Cabeceras de seguridad (antes en Caddyfile), `output: standalone`, `staleTimes` de navegación, orígenes de dev para túneles |
| `src/proxy.ts` | Middleware de Next: refresco de sesión + quién puede ver cada página (delega en `lib/supabase/middleware`) |
| `Dockerfile` / `docker-compose.yml` | Imagen standalone sin privilegios con HEALTHCHECK; stack app + caddy + cron (cada minuto desde el 7/9/2026, antes cada 5 min — ver "Rampa de los topes" en `docs/PRODUCCION.md`) |
| `docker-compose.dokploy.yml` | El mismo stack menos Caddy (Dokploy trae Traefik); mismo cambio de ritmo del cron que `docker-compose.yml` |
| `.env.local.example` / `.env.production.example` | Plantillas versionadas (las reales `.env.local`/`.env.production` no lo son). 7/9/2026 ("La respuesta llega en siete segundos"): el bloque de las cinco variables de ritmo de la IA (`AGENT_MAX_CONCURRENT_TURNS`, `AGENT_MAX_TURNS_PER_MINUTE`, `AGENT_QUEUE_MAX_PER_RUN`, `AI_MAX_CONCURRENT_REQUESTS`, `AI_MAX_REQUESTS_PER_MINUTE`) reescrito con los defaults destopados y la historia de por qué van siempre juntas (ver `queue.ts`/`rate-limit.ts`/"Rampa de los topes" en `docs/PRODUCCION.md`) |
| `Caddyfile` | TLS automático y cabeceras cuando el proxy es Caddy |
| `moto_catalog_schema.sql` / `moto_catalog_data.sql` | Fuente del catálogo de motos/repuestos (5.438 productos); de aquí salen la migración y el seed del catálogo |
| `vitest.config.ts` / `vitest.setup.ts` / `vitest.setup.test.ts` / `vitest.server-only-stub.ts` | Vitest con entorno `node` por defecto (los tests de UI declaran jsdom por docblock); `testTimeout` 15s contra la contención de workers; stub que anula `server-only` en pruebas; `vitest.setup.ts` sube el `asyncUtilTimeout` de los `waitFor`/`findBy` de Testing Library a 5 s (solo jsdom) para que la espera escale con la carga en vez del 1 s de fábrica; el plugin de React (`@vitejs/plugin-react`) solo transforma `.tsx`/`.jsx` (29/8/2026: su include de fábrica es `/\.[tj]sx?$/` y pasaba Babel sobre todo `.ts` del grafo, incluidos los tests de entorno node y src/lib, que no pueden tener JSX). 10/9/2026: `execArgv: ["--no-experimental-webstorage"]` para que jsdom tenga el mismo `localStorage` real en Node 22 (CI) y en Node 26 (local), y `vitest.setup.ts` lo vacía en un `beforeEach`; `vitest.setup.test.ts` es el resguardo de las dos cosas (el CI estuvo rojo desde el 6/9 por estado heredado entre tests de `inbox-sidebar.test.tsx`). |
| `liminalwork.md` | Metodología de trabajo (fuente de la skill `liminalwork`) |
| `AGENTS.md` | Aviso de `next dev`: esta versión de Next 16 difiere de los datos de entrenamiento; leer `node_modules/next/dist/docs/` |
| `CREDENCIALES.txt`, `.env.local*` | Solo en disco, ignorados por git; lo versionado son los `.example` |

## `src/app` — páginas (App Router)

| Ruta | Qué es |
|---|---|
| `page.tsx` | Tablero (Dashboard) en la raíz: monta `DashboardView` con agentes, conversaciones del tablero y actividad del día. B3 (5/9/2026): suma `fetchBusinessHours` (lectura liviana, nunca lanza) al mismo `Promise.all` y le pasa `businessHours` a `DashboardView` para que "Con asesor" mida en horario laboral (la línea anterior de este glosario decía "redirección de entrada", que es lo que hace `proxy.ts`, no esta página). T2, corrida "Los números del día" (10/9/2026): suma `fetchLeadTotal` (`dashboard-data.ts`) al mismo `Promise.all` y le pasa `initialLeadTotal` a `DashboardView` — a diferencia de `fetchBusinessHours`, esta SÍ lanza si Supabase falla (no tiene un default razonable que inventar para "cuántos leads hay") |
| `layout.tsx`, `globals.css`, `theme.css` | Marco global y tokens de tema claro/oscuro. 15/9/2026: el título de la pestaña sale de `APP_TITLE` (`brand.ts`), igual que el `<h1>` del login y las seis cabeceras `dash-brand-name` |
| `inbox/` (+`[id]/`) | Bandeja compartida: la página arma los datos iniciales —desde el 30/8/2026 siembra "Pendientes" (`{activeOnly:true, awaitingReplyOnly:true, limit: INBOX_PAGE_SIZE}`), no "No leídas"— y monta `crm-shell`. T1 (8/9/2026): calcula `since = currentDayRange(CRM_TIME_ZONE).from.toISOString()` y lo suma a las tres consultas de siembra (`conversations`/`inboxCounts`/`pendingConversations`) — el servidor no tiene `localStorage`, así que siempre siembra con el corte de HOY (el default de `dayScope` en `crm-shell.tsx`); si el visor tenía "Ver todo" guardado, el shell lo restaura después de montar y hace su propio refetch. T4 ("Los números del día", 10/9/2026): `loadAgentDay` (mismo patrón try/catch que `loadBcvRate`, arriba: un tropiezo del RPC no puede tumbar la bandeja) suma `fetchAgentDaySummary`/`fetchAiAssignmentsToday` (`agent-day-data.ts`) al mismo `Promise.all`, SIEMPRE con el `since` de HOY —el mismo de las otras tres consultas de siembra, nunca el que la bandeja pudiera tener en "Ver todo"— y le pasa `initialAgentDay`/`initialAiAssignments` a `crm-shell`. T4b (plan "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026): suma `fetchActiveCatalogLinks(supabase)` al mismo `Promise.all` (nunca lanza, cae a `[]`) y le pasa el resultado como `initialCatalogLinks` a `crm-shell` — la siembra de los catálogos ACTIVOS que el composer necesita para resolver `{{catalogo:<key>}}`/`{{catalogos}}` al usar un mensaje rápido |
| `clientes/` | Directorio de clientes (ficha, notas, etiquetas, historial comercial) |
| `inventario/` | Inventario: lo que se ve aquí es lo que la IA cotiza (misma tabla `products`) |
| `ventas/` | Ventas cerradas: verificación y reversión (supervisor). `factura/[id]/` (T5, plan "Seis frentes del buzón", 8/9/2026): hoja imprimible de una factura —`notFound()` si no existe—, monta `InvoiceSheet` (sin PDF en servidor: se imprime con `window.print()` desde el navegador). `error.tsx` (T7, plan "Seba sale sin pisar a nadie", 19/9/2026, hallazgo A4): límite de errores de la ruta — acá no hay lectura que degradar (`saint_invoice_number` viaja DENTRO del `select` de `fetchSales`), así que solo existe esta pantalla con "Reintentar" (prop `retry`, estable desde Next 16.3.0) y el mismo marco `.dash`/`.dash-frame` + `AppRail` que `SalesView`, respetando la grilla de dos columnas (trampa del FRAGMENTO, 9/9/2026) |
| `agent-control/` | Panel de control de la IA. T7, mismo plan (19/9/2026, hallazgo A4): `error.tsx` — mismo patrón que `ventas/error.tsx` (rail + "Reintentar" con `retry`; importa `dashboard.css` a mano, corrección del 19/9/2026 hallazgo 5, porque el componente de vista que la traía no llega a montarse cuando la página lanza); `degradable-reads.ts` (+ su test) — `readListIfTableExists` envuelve las dos lecturas MÁS NUEVAS del `Promise.all` de `page.tsx` (`fetchLessons`, `fetchCatalogLinks`, que LANZAN si su tabla falta) y las cae solas a `[]` con `console.error` SOLO cuando el error es "la tabla no existe" (`42P01`/`PGRST205`, corrección del 19/9/2026 hallazgo 6); cualquier otro error (timeout, 5xx, corte de red) se relanza y lo atrapa `error.tsx`, en vez de fingir un panel vacío |
| `login/` | Acceso |

## `src/app/api` — rutas de servidor

| Ruta | Qué es |
|---|---|
| `webhooks/whatsapp/route.ts` | Entrada de Meta: handshake, verificación de firma HMAC, mensajes entrantes (texto/multimedia/reply), estados de envío; escribe con service role y encola turnos. La bienvenida automática se reclama en la base (`claimWelcome` sella `conversations.welcome_sent_at` con un UPDATE condicional antes de enviar; se devuelve a `null` solo si Meta rechaza la plantilla), no en memoria de la invocación; el insert de la bienvenida lleva `is_auto_reply: true` (T0.1/T0.2, 5/9/2026) para que el trigger no la cuente como respuesta real. El SELECT de la conversación existente trae `status`/`referral` (T2.1, 5/9/2026; ganó `ai_enabled`/`assigned_agent_id` en el anexo A2, 5/9/2026, y los perdió de nuevo en T2b del plan "Seba atiende el mostrador", 18/9/2026, cuando dejaron de hacer falta): si venía `closed`, reabre con un RECLAMO — `UPDATE {status: "open", ai_enabled: true, assigned_agent_id: null, welcome_sent_at: null}` con `.eq("status", "closed").select("id")` — ANTES de guardar el mensaje entrante; solo si el reclamo afectó una fila (dos webhooks concurrentes del mismo lote no duplican nada) deja el evento "El cliente volvió a escribir" y `recordHandoff({toKind: "ai", reason: "reabierta_por_cliente"})`. Desde T2b un chat reabierto arranca SIEMPRE de cero (D2 del plan): la IA se enciende, se desasigna cualquier asesor y `welcome_sent_at` se borra para que Seba vuelva a presentarse (`claimPresentation`, `agent.ts`) — ya no hay ramas por `ai_enabled`/`assigned_agent_id` como en el anexo A2, así que el traspaso es siempre `ai`. El trigger AFTER `handle_conversation_ownership_change()` (migración 20260916010000) puede escribir, DENTRO del mismo UPDATE, `desasignada_por_asesor`/`devuelto_a_ia` si el chat estaba escalado al cerrarse (`created_by = 'system'`); la fila `reabierta_por_cliente` queda SIEMPRE última, cerrando cualquier escalada vieja que `escalationOpen` pudiera ver abierta. Este UPDATE corre ANTES del insert del mensaje entrante (hallazgo 5 del plan): el trigger BEFORE `handle_conversation_ai_resume()` sella `ai_resume_cutoff_at` con el `last_customer_message_at` VIEJO, así que el mensaje que dispara la reapertura —insertado después, con el `created_at` de Meta— queda por delante del sello y la guarda `mensaje_previo_a_devolucion` de `agent.ts` no lo calla. `new-contact-race.test.ts` cubre la carrera de contacto nuevo y `welcome-race.test.ts` la carrera de la bienvenida (y ahí se afirma el `is_auto_reply: true` de la fila insertada) — los tres archivos comparten el espejo de mocks (incluido `@/lib/redis`) y la importación del route en `beforeAll`; `route.test.ts` suma el escenario de reapertura con su propio `conversationRow`/`conversationUpdates`/`handoffCalls` mutables. T3.4 (5/9/2026) atiende dentro del mismo `for` los otros tres `field` de la WABA además de `messages` -- `message_template_status_update`, `phone_number_quality_update` y `account_update` --, ninguno con `metadata.phone_number_id`: el canal se resuelve por coincidencia de dígitos de `display_phone_number`/`phone_number` contra `whatsapp_channels.phone_number` (`resolveHealthChannel`) o, si no hay coincidencia, por el único canal `connected`; guarda `quality_rating`/`messaging_limit`/`account_restrictions`/`health_updated_at` en el canal y `templates.status` por nombre e idioma (CHECK ampliado a `paused`/`disabled` en la migración 20260905060000), con `log.error` cuando la calidad baja, llega una restricción de cuenta, o una plantilla queda `paused`/`disabled`/`rejected`. T3.2 (5/9/2026, migración 20260905050000): dentro de la rama `field === "messages"`, `interactive.button_reply`/`list_reply` y `type: "button"` (botón de plantilla) guardan `message_type: "interactive"` con `content: "Respondió: {title}"`, `customerText` el título y `payload` con `{type, id}` o `{type: "button", template}`; `order` guarda `message_type: "order"` con el resumen en español de `describirPedido()` (ítems y total por moneda), `customerText = order.text` si vino, y `payload.productItems` crudo; el `else` final (antes una frase fija) pasa a `content: null`, `message_type: "unsupported"`, `payload: {type: message.type}` (F10); `context.referred_product` se funde en el `payload` del mensaje sea cual sea su tipo. `message.referral` (el anuncio "Click to WhatsApp" que originó el hilo) actualiza `conversations.referral` UNA sola vez por conversación (`existingConversation?.referral` corta la reentrega de Meta) y deja el evento de sistema `Llegó desde el anuncio "…"`. `value.errors` deja `log.error("webhook_error_meta")`; el `error` del `update` de `whatsapp_status` de los estados, antes ignorado, ahora deja `log.error("webhook_error_actualizar_estado")`. `whatsapp_status` admite `"played"` (nota de voz reproducida) igual que cualquier otro estado. S1 del plan "El cliente que cambió de número" (6/9/2026): `type: "system"` entra tras validar el teléfono viejo (`phoneNumberFromWaId(message.from)`) y antes del upsert de contacto — `handleSystemMessage()`. Con `system.type === "user_changed_number"` y un `wa_id`/`new_wa_id` válido: si el número nuevo no tiene contacto, mueve `contacts.phone_number` (D1 — conversación, historial, pines, pedido y bitácora cuelgan de `contact_id`, así que siguen solos); si ya tiene contacto (o el `UPDATE` choca con `23505`, otro webhook ganó la carrera), no fusiona nada (D2) y deja `log.error("webhook_cambio_numero_conflicto")`. Cualquier otro `system.type` (`customer_identity_changed`, uno nuevo, o un número que no resultó válido) deja un evento genérico con `payload.systemType`. Sin contacto o sin conversación para el número viejo, no crea nada (`log.info("webhook_system_sin_conversacion")`). Nunca reabre una conversación `closed` ni registra traspaso: eso sigue siendo exclusivo del mensaje normal del cliente. El `system_event` que deja es inerte para `handle_new_message()` (20260905010000): no mueve preview, `last_reply_at` ni `awaiting_reply`. Desde el 6/9/2026 (D3, misma corrida, S3a), un `type: "unsupported"` que llega SOLO —sin ninguna foto/video/audio/documento/sticker del mismo remitente en el lote— ya no se descarta: se guarda como `message_type: "unsupported"` con `content` null y el tipo real de Meta más el código de error en `payload` (`{type, code}`), visible en el chat y en la lista (el trigger mueve `last_message_at`/preview) pero —desde la migración 20260907010000, corrida "La ventana de 24 h dice la verdad", 7/9/2026— SIN mover `last_customer_message_at`/`unread_count`/`awaiting_reply`: Meta no lo cuenta para su ventana de 24 h y el CRM tampoco (antes sí lo hacía, y ese fue el bug de `aa75ef33…` del 6/9/2026: caja habilitada con la ventana cerrada del lado de Meta); tampoco encola turno de IA (`esUnsupportedSolo` lo deja fuera de `touchedByCustomer`; un texto de la misma conversación en el mismo lote sí la encola) y con `log.info("webhook_unsupported_guardado")`. El que llega junto a una galería se sigue descartando igual que antes. La vista previa de la bandeja quedaba en "Unsupported"/"Image" por el `initcap` del trigger hasta la migración 20260908020000 ("La bandeja habla español", 8/9/2026): desde ahí `message_preview_label()` la escribe en español. 14/9/2026 (corrección hallada en la verificación final de "La voz cercana y la espera visible"): un error de `agent_can_run()` (RPC caída, red cortada) ya no se trata como `false` antes de encolar — sigue de largo sin escribir el traspaso `agente_no_puede_correr` (log `webhook_interruptor_no_consultable`) y deja que `runAgentTurn` decida al abrir el turno, donde T5 lanza y la cola reintenta; mismo criterio que `agent.ts`. Solo un `false` real sigue dejando `webhook_no_encola_ia_apagada` + traspaso. T6 (15/9/2026): la extensión del archivo descargado de Meta la decide `extensionForMime` (`whatsapp/media-extension.ts`) en vez de un lookup exacto contra el `Content-Type`; corrige que toda nota de voz (`audio/ogg; codecs=opus`) quedara como `.bin` |
| `messages/send/route.ts` | Envío del composer: canal `connected` → Cloud API real (con reintentos solo ante 5xx/red); si no, simulado. El token nunca toca el navegador. Plantillas con variables (T3.3, 5/9/2026): `kind: "template"` recibe `variables: string[]` (además de `content` con el cuerpo CRUDO, `{{n}}` sin sustituir); antes de guardar sustituye con `substituteTemplateVariables` (`whatsapp/template-variables.ts`) para que la fila guarde el mensaje real que le llegó al cliente, y arma los `components` posicionales con `buildTemplateBodyComponents` para pasárselos a `sendWhatsappTemplate`. T3a (8/9/2026): `mediaType` admite `"sticker"`; `content` se fuerza a `null` cuando `mediaType === "sticker"` aunque el llamador mande uno —Meta rechaza un sticker con caption, ver `meta-client.ts`. "El sticker que no entra" (8/9/2026, mismo día): en canal real y `mediaType === "sticker"`, llama a `checkStickerBeforeSend` (`whatsapp/sticker-guard.ts`) y corta con 422 ANTES de insertar la fila si no pasa —mismo criterio que la guarda de teléfono que ya estaba—; en demo no corre, no hay a quien Meta rechazarle nada |
| `conversations/[id]/read/route.ts`, `.../typing/route.ts` | T3.1 (4/9/2026): doble check azul y "escribiendo…" hacia Meta, un endpoint por señal. Mismo patrón de sesión que `.../close` (`@/lib/supabase/server` + `fetchCurrentAgent`, 401 sin ella); sin cliente admin, porque acá no hay ninguna RPC de `service_role` de por medio. Resuelven el `phone_number_id`/`status` del canal y el `whatsapp_message_id` del último mensaje ENTRANTE (Meta exige ese wamid como "message_id" para las dos señales); con canal no `connected`, sin `WHATSAPP_ACCESS_TOKEN` o sin ningún mensaje entrante, no llaman a Meta pero responden 200 igual — nunca rompen la bandeja. `crm-shell.tsx` llama a `.../read` (vía `sendReadReceipt`, `mutations.ts`) junto con `markConversationRead`, nunca en su lugar; `composer.tsx` llama a `.../typing` (vía `sendTypingSignal`) al primer carácter y cada 20 s mientras haya texto |
| `cron/process-queue/route.ts` | Red de seguridad de la cola de turnos (cada minuto desde el 7/9/2026, antes cada 5 min); exige `CRON_SECRET`, falla cerrado con 503. Resguardo en `route.test.ts`: los caminos cerrados afirman que ni la cola ni el reconciliador se llamaron. Desde el 30/8/2026 reconcilia ANTES de drenar, para que lo rescatado se atienda en la misma pasada y no espere otros 5 min |
| `agent/backlog/route.ts` | Repaso del atraso al encender la IA: encola lo que quedó esperando mientras estuvo apagada. Resguardo en `route.test.ts` (incluye el DEFECTO CONOCIDO D1: el lock queda tomado 30 min en los caminos que no encolan). 7/9/2026: los comentarios de cabecera y de `SWEEP_LOCK_SECONDS` dejan de asumir un ritmo fijo de drenado ("diez cada cinco minutos"); el freno de emergencia real sigue siendo `agent_can_run()` por turno, no la velocidad de la cola |
| `agent/stop/route.ts` | Freno de emergencia: apaga la IA y también lo que ya estaba en marcha. Resguardo en `route.test.ts`: orden interruptor→purga, degradación con Redis caído, línea de auditoría `ia_apagada` |
| `dev/simulate-message/route.ts` | Simulador del panel: mensaje entrante sin pasar por Meta, turno síncrono. Su test afirma que la guarda de producción corta ANTES de tocar dependencia alguna. C3 ("El reloj dice la verdad", 5/9/2026): el contacto y el canal de prueba fijos usan números E.164 puros (`+580000000001`/`+580000000000`, antes "+00 000 0000001" con espacios) porque `turn-target.ts` exige `isDeliverablePhoneNumber` y sin `conversationId` TODO turno simulado moría con "Identidad no verificable"; el `upsert` va por id fijo, así que la fila vieja se corrige sola en la próxima llamada. Su test cubre además que sin `conversationId` el turno llega a `runAgentTurn` (mockeado) |
| `media/[...path]/route.ts` | Sirve el bucket privado `whatsapp-media` con la sesión del CRM por delante. Resguardo en `route.test.ts`: 401 sin sesión sin llegar a firmar, y el path viaja sin sanear (DEFECTO CONOCIDO D5) |
| `health/route.ts` | 200 solo si alcanza la base y tiene sus variables; para el monitor externo. Su test afirma los CÓDIGOS HTTP (lo único que leen monitor y HEALTHCHECK), no solo el JSON. Desde el 30/8/2026 informa además `unassigned_waiting` (vía la RPC `unassigned_waiting_count`, expuesto SIN sesión por decisión del operador) y `redis_persistence`: ninguno de los dos cambia el código HTTP — un lead esperando no es una caída. T3.4 (5/9/2026) suma `channel_quality`/`channel_messaging_limit` (`checkChannelHealth()`, mismo criterio "connected primero, si no el primer canal que exista" que `fetchWhatsappChannelHealth` en `src/lib/data.ts`): tampoco cambian el código, un número degradado sigue entregando hasta que Meta lo restrinja de verdad |
| `workflows/` | (dentro de `dev/`) utilidades de desarrollo |

## `src/lib` — núcleo compartido

| Archivo | Qué es |
|---|---|
| `types.ts` | Tipos de dominio de toda la app (formas mínimas por vista, a propósito). `BoardConversation` gana `lastReplyAt`/`lastReplySender` (T0.2, 5/9/2026): la "última respuesta real" que separa T0.1, distinta de `lastMessageAt` (que sigue siendo "último mensaje visible") — es lo único que `awaitingReply()` (`dashboard.ts`) compara contra `lastCustomerMessageAt` desde esa migración. `InboxFilter` suma `"escalated"` (T1.5, mismo día): la píldora "Escaladas". `Message` gana `whatsappErrorCode` (T3.3, 5/9/2026, número crudo detrás de `whatsappError`): lo necesita la burbuja para saber si el motivo tiene, además, un botón que hacer algo (`failureAction` en `whatsapp/failure-reason.ts`). T3.2 (5/9/2026): `MessageType` suma `"interactive"`/`"order"`/`"unsupported"`, `WhatsappMessageStatus` suma `"played"`; `Message` gana `payload: MessagePayload | null` (id del botón/ítem respondido, payload de plantilla, ítems del pedido); `Conversation` (no `BoardConversation`/`ConversationSummary`: la bandeja no lo pinta) gana `referral: ConversationReferral | null` — `{headline, sourceUrl, receivedAt}`, lo mínimo que necesita el banner de la cabecera del chat contra la ventana de 72 h. B3 (5/9/2026): `AgentSettings.businessHours?: BusinessHours` — opcional a propósito (tres fixtures de test lo fabrican a mano sin el campo); `fetchAgentSettings` SIEMPRE lo llena y quien lo lea sin garantía cae a `DEFAULT_BUSINESS_HOURS`. T3a (8/9/2026, apéndice al final del archivo): `Sticker` — `{id, url, name, animated, createdBy, createdAt}`, `url` ya resuelta con `mediaUrlFor` igual que `Message.mediaUrl`. T5 (8/9/2026): anexa `Invoice`/`InvoiceStatus`/`InvoiceItem`/`InvoiceCustomerSnapshot` al final del archivo — ver `invoices.ts`/`invoices-data.ts` en este glosario para el detalle. B3 (5/9/2026): `AgentSettings.businessHours?: BusinessHours` — opcional a propósito (tres fixtures de test lo fabrican a mano sin el campo); `fetchAgentSettings` SIEMPRE lo llena y quien lo lea sin garantía cae a `DEFAULT_BUSINESS_HOURS`. T1 del plan "Seis frentes del buzón" (8/9/2026): gana `InboxDayScope = "today" \| "all"` — el interruptor "Ver todo" de la bandeja; `"today"` es el default, calculado con `useInboxDay` (`src/lib/use-inbox-day.ts`). 8/9/2026, "La bandeja habla español": `MessagePayload` gana `systemType`/`previousPhone`/`newPhone` — solo en un `system_event` de `user_changed_number` (webhook `handleSystemMessage`, 6/9/2026), lo que lee el botón de la burbuja. T5, plan "Seba atiende el mostrador" (18/9/2026, requisito 7): suma `LessonScope`/`LessonKind`/`AiLesson` — el espejo de `public.ai_lessons` (migración 20260917020000), "Lecciones de Seba", junto a `KnowledgeEntry`. T2, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D3): suma `CatalogLink` — el espejo de `public.catalog_links` (migración 20260918010000), junto a `AiLesson` |
| `data.ts` | Capa de LECTURA de la bandeja/chat contra Supabase (55K; mapea filas crudas a tipos de dominio). `fetchConversations` gana `unreadOnly` y `assignedTo` (`FetchConversationsOptions`) para resolver contra la base las píldoras "No leídas" y "Mías" — antes "Mías" filtraba en memoria sobre la ventana cargada y un chat asignado viejo no aparecía. `InboxCounts` gana `unread` (mismo predicado que `unreadOnly`); `pending`/`pendingStale` seguían ahí solo para el panel de inicio hasta el 28/8/2026 tarde — desde el 30/8/2026 `pending` volvió a tener píldora propia en la bandeja (además de seguir alimentando el panel de inicio), la única de las cuatro que cuenta contra toda la base con `awaiting_reply and status <> 'closed'`, no contra lo que la bandeja tenga cargado. Paginación por CURSOR (29/8/2026): `offset` salió de `FetchConversationsOptions`, entró `cursor?: ConversationCursor` (`inbox-paging.ts`) — un `offset` de posición se rompía apenas una fila subía al tope en medio de la bajada de la bandeja (confirmado en producción, ~3 reordenamientos/minuto, filas que no volvían nunca). `fetchConversationRows` ordena por `last_message_at desc nulls last, id desc` (el desempate por `id` es necesario: hay empates reales de `last_message_at`) y acumula sus disyunciones (`pendingWindow: "stale"`, `unreadOnly`, el predicado del cursor) en un arreglo para emitir un solo `.or()` con `orExpression` — dos `.or()` en la misma consulta no son fiables. El recorrido MULTIPÁGINA INTERNO (las llamadas sin `limit`: tablero y Control de IA vía `fetchBoardConversations`) también avanza por cursor desde el 29/8/2026: cada página interna pide `range(0, pageSize-1)` con el predicado armado desde la última fila de la página anterior, en vez del `range(rows.length,…)` posicional que perdía en silencio la fila del borde cuando algo salía del conjunto (un chat que se cierra con `activeOnly`) y duplicaba el borde cuando algo subía al tope; el cursor de ENTRADA (`options.cursor`) gobierna solo la primera página interna, después lo reemplaza el de continuación (`cursorFromRow`, gemelo crudo de `cursorAfterPage`), inyectado en el mismo acumulador `orGroups` para que la distribución del OR con `unreadOnly` siga saliendo en un solo `.or()`. `CONVERSATION_BOARD_COLUMNS`/`CONVERSATION_DETAIL_SELECT` ganan `last_reply_at, last_reply_sender` (T0.2, 5/9/2026), y los mappers (`mapBoardConversation`/`mapConversationSummary`/`mapConversation`) los pasan crudos, sin `new Date`. `FetchConversationsOptions.order?: "recent" | "oldest"` (T1.3, 5/9/2026, píldora "Más antiguas"): con `"oldest"` el orden base se da vuelta entero (`last_message_at asc nulls first, id asc`) y el predicado del cursor se arma espejado — la zona nula cambia de lugar (al principio en vez de al final), así que un cursor CON fecha ya no necesita el tercer término que cruza hacia ella (quedó atrás) y un cursor SIN fecha sí necesita uno nuevo (`last_message_at.not.is.null`) para cruzar HACIA la zona con fecha. Verificado contra PostgREST local el 5/9/2026, mismo método que el descendente el 29/8/2026. `escalatedOnly` (T1.5, mismo día, píldora "Escaladas"): `journey_stage = 'assigned' and ai_enabled = false and status <> 'closed'` por `.eq()`/`.neq()`, más `(last_reply_sender is distinct from 'agent' or awaiting_reply)` como un grupo más de `orGroups` — usa el índice `conversations_escalated_idx` de T0.1. `fetchInboxCounts` gana `escalated` con el mismo predicado (`data-inbox-counts.test.ts`). `fetchPinnedIds` (T2.2, 5/9/2026): `conversation_pins` filtrado por `agent_id` (explícito en el `.eq()`, aunque la RLS de la tabla ya filtre lo mismo — mismo motivo que `viewerId` en `fetchInboxCounts`: la condición explícita es la que un mock de test puede afirmar), devuelve un `Set<string>` de ids, no filas. `fetchWhatsappChannelHealth` (T3.4, 5/9/2026): calidad/límite/restricciones/fecha del canal que el webhook fue guardando; prefiere el canal `connected` y si no hay ninguno cae al primero que exista, null solo si no hay ningún canal creado. `mapMessage` gana `whatsappErrorCode: row.whatsapp_error_code` (T3.3, 5/9/2026): crudo, al lado de `whatsappError` (ya resuelto por `failureReason`) — la columna ya viajaba en `MESSAGE_SELECT`, faltaba mapearla al tipo de dominio. T3.2 (5/9/2026): `MESSAGE_SELECT` suma `payload` (`mapMessage` lo pasa crudo); `CONVERSATION_DETAIL_SELECT` suma `referral` (`mapConversation` lo pasa crudo) — ninguno de los dos entra a `CONVERSATION_BOARD_COLUMNS`/`CONVERSATION_LIST_SELECT`: la bandeja no pinta el banner de anuncio, solo el chat abierto. B3 (5/9/2026): `fetchAgentSettings` suma `business_hours` al select y devuelve `businessHours` vía `parseBusinessHours`; `fetchBusinessHours` es la lectura liviana (sin el RPC `agent_spend_today`) que nunca lanza —error o fila rota → default— para quien solo necesita el horario: `src/app/page.tsx` la usa para pasarle `businessHours` a `DashboardView`. Usa `console.error` y no `lib/log.ts` porque `data.ts` también se importa desde el cliente y `log.ts` es `server-only`. T7 (corrida "La IA ve lo que llega", 8/9/2026): `unansweredFreeWork` en `fetchBacklogConversationIds`/`fetchBacklogCounts` pide `"id, last_customer_message_at"` en vez de solo `"id"` y llama a `conversationsWrittenByHumans` (`human-handled.ts`) con `{id, lastCustomerMessageAt}` de cada fila y `{now}` — el diálogo de encendido de la IA deja de descontar para siempre un chat con una nota vieja de un asesor: solo lo descuenta si el asesor se adelantó al cliente o escribió hace menos de `AI_HUMAN_GRACE_MINUTES`. T7 (corrida "La IA ve lo que llega", 8/9/2026): `unansweredFreeWork` en `fetchBacklogConversationIds`/`fetchBacklogCounts` pide `"id, last_customer_message_at"` en vez de solo `"id"` y llama a `conversationsWrittenByHumans` (`human-handled.ts`) con `{id, lastCustomerMessageAt}` de cada fila y `{now}` — el diálogo de encendido de la IA deja de descontar para siempre un chat con una nota vieja de un asesor: solo lo descuenta si el asesor se adelantó al cliente o escribió hace menos de `AI_HUMAN_GRACE_MINUTES`. T6 (8/9/2026, "Agregar contacto" desde la bandeja): `fetchDefaultChannel` — mismo criterio "connected primero, si no el primero que exista" que `fetchWhatsappChannelHealth`, null solo sin ningún canal creado — resuelve el canal con el que `createContactConversation` (`mutations.ts`) crea la conversación de un contacto que todavía no escribió. T1 del plan "Seis frentes del buzón" (8/9/2026, "habló hoy"): `FetchConversationsOptions` gana `since?: string` — se suma como un grupo más de `orGroups` (`last_message_at.gte.<since>` o, sin `last_message_at` todavía [T6 crea la conversación antes del primer mensaje], `and(last_message_at.is.null,created_at.gte.<since>)`), nunca como un `.or()` propio. `fetchInboxCounts` gana un 4º parámetro `{since}`: los cinco conteos que ya tenían su propio `.or()` (`pendingStale`/`unread`/`escalated`) lo cruzan con el de `since` en una sola llamada vía `orExpression`, y `fetchUnassignedConversationIds`/`fetchUnassignedConversations` también lo reciben (la píldora "Sin dueño" también corta por "hoy"). 8/9/2026, "La bandeja habla español y el número nuevo queda a un clic": `fetchConversationIdByPhone(supabase, phone)` — `contacts` por `phone_number` → la conversación más reciente del contacto por `last_message_at` (`ascending: false, nullsFirst: false`, `limit(1)`; el UNIQUE es `(contact_id, whatsapp_channel_id)`, puede haber más de una), `null` sin contacto o sin conversación — resuelve el botón "Abrir el chat de +58…" del aviso de cambio de número; test `data-conversation-by-phone.test.ts` (3 casos; quitar el `order` rompe "la más reciente"). T3, "Los números del día" (10/9/2026): `InboxCounts` gana `mineUnread` — mismo OR que `unread` (`unread_count > 0 OR manually_unread`) más `assigned_agent_id = viewerId` por `.eq()`, para la tarjeta "Tuyas sin leer" del panel de inicio; va justo al lado de `unread` en el `Promise.all` de `fetchInboxCounts`, mismo motivo de no competir por índices (`data-inbox-counts.test.ts`). T2, "La IA no vuelve a pedir lo que ya pidió" — revisión (16/9/2026, migración `20260916010000`): `unansweredFreeWork` (dentro de `fetchBacklogConversationIds`/`fetchBacklogCounts`, el diálogo "encender la IA") suma `.eq("new_since_ai_resume", true)` en el WHERE, junto a `awaiting_reply` — sin este filtro el diálogo volvía a ofrecer un chat con un mensaje del cliente ANTERIOR a la última devolución humana (la despedida automática de una escalada, o un "¿ya me atienden?" escrito mientras esperaba al asesor) y encenderla mandaba la misma promesa dos veces; `new_since_ai_resume` (columna generada) compara contra el sello `ai_resume_cutoff_at`, que solo se mueve con una devolución, nunca con una salida — reemplaza al diseño descartado del 15/9 (`awaiting_any_reply` contra `last_message_at`, migración `20260915020000`, deshecha). Hallazgo 2 del plan "Seba atiende el mostrador" (18/9/2026, D2): `unansweredFreeWork` suma `.or("last_message_direction.eq.inbound,last_message_status.eq.failed")` — mismo predicado y misma historia que `reconciler.ts`: con la escalada sin apagar `ai_enabled`, una escalada sin asesor de noche (P1) no puede volver a ofrecerse en este diálogo una vez que su despedida ya salió bien. T5 del mismo plan: suma `fetchLessons` (mismo patrón que `fetchPlaybooks`/`fetchKnowledgeEntries` — todas, activas e inactivas, para Control IA), con `author:agents(display_name)` embebido; el turno de la IA NO la usa, lee directo con `fetchTurnLessons` (`lib/ai/lessons.ts`). T2, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D3): suma `fetchCatalogLinks` (todos, activos e inactivos, para el panel de Control IA; lanza) y `fetchActiveCatalogLinks` (solo activos, por `sort_order`; NUNCA lanza —cae a `[]` con `console.error`, mismo criterio que `fetchBusinessHours`— porque un enlace de catálogo es un dato de conveniencia que no debe tumbar un turno de IA ni dejar la bandeja sin abrir; T7, 19/9/2026: la consulta viaja en un `try/catch` propio, no solo en el `if (error)` de siempre — una excepción de verdad, p. ej. un `fetch failed` de red antes de que Supabase devuelva `{data, error}`, se colaba entera y rompía la promesa "nunca lanza"; mismo `console.error`, mismo `[]`); la usan el turno (con el cliente admin, T3) y el shell de la bandeja para los mensajes rápidos (T4b). T1a, mismo plan (D1): `dayCutGroup(since)` (función de módulo, junto a `fetchConversationRows`) arma el grupo OR de CUATRO términos —`last_message_at` desde `since`, o sin `last_message_at` con `created_at` desde `since`, o `unread_count > 0`, o `manually_unread`— que reemplaza al grupo de dos términos que traía `since` hasta esa fecha: "habló hoy" **o** "no leída", para que un chat con mensajes sin leer no se esfume de la bandeja "solo hoy" aunque su último mensaje sea de ayer. Lo usan `fetchConversationRows` (agregado a `orGroups`, nunca como `.or()` propio) y `fetchInboxCounts`; en los conteos `unread`/`mineUnread` el grupo NO se cruza a propósito —una fila no leída ya pasa el corte sola, cruzarlo ahí era lo que hacía desaparecer del CONTEO justo lo que esa píldora existe para mostrar—. `EXPLAIN ANALYZE` local (28 filas de la base sembrada) NO fue concluyente para decidir si el planner hace `BitmapOr` sobre `conversations_unread_pill_idx` o cae a `Seq Scan`: falta medirlo en producción tras el deploy, con volumen real |
| `dashboard-data.ts` / `dashboard-data.test.ts` | NUEVO, T2, corrida "Los números del día" (10/9/2026). `fetchLeadTotal(supabase)`: cuenta `conversations` con `last_customer_message_at is not null` (`count: "exact", head: true`, sin traer filas) — un lead es una fila de la tabla, una por contacto y canal; el `not null` excluye los contactos agregados a mano desde la bandeja (T6, 8/9/2026) que todavía no escribieron ni una vez. A propósito NO recibe `dayStart`: es la mitad "acumulado" del pulso del Recorrido, la otra mitad ("Nuevos"/"Con la IA"/"Con asesor") ya corta por hoy en `dashboard.ts`. Lanza si hay error, a diferencia de `fetchBusinessHours` — no hay un default razonable para "cuántos leads hay". Vive aparte de `data.ts` (no se tocó: otra tarea lo estaba editando en paralelo) siguiendo la convención de `lib/*-data.ts` del repo. |
| `mutations.ts` | Capa de ESCRITURA: enviar, asignar, etiquetar, cerrar venta, eventos de sistema en la burbuja. `closeConversation`/`reopenConversation` (T2.1, 5/9/2026) son la excepción a "toca Supabase directo": pasan por `fetch` a `api/conversations/[id]/close`/`.../reopen` porque `record_handoff()` solo la puede llamar `service_role`, mismo motivo por el que enviar mensajes pasa por `api/messages/send` en vez de tocar Meta desde el navegador. `pinConversation`/`unpinConversation` (T2.2, mismo día) SÍ tocan Supabase directo (`conversation_pins`, RLS `agent_id = auth.uid()` ya basta): el error del cuarto pin —el trigger `conversation_pins_limit_before_insert` de 20260905040000_conversation_pins.sql— sube tal cual, en español, listo para mostrarse. `sendReadReceipt`/`sendTypingSignal` (T3.1, 4/9/2026) son la misma excepción, un escalón más simple: `fetch` a `api/conversations/[id]/read`/`.../typing`, sin devolver nada y sin lanzar nunca (`try/catch` mudo) — un check o un typing que no salió no puede tumbar la bandeja ni bloquear a quien los llama. `sendTemplateMessage` (T3.3, 5/9/2026) gana `variables: string[]` (default `[]`): sigue mandando `content: template.bodyPreview` SIN sustituir —la sustitución la hace la ruta, una sola vez— y las variables viajan aparte para que `api/messages/send` arme los `components`. B5 (5/9/2026): `updateBusinessHours(supabase, agent, hours)` — mismo patrón que `setDailySpendCap` (update de `agent_settings.business_hours` + `updated_by`/`updated_at`, lanza si hay error; RLS ya limita a supervisor/admin); el panel valida el borrador antes de llamarla. Guarda de identidad (6/9/2026): `createPlaybook`/`updatePlaybook` corren `revealsIdentity(responseText)` (`identity-guard.ts`) ANTES de tocar la base y lanzan `PlaybookIdentityError` ("El texto describe a la tienda como automatizada: «…»" o "…como una persona: «…»") si el texto del escenario calza — es la única otra vía, aparte del modelo, por la que sale texto al cliente sin pasar por la guarda en caliente; `playbooks-panel.tsx` la muestra en el toast. Tests: `mutations-playbook-identity.test.ts` (rechazo antes de tocar la base, en los dos sentidos, y los dos negativos del catálogo/uso real que exige la trampa de `automátic*` suelto). T4 (8/9/2026): `updateProductWeight(supabase, productId, weightKg: number \| null)` (UPDATE con `updated_at`); `weightKg: null` es un guardado legítimo, vuelve el repuesto a "sin cargar". T3a (8/9/2026, apéndice al final del archivo, biblioteca de stickers): `saveStickerFromMessage(supabase, message, agent)` guarda en la biblioteca el sticker de un mensaje ya recibido. Reescrita el 8/9/2026 ("El sticker que no entra", tras el 131053 real de producción, mensaje `642661b5…`): ya NO usa `storage.copy` —el camino rápido que movía el archivo dentro del mismo bucket sin bajarlo— ni su respaldo por `/api/media/...`; ahora BAJA el archivo primero, lo mide con `isAnimatedWebp`/`isWithinStickerLimit` (`sticker-image.ts`) y recién si entra en el límite (100 KB estático / 500 KB animado) sube y hace el insert — si no entra, tira `Error(stickerRejectionMessage(...))` ANTES de tocar storage o la tabla `stickers`, así que un sticker rechazado no deja nada escrito a medias. Guarda `animated` con el valor REAL detectado, no un default `false`: ese default mal puesto es justo lo que dejó pasar el sticker roto de producción. `createSticker(supabase, blob, name, agent)` sube un WebP armado desde cero (T3b, con canvas) e inserta. `deleteSticker(supabase, sticker)` borra la FILA primero —la RLS de `stickers` decide si este agente puede, y el error sube tal cual— y solo si eso funcionó borra el objeto del bucket, para no dejar un archivo huérfano sin fila si la RLS frena el borrado. `sendStickerMessage(conversationId, mediaUrl)` es el mismo camino que `sendMediaMessage` (`kind: "media"`, `api/messages/send`) pero con `mediaType: "sticker"` y sin `content`: Meta rechaza un sticker con caption. T5, "Seis frentes del buzón" (8/9/2026): `createInvoiceForSale`/`issueInvoice`/`voidInvoice` — ver `invoices.ts`/`invoices-data.ts` en este glosario. T6 (8/9/2026): `createContactConversation(supabase, {displayName, phoneNumber, agent})` — "Agregar contacto" desde la bandeja: pide el canal con `fetchDefaultChannel` (`data.ts`), inserta en `contacts` y en `conversations` (`status: "open"`), y ante `23505` (unique_violation) en cualquiera de los dos —el teléfono o el par contacto/canal ya existían— recupera la fila existente en vez de fallar y marca `existed: true`. Solo al crear de verdad (`existed: false`) deja una nota interna (`is_internal_note: true`, `sender_type: "system"`) "Contacto agregado desde la bandeja por «agente»" — no usa `insertSystemEvent` de arriba porque esa deja `is_internal_note` en su default `false`. Sin traspaso a `conversation_handoffs`: `awaiting_reply` es una columna GENERADA que nace en `false` sin mensaje del cliente, así que la invariante "ningún lead invisible" no aplica todavía. Tests: `mutations-playbook-identity.test.ts` (rechazo antes de tocar la base, en los dos sentidos, y los dos negativos del catálogo/uso real que exige la trampa de `automátic*` suelto). T5, plan "Seba atiende el mostrador" (18/9/2026, requisito 7): `createLesson`/`setLessonActive`/`deleteLesson` — mismo patrón que `createPlaybook`/`setPlaybookActive`/`deletePlaybook`. `createLesson` corre `revealsIdentity(content)` ANTES de insertar y lanza `LessonIdentityError` si calza (mismo criterio que `PlaybookIdentityError`); recorta `messageExcerpt` a 80 caracteres (`LESSON_EXCERPT_MAX_CHARS`) aunque el CHECK de la base permita hasta 200 — el extracto es solo contexto para la lista del panel. Tests en `mutations.test.ts`. T1, "El precio se lee en bolívares" (19/9/2026): `updateProductPrice` se borró junto con sus tests (no tenía ninguno propio en `mutations.test.ts`) — el precio deja de editarse desde el CRM, llega de `products` cargada por fuera (D2 del plan). T2, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D3): `createCatalogLink`/`updateCatalogLink`/`deleteCatalogLink`/`setCatalogLinkActive` — mismo patrón que `createKnowledgeEntry`/`updateKnowledgeEntry`/`deleteKnowledgeEntry`/`setKnowledgeEntryActive`, con `updated_by: agent.id`. La validación de formato vive en `validateCatalogLinkDraft` (`catalog-links.ts`) y la corre el panel ANTES de llamar acá — no se repite adentro porque, a diferencia de `createPlaybook`/`createLesson`, un enlace de catálogo no es texto que el modelo pueda usar para describirse (sin riesgo de identidad que cerrar). `sort_order` es la única columna que puede faltar en el borrador A PROPÓSITO: sin ella, un INSERT toma el DEFAULT de la base (0) y un UPDATE simplemente no la toca — la misma función arma el `row` para las dos mutaciones. Tests en `mutations.test.ts`. T5, mismo plan (D9-D11): `ContactSaleDetails` gana `saintInvoiceNumber: string` (obligatorio, NO es `invoices.number`). `closeSaleWithContactInfo` corre `validateSaleDraft` (`sale-draft.ts`) como SEGUNDA barrera —el modal ya validó, pero esta función se puede llamar desde cualquier otro lado— y lanza el primer mensaje ANTES de tocar la base; `whatsappNumber` recibe un valor fijo no vacío en esa llamada porque la función nunca recibe ni escribe el teléfono real (de solo lectura, garantizado por `contacts.phone_number not null unique`). `orders.insert` suma `saint_invoice_number` ya NORMALIZADO (`normalizeSaint`, recortado y sin espacios dobles) y el evento de sistema pasa a nombrar la factura ("Venta cerrada por… · Factura Saint 00123"). Corrección R2 (revisión `code-review high`, 19/9/2026): el `if (items.length === 0)` de acá arriba pasó a llamar a `validateSaleCart(items.length)` (`sale-draft.ts`) —la MISMA función que corre el toast del modal— en vez de un mensaje escrito a mano suelto; antes `itemCount` viajaba hasta `validateSaleDraft` sin que esa función lo mirara nunca. Tests en `mutations.test.ts`. T10, plan "Seba sale sin pisar a nadie" (19/9/2026, decisión D-A): `assignToMe`/`intervene` leen `readAssignedAgentId` (el `assigned_agent_id` vigente ANTES de tocar nada) y suman un SEGUNDO `UPDATE` propio —`silenceAiForManualTakeover(supabase, conversationId, previousAssignedAgentId)`, `ai_enabled = false`— DESPUÉS del que mueve `assigned_agent_id`, nunca en el mismo `UPDATE`: el trigger `handle_conversation_ownership_change` (20260917010000) solo deja la fila `reclamado` si `ai_enabled` no cambia en ESE `UPDATE`, y solo deja `silenciada_por_asesor` si `assigned_agent_id` no cambia en el suyo — un `UPDATE` conjunto de las dos columnas no habría dejado NINGUNA fila en `conversation_handoffs`, contra la invariante "ningún lead invisible". Motivo: con la guarda de apertura del turno reducida a `if (!convo.ai_enabled)` (T4, "Seba atiende el mostrador", 18/9/2026), un chat asignado con la IA encendida ya no corta el turno solo por tener dueño — sin este apagado, "Asignarme"/"Intervenir" dejarían a Seba contestando antes de que el asesor alcance a escribir. Si `ai_enabled` ya era `false`, el segundo `UPDATE` es inocuo (el trigger exige `old.ai_enabled = true` para escribir `silenciada_por_asesor`). Corrección post-revisión (`code-review high`, 19/9/2026, hallazgo 3): la primera versión, si el segundo `UPDATE` fallaba, lanzaba directo sin compensación — el chat quedaba ASIGNADO con Seba ENCENDIDA, el mismo C1 que T10 existe para cerrar, ahora por un corte de red. Ahora `silenceAiForManualTakeover` reintenta UNA vez; si vuelve a fallar, compensa devolviendo `assigned_agent_id` al valor previo y lanza el error ORIGINAL igual (si la propia compensación también falla, queda un `console.error` y se lanza el error original de todos modos — este archivo corre en el navegador, no puede importar `lib/log.ts`). El orden de los dos `UPDATE` (asignar primero, apagar después) no se invierte: apagar la IA primero y fallar el de asignar dejaría el chat SIN asesor Y SIN IA. T11, mismo plan (19/9/2026, cierra la decisión abierta #2 de la revisión `code-review high` del mismo día): `unassign` gana `readConversationBeforeUnassign` (lee `assigned_at`/`ai_enabled`/`status` ANTES de desasignar — si falla, `console.error` y no se reenciende nada, modo seguro) y, tras el `UPDATE` que pone `assigned_agent_id = null`, llama a `reenableAiIfAdvisorNeverWrote`: reenciende `ai_enabled` con un SEGUNDO `UPDATE` aparte del que desasigna (mismo motivo que T10: el trigger deja `desasignada_por_asesor`/`devuelto_a_ia` en dos pasos, y el BEFORE `handle_conversation_ai_resume` sella `ai_resume_cutoff_at`) SOLO si la IA estaba apagada, el chat no está cerrado, `assigned_at` no es `null`, `aiWasSilencedByThisTakeover` encuentra una fila `silenciada_por_asesor` con `created_at >= assigned_at` (la apagó el PROPIO tomar-a-mano, no una pausa manual de antes de asignarse el chat — condición agregada tras la primera versión de T11, mismo día: sin ella, un chat pausado a propósito y asignado DESPUÉS se reencendía al desasignar, pisando la pausa manual) y `advisorWroteToCustomerSince` dice que el asesor NUNCA le escribió de verdad al cliente desde `assignedAt` (mismo predicado que apaga la IA por trigger: `sender_type = 'agent'`, `direction = 'outbound'`, `is_internal_note = false`; una nota interna no cuenta). Nunca lanza: cualquier fallo de la cadena deja la IA apagada (`console.error`, mismo motivo que el resto del archivo — corre en el navegador). Límite conocido y aceptado: reencender a mano, pausar de nuevo y desasignar sin escribir, todo con el chat asignado, deja una fila `silenciada_por_asesor` indistinguible de la del tomar-a-mano y la IA se reenciende igual — distinguir los tres caminos que escriben esa razón exigiría una columna nueva. Tests en `mutations.test.ts` |
| `data-tags.test.ts` | El filtro por etiqueta de la bandeja: `fetchTagsInUse` (solo las etiquetas realmente aplicadas) y `tagId` en `FetchConversationsOptions`. Su test clave es el que afirma que una conversación filtrada por una etiqueta sigue trayendo TODAS las del contacto: resolver el filtro con un embed `!inner` dentro del select de la lista recortaría los demás chips de color, y ese es el motivo de que `tagId` se resuelva antes, contra `contact_tags`, y viaje como `contactIds`. Su test clave es el que afirma que una conversación filtrada por una etiqueta sigue trayendo TODAS las del contacto: resolver el filtro con un embed `!inner` dentro del select de la lista recortaría los demás chips de color, y ese es el motivo de que `tagId` se resuelva antes, contra `contact_tags`, y viaje como `contactIds`. Su test clave es el que afirma que una conversación filtrada por una etiqueta sigue trayendo TODAS las del contacto: resolver el filtro con un embed `!inner` dentro del select de la lista recortaría los demás chips de color, y ese es el motivo de que `tagId` se resuelva antes, contra `contact_tags`, y viaje como `contactIds`. Su test clave es el que afirma que una conversación filtrada por una etiqueta sigue trayendo TODAS las del contacto: resolver el filtro con un embed `!inner` dentro del select de la lista recortaría los demás chips de color, y ese es el motivo de que `tagId` se resuelva antes, contra `contact_tags`, y viaje como `contactIds` |
| `agent-day-data.ts` / `agent-day-data.test.ts` | T3, "Los números del día" (10/9/2026). Lo que pinta el panel de inicio del asesor: `fetchAgentDaySummary(supabase, {from, to})` llama al RPC `agent_day_summary` (migración 20260910010000, `auth.uid()` decide adentro, no un parámetro) y convierte `bigint`/`numeric` de texto a número (mismo motivo que `Number(row.monto_hoy)` en `fetchAgentMetrics`, `data.ts`); sin fila devuelve ceros (defensivo — el RPC siempre trae una, son subconsultas escalares); con error, lanza. `fetchAiAssignmentsToday(supabase, agentId, since, limit=5)` trae de `conversation_handoffs` los traspasos `to_kind='human' and to_id=agentId and reason='escalada'` desde `since` — `reason='escalada'` porque es el ÚNICO traspaso que significa "la IA ACABA de asignarte esto" (misma regla que documenta `isAssignmentNotice`, `assignment-notice.ts`: `asignada` se reescribe en cada mensaje del cliente a una conversación que YA tiene dueño, no es una asignación nueva). Nombre del contacto con el mismo orden que `mapContactName` (`data.ts`): `display_name ?? profile_name ?? phone_number`. `dayRangeFrom(dayStart)` arma `{from, to}` con `to = from + 24h` a partir del string de `useInboxDay`/`currentDayRange` — la misma medianoche que ya usa `since` en `fetchInboxCounts`/`FetchConversationsOptions`, para no recalcularla en un segundo sitio |
| `brand.ts` / `brand.test.ts` | Único lugar donde vive el nombre del negocio: `BUSINESS_NAME = "SBK Motors"` y `APP_TITLE` (T2, "La voz de mostrador con nombre propio", 15/9/2026). Módulo PURO sin imports. Hasta ese día "SBK Motorcycles" estaba escrito a mano en ~20 archivos (prompt, clasificador, herramientas, factura, título, login, seis cabeceras) y uno llegaba al cliente en cada saludo. Lo importan `prompt.ts` (interpolado en el template literal de módulo: el prefijo cacheable sigue siendo idéntico entre turnos), `classify.ts`, `tools.ts`, `knowledge.ts`, `invoices.ts`, `layout.tsx`, `login-form.tsx` y las seis vistas con `dash-brand-name`. Los identificadores de infraestructura (`sbk-motorcycles-crm` en la imagen, `REMOTE_DIR`, `.claude/launch.json`) y las citas históricas de frases reales en comentarios/tests conservan el nombre viejo a propósito. 18/9/2026 (T2a, plan "Seba atiende el mostrador", requisito 1 del cliente): suma `AI_NAME = "Seba"`, el nombre propio del agente de IA — mismo patrón de fuente única que `BUSINESS_NAME`. Lo importan `seba.ts` (el saludo literal y los textos fijos), `prompt.ts` (sección 1, interpolado en el template del módulo) y `message-bubble.tsx` (la etiqueta de la burbuja de un mensaje de la IA, antes "IA" a secas) |
| `customers.ts` / `customers-data.ts` | Sección Clientes: lógica pura (URL, paginación, gasto) / consultas alrededor de la persona |
| `invoices.ts` / `invoices-data.ts` | T5, plan "Seis frentes del buzón" (8/9/2026): las bases de la factura. `invoices.ts` es puro —sin `SupabaseClient`—: `formatInvoiceNumber` ("SBK-000001"), `computeInvoiceTotals`/`buildInvoiceDraft` (redondean CADA renglón a su propio centavo antes de sumar, no al final — evita el 1.0049999999999999 de sumar en USD de coma flotante), `INVOICE_ISSUER` (RIF/dirección/teléfono/ciudad en `null` a propósito: datos fiscales que el operador no definió, la hoja los pinta "Por definir"), `DEFAULT_TAX_RATE = 0` (IVA pendiente de decisión). `invoices-data.ts` lee (`fetchInvoicesForSale`, `fetchInvoice`, `fetchOrderItems`) y exporta `INVOICE_SELECT`/`mapInvoice` para que `mutations.ts` los reuse al devolver la fila recién escrita sin una segunda consulta. `mutations.ts` escribe: `createInvoiceForSale` (lee `conversations.order_id`, falla con "Esta venta no tiene orden registrada" sin uno), `issueInvoice`/`voidInvoice` (RLS los exige de supervisor/admin; `voidInvoice` no recibe `agent` a propósito, la migración no tiene columna `voided_by`). 15/9/2026: `INVOICE_ISSUER.name` toma `BUSINESS_NAME` de `brand.ts` (SBK Motors) en vez del literal |
| `inventory.ts` / `inventory-data.ts` / `inventory-freshness.ts` | Sección Inventario: lógica pura / consultas a `products` / cuán viejo es el inventario antes de que la IA lo cotice. T4, "Seis frentes del buzón" (8/9/2026): `weight_kg numeric(8,3)` (migración 20260909030000_products_weight.sql, `add column if not exists`) guarda el peso que Cashea exige para calcular el envío gratis; la IA NO lo lee, fuera de alcance de T4. `parseWeightInput` (coma o punto, vacío → `null` válido —"sin cargar", el estado de nacimiento de todo producto—, sin negativos, hasta 3 decimales, tope `MAX_WEIGHT_KG = 9999.999`) y `formatWeightInput` (borrador del campo: "" si null, si no 3 decimales fijos) reusan `ParseResult<T>`, que pasó a genérico (`value: T`, default `number`) para poder devolver `number \| null` sin romper `parseStockInput`/`parsePriceInput`. `InventoryFilter` gana `"sin-peso"` (`.eq("is_active", true).is("weight_kg", null)`); `summarizeInventory`/`fetchInventoryTotals` ganan `withoutWeight`, contado SOLO sobre los activos (mismo criterio que agotados/bajos: un producto desactivado no es un hueco por llenar). T1, "El precio se lee en bolívares" (19/9/2026): `parsePriceInput` se borró —el precio dejó de editarse desde el CRM, llega de `products` cargada por fuera (D2 del plan)— y la reemplaza `priceDisplay(product, rate)`, que devuelve `{ principal, pie }` ya formateados: con tasa, el bolívar sube a `principal` y la otra moneda baja al `pie` (USD → `Bs. …`/`$ …`; VES → `Bs. …`/`$ …` convertido); sin tasa (o tasa ≤ 0) `principal` queda en la moneda propia del producto y `pie` es `null`, sin inventar un número. No delega en `priceInBs` para el caso VES sin tasa porque esa función devuelve el precio igual (ya está en bolívares) sin mirar si hay tasa, y acá sí hace falta saberlo para decidir el `pie`. | `inventory.ts` / `inventory-data.ts` / `inventory-freshness.ts` | Sección Inventario: lógica pura / consultas a `products` / cuán viejo es el inventario antes de que la IA lo cotice. | `inventory.ts` / `inventory-data.ts` / `inventory-freshness.ts` | Sección Inventario: lógica pura / consultas a `products` / cuán viejo es el inventario antes de que la IA lo cotice. | `inventory.ts` / `inventory-data.ts` / `inventory-freshness.ts` | Sección Inventario: lógica pura / consultas a `products` / cuán viejo es el inventario antes de que la IA lo cotice |
| `dashboard.ts` / `dashboard.test.ts` / `dashboard-tickets.test.ts` | Reclamos (contacto etiquetado "Reclamo*"), ventana de 24h (`withinFreeformWindow`), recorrido, e `isStalePending` — ventana de 24h sin respuesta, hoy solo del Dashboard y el `AgentHomePanel` (la reforma del 28/8/2026 tarde le quitó a `inbox-sections.ts` la píldora "Pendientes" entera y con ella el test de contrato que las mantenía de acuerdo). El contrato se repuso el 30/8/2026 en `ventana-24h-contrato.test.ts`, pero amarrando otras patas: `inbox-sections.ts` ya no corta por ventana de 24h (su "Pendientes" vuelto parte por LECTURA), así que hoy ata `dashboard.ts` (`isStalePending`/`withinFreeformWindow`) ↔ `fetchInboxCounts.pendingStale` ↔ `fetchConversations({pendingWindow})`, las tres patas que sí siguen midiendo la misma ventana. `awaitingReply()` (T0.2, 5/9/2026) compara `lastReplyAt` contra `lastCustomerMessageAt` — antes comparaba `lastMessageAt`, que cualquier mensaje visible movía; gemelo SQL de esta función: `supabase/tests/awaiting_reply.sql`. `dashboard.test.ts` (nuevo) prueba `awaitingReply` con los cuatro casos espejo de los pasos (2)(4)(5)(6) de ese `.sql`. A1 ("El reloj dice la verdad", 5/9/2026): `stageOf` deja de confiar en `journey_stage` a ciegas — escalera de cinco peldaños (asesor asignado → herramienta corriendo → clasificando → primer contacto → consulta), todas bajo `awaitingReply` salvo la de asesor asignado; `journey_stage = 'assigned'` sin asesor ya no cuenta como "Con asesor" (cae hasta "Consulta"), y un `classifying`/`tool_running` escrito sin `awaitingReply` es un resto congelado que tampoco se honra. Reloj nuevo `waitingMinutes(c, now, hours = DEFAULT_BUSINESS_HOURS)`: mide desde `lastCustomerMessageAt` (nunca `lastMessageAt`, que una nota o un evento adelantan sin que el cliente haya hecho nada), `null` si la conversación está cerrada o no espera respuesta; en "Con asesor" usa `businessMinutesBetween` (`business-hours.ts`, B2) — de noche o domingo un asesor no está atascado. `isStalled(c, now, hours)` es la fórmula única exportada para que la UI no la duplique. `STAGE_DEFINITIONS.stallMinutes` pasa a `number | null` (first_contact nunca se atasca; umbrales 15/5/3/60 laborales). `minutesInStage` YA NO es el reloj del tablero — queda solo para `ticketQueue`. `buildJourney` gana `hours` con default y reordena: primero las que esperan (mayor espera arriba), luego por `lastMessageAt` descendente. `stageDetail` gana "esperando su siguiente mensaje" (first_contact) y "sin respuesta del cliente" (inquiry sin `awaitingReply`). `dashboard.test.ts` suma los cinco peldaños, la tabla de casos del artefacto (Diana, Carlos, Laura, Ana ×2, "Cliente de prueba", Roberto) y que `buildJourney` cuenta 1 solo atascado sobre esa base. T1, corrida "Los números del día" (10/9/2026): `stageOf`/`waitingMinutes`/`isStalled`/`buildJourney` ganan un `dayStart?: string | null` opcional AL FINAL de la firma (no rompe a `dashboard-view.tsx`, que sigue llamando sin él). `buildJourney` filtra, además de `isActive`, con una réplica PRIVADA de `matchesDay` (`inbox-filters.ts`) — no se importa porque ESE módulo ya importa de acá (`awaitingReply`/`contactName`) y la vía contraria cerraría un ciclo. El peldaño 4 de la escalera (`first_contact`) deja de exigir `!awaitingReply` y suma "creada hoy" cuando hay `dayStart`: pasa de "ya la atendieron y el cliente calló" (nunca se atasca, `stallMinutes: null`) a "escribió hoy y la IA todavía no responde" (`stallMinutes: 15`, SÍ se atasca); `stageDetail` para esa etapa muestra el `intent` mientras espera y "esperando su siguiente mensaje" cuando ya no. El orden dentro de cada etapa se da vuelta: de "mayor espera arriba" (5/9/2026) a "más nuevo arriba" por `lastCustomerMessageAt ?? lastMessageAt ?? createdAt` descendente, desempate por `id` — el punto rojo y el contador de atascados siguen marcando la urgencia. `inbox-filters.ts` (`matchesDay`) cambia su parámetro de `ConversationSummary` a un `Pick` estructural `{lastMessageAt, createdAt}` para poder servir también a `BoardConversation`. Nuevo `describe("buildJourney con dayStart — el tablero del día", …)` en `dashboard.test.ts`. Corrida "El Recorrido cuenta los números nuevos del día" (10/9/2026, misma tarde, segunda vuelta): medido en producción, el peldaño 4 daba CERO sobre 119 leads nuevos del día porque dependía de `welcomeSentAt` y `WHATSAPP_WELCOME_TEMPLATE` está vacía —ese campo nunca se sella—; sacarle solo esa condición no arreglaba nada de fondo (habría mostrado 1 de 119, porque casi ningún lead nuevo se queda quieto en un peldaño de escalera). El operador decidió que "Primer contacto" dejara de ser un peldaño EXCLUSIVO y pasara a ser una COLUMNA DE COHORTE: todo lo que entró hoy, aparezca donde aparezca ADEMÁS en su etapa real — una conversación puede salir en DOS columnas del tablero a la vez, a propósito. `stageOf` pierde el peldaño 4 y su parámetro `dayStart`: la escalera baja a CUATRO (asesor asignado → herramienta corriendo → clasificando → consulta) y el tipo de retorno pasa a `Exclude<JourneyStageId, "first_contact">` —el compilador garantiza que nunca vuelve a devolver esa etapa—; `waitingMinutes`/`isStalled` pierden el mismo parámetro, que ya no tenían a quién enhebrarle. Nace `isFirstContact(conversation, dayStart?)`: `createdAt >= dayStart` (no `welcomeSentAt`) más `lastCustomerMessageAt != null` —este segundo requisito deja fuera al contacto agregado a mano desde la bandeja (T6) que todavía no escribió—; sin `dayStart` devuelve `false` (lo contrario del viejo `stageOf`: sin día no hay cohorte que contar, dejar pasar todo duplicaría cada tarjeta). `buildJourney` arma la columna `first_contact` con `isFirstContact` y las otras cuatro con `stageOf`, ya no las cinco con la escalera. Nace `countStalled(conversations, now, hours?, dayStart?)`: cuenta conversaciones ÚNICAS atascadas sobre el mismo conjunto que `buildJourney` (`isActive` + `matchesDay`) — la usa el badge "N atascados" del encabezado, que antes sumaba `stage.stalled` de las cinco columnas y contaba dos veces la misma tarjeta cuando ya podía salir en dos a la vez. `STAGE_DEFINITIONS.first_contact.stallMinutes` vuelve a `null` (fue `15` entre las dos corridas del mismo día): ahora es documentación, no comportamiento — `stageOf` ya nunca devuelve `"first_contact"`, así que `isStalled` jamás llega a mirar ese umbral por esa vía; cada tarjeta de la cohorte se marca atascada con el umbral de su etapa REAL (60 min laborales con asesor, 15 sin él), para que "atascado" siga teniendo una sola definición. `stageDetail(c, "first_contact")` deja de tener texto propio y delega en `stageDetail(c, stageOf(c))` — sin recursión infinita, porque `stageOf` nunca vuelve a caer en `"first_contact"`. |
| `ventana-24h-contrato.test.ts` | Repuesto el 30/8/2026 (el original se perdió el 28/8/2026 tarde cuando la reforma le quitó a `inbox-sections.ts` la píldora "Pendientes" y con ella el contrato que la ataba a `dashboard.ts`). Amarra tres patas que tienen que medir la ventana de 24h con la misma vara —`withinFreeformWindow`/`isStalePending` en memoria (`dashboard.ts`), el contador `fetchInboxCounts.pendingStale` y la consulta paginada `fetchConversations({pendingWindow})`, ambas en `data.ts`— con fakes de `.select`/`.or`/`.range` de PostgREST y una tabla de filas alrededor del borde exacto del corte; confirma en su cabecera, leyendo el código, que `inbox-sections.ts` YA NO es una pata de este contrato (su `now` no lo usa ninguna rama). Gana (T0.2, 5/9/2026) un caso que amarra `isWithin24hWindow` (`whatsapp-window.ts`) como cuarta pata: mismo criterio de ventana, otra implementación (la usa `claimWelcome` del webhook). Su fake de conteos gana (T1.5, mismo día) las tres columnas de "Escaladas" —ninguna fila de este archivo tiene `journeyStage: "assigned"`, así que ese conteo da 0 en todos sus tests; el punto es que la quinta consulta de `fetchInboxCounts` no reviente contra este fake, no medirla acá |
| `invariante-leads-contrato.test.ts` | El gemelo en TypeScript de `supabase/tests/invariante_leads.sql`: prueba `isUnassignedLead` (`inbox-filters.ts`) con los MISMOS casos, mismos nombres, mismo orden que el `.sql` — si una de las dos definiciones cambia sin la otra, uno de los dos archivos se pone rojo. Caso 7 (anexo A1, 5/9/2026): la despedida de la IA al escalar sin asesores lleva `is_auto_reply = true`, así que `awaitingReply` sigue en `true` y el traspaso más reciente (`unassigned`) SÍ cuenta como sin dueño. Caso 8 (anexo A2, 5/9/2026): cerrada, reabierta por el cliente y CON asesor asignado — el traspaso más reciente es `human`, no `unassigned` — NO cuenta. Caso 9 (anexo B2, 5/9/2026): un escenario que escaló sin asesores cuyo texto salió con `is_auto_reply = false` (todavía no se sabía si haría falta un asesor) y se marcó `true` DESPUÉS con un UPDATE aparte — el trigger de B1 recalculó `awaiting_reply` de vuelta a `true`, y el traspaso más reciente sigue siendo `unassigned`/`escalada_sin_asesor` — SÍ cuenta |
| `inbox-filters.ts` | Las seis píldoras de la bandeja (Pendientes/Sin dueño/Escaladas/No leídas/Mías/Todos), iguales para todos los roles; `DEFAULT_INBOX_FILTER = "pending"` desde la tercera reforma (30/8/2026, medida contra producción: "No leídas" es subconjunto ESTRICTO de "Pendientes" —282 filas contra 51— así que 231 chats leídos-y-sin-responder no tenían ninguna píldora que los alcanzara). `case "pending"` en `matchesFilter`: `status !== "closed" && awaitingReply(conversation)`, el mismo predicado que ya vivía en `dashboard.ts` — la píldora no reinventa el corte de 24h, solo reintroduce en la lista el que ya existía. `case "escalated"` (T1.5, 5/9/2026): `journeyStage === "assigned" && !aiEnabled && status !== "closed" && (lastReplySender !== "agent" || awaitingReply(c))`. Hasta el anexo A1 (5/9/2026) el `|| awaitingReply` rescataba la escalación sin asesores donde la IA se despedía con un mensaje de cortesía y ESE mensaje apagaba `awaitingReply` sin que ningún humano hubiera escrito nada; desde A1 esa despedida sale con `is_auto_reply = true` y ya no la apaga, así que la pata `lastReplySender !== "agent"` queda como la única necesaria para ese caso — SE CONSERVA de todos modos porque sigue cubriendo escalaciones anteriores a A1 y cualquier otra respuesta de la IA previa a la escalación. Exporta `isUnread` (LA definición de "sin leer": `unreadCount > 0 || manuallyUnread`, corte GLOBAL de equipo — cerrar un chat no es leerlo). `SERVER_FILTER_LIMIT`/`serverFilterTruncated` (tope mudo de 200 filas y su aviso de recorte) se retiraron el 29/8/2026: las píldoras que resuelven contra la base pasaron a paginar por cursor (`inbox-paging.ts`) con `INBOX_PAGE_SIZE`, igual que "Todos" — el recorte silencioso que el aviso vigilaba dejó de existir. La guardia anti-crecimiento vive en su test, subida de 3 a 4 píldoras el 30/8/2026 junto con el regreso de "Pendientes", de 5 a 6 el 5/9/2026 junto con "Escaladas". `applyInboxFilters` gana `pinnedIds` (T2.2, mismo día, hasta tres chats fijados por asesor — `conversation_pins`): NO es un filtro, es un segundo `sort` ESTABLE aplicado DESPUÉS del `sort` normal — sube los fijados como grupo sin desordenar entre ellos ni entre el resto; una conversación fijada que no calza con la píldora activa sigue sin aparecer. C1 ("El reloj dice la verdad", 5/9/2026): `InboxCriteria` gana `unassignedIds?: ReadonlySet<string> | null` y `case "unassigned"` deja de devolver `true` sin condición: pasa solo si el id está en el set que trajo `fetchUnassignedConversations` — bug visto en la verificación visual del 5/9/2026: el conteo ya salía bien, pero la LISTA pintaba toda la ventana local que `searchableConversations` mezcla en `inbox-sidebar.tsx`; con el set en null (consulta en vuelo) no pasa nada, y la sidebar ya muestra "Buscando…". T1 del plan "Seis frentes del buzón" (8/9/2026, "habló hoy"): gana `matchesDay(conversation, dayStart)` — `lastMessageAt >= dayStart`, o sin `lastMessageAt` (T6 crea la conversación antes del primer mensaje) `createdAt >= dayStart`; `null` con `dayStart` (interruptor "Ver todo") deja pasar cualquier fecha. Comparación NUMÉRICA (`Date.parse`), no de texto — `dayStart` sale de `toISOString()` (sufijo `Z`) pero Supabase devuelve `timestamptz` con offset explícito (`+00:00`), y los dos textos no ordenan igual como string. `InboxCriteria` gana `dayStart?: string | null`; `applyInboxFilters` solo lo aplica cuando la búsqueda está vacía — con algo escrito en el cuadro, la búsqueda mira TODO el historial, decisión del operador. T1, corrida "Los números del día" (10/9/2026): `matchesDay` cambia su parámetro de `ConversationSummary` a un `Pick` estructural `{lastMessageAt: string | null; createdAt: string}` para que `buildJourney` (`dashboard.ts`) pueda aplicar el mismo corte sobre `BoardConversation` — ese archivo NO importa esta función (crearía un ciclo, `inbox-filters.ts` ya importa `awaitingReply`/`contactName` de `dashboard.ts`) sino que la replica en privado. T1b, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D1/D2): nace `passesDayCut(conversation, dayStart, keepId?)` — el corte que de verdad ve la bandeja desde esta fecha, envuelve a `matchesDay` con dos excepciones: sin leer (`isUnread`) pasa sin importar cuándo habló (D1, misma definición GLOBAL que ya usa "No leídas"), y la conversación SELECCIONADA (`keepId`) también pasa (D2) — al abrir un chat viejo sin leer, `markRead` lo pone en cero al instante, y sin este respaldo D1 sola dejaría que la fila se esfumara de la lista mientras el asesor la sigue mirando; al cambiar de chat, `keepId` deja de protegerla y sale como cualquier chat de ayer. `dayStart` nulo ("Ver todo") deja pasar cualquier cosa, igual que `matchesDay`. `applyInboxFilters` deja de invocar `matchesDay` directo y pasa a usar `passesDayCut`; `InboxCriteria` gana `keepId?: string | null`, que `inbox-sidebar.tsx` llena con `selectedId` (el mismo prop que ya tenía, sube al arreglo de dependencias del `useMemo`). `matchesDay` NO cambia de significado con este plan: sigue siendo, nada más, "habló hoy" — la copia privada de `dashboard.ts` (el Recorrido, sin noción de "leído") la sigue usando tal cual; lo que cambió es quién decide qué se pinta en la bandeja |
| `inbox-sections.ts` | Partición pura de la lista de bandeja en secciones por píldora: `pending` partida en "Sin abrir"/"Leídas sin responder" (por LECTURA, con `isUnread` — no por ventana de 24h, que sigue siendo terreno de `dashboard.ts`), `unassigned` una sola sin encabezado, `escalated` partida en "Sin asesor"/"Con asesor" (T1.5, 5/9/2026 — por si YA hay un asesor a cargo, no por lectura), `unread` una sola sin encabezado, `mine` partida en Sin leer/Leídas (mismo `isUnread`), `all` una sola; no reordena, respeta el sort activo. El parámetro `now` de `buildInboxSections` no lo usa ninguna rama (se conserva en la firma porque las seis píldoras comparten una sola función) — la prueba de que el módulo no corta por tiempo, solo por lectura |
| `inbox-paging.ts` | Cursor de paginación de la bandeja (29/8/2026): `ConversationCursor` (`lastMessageAt`/`id`, orden del servidor) y `cursorAfterPage` (última fila de una página, o `null` si vino vacía). Cursor por VALOR, no por posición: reemplaza el `offset` que `crm-shell.tsx` usaba para "cargar más", que perdía filas cuando una conversación subía al tope en medio de la bajada de la lista. Gana `mergeById` (une dos tramos sin repetir, el primero manda), movida de `crm-shell.tsx` tras una revisión de código: vivía duplicada con el dedupe-append que `loadMoreServerRows` reimplementaba a mano en `inbox-sidebar.tsx`. Gana `reconcileHead` (29/8/2026): une una cabecera fresca con lo acumulado y suelta lo que salió del conjunto —en "Pendientes"/"No leídas"/"Mías" la pertenencia es un predicado y las filas se van, cosa que `mergeById` no puede saber—, por posición e id y nunca por fecha. El módulo se queda PURO a propósito: sus tests corren en entorno node y `data.ts` lo importa desde el servidor. `reconcileHead` vale igual con `order: "oldest"` (T1.3, 5/9/2026): nunca mira qué significa "más profundo" en términos de fecha, solo posición dentro de `accumulated` — dado vuelta el orden de la consulta, cambia cuál fila queda "más profunda" (la más nueva en vez de la más vieja), no el algoritmo |
| `message-search.ts` | Buscar conversaciones por lo que se dijo adentro (no solo nombre/número) |
| `message-grouping.ts` | Galerías de fotos/videos consecutivos y separadores de fecha |
| `outbox.ts` | Cola de envío del composer (pura, sin red): reintento, descarte, limpieza |
| `sale-cart.ts` | Carrito de la venta en curso: lo cotizado por la IA más lo agregado a mano |
| `sale-draft.ts` / `sale-draft.test.ts` | T5, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D9-D11). Módulo PURO (sin React, sin Supabase) con los NUEVE campos obligatorios de "Cerrar venta": `SaleDraft` (los nueve campos), `SaleField` (nueve claves; cédula tipo+número cuenta como UNA sola, `"cedula"`), `SALE_FIELD_LABELS` (el ORDEN de sus claves es significativo: `close-sale-modal.tsx` recorre `Object.keys(...)` para decidir cuál es "el primer campo inválido" a enfocar, D10), `validateSaleDraft(draft)` (un mensaje por campo, `{}` sin errores — corre DOS veces con la misma regla: en el modal al intentar guardar, y dentro de `closeSaleWithContactInfo` como segunda barrera antes de escribir nada), `isValidCedulaNumber` (5 a 10 dígitos) y `normalizeSaint` (recorta y colapsa espacios internos repetidos a uno solo — más estricto que el CHECK de la base, que solo exige `btrim`). Corrección R2 (revisión `code-review high`, 19/9/2026): `SaleDraft` traía además `itemCount: number`, que `closeSaleWithContactInfo` le pasaba a `validateSaleDraft` sin que esa función lo mirara nunca —la regla "al menos un repuesto" quedaba viviendo solo en un toast a mano del modal, desconectada de cualquier segunda barrera de verdad—. `itemCount` se retiró de `SaleDraft` y nació `validateSaleCart(itemCount): string \| null`, la función pura que ahora corren TANTO el toast del modal COMO `closeSaleWithContactInfo`, para que un carrito vacío nunca pueda crear una orden de $0,00 con venta `won`. |
| `playbook-price.ts` | Detecta escenarios con precio escrito a mano (que envejece sin que nadie lo toque) |
| `catalog-links.ts` | T2, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D3/D4). Módulo puro (sin React, sin Supabase) de la fuente única `catalog_links`: reemplaza las URLs de Google Drive pegadas a mano en escenarios y mensajes rápidos —el catálogo de cascos tuvo cuatro IDs distintos en 25 días—. `validateCatalogLinkDraft(draft, existing)` valida clave/etiqueta/URL campo por campo (`existing` va SIN la propia fila al editar, para no marcarla como repetida contra sí misma); `slugifyKey(label)` propone la clave quitando tildes y cambiando espacios por guiones ("Exploradoras y Bombillos" → `exploradoras-y-bombillos`); `CATALOG_MARKER`/`CATALOG_LIST_MARKER` son regex de módulo con flag `g` — seguras con `.replace()` (reinicia `lastIndex` por spec en cada llamada) pero NO con `.test()`/`.exec()` repetidos sobre el mismo objeto, ver el docblock; `resolveCatalogMarkers(text, links)` reemplaza `{{catalogo:<key>}}` por la URL y `{{catalogos}}` por `formatCatalogList` (solo activos, por `sort_order`), y deja SIN RESOLVER (D6) el marcador de una clave inactiva o inexistente, devuelto en `missing`; `catalogMarkerFor(key)` arma el marcador canónico para el botón "Insertar catálogo" (T4a/T4b); `hasRawUrl(text)` avisa un enlace escrito a mano, mismo patrón que `hasHardcodedPrice`. Ajuste hallado implementando T3 (18/9/2026): `{{catalogos}}` con CERO catálogos activos ya NO se reemplaza por una lista vacía — cuenta como `missing` (clave sintética `"catalogos"`) y el marcador queda tal cual, mismo criterio D6 que un `{{catalogo:<key>}}` roto, para que `matchPlaybook` (`playbooks.ts`) saque el escenario de los candidatos en vez de mandar "Ver también: " sin nada detrás. Corrección de la revisión `code-review high` del 19/9/2026 (punto 1): un marcador MAL ESCRITO (`{{catalogo:cascos_nuevos}}` con guion bajo, `{{catalogo: exploradoras y bombillos}}` con espacios, `{{catalogo}}`/`{{catalogo:}}` sin clave, o `{{catalogo:cascos` sin cerrar) no calzaba `CATALOG_MARKER`/`CATALOG_LIST_MARKER` —la regex estricta exige una clave `[a-z0-9-]+`— así que `missing` quedaba `[]` y el texto crudo se fugaba tal cual al cliente, rompiendo D6. `resolveCatalogMarkers` suma un paso final con `LOOSE_UNRESOLVED_MARKER` (`/\{\{\s*cat[aá]logos?\b[^}]*\}?\}?/gi`, solo para DETECTAR, nunca para reemplazar texto): cualquier resto que huela a `{{catalogo…}}` después de resolver los marcadores bien formados entra a `missing` con su texto crudo recortado a 60 caracteres como "clave" — sin duplicar lo que un marcador bien formado (clave inactiva, o `{{catalogos}}` sin ningún activo) ya contó en el paso anterior. Los cuatro consumidores (fase 0 del turno, el badge de `playbooks-panel.tsx`, la marca de `quick-replies-modal.tsx`, el toast de `composer.tsx`) no cambiaron: todos miran solo `missing.length > 0` |
| `bcv-schedule.ts` | Cuándo volver a preguntar la tasa al BCV (no publica todos los días) |
| `chart-scale.ts` | Líneas de referencia de los gráficos |
| `format.ts` | Formatos de hora/moneda coherentes en toda la app |
| `time-zone.ts` | Una sola zona horaria: la del equipo (`NEXT_PUBLIC_CRM_TIME_ZONE`), no la del navegador. B2 (5/9/2026): gana `crmWeekday(instant, timeZone)` → 0..6 (0 = domingo) en la zona del equipo con `Intl`, nunca `getDay()` — misma técnica que `crmMinuteOfDay` |
| `business-hours.ts` | Único dueño del horario de atención (B2, "El reloj dice la verdad", 5/9/2026; puro: sin React ni Supabase). `parseBusinessHours` valida el jsonb de `agent_settings.business_hours` (forma, `HH:MM`, inicio < fin) y ante cualquier cosa rota devuelve `DEFAULT_BUSINESS_HOURS` (L–V 08:00–18:00) sin lanzar, porque lo llama un turno de IA. `DAY_BANDS`/`dayBand`/`greetingFor` son LA fuente de las franjas mañana/tarde/noche (`greeting-window.ts` las importa de acá). `businessStatus` dice si la tienda está abierta, a qué hora cierra y cuándo abre (`dayLabel` es "hoy" o "el lunes" — NUNCA "mañana": chocaba con la franja "mañana" en el mismo renglón del prompt, decisión del 5/9/2026), `describeSchedule` la frase "lunes a viernes de 8:00 am a 6:00 pm", `businessMinutesBetween` los minutos de horario laboral entre dos instantes (el reloj de "Con asesor" en el tablero, A1) y `turnClockLine` el renglón completo del `TURNO ACTUAL` (B3). Todo con instantes UTC y zona explícita, sin depender del `TZ` del proceso. 14/9/2026 (T2, "La voz cercana y la espera visible"): `turnClockLine` deja de traer franja y saludo —el cliente reportó saludos por hora en mitad de conversaciones ya empezadas, y 3 con la franja equivocada en 72 h, porque el saludo se decidía por el reloj y no por si era el primer mensaje—; el renglón de `TURNO ACTUAL` queda en hora + horario + estado, y el saludo lo decide el sufijo `needsGreeting` de `prompt.ts`. `dayBand`/`greetingFor`/`DAY_BANDS` no se tocan: los sigue usando `playbooks.ts` para los disparadores horarios de los escenarios. 15/9/2026 (T3): `greetingFor` recupera su llamador en `prompt.ts` (`buildInstructions`, solo en el sufijo del primer mensaje); `turnClockLine` sigue sin franja a propósito, documentado en su docblock. 15/9/2026 (T4): el módulo que acotaba por franja horaria los escenarios de saludo del panel (`greeting-window.ts`) se retiró entero; `DAY_BANDS`/`dayBand` quedan como la ÚNICA fuente de franjas, usada por `playbooks.ts` (`buildPrompt`) y `prompt.ts` (`buildInstructions`) |
| `venezuela.ts` | Los 24 estados |
| `storage.ts` | La URL que se guarda en `messages.media_url`: relativa al CRM, no al bucket |
| `media-link.ts` | URL firmada de 10 min para que Meta descargue el adjunto saliente |
| `sticker-image.ts` | T3a ("Seis frentes del buzón", 8/9/2026). Módulo PURO (sin DOM, sin imports) con la geometría y los límites de los stickers de WhatsApp: `STICKER_SIDE` (512), `STICKER_STATIC_MAX_BYTES`/`STICKER_ANIMATED_MAX_BYTES` (100 KB / 500 KB, los límites de Meta), `fitInSquare(width, height, side?)` encaja con "contain" centrado y nunca agranda una imagen ya más chica que el lado, `qualityLadder()` los pasos de calidad WebP para reintentar la codificación (0.92 → 0.5 de a ~0.07) e `isWithinStickerLimit(bytes, animated?)`. El canvas que arma el archivo de verdad es de T3b, que consume estas funciones. "El sticker que no entra" (8/9/2026, tras el 131053 real de producción — mensaje `642661b5…`, conversación `3b654d2c…`, sticker animado de 973.668 bytes guardado con `animated: false`): suma `isAnimatedWebp(bytes)` (lee `RIFF`+`WEBP`+`VP8X` y el bit `0x02` del byte de flags en el offset 20; nunca tira, un buffer corto o que no arranca `RIFF` da `false` sin más), `stickerLimitFor(animated)` (100 KB/500 KB según el bit leído, no según la columna `animated` de la fila) y `stickerRejectionMessage(bytes, animated)` (texto para el asesor con tipo, peso y límite en KB — no alcanza con "es muy pesado", el asesor no puede adivinar que el límite de un animado es cinco veces más chico) |
| `stickers-data.ts` | `fetchStickers(supabase)`: la biblioteca completa, más recientes primero, mapeando `storage_path` a la ruta propia del CRM con `mediaUrlFor` (T3a, 8/9/2026) |
| `sticker-canvas.ts` | T3b ("Seis frentes del buzón", 8/9/2026). `"use client"`, no PURO (toca `<canvas>`/`createImageBitmap`) — arma el WebP final del sticker que crea el asesor: `renderStickerWebp(file, deps?)` decodifica la imagen, la encaja en 512×512 con `fitInSquare` (`sticker-image.ts`, T3a) sobre un lienzo transparente y recorre `qualityLadder()` exportando a `image/webp` hasta que `isWithinStickerLimit` da `true`; `null` si ni la calidad más baja entra. Las dependencias de canvas van inyectadas (`StickerCanvasDeps`: `decodeImage`/`createCanvas`, default al DOM real) para poder probarlo en jsdom con fakes — jsdom no implementa un `<canvas>` de verdad |
| `composer-text.ts` | T3b (8/9/2026). Módulo PURO (sin DOM): `insertAtCaret(text, selectionStart, selectionEnd, insertion)` inserta (o reemplaza la selección) y devuelve `{text, caret}` con el cursor justo después de lo insertado; índices acotados a los límites reales del texto. Lo usa `composer.tsx` para meter un emoji donde estaba el cursor — el picker nunca tuvo el foco del cuadro, así que la aritmética vive separada y se prueba sin levantar un textarea |
| `whatsapp-window.ts` / `whatsapp-window.test.ts` | La regla de las 24h de Meta: `WINDOW_MS`, `isWithin24hWindow` y `hoursUntilWindowCloses` (las usan el webhook, `agent.ts` y la lista). T2 de la corrida "La ventana de 24 h dice la verdad" (7/9/2026) suma `windowClosedByMeta(messages)` —true si el saliente más reciente con `whatsappStatus === 'failed'` y `whatsappErrorCode === 131047` no tiene ningún entrante posterior que no sea `unsupported`; ordena por `createdAt` porque el array no viene garantizado en orden— e `isComposerWindowOpen(lcma, messages, now?)` = `isWithin24hWindow && !windowClosedByMeta`: espejo en memoria de los dos candados de la migración 20260907010000, red de seguridad del composer para el hueco entre el rechazo de Meta y el refresh por realtime. El test (nuevo, 7/9/2026) espeja por nombre los casos 1-5 y 7-9 de `supabase/tests/ventana_24h.sql` (el 6, backfill, es solo SQL) |
| `redis.ts` | Conexión compartida a Redis (una por proceso) |
| `log.ts` | Registro estructurado JSON (`{"level","event","ts",...}`), eventos en español, oculta valores sensibles. `errorText` es el ÚNICO traductor de errores a texto de log (8/9/2026): antes de mirar `instanceof Error` revisa si el objeto trae `message` + `code` (cubre `PostgrestError`, `AuthError`, `StorageError` — `PostgrestError` sí extiende `Error`, así que si `instanceof` ganara primero el código se perdía igual) para devolver `"código: mensaje"`; si no hay código usa solo el mensaje; strings tal cual; `undefined`/`null` literales; cualquier otro objeto cae a `JSON.stringify` acotado a 300 caracteres. Corrige `turno_lock_no_liberado` y `webhook_error_actualizar_estado`, que salían con `detail: "[object Object]"` pese a llamar ya a `errorText` |
| `permisos-funciones.test.ts` | Guardián estático: lee las migraciones de `supabase/migrations` y falla si una función `security definer` nueva no trae su `revoke execute ... from anon, authenticated` explícito por firma en la misma migración que la crea |
| `agent-tool-keys.ts` | Claves de `agent_tools` sin `server-only`, para que el panel las use |
| `use-clock.ts`, `use-debounced-callback.ts`, `use-element-width.ts`, `use-long-press.ts`, `use-theme.ts` | Hooks utilitarios (reloj cuantizado, agrupación de refrescos, ancho real, long-press, tema) |
| `use-inbox-day.ts` | T1 del plan "Seis frentes del buzón" (8/9/2026): `useInboxDay(scope: InboxDayScope): string \| null` — la medianoche de HOY en `America/Caracas` (ISO, `currentDayRange` de `time-zone.ts`), o `null` con `scope === "all"` (interruptor "Ver todo"). ÚNICA fuente del corte: el mismo string viaja a `FetchConversationsOptions.since`/`fetchInboxCounts` (`data.ts`) y a `matchesDay` (`inbox-filters.ts`) — las tres partes calculando la medianoche cada una por su cuenta habría sido la forma de que un chat entrara en la lista pero no en el conteo. `useClock()` cuantiza al minuto para enterarse de que rodó el día sin recargar, pero el `useMemo` depende de `dayStartMs` (el instante de la medianoche, recalculado cada render con `currentDayRange` — barato, misma técnica que `business-hours.ts`), no del tick del reloj: el string que devuelve solo cambia dos veces por día, al rodar la medianoche o al tocar el interruptor, nunca cada minuto |
| `read-on-arrival.ts` | T1.1 (4/9/2026): `decideReadOnArrival({visibilityState, hasFocus})` → `"mark" \| "defer"` y `shouldFlushDeferred(visibilityState)`. Antes, el INSERT de un mensaje entrante en `messages-{id}` (`crm-shell.tsx`) marcaba leído sin preguntar nada; con dos pestañas abiertas y el mismo chat de fondo en una, un mensaje nuevo apagaba "No leídas" ahí sin que nadie lo hubiera visto. Puro y sin DOM a propósito: lo prueba `read-on-arrival.test.ts` con una tabla de casos, y lo consume `crm-shell.tsx` pasando `document.hidden ? "hidden" : "visible"` (no `document.visibilityState` directo: en jsdom viajan desincronizados, y `use-live-refresh.ts` ya usa `document.hidden` como la señal de pestaña oculta) |
| `realtime-status.ts` | `nextRealtimeAction(previous, status)` (F9, 4/9/2026): módulo puro que decide qué hacer con el callback de `channel.subscribe((status, err) => …)` de `@supabase/realtime-js` — caer por primera vez (`CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED`) avisa (`log_down`), seguir caído no repite el aviso, y volver `SUBSCRIBED` tras una caída pide resincronizar (`resync`). Lo usan `use-live-conversations.ts` y los cinco canales angostos de `crm-shell.tsx` (`unassigned-handoffs`, `agent-settings-changes`, `quick-replies-changes`, `tags-changes`, `messages-{id}`); todos caen a `console.warn`, no a `log.ts` (`server-only`, no se puede importar desde el navegador) |
| `use-live-conversations.ts` / `use-live-refresh.ts` / `use-live-sales.ts` | Realtime: mantener bandeja/ventas al día aplicando eventos en memoria y agrupando refetches. El `patch` de `applyConversationRow` aplica `last_reply_at`/`last_reply_sender` (T0.2, 5/9/2026) igual que el resto de columnas propias de la fila. F9 (4/9/2026): `use-live-conversations.ts` resincroniza con `realtime-status.ts` al reconectar tras una caída del canal; `use-live-refresh.ts` guarda `lastRefreshAt` y, al volver la pestaña, refresca si hay algo pendiente O si pasaron más de `STALE_AFTER_MS` (2 min) sin un refresco real — cubre la caída que no anota nada porque el canal simplemente dejó de escuchar |
| `use-inbox-pager.ts` | El paginador por cursor de la bandeja, uno solo para las tres píldoras (29/8/2026): cursor + candado en vuelo + sesión + `reachedEnd` + estado de la primera página (loading/ready/error). Todo lo que decide se lee de un ref y no del render: dos eventos de scroll del mismo frame veían el mismo snapshot y disparaban dos consultas con el mismo cursor. Nació de una revisión que encontró la misma máquina escrita dos veces —`cursorRef`+flags en el shell, `serverRows`+`serverSessionRef`+`serverBusyRef` en el sidebar— endurecida a medias en cada copia. El shell lo usa sembrado (`seed`); el sidebar, uno por píldora (`sessionKey`). Un fallo NUNCA enciende `reachedEnd`: por eso existe `status: 'error'` con `retry()` |
| `sales-day.ts` / `sales-day.test.ts` | T5, corrida "Los números del día" (10/9/2026). Módulo PURO (sin React, sin Supabase): corta el histórico completo que trae `fetchSales` por día de `America/Caracas` en memoria, para la pantalla de Ventas. `crmDayKey`/`todayKey` usan `Intl.DateTimeFormat("en-CA", {timeZone})` (misma técnica que `time-zone.ts`) — **`dayKey` de `format.ts` NO sirve acá**: agrupa con `new Date(iso).getFullYear()/getMonth()/getDate()`, que lee la zona del NAVEGADOR, no la del equipo. `saleDayKey(sale)` usa `dealClosedAt ?? createdAt`. `summarizeSalesDay` cuenta/suma `count`/`amountUsd` solo sobre `dealStatus === "won"` con `dealCurrency` "USD" o `null` (`closeSale` cierra siempre en USD, así que null no se descarta); `amountVes` suma aparte las won en VES para no mezclar monedas; `returned` cuenta las devueltas del día; montos a `Math.round(x*100)/100` (mismo criterio que `computeInvoiceTotals` en `invoices.ts`). `salesDayHistory` devuelve solo los días CON ventas, más reciente primero. `shiftDayKey` (aritmética en `Date.UTC` sobre la clave, sin zona) mueve las flechas ‹ › del histórico; `formatDayKey` escribe "10 de septiembre de 2026" con `date-fns`/`locale: es` sobre `Date.UTC(y,m-1,d,12)` (mediodía UTC, mismo truco que `inventario-view.tsx` con `T12:00:00Z`, para que ningún huso corra el día) |
| `assignment-notice.ts` | NUEVO, T5 ("El aviso que llega", 8/9/2026). Módulo PURO (sin React, sin Supabase, sin DOM): decide si una fila NUEVA de `conversation_handoffs` (llega por realtime como INSERT) merece avisarle al asesor que la IA le acaba de asignar algo. `isAssignmentNotice(handoff, myAgentId)` exige las tres a la vez —`to_kind === "human"`, `to_id === myAgentId`, `reason === "escalada"`—: `reason` es la trampa real, `asignada` (`agent.ts`) se escribe cada vez que llega un mensaje a un chat que YA tiene dueño, no es una asignación nueva, y filtrar solo por `to_kind`/`to_id` haría saltar el aviso en cada mensaje del cliente durante el resto de la conversación; `created_by` no sirve como filtro, vale `'system'` en casi todo. `markAssignmentNoticeSeen`/`shouldShowAssignmentNotice`: dedupe por id de handoff en un `Set` de MÓDULO (no de instancia, tope 200 FIFO) — necesario porque `AppRail` se monta también dentro de `section-skeleton.tsx` y durante una navegación coexisten dos instancias recibiendo el mismo INSERT |

## `src/lib/ai` — el agente vendedor

| Archivo | Qué es |
|---|---|
| `history-line.ts` | T2 (8/9/2026, "La IA ve lo que llega"). Módulo PURO (sin `server-only`, sin más import que tipos): convierte una fila de `messages` en la línea que ve el modelo. `historyLine(row)` mantiene las reglas ya vigentes (nota interna, `sender_type === "system"`, `message_type === "unsupported"` → `null`; texto/`location`/`contacts`/`interactive`/`order`/`template`/`system_event` → `content` tal cual) y agrega: foto/video/nota de voz/documento/sticker del CLIENTE sin `content` legible ya no se descartan — arman un marcador entre corchetes (`[El cliente envió una foto sin texto; no puedes verla]`, con pie si trae `content`: `[El cliente envió una foto. Pie: …]`); lo mismo saliente del ASESOR (`[El asesor envió una foto]`). `messages.content` en la base NO se toca — el marcador vive solo en memoria, lo que sale de `historyLine`. Sus textos exactos tienen que coincidir con la sección 7 (`MEDIA_RULES`) de `prompt.ts`. `isHistoryMarker(text)` reconoce esas líneas (`^\[(El cliente\|El asesor) envió `) para que `agent.ts` las trate distinto de texto real (fase 0, `lastCustomerMessage`, `alreadySentPlaybook`/`alreadyRedirected`). T6 ("La voz cercana y la espera visible", 14/9/2026): `mediaStreakWithoutText(history)` cuenta desde el final la racha de adjuntos del cliente SIN pie (los cuatro marcadores fijos de foto/video/audio/documento; el sticker no cuenta ni corta) hasta la primera línea de cliente con texto, y dice si en medio la IA ya respondió (`yaPreguntamos`); `agent.ts` escala al segundo adjunto con pregunta ya hecha en vez de repetir "¿qué repuesto buscas?" hasta 10 veces (494 fotos y 117 audios en 72 h). Parámetro tipado como interfaz mínima `{ role; content }[]` para no importar nada. T3, plan "Seba sale sin pisar a nadie" (19/9/2026, hallazgo A3): `customerBurst(history)` — las líneas del CLIENTE consecutivas desde el final del historial, en orden CRONOLÓGICO, hasta la primera línea —yendo hacia atrás— que no sea del cliente (una respuesta de la IA o de un asesor corta la ráfaga); un marcador de media entra como una línea más, sin tratamiento especial (no es ni saludo ni cortesía, así que cualquier guarda que exija que TODA la ráfaga lo sea ya lo descarta solo). Historial vacío o que termina en una línea que no es del cliente da `[]`. Comparte forma con `mediaStreakWithoutText`. La usa `agent.ts` para que `soloSaludo` y la guarda de cortesía tras escalada dejen de mirar solo la última línea: la cola agrupa ráfagas de mensajes seguidos (CLAUDE.md, "La respuesta llega en siete segundos"), y "Precio del casco LS2" + "Buenas tardes" en dos mensajes seguidos se leía como un simple saludo, dejando la pregunta real sin contestar. Corrección post-revisión (`code-review high`, 19/9/2026, hallazgos 4 y 8) sobre esa primera versión: (1) ahora se corta también por TIEMPO — `CUSTOMER_BURST_GAP_MINUTES = 10` entre dos líneas consecutivas del cliente, aunque no haya nada del CRM en el medio, porque un chat cerrado sin respuesta (`pausada`) no deja ninguna línea del asistente que corte la ráfaga vieja, y sin este tope un "hola" de HOY se pegaba a una pregunta sin contestar de hace DÍAS; una línea sin `createdAt` parseable (o comparada contra una que no lo tiene) corta la ráfaga ahí, de forma conservadora. `agent.ts` arma, solo para esta llamada, un arreglo paralelo `history.map((m, i) => ({ role, content, createdAt: historyCreatedAt[i] }))` — `ModelMessage` (el tipo que viaja hasta `agent.generate`) no trae fecha propia. (2) un STICKER del cliente (`CUSTOMER_STICKER_MARKER`) ya NO cuenta como línea de la ráfaga ni la corta —se salta sin tocar la marca de tiempo de referencia—, porque antes hacía fallar el `every(isCourtesyOnly)` de la guarda de cortesía y "gracias" + sticker con una escalada abierta dejaba pasar una segunda despedida encima de la primera |
| `agent.ts` | Orquestador del turno: fase 0 (escenario) ‖ fase 1 (intención) en paralelo → tool loop (máx. 5 pasos); mide tiempos por tramo. Antes de hablarle al cliente, `deliver()` confirma que el lock de `conversation-lock.ts` sigue siendo suyo (renovación fenceada por token); si lo perdió, no envía y queda `turno_lock_perdido_sin_enviar`. `deliver()` es genérica desde T0.3 (5/9/2026): devuelve el `DeliveryOutcome` de `enviar()` (o `null` si una guarda lo frenó), y los TRES consumidores que hablan de verdad —el escenario de fase 0, la redirección de fuera de tema y la respuesta final del tool loop— lo pasan por `deliveryFailed()` (renombrada de `rejectedByMeta`, S6, 8/9/2026): si el envío falló (`whatsapp_status: "failed"`) bifurca por `entrega.origenDelFallo` -- "red" (un TypeError/DNS/timeout que nunca llegó a la Graph API) deja `log.error("turno_envio_fallo_de_red")` y `recordHandoff(unassigned, entrega_fallida)`; "meta" (o `null`, compatibilidad) es el camino de siempre: deja `turno_rechazado_por_meta` en el registro y `recordHandoff(unassigned, rechazado_por_meta)` -- ninguno de los dos reintenta — salvo que en el tool loop ya haya corrido una escalación (`outcome.escalated`, de la herramienta o de la red de seguridad de devolución/queja): esa ya dejó su propio traspaso más específico, y un segundo `rechazado_por_meta` lo pisaría en el conteo de "Sin dueño", así que solo queda el log con `traspaso_omitido: "escalada_previa"`. `fireTypingIndicator` (T3.1, 4/9/2026) dispara "escribiendo…" justo al arrancar el tool loop —sin esperarlo, `sendTypingIndicator` ya no lanza— solo con la ventana de 24h abierta (`isWithin24hWindow`), el canal `connected` y algún mensaje entrante con wamid (`lastInboundWamid`, consulta aparte de `loadHistory`: esa trae texto para el modelo, no wamids). `loadHistory` (T3.2, 5/9/2026) suma `message_type` al `select` y salta explícito `message_type === "unsupported"` (antes lo sacaba solo la nulidad de `content`, que sigue dándose pero ya no es de lo único de lo que depende); un `order` entra igual que cualquier texto, con el resumen en español que ya arma el webhook en `content`. Anexo A1 (5/9/2026): el envío final del tool loop pasa `sendAgentText(..., { isAutoReply: outcome.escalated && outcome.unassigned === true })` — cubre los dos caminos por los que la IA se despide sin asesor (el texto fijo de la red de seguridad de devolución/queja y el que redacta el modelo tras la herramienta `escalarAAsesor`), copiando `outcome.unassigned` de `forced.unassigned` en la red de seguridad igual que ya se copiaba `escalated`/`assignedAgentName`. Anexo A2 (5/9/2026), `runAgentTurn`: las dos guardas silenciosas de apertura se invirtieron de orden — se mira `assigned_agent_id` ANTES que `ai_enabled` — para que un chat con dueño y la IA apagada en él (el estado normal tras una escalación o un cierre manual) deje `asignada`/`human` en la bitácora en vez de `pausada`/`unassigned`; el efecto observable (return sin enviar nada) no cambia. Anexo B2 (5/9/2026), `runPlaybook`: un escenario con `afterSend: "escalate"` manda su texto ANTES de escalar (T0.3 exige ese orden), así que ese mensaje sale con `is_auto_reply = false` sin saber todavía si haría falta un asesor. Si `escalateConversation` acaba de descubrir que no hay NADIE (`result.unassigned`), un UPDATE marca `is_auto_reply = true` en todo lo que la IA mandó en este turno (`conversation_id` + `direction: outbound` + `sender_type: ai` + `not is_internal_note` + `created_at >` el `last_customer_message_at` de la conversación, que `runTurnPhases` le pasa a `runPlaybook` como parámetro nuevo) — el trigger que sumó B1 (20260905070000) recalcula `last_reply_at`/`awaiting_reply` al ver ese cambio. Si `last_customer_message_at` viniera nulo (el tipo lo permite; no pasa en un turno real porque `withinFreeformWindow` ya lo exige antes) no se marca nada y queda `turno_escenario_sin_fecha_cliente` en el registro, para no arriesgar marcar la despedida de un turno anterior. Si el UPDATE falla, `log.error("turno_escenario_despedida_no_marcada")` y el turno sigue igual — es observabilidad de `awaiting_reply`, no una barrera; si sale bien, `log.info("turno_escenario_sin_asesor_marcado")`. A3 ("El reloj dice la verdad", 5/9/2026): `deliveryFailed`, en la rama que registra `rechazado_por_meta`, también pone `journey_stage = null` y `active_tool = null` en un solo UPDATE — antes ese `return` salía antes del reseteo normal y el tablero veía "Clasificando"/"Herramienta" congelados para siempre; NO se toca en la rama `yaEscalada`, donde `escalateConversation` ya dejó `assigned` antes del envío. Si el UPDATE falla, `log.error("turno_etapa_no_reseteada_tras_rechazo")` y el turno sigue: la fila de traspaso ya quedó escrita. B3 (5/9/2026): `runAgentTurn` suma al `Promise.all` de arranque (`agent_can_run` + conversación) una lectura de `agent_settings.business_hours`, la pasa por `parseBusinessHours` (fila rota, sin fila o error → default y `log.warn("turno_horario_no_legible")`, el turno nunca se cae por el horario) y la enhebra a `runTurnPhases` → `matchPlaybook`, `buildInstructions` y el objeto `deps` de las herramientas (donde B4 la toma para la despedida al escalar). B4 (5/9/2026): `runPlaybook` recibe `businessHours` y lo enhebra a `escalateConversation` en el camino de escenario con `afterSend = "escalate"`; la despedida fija de la red de seguridad (devolución/queja sin escalar) sigue sin horario, a propósito. D2 (6/9/2026): la guarda (b) de `deliver()` —`stillEnabled`, que vuelve a consultar `agent_can_run`— deja `agente_no_puede_correr` en vez de `pausada`; esa razón queda reservada para el `ai_enabled` de ESTE chat, que solo mira la guarda de `openTurn`. Guarda de identidad (6/9/2026): `applyIdentityGuard(...)` es el único punto de aplicación, dentro de `runTurnPhases`, sobre el texto final del tool loop —después de la red de seguridad de devolución/queja, justo antes del `deliver(... sendAgentText ...)` de la redacción—. Tres pasos: (1) `revealsIdentity` detecta; (2) si calza, UNA reescritura con `generateText` (no `ToolLoopAgent`: las herramientas ya corrieron y volver a dárselas arriesga una segunda escalación), modelo `getAgentModel("low")`, `maxRetries: 0`, tokens sumados a `turnTokens`, y el resultado vuelve a pasar por `revealsIdentity`; (3) si sigue calzando —o si `generateText` falló— ese texto NO sale: se escala con `escalateConversation` motivo `seguimiento` ("La respuesta redactada se describía como automatizada y no pudo corregirse. Retomar el hilo.") y sale la despedida fija de siempre (`DESPEDIDA_SIN_ASESOR`/`DESPEDIDA_CON_ASESOR`, `is_auto_reply` con la regla de siempre); si `outcome.escalated` ya era `true` (el modelo escaló antes de redactar) no se escala dos veces, solo se reemplaza el texto. Rastro: `log.warn("identidad_reescrita", {conversationId, categoria, fragmento})` cuando el reescrito arregló, `log.error("identidad_bloqueada", …)` cuando no, y el `summary` de `agent_turns` lleva el prefijo `[identidad reescrita]`/`[identidad bloqueada]`. `persona` y `automatizacion` bloquean igual (decisión del operador, 6/9/2026). `OFF_TOPIC_REPLY`, las despedidas fijas y los escenarios de `ai_playbooks` NO pasan por la guarda en caliente —son constantes o texto del supervisor—: las constantes se cubren con un test estático en `agent.test.ts`, los escenarios con la validación al guardar (ver `mutations.ts`). De paso (6/9/2026) la red de seguridad de devolución/queja copia también `outcome.motivo = intent`: su fila de `agent_turns` decía «Motivo: undefined.». Sin migración. T2 (8/9/2026, "La IA ve lo que llega"): `loadHistory` deja de descartar filas sin `content` — pasa cada fila por `historyLine` (`history-line.ts`) y usa lo que devuelva; `lastCustomerMessage` devuelve `null` si la última línea de cliente es un marcador (`isHistoryMarker`); `alreadySentPlaybook`/`alreadyRedirected` saltan marcadores salientes al buscar "nuestra última respuesta". Nueva `lastUserLineIsMarker(history)`: si la última línea del cliente es un marcador, `runTurnPhases` NO llama a `matchPlaybook` (usa `{ playbook: null, usage: ZERO_USAGE }` de `playbooks.ts`) y deja `log.info("turno_ultimo_mensaje_sin_texto")`; `classifyIntent` sigue corriendo igual. T4 (8/9/2026, misma corrida, Bug 2): el `if (history.length === 0) return;` deja de ser mudo — deja `log.warn("turno_sin_contenido_legible")`, `recordHandoff(unassigned, sin_contenido_legible)` y limpia la etapa (caso `cea69118…`; red de seguridad desde T2, que hace que un historial vacío casi no se dé). Nueva `resetStage(supabase, conversationId, evento)`: el UPDATE `journey_stage/active_tool = null` con manejo de error (`log.error("turno_etapa_no_reseteada", { evento, detail: errorText(error) })`, mismo patrón de observabilidad que `deliveryFailed`, que también la reusa en la rama "red") que ahora comparten esa salida, el `return` de clasificación fallida y el `catch` del tool loop — las dos últimas SIN traspaso nuevo (`agent_turns` ya queda con `action: "error"` y el reconciliador la recoge sola). Ajuste de medición: la construcción de la promesa de `matchPlaybook`/`ZERO_USAGE` se movió DENTRO del callback de `medir(..., "clasificacionMs", ...)`, junto a `classifyIntent`, para que el tramo se mida desde que la llamada se dispara de verdad. S6 (corrida "La IA ve lo que llega", hallazgo 4, 8/9/2026): el corte de red de OpenRouter del 7/9 a las 11:57 UTC dejó envíos `failed` con `detalle: "fetch failed"` que el código de entonces confundía con un rechazo de Meta -- `deliveryFailed` los distingue por `entrega.origenDelFallo` (`send.ts`) y ya no escribe `rechazado_por_meta` para un corte de red. T7 (misma corrida, 8/9/2026): la guarda de apertura de `runAgentTurn` y `deliver()` pasan `lastCustomerMessageAt` a `humanHasWritten`/`humanWroteMeanwhile` — la regla dejó de ser "¿alguna vez escribió un asesor?" y pasó a mirar fechas (ver `human-handled.ts`); `deliver()` gana ese parámetro y los tres llamadores (`runPlaybook`, la redirección de fuera de tema, la redacción final) se lo pasan. T0 ("La respuesta llega en siete segundos", 7/9/2026): `TurnTiming` separa `esperaMs` en `debounceMs` (diseño: la ventana de silencio) y `colaMs` (atraso: del vencimiento al arranque) a partir de `options.vencioEn` —un único `Date.now()` en `newTurnTiming` garantiza `debounceMs + colaMs === esperaMs`; ambos `null` en turnos fuera de la cola, como `api/dev/simulate-message`— y suma `herramientas` (nombres de `result.steps[].toolCalls[].toolName` unidos por coma, `""` si el tool loop no usó ninguna, `null` si no llegó) para poder medir T4 sin tocar el prompt; `runAgentTurn(conversationId, options?: { vencioEn?: number })`. T2 (8/9/2026, plan "Seis frentes del buzón") le había sumado al `select` de la conversación en `runAgentTurn` la columna `handoff_confirmation_pending_at`, y al `deps` que arma `runTurnPhases` para las herramientas `lastCustomerMessageAt`/`handoffConfirmationPendingAt` (de la misma fila `convo`), para que `buildEscalateTool` (`tools.ts`) reconfirmara el pase a ventas. Revertido el 9/9/2026 (corrida "El pase a ventas al primer sí"): esa reconfirmación hizo bucle en producción —el cliente contestaba "ok"/"está bien"/"dale" y el modelo no lo contaba como el segundo "sí" literal—, el operador aprobó volver a escalar con el primer aviso, y ambas cosas salieron del `select` y de `deps` sin reemplazo. T5 ("La voz cercana y la espera visible", 14/9/2026): `DESPEDIDA_CON_ASESOR` se reemplaza por `despedidaConAsesor(status?)`, que nombra la próxima apertura si la tienda está cerrada (Decisión 6); `isAutoReply` del envío final y del camino de escenario `escalate` dependen solo de `outcome.escalated`/`result.escalated`, ya no de `unassigned` (Decisión 3: la promesa de un asesor tampoco es una respuesta —170 promesas ≥ 30 min invisibles en Pendientes, 23 sin cumplir—, la conversación sigue `awaiting_reply` y "Con asesor" empieza a contar atascados); `stillEnabled` y la apertura de `runAgentTurn` distinguen "la RPC no contestó" (lanza, la cola reintenta, `turno_interruptor_no_consultable`) de "la RPC contestó que no" (`agente_no_puede_correr`/`turno_saltado_ia_apagada`) —13 turnos en 72 h figuraban "IA apagada" por cortes de base—; `logTurn` y el `update({intent})` dejan de tragarse su error (`turno_bitacora_no_escrita`/`turno_intencion_no_guardada`). La red de seguridad de devolución/queja ahora sí pasa `businessHours` a `escalateConversation`. T3 (14/9/2026): el `select` de `runAgentTurn` suma `display_name`/`profile_name` del contacto; `runTurnPhases` calcula `customerName` con `customerFirstName` y lo pasa a `buildInstructions`. `DESPEDIDA_SIN_ASESOR` y las tres ramas de `despedidaConAsesor` se reescriben cálidas (misma firma que dejó T5): cada una dice por qué se pasa el caso, qué va a pasar y agradece la paciencia, cumpliendo la regla "CÓMO SUENAS" del prompt. T4 (14/9/2026): `runTurnPhases` suma la guarda de cortesía tras una escalada abierta, justo después de calcular `customerMessage` (`lastCustomerMessage`) y ANTES de fase 0/clasificar: si el último mensaje es solo cortesía (`isCourtesyOnly`) y `escalationOpen` dice que la escalada sigue abierta, el turno se calla dejando traspaso `cortesia_tras_escalada` con el mismo dueño que ya tenía la conversación (`turno_cortesia_tras_escalada`, fila en `agent_turns`), sin llamar a `matchPlaybook` ni a `classifyIntent`; `awaiting_reply` no se toca a propósito. La rama "con asesor asignado" de esa guarda es defensiva e inalcanzable hoy: `openTurn` corta antes con `asignada` cuando hay asesor, así que el caso real es el de la devolución masiva del 13/9 (asesor quitado, IA reactivada, último traspaso `escalada`). T6 (14/9/2026): guarda de adjuntos en `runTurnPhases`, justo después de la de cortesía y antes de fase 0: si `mediaStreakWithoutText` da `adjuntos >= 2 && yaPreguntamos`, escala en código con motivo `seguimiento` (resumen con el conteo), manda `despedidaMedia(unassigned, status?)` —`DESPEDIDA_MEDIA` más la línea de horario/sin asesor cuando aplica— por el mismo `deliver()` con `isAutoReply: true`, y cierra con `logTurn` `escalated` sin tokens: ni fase 0 ni clasificación ni tool loop (ahorra 3 llamadas al modelo). `SendPhase` gana `adjuntos_sin_texto`. T3, "La IA no vuelve a pedir lo que ya pidió" — revisión (16/9/2026, migración `20260916010000`): el `select` de `runAgentTurn` suma `ai_resume_cutoff_at`, y una guarda nueva en la apertura del turno —después de `pausada`, antes de `humanHasWritten`— compara `last_customer_message_at` contra ese sello: si es anterior O IGUAL, el turno no llama a fase 0 ni al modelo, deja `log.info("turno_mensaje_previo_a_devolucion")` y `recordHandoff(unassigned, mensaje_previo_a_devolucion)`. Caso real: un cliente pide un asesor, la IA escala y se despide; un asesor desasigna y reactiva la IA a mano; en menos de un minuto el reconciliador reencolaba la conversación y la IA repetía la misma promesa sobre el MISMO mensaje viejo (mecanismo que el 13/9 volvió a escalar 63 casos). Reemplaza a la guarda `awaiting_any_reply` del diseño descartado del 15/9 (comparaba contra la última SALIDA hacia el cliente, vulnerable a la carrera de ráfaga: si el cliente escribe mientras la IA redacta, la respuesta queda fechada después de ese mensaje) — esta versión compara contra un sello que solo se mueve con una devolución humana, nunca con una salida, así que no sufre esa carrera | T4, "Seba atiende el mostrador" (18/9/2026, D2/D3, requisito 6 del cliente): las dos guardas de apertura (`assigned_agent_id`/`ai_enabled`, anexo A2) se FUSIONAN en `if (!convo.ai_enabled) { assigned ? asignada/human : pausada/unassigned; return }` — asignado + IA ENCENDIDA ya no corta el turno, así que un chat con asesor corre `runTurnPhases` completo (fase 0 incluida: un escenario puede calzar en un chat asignado). `runTurnPhases` calcula `const esperandoAsesor = Boolean(convo.assigned_agent_id)` y lo suma con OR a cada `isAutoReply` de las salidas que hablan (tool loop final, fuera de tema, y `runPlaybook` vía su nuevo parámetro `assignedAgentId` → `sendPlaybookReply(..., { isAutoReply: esperandoAsesor })`, `send.ts`): un chat ya asignado nunca apaga `awaiting_reply` con una cortesía de Seba. Nuevo helper `stageFor(assignedAgentId, etapa)` (`assignedAgentId ? "assigned" : etapa`) gobierna las seis escrituras de `journey_stage` que antes alternaban `"classifying"`/`"tool_running"`/`null` a secas (la apertura de `runTurnPhases`, el `onToolExecutionStart` del tool loop, el reseteo final, el de `runPlaybook`, y los DOS de `resetStage`/`deliveryFailed`, que ahora reciben `assignedAgentId` como parámetro) — sin esto la píldora "Escaladas" (`inbox-filters.ts`, que mira el campo CRUDO) perdía el chat apenas el turno arrancaba a trabajar. La guarda de cortesía tras escalada (más arriba en esta misma línea) deja de ser defensiva: con un asesor asignado y la IA encendida, su rama `convo.assigned_agent_id ? "human" : "unassigned"` por fin es alcanzable. T2b, mismo plan (18/9/2026, requisito 1 del cliente): nuevo paso de PRESENTACIÓN en `runTurnPhases`, justo después de calcular `customerMessage` y ANTES de la guarda de cortesía — reemplaza a `needsGreeting` (retirada; miraba si había algún `assistant` en los últimos 15 mensajes del historial, así que un chat reabierto con respuestas viejas nunca hubiera vuelto a saludar). `welcome_sent_at === null` es la condición completa (desde T0 de este plan, esa columna dejó de significar "salió la plantilla" y pasa a significar "Seba ya se presentó"); nuevo helper `claimPresentation(supabase, conversationId)` (`UPDATE conversations SET welcome_sent_at = now() WHERE id = ? AND welcome_sent_at IS NULL RETURNING id`, mismo patrón que `claimWelcome` de `route.ts`) reclama el sello ANTES de mandar nada. Si el reclamo gana: `soloSaludo = customerMessage !== null && (isGreetingOnly(customerMessage) || isCourtesyOnly(customerMessage))` (`isGreetingOnly`, nueva en `saludo.ts`) decide si el saludo de Seba (`sebaGreeting(dayBand(new Date()))`, `seba.ts`) ES la respuesta completa del turno; el envío pasa por el mismo `deliver()` de siempre con la fase nueva `"presentacion"` (`SendPhase` la suma) y `isAutoReply: !soloSaludo` — si el cliente solo saludó, el saludo apaga `awaiting_reply` como cualquier respuesta real; si sigue una redacción, no la apaga todavía (si esa redacción termina escalando, `awaiting_reply` tiene que seguir en `true`). Si `deliver()` frena (`!salida`) o `deliveryFailed` dice que Meta rechazó el envío, el sello se revierte a `null` (`UPDATE welcome_sent_at = null`, mismo patrón que `bienvenida_rechazada_por_meta` de `route.ts`) y el turno termina ahí — `deliver()`/`deliveryFailed` ya dejaron su propio traspaso, no hace falta uno nuevo. Si `soloSaludo`, el turno se cierra con `resetStage` + `logTurn` (`action: "answered"`, resumen "Seba se presentó; el cliente solo saludó.") SIN pasar por fase 0, fase 1 ni el tool loop — tres llamadas al proveedor que un "hola" pelado no iba a necesitar. Si no, `introducedThisTurn = true` viaja a `buildInstructions` (reemplaza a `needsGreeting` en `prompt.ts`, semántica invertida) para que el modelo sepa que el saludo YA salió, en un mensaje aparte, y no lo repita T3 (18/9/2026, requisitos 2/3/4/5 del cliente): `catalogOutcome: CatalogOutcome` (tools.ts) nace junto a `outcome` y viaja a `buildCatalogTool(deps, catalogOutcome)`; después de la red de devolución/queja y ANTES de la guarda de identidad corre la red de seguridad del catálogo — si `catalogOutcome.ran && !outcome.escalated && !catalogOutcome.generico`, escala en código con `confirmar_inventario`/`sin_stock`/`no_identificado` (esa precedencia, `conExistencia` gana a `agotados`) y, si el texto del modelo no matchea `/asesor/i`, le anexa con un salto de línea el texto fijo del motivo (`TEXTO_CONFIRMAR_INVENTARIO`/`TEXTO_SIN_STOCK`/`TEXTO_NO_IDENTIFICADO`, `seba.ts`) — un genérico bloquea la red ENTERA aunque otra llamada del mismo turno haya dejado otro campo en `true` (se acumulan, nunca se resetean). Al correr ANTES de la guarda de identidad, el texto anexado pasa por `applyIdentityGuard` como cualquier otro. T5, mismo plan (18/9/2026, requisito 7): `fetchTurnLessons` (`lib/ai/lessons.ts`) entra como cuarto miembro del `Promise.all` de arranque de `runAgentTurn` (junto a `agent_can_run`, la conversación y `agent_settings`) — nunca lanza, así que no gana ninguna rama de error nueva — y `runTurnPhases` lo recibe como parámetro y lo pasa tal cual a `buildInstructions({..., lessons})`. H1, misma corrida (18/9/2026, "el repuesto manda"): escenario a mano contra la base local mostró que "¿tienen pastillas de freno?" calzaba el escenario del panel "Catálogo general" y el turno lo mandaba tal cual, sin consultar `products`, sin la pregunta de filtro (requisito 5) y sin escalar. Dentro del bloque de fase 0 (`if (match.playbook) { … if (!yaSalioHacePoco) { … } }`), justo antes de llamar a `runPlaybook`, se calcula `cedeAlCatalogo = classified.ok && classified.result.intent === "consulta_disponibilidad"` — la clasificación ya corrió en paralelo con `matchPlaybook`, así que no cuesta una llamada más—: con la intención confirmada como consulta de inventario, el escenario NO se manda; queda `log.info("escenario_cedido_al_catalogo", { conversationId, escenario })` y el turno sigue por el flujo genérico (tool loop, catálogo real, T3 arriba). Si la clasificación falló (`!classified.ok`) o la intención es otra, el escenario sale igual que siempre — el costo de la clasificación descartada ya se contaba en `classifiedTokens` antes de esta rama, sin cambios. T3, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D3-D6): quinta consulta del `Promise.all` de arranque, `fetchActiveCatalogLinks(supabase)` (`data.ts`, junto a `business_hours`, nunca lanza) — `links` viaja a `runTurnPhases`, que lo enhebra a `matchPlaybook` (fase 0 descarta escenarios con marcador sin resolver), `alreadySentPlaybook` (compara el texto YA RESUELTO contra el historial) y `runPlaybook` → `sendPlaybookReply` (manda el texto resuelto). Riesgo aceptado: si el supervisor cambia la URL de un catálogo entre dos turnos, `alreadySentPlaybook` deja de reconocer el envío anterior y el escenario se repite una vez, con el link nuevo. Corrección de la revisión `code-review high` del 19/9/2026 (punto 6): esa quinta consulta pasa de `fetchActiveCatalogLinks` (`data.ts`) a `fetchTurnCatalogLinks(supabase, conversationId)` (nuevo, `lib/ai/catalog-links.ts` — ver su fila en este mismo listado), que avisa con `log.warn("turno_enlaces_no_legibles")` en vez del `console.error` de `data.ts` (esa función también la usa el navegador, así que no puede importar `lib/log.ts` sin arrastrarlo al bundle del cliente). `agent.ts` deja de importar nada de `@/lib/data`. T1, plan "Seba sale sin pisar a nadie" (19/9/2026, hallazgo C2): `runAgentTurn` desestructura `{ data: conversation, error: conversationError }` en vez de `{ data: conversation }` a secas — si la fila existe pero la consulta falla (un 400 de PostgREST porque falta la migración `20260916010000`, que agrega `ai_resume_cutoff_at` al mismo `select`), antes `conversation` quedaba `undefined` y caía en la rama muda "la conversación no existe" —pensada para un id borrado por FK, no para un corte de infraestructura—: la IA quedaba callada sin traspaso ni log, y la cola contaba el turno como resuelto. Ahora, con `error`, `log.error("turno_conversacion_no_consultable", { conversationId, detail: errorText(error) })` + `throw` ANTES de `entrega.intentado = true`, así la cola reintenta en vez de archivar el turno como si el lead no existiera; `data === null` SIN error sigue por la rama de siempre. T2, mismo plan (hallazgo A2): las dos salidas mudas de clasificación fallida y del `catch` del tool loop —cuyo comentario decía "el reconciliador la recoge sola", cierto hasta el 18/9/2026 cuando Seba empezó a presentarse por código (T2b) ANTES de este punto— ahora, SOLO si `introducedThisTurn` es `true` (el saludo salió en este mismo turno), dejan `recordHandoff({ reason: "entrega_fallida", toKind/toId según convo.assigned_agent_id })`: si Seba ya saludó, el último mensaje visible de la conversación ya no es del cliente sino un saliente que sí se entregó, y el predicado nuevo del reconciliador (`last_message_direction.eq.inbound` / `last_message_status.eq.failed`) deja ese caso afuera para siempre — sin el saludo, el último mensaje sigue siendo del cliente y no se escribe nada nuevo, el reconciliador lo recoge como antes. T3, mismo plan (hallazgo A3 más el hallazgo nuevo de la guarda de cortesía): `soloSaludo` y la guarda de cortesía tras escalada dejan de mirar solo `customerMessage` (la última línea) y pasan a exigir que TODA `rafagaCliente` (`customerBurst(history)`, ver `history-line.ts` en este glosario) sea saludo/cortesía — "¿tienen la bomba de aceite?" + "gracias" en dos mensajes seguidos, con una escalada abierta, callaba el turno ENTERO sin contestar la pregunta real | T12, plan "Seba sale sin pisar a nadie" (19/9/2026, cierra la decisión abierta #1): `runTurnPhases` reconoce, justo después de `loadHistory` y ANTES de calcular nada más, si este turno es un REINTENTO de uno que ya mandó la presentación de Seba y se cayó DESPUÉS (clasificando o en el tool loop) — la última línea del historial es del asistente y calza byte a byte con `isSebaGreeting` (`seba.ts`). De ser así, el saludo se recorta de `history` Y `historyCreatedAt` (mismo índice, `.pop()` en los dos) y `introducedThisTurn`/`saludoPendienteDeRespuesta` quedan en `true` ANTES de `customerMessage`, `rafagaCliente`, fase 0, la clasificación o el tool loop — el reintento ve exactamente lo que vio el primer intento, sin volver a saludar. `introducedThisTurn` se declara acá (ya no dentro del bloque de presentación, T2b): las dos formas de quedar `true` —recorte de reintento, o saludo recién mandado este turno— son mutuamente excluyentes (`convo.welcome_sent_at` no puede ser `null` en un reintento, porque `claimPresentation` ya lo selló). Turno espurio (test d2 del plan): si `saludoPendienteDeRespuesta` y la ráfaga recortada sigue siendo solo saludo/cortesía, el turno cierra con `resetStage("turno_saludo_ya_respondido", ...)` sin llamar al modelo ni escribir traspaso — colocado A PROPÓSITO DESPUÉS de la guarda de cortesía tras escalada (no antes): las dos comparten el caso `["gracias"]`, y con una escalada abierta esa guarda tiene que ganar (deja su propio `cortesia_tras_escalada`) en vez de que este cierre silencioso se la coma antes de evaluarla. En las dos salidas que hoy pueden caer justo después del saludo —clasificación fallida y el `catch` del tool loop— T12 reemplaza el `recordHandoff(entrega_fallida)` que dejaba T2 (arriba en esta misma línea): ahora, si `introducedThisTurn`, `resetStage` se conserva pero en vez del traspaso se lanza `ProviderFailedAfterGreetingError` (`turn-delivery.ts`) — ese traspaso volvía el caso IRRECUPERABLE cuando reintentar es seguro (lo único que salió es la presentación, ya sellada). En el `catch` de `runAgentTurn`, ANTES de `if (!entrega.intentado) throw err`, `isProviderFailedAfterGreeting(err)` deja pasar el error TAL CUAL (`log.warn("turno_reintentable_tras_saludo")`) sin envolverlo en `NonRetryableTurnError`, para que la cola (`queue.ts`, sin tocar) lo reintente como cualquier fallo transitorio |
| `queue.ts` | Cola de turnos: el webhook encola y sigue; debounce adaptativo (6s ráfaga / 2s pregunta cerrada, `CIERRA_LA_IDEA`); el turno que encuentra la conversación tomada se pospone 30s y vuelve a la cola (`deferred`), sin gastar intentos ni contar como atendido. 7/9/2026 ("La respuesta llega en siete segundos", S1): los tres frenos —`AGENT_MAX_CONCURRENT_TURNS` (default 3→8), `AGENT_MAX_TURNS_PER_MINUTE` (default 4→30) y el tope por pasada, ahora `maxPerRun()` leyendo `AGENT_QUEUE_MAX_PER_RUN` (antes `MAX_PER_RUN` fijo en diez; default 30)— salen todos del entorno con el mismo patrón (número finito y positivo o default, nunca `NaN`); medido contra producción que la cola, no el modelo, era la espera (hasta 90 min con 360 turnos a 4/min). T0 (7/9/2026): `atender()` reclama con `cola.claimDue()` (`{ conversationId, vencioEn }`) y pasa `vencioEn` a `runAgentTurn`; los cuatro re-encolados (ritmo, cupo, lock, error) usan `cola.defer(...)` en vez de `enqueue`, para no perder el vencimiento original en cada reintento. T3 (7/9/2026): cada `defer` de ritmo/cupo/lock (no el de error) anota su plazo; al terminar una pasada con diferidos y sin continuación pendiente, `registrarDiferidos` programa un `setTimeout` (3/20/30 s + margen, `.unref()`) que reintenta con `maxPerRun()` —nunca con el `limit` de quien la disparó— y puede encadenarse a sí mismo si vuelve a dejar diferidos; estado de módulo con tres valores (`inactiva`/`programada`/`corriendo`) para que una pasada ajena no duplique el timer mientras la continuación corre; `stopAgentQueue` la cancela; `resetContinuacionParaPruebas` solo para tests. Antes, un turno frenado por ritmo (20 s) solo lo reclamaba el próximo webhook ajeno o el cron. Una pasada que agota su tope SIN diferir nada no programa continuación: ese resto lo drena el cron (cada minuto, 30 por pasada = el mismo ritmo que `AGENT_MAX_TURNS_PER_MINUTE`) |
| `redis-queue.ts` | La cola en Redis con scripts Lua atómicos: cupos globales, ritmo, sweep lock. T0 ("La respuesta llega en siete segundos", 7/9/2026): `CLAIM_SCRIPT` devuelve `[conversationId, vencimiento]` —el guardado en `liminal:agent:vencimiento:{id}` por un reclamo anterior o, si no, el score crudo, que además siembra esa clave (TTL 1 h) para el próximo reclamo—; `claimDue()` devuelve `{ conversationId, vencioEn }`; nueva `defer(id, seconds)` reencola un reintento del SISTEMA conservando (NX) el vencimiento más viejo si la conversación sigue en la cola; `enqueue` borra esa clave (mensaje nuevo del cliente = ventana nueva) y `purge` la barre con SCAN |
| `conversation-lock.ts` | Un solo turno de IA por conversación, con lease de 90s en `ai_turn_lock_until` + dueño en `ai_turn_lock_token`, renovado cada 30s por el propio turno y soltado solo por quien lo tomó; un proceso que muere deja la conversación libre en ≤90s en vez de para siempre; encontrarlo tomado ya no es silencio, es `ConversationBusyError` |
| `turn-target.ts` | Identidad congelada del turno: a qué chat/cliente se le habla; cierra el riesgo de cruzar respuestas. T2 (8/9/2026) le había sumado a `AgentConversation` el campo `handoff_confirmation_pending_at: string \| null`, viajando en la misma fila que lee `runAgentTurn` para que `buildEscalateTool` (vía `deps` en `agent.ts`) reconfirmara el pase a ventas — nunca fue parte de la identidad del turno en sí. Se fue el 9/9/2026 con la reconfirmación entera (corrida "El pase a ventas al primer sí"): el bucle del "ok"/"está bien"/"dale" en producción hizo que el operador aprobara volver a escalar con el primer "sí". T3 (14/9/2026): `AgentConversation.contact` suma `display_name?`/`profile_name?` (opcionales) para que `agent.ts` arme el nombre del cliente con `customer-name.ts`; no forman parte de la identidad del turno en sí, viajan en la misma fila por comodidad. T3, "La IA no vuelve a pedir lo que ya pidió" — revisión (16/9/2026): `AgentConversation` suma `ai_resume_cutoff_at: string \| null` (migración `20260916010000`) — el sello de la última devolución humana que la guarda nueva de `runAgentTurn` compara contra `last_customer_message_at`; reemplaza al campo del diseño descartado del 15/9 (`awaiting_any_reply`, comparaba contra la última salida y no contra la devolución, vulnerable a la carrera de ráfaga). T2b, plan "Seba atiende el mostrador" (18/9/2026): el comentario de `welcome_sent_at` se reescribe — desde la migración `20260917010000` (T0 del plan) esa columna deja de significar "salió la PLANTILLA de bienvenida" (`WHATSAPP_WELCOME_TEMPLATE`, vacía en producción) y pasa a significar "Seba ya se presentó en esta conversación" (`claimPresentation`, `agent.ts`); las dos cosas comparten la misma columna a propósito y son excluyentes por diseño |
| `customer-name.ts` | T3 ("La voz cercana y la espera visible", 14/9/2026): `customerFirstName(displayName, profileName)` — primer nombre del cliente listo para el prompt, o `null` si lo guardado no parece un nombre de persona (teléfono, dígitos, símbolos/emojis, una sola letra). Prefiere `display_name` (lo edita un asesor) sobre `profile_name` (lo manda Meta) y cae al segundo si el primero no pasa el filtro (decisión del subagente: un apodo raro del asesor no debe tapar un nombre real que sí trajo Meta). Capitaliza ("JOSE RIERA" → "Jose"); "SBK Motos" → "Sbk", que el prompt descarta por su cuenta. Módulo PURO, sin imports |
| `saludo.ts` | Tres preguntas PURAS sobre la FORMA de un texto (T4 del 14/9/2026, reescrito el 15/9/2026 en "La voz de mostrador con nombre propio"): `isCourtesyOnly` dice si el ÚLTIMO mensaje del cliente es SOLO cortesía de cierre (falla a `false` ante cualquier palabra fuera de su lista o más de 6 palabras); la usa la guarda de cortesía de `runTurnPhases` (agent.ts). `isGreetingPlaybook` dice si el texto YA REDACTADO de un escenario empieza saludando (hola/buenas/buenos/buen día/bienvenid; falla ABIERTO): desde el 15/9 `matchPlaybook` la aplica SIEMPRE, sin mirar lo que escribió el cliente, porque el saludo lo pone el código en `buildInstructions`. `isPureGreeting` se retiró el 15/9 (sin llamadores) y vuelve el 18/9/2026 (T2a, "Seba atiende el mostrador") como `isGreetingOnly`: el mensaje del CLIENTE es SOLO un saludo ("hola", "buenas tardes!", "hola que tal"; tope 6 palabras, lista propia de palabras de saludo, no la de cortesía) — el turno la usa para decidir si, tras el saludo literal de Seba (`seba.ts`), hace falta seguir redactando (`agent.ts`, T2b). Normaliza como `identity-guard.ts` (NFD sin marcas, minúsculas) |
| `seba.ts` | Nuevo 18/9/2026 (T2a, plan "Seba atiende el mostrador", requisitos 1-5 del cliente). Módulo PURO (sin `server-only`, sin imports de Supabase; solo `brand.ts` en valor y el TIPO `DayBand` de `business-hours.ts`, que se borra en la compilación). `presentationGreetingFor(band)` — distinto de `greetingFor` de `business-hours.ts`: el cliente dictó "buen día" y no "buenos días", así que no reutiliza esa función. `sebaGreeting(band)` arma el saludo literal del primer mensaje, byte a byte con lo que dictó el cliente; lo manda el TURNO como mensaje propio ANTES de cualquier redacción del modelo (`agent.ts`, tarea T2b) — nunca el modelo. Los tres textos fijos de los requisitos 2/3/4 (`TEXTO_CONFIRMAR_INVENTARIO`, `TEXTO_SIN_STOCK`, `TEXTO_NO_IDENTIFICADO`) y la pregunta de filtro del requisito 5 (`PREGUNTA_FILTRO`) viven acá para que `prompt.ts` (sección 5.1 y 3) y `tools.ts` (T3, instrucción del catálogo) los interpolen sin duplicarlos. Los cuatro textos y las tres franjas del saludo pasan `revealsIdentity` — probado en `seba.test.ts` con `band` fijo a mano, nunca el reloj real. T12, plan "Seba sale sin pisar a nadie" (19/9/2026, cierra la decisión abierta #1): suma `isSebaGreeting(text)` — ¿es EXACTAMENTE la presentación, en cualquiera de las tres franjas? Construida sobre `sebaGreeting`, sin repetir los tres literales a mano. `agent.ts` la usa para reconocer, en un REINTENTO tras `ProviderFailedAfterGreetingError`, que la última línea del historial ya es el saludo que salió en el intento anterior — no algo que el modelo tenga que volver a redactar ni contestar |
| `lessons.ts` | Nuevo 18/9/2026 (T5, plan "Seba atiende el mostrador", requisito 7 del cliente, "Lecciones de Seba"). Módulo PURO a propósito, sin `server-only` (mismo motivo que `identity-guard.ts`): `buildGlobalLessonsBlock`/`buildChatLessonsLine` los reusa `prompt.ts` (servidor). `MAX_GLOBAL_LESSONS = 15`, `MAX_CHAT_LESSONS = 5`, `MAX_LESSON_CHARS = 200` (clip defensivo aunque el CHECK de la base ya lo garantice). `buildGlobalLessonsBlock(lessons: string[])` — el bloque que va DENTRO del prefijo cacheado, cabecera fija "LECCIONES DEL EQUIPO…"; vacío ("") sin lecciones, para que `cacheablePrefix` (`prompt.ts`) siga siendo exactamente `SYSTEM_PROMPT` en el caso común. `buildChatLessonsLine(lessons: string[])` — la línea que se agrega al SUFIJO, solo lecciones de la conversación actual. Ninguno reordena: respetan el orden en que `fetchTurnLessons` ya los entrega (`created_at desc`). `fetchTurnLessons(supabase, conversationId)` hace DOS consultas contra `ai_lessons` (`scope='global'`/`scope='conversacion'`, ambas `kind='nota'` — las `kind='sinonimo'` NUNCA llegan acá, son datos para `catalog-search.ts`, no prosa) y NUNCA lanza: ante error de la base o excepción, vacío + `log.warn("turno_lecciones_no_legibles")` (`errorText`) — cuarto miembro del `Promise.all` de `agent.ts` (`runAgentTurn`) |
| `catalog-links.ts` | Nuevo 19/9/2026, corrección de la revisión `code-review high` sobre T3 del plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, punto 6). Homónimo de `src/lib/catalog-links.ts` (el módulo puro de la tabla de arriba) pero es OTRO archivo, en `src/lib/ai/`: mismo patrón EXACTO que `fetchTurnLessons` de este mismo listado. `fetchTurnCatalogLinks(supabase, conversationId)` hace su propia consulta a `catalog_links` (`is_active = true`, por `sort_order`) y NUNCA lanza: ante error o excepción, `[]` + `log.warn("turno_enlaces_no_legibles")` (`errorText`). Antes `agent.ts` importaba `fetchActiveCatalogLinks` de `@/lib/data` —también la usan `crm-shell.tsx`/`app/inbox/page.tsx` del lado del navegador, así que no puede importar `lib/log.ts` (server-only) y avisa sus errores con `console.error`—: una lectura fallida de enlaces se perdía en la consola del servidor sin quedar en la bitácora estructurada, y el único rastro visible era `escenarios_enlace_sin_resolver` en `matchPlaybook`, que describe la consecuencia (fase 0 descarta escenarios con marcador) como si fuera un problema de CONFIGURACIÓN del escenario. `fetchActiveCatalogLinks` de `data.ts` NO se tocó ni se borró: sigue siendo la fuente para el navegador |
| `turn-delivery.ts` | Barrera contra el doble envío: un turno que ya respondió no se reintenta (`NonRetryableTurnError`/`isNonRetryable`). T12, plan "Seba sale sin pisar a nadie" (19/9/2026, cierra la decisión abierta #1): suma `ProviderFailedAfterGreetingError`/`isProviderFailedAfterGreeting` — la ÚNICA excepción a esa regla. `agent.ts` la lanza desde las dos salidas que pueden caer justo después de que Seba se presentó (clasificar, o el tool loop) cuando `introducedThisTurn` es `true`: lo único que salió es la presentación, ya sellada por `claimPresentation`, así que un reintento no la duplica. Deliberadamente NO es un `NonRetryableTurnError` (los tests de este archivo confirman que las dos funciones nunca están de acuerdo sobre el mismo error) para que `queue.ts`, sin tocar, la reintente como cualquier fallo transitorio |
| `reconciler.ts` | La red de seguridad del lado de Postgres: si Redis perdió un turno, nadie lo reencola nunca porque el único que encola es el webhook. Busca en la base lo que sigue esperando, descarta lo que ya está en cola, con lock vigente o atendido por una persona AHORA, y reencola hasta 50 por pasada. Confirmado en T0.3 (5/9/2026): el predicado nunca lee `conversation_handoffs` (ni `to_kind` ni `reason`), solo columnas de `conversations` — una huérfana por `rechazado_por_meta` se rescata igual que cualquier otra, sin importar el motivo que la dejó así. T7 (corrida "La IA ve lo que llega", 8/9/2026): el `select` suma `last_customer_message_at`, y `conversationsWrittenByHumans` recibe `{id, lastCustomerMessageAt}` de cada candidata sin lock — una nota de un asesor de hace semanas ya no la saca del reconciliador para siempre; solo bloquea si el asesor se adelantó al cliente o escribió hace menos de `AI_HUMAN_GRACE_MINUTES`. T2, "La IA no vuelve a pedir lo que ya pidió" — revisión (16/9/2026, migración `20260916010000`): el predicado suma `.eq("new_since_ai_resume", true)` en el WHERE — sin este filtro, un asesor que desasigna y reactiva la IA a mano en menos de un minuto dejaba que esta misma consulta reencolara el mismo mensaje de la escalada, y la IA repetía la misma despedida (mecanismo que el 13/9 volvió a escalar 63 casos); también tapa al cliente que escribe "¿ya me atienden?" mientras espera al asesor —queda pendiente pero es ANTERIOR a la devolución— que un reloj de salidas (el diseño descartado del 15/9, `awaiting_any_reply`) habría dado por nuevo apenas alguien reactivara la IA | Hallazgo 2 del plan "Seba atiende el mostrador" (18/9/2026, D2): la consulta suma `.or("last_message_direction.eq.inbound,last_message_status.eq.failed")` — con la escalada sin apagar `ai_enabled` (requisito 6), una `escalada_sin_asesor` de noche (P1) deja `awaiting_reply=true` con la IA encendida y sin nadie asignado, y el sello de devolución sin moverse (no hubo ninguna); sin este `.or()` el reconciliador volvía a encontrarla cada minuto y la reencolaba para siempre. Solo reencola si el ÚLTIMO mensaje VISIBLE es del cliente o si el último saliente falló — una despedida que salió bien (`outbound`, no `failed`) se deja quieta. Mismo predicado en `unansweredFreeWork` (`data.ts`, el botón "encender la IA") |
| `handoffs.ts` | La bitácora de traspasos: cada salida silenciosa (turno, cola, webhook) deja dicho a quién pasó la conversación y por qué. `recordHandoff` para quien ya tiene cliente; `recordHandoffAdmin` se fabrica el suyo. NUNCA lanzan: registrar el traspaso no puede tumbar un turno. `HandoffReason` suma (T0.3, 5/9/2026) `escalada`/`escalada_sin_asesor` (las usa `escalate.ts`) y `rechazado_por_meta` (la usa `agent.ts` cuando Meta rechaza un envío que ya pasó todas las guardas de `deliver()`); T2.1 (5/9/2026) suma `cerrada_por_asesor`/`reabierta_por_asesor` (las rutas `api/conversations/[id]/close` y `.../reopen`) y `reabierta_por_cliente` (el webhook, cuando reabre sola una conversación cerrada). T4 (8/9/2026, "La IA ve lo que llega") suma `sin_contenido_legible`: el turno arrancó sin nada legible para el modelo tras describir la media con `historyLine` (T2) — caso `cea69118…` — y `agent.ts` la usa en el `return` por historial vacío de `runTurnPhases`. T4 (14/9/2026): `HandoffReason` suma `cortesia_tras_escalada` (migración 20260914010000) y el archivo gana `escalationOpen(supabase, conversationId)`: dice si la ÚLTIMA fila de `conversation_handoffs` de la conversación es `escalada`/`escalada_sin_asesor` sin que ningún `messages` de `sender_type = 'agent'` haya quedado después; falla cerrado a `false` (`escalada_abierta_no_consultable`: ante error la IA atiende normal). La usa la guarda de cortesía de `runTurnPhases` para no despedirse de quien ya espera a un asesor. T5 ("La voz de mostrador con nombre propio", 15/9/2026): `escalationOpen` deja de mirar la última fila a secas y mira la última fila QUE CAMBIA DE MANOS: la constante `RAZONES_QUE_NO_CIERRAN_LA_ESCALADA` (`asignada`, `pausada`, `agente_no_puede_correr`, `cortesia_tras_escalada`, `humano_intervino`, `humano_se_adelanto`) se excluye en la consulta con `.not("reason", "in", "(a,b,c)")` (misma sintaxis que `customers-data.ts`). Cierra el hueco medido el 14/9: la `asignada` que `openTurn` graba en cada mensaje a un chat con dueño, la `pausada` de la IA apagada y la propia `cortesia_tras_escalada` tapaban la `escalada` y la guarda no disparaba. Una razón desconocida futura no está en la lista y cuenta como cierre (mismo sesgo hacia responder). `agregar una razón que se escriba por mensaje exige sumarla a la constante`. T3, "La IA no vuelve a pedir lo que ya pidió" — revisión (16/9/2026, migración `20260916010000`): `HandoffReason` suma `devuelto_a_ia`/`desasignada_por_asesor` (las escribe el trigger AFTER `handle_conversation_ownership_change`, nunca TypeScript — existían en el CHECK desde `20260830040000` pero nadie las escribía de verdad) y `mensaje_previo_a_devolucion` (la escribe la guarda nueva de `agent.ts`, apertura de `runAgentTurn`). `RAZONES_QUE_NO_CIERRAN_LA_ESCALADA` suma SOLO `mensaje_previo_a_devolucion` — `devuelto_a_ia`/`desasignada_por_asesor` SÍ cierran la escalada para `escalationOpen`, porque un humano decidió de verdad devolverle el chat a la IA; si no cerraran, un "gracias" escrito DESPUÉS de la devolución quedaría callado por la guarda de cortesía, contra la decisión del operador de que la IA vuelva a responder lo que el cliente escriba después de que se la devuelven | T0/T4, "Seba atiende el mostrador" (18/9/2026, migración `20260917010000`): `HandoffReason` suma `silenciada_por_asesor` — la escribe el trigger `handle_conversation_ownership_change` de esa migración, NUNCA TypeScript, cuando `ai_enabled` se apaga SIN que `assigned_agent_id` cambie en el mismo UPDATE (un asesor manda su primer mensaje real vía `handle_agent_message_silences_ai`, o alguien pausa la IA a mano). SÍ cierra la escalada para `escalationOpen` (NO entra en `RAZONES_QUE_NO_CIERRAN_LA_ESCALADA`): un humano tomando el chat de verdad es un movimiento de dueño real, mismo criterio que `devuelto_a_ia`/`desasignada_por_asesor`/`reclamado`. T8, plan "Seba sale sin pisar a nadie" (19/9/2026, hallazgo M6): `RAZONES_QUE_NO_CIERRAN_LA_ESCALADA` suma `reabierto` — la escribe el reconciliador (`reconciler.ts`) cada vez que reencola un turno huérfano, sin que nadie cambie de dueño (solo "vuelvo a intentar"); sin sumarla, un reencolado sobre una escalada abierta tapaba la fila `escalada`/`escalada_sin_asesor` como si fuera "lo último que pasó de verdad", `escalationOpen` daba `false` y la guarda de cortesía dejaba de disparar — la IA podía despedirse dos veces sobre un cliente que seguía esperando al mismo asesor |
| `rate-limit.ts` | El único cuello hacia el proveedor del modelo: cuenta PETICIONES, no turnos. 7/9/2026: defaults subidos junto con `queue.ts` — `AI_MAX_CONCURRENT_REQUESTS` 3→12, `AI_MAX_REQUESTS_PER_MINUTE` 15→120; el techo de 20/min que justificaba el 15 era de la cuenta gratuita de OpenRouter (confirmado contra `/api/v1/key` de producción: `is_free_tier: false`, `limit: null`). Un turno gasta ≈3,4 peticiones, así que este tope y el de turnos se suben siempre juntos |
| `classify.ts` | Fase 1: clasificación barata de intención; decide qué herramientas recibe el modelo. T7 ("La voz cercana y la espera visible", 14/9/2026, Decisión 8): `fuera_de_tema` suma patrocinios/publicidad, listas de precios de otros negocios, cadenas y números equivocados; antes caían en `otro` y arrancaban el tool loop completo para alguien que no era un cliente. Sigue "ante la duda, otro" |
| `playbooks.ts` | Fase 0: reconocimiento de escenario (respuestas predeterminadas del supervisor, enviadas tal cual; no se repiten en 6h). B3 (5/9/2026): `buildPrompt` y `matchPlaybook` reciben `businessHours` (4.º parámetro, con default) y la línea de fecha del prompt de escenarios suma "— franja: tarde" más horario y estado calculados con `dayBand`/`describeSchedule`/`businessStatus`, el mismo criterio que el flujo genérico. `ZERO_USAGE` exportada (8/9/2026): `agent.ts` la reusa para el "sin escenario, sin costo" cuando salta `matchPlaybook` porque el último mensaje del cliente es un marcador de media. T4 (14/9/2026): `matchPlaybook` gana un quinto argumento opcional `lastCustomerText`; si el cliente saludó Y preguntó algo en el mismo mensaje, los escenarios que EMPIEZAN saludando (`isGreetingPlaybook`, saludo.ts) salen de los candidatos antes de llamar al modelo (log `escenarios_saludo_descartados` con el conteo), y `buildPrompt` le dice al clasificador que el saludo no cuenta si viene con pregunta. Sin el argumento no filtra nada (llamadores viejos). T4 ("La voz de mostrador con nombre propio", 15/9/2026): el quinto argumento se retiró y el descarte de los escenarios cuyo texto empieza saludando (`isGreetingPlaybook`, saludo.ts) es INCONDICIONAL, porque el saludo ya lo pone `buildInstructions` una sola vez por conversación; un "hola" pelado va al flujo genérico. `playbooksAtTime` (el reloj de `greeting-window.ts`) desapareció con el módulo: sus tres patrones eran subconjunto del descarte. Log `escenarios_saludo_ignorados` con `{ ignorados, nombres }` (`nombres` unidos por coma: `LogContext` no acepta arreglos). Ojo: los tests de este archivo pasan `now` fijo; el 14/9 un escenario "¡Buenas tardes!" sin hora fija fallaba después de las 19:00. T3, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D6): `matchPlaybook` gana un quinto parámetro `links: CatalogLink[] = []` (default para no romper llamadores viejos); tras el descarte de saludo corre un segundo descarte, `hasUnresolvedCatalogMarker(playbook, links)` (mira `response_text` y, si el adjunto es `link`, `attachment_url`, contra `resolveCatalogMarkers` de `catalog-links.ts`), mismo patrón que el de saludo — deja `escenarios_enlace_sin_resolver` con `{ ignorados, nombres }` |
| `prompt.ts` | Identidad y reglas del agente: un solo bloque idéntico en todos los turnos (cacheable). B3 ("El reloj dice la verdad", 5/9/2026): `TurnContext` gana `businessHours` (default `DEFAULT_BUSINESS_HOURS`) y el bloque `TURNO ACTUAL` ya no lleva la hora en texto sino `turnClockLine` (`business-hours.ts`): franja, saludo, horario de atención y si la tienda está abierta, ya calculados — la sección 6 del `SYSTEM_PROMPT` dice "usa la franja y el saludo tal cual" y la regla vieja "Saber la hora no es saber el horario" se reemplazó por "el horario te llega en TURNO ACTUAL, no inventes otro"; la sección 5.1 suma la regla del operador: fuera de horario se sigue vendiendo, y si el cliente quiere comprar con la tienda cerrada se le explica el paso a ventas y se nombra el horario regular. `SYSTEM_PROMPT` sigue siendo prefijo exacto; el umbral de "sufijo corto" del test subió de 120 a 150 tokens estimados. 8/9/2026 (S2, "La IA ve lo que llega"): nueva sección `7. LO QUE TE LLEGA SIN TEXTO`, exportada aparte como `MEDIA_RULES` e interpolada dentro de `SYSTEM_PROMPT` (sigue siendo prefijo cacheable, no sufijo) — le dice al modelo qué hacer con los marcadores entre corchetes que arma `history-line.ts` (foto/video sin adivinar, pedir el pie si trae, nota de voz por escrito, sticker sin comentar, documento a un asesor) sin explicar nunca por qué no puede ver ni escuchar. El test la pasa sola por `revealsIdentity` (`identity-guard.ts`) y confirma que da `null`. T2 (8/9/2026, plan "Seis frentes del buzón") había agregado `SALES_HANDOFF_RULES`, mismo patrón que `MEDIA_RULES`, pidiéndole al modelo confirmar dos veces antes de escalar un pase a ventas. En producción hizo bucle: el cliente contestaba "ok", "está bien" o "dale" en el primer paso hacia el cierre, el modelo no lo contaba como el segundo "sí" literal que el prompt exigía, y la conversación quedaba dando vueltas sin escalar nunca. El operador aprobó volver a escalar al primer sí. El 9/9/2026 (corrida "El pase a ventas al primer sí") `SALES_HANDOFF_RULES` se reemplazó por `SALES_ACCEPTANCE_RULES` —misma forma: exportada aparte para que el test la pase sola por `revealsIdentity`, interpolada en la sección 3 dentro del prefijo cacheable—, que manda pasar el caso de una vez sin pedir una segunda confirmación y nombra explícitamente las formas de aceptar que no son un "sí" literal ("ok", "está bien", "dale", "listo", "claro", "por favor", un pulgar arriba), que fue la causa real del bucle. La sección 5.1 y el párrafo de fuera de horario volvieron a mandar escalar directo con motivo `intencion_compra`, sin remitir a ninguna reconfirmación. La máquina de estados que hacía de red de seguridad en código (`handoff-confirmation.ts`, `buildEscalateTool` en `tools.ts`) ya no existe: esta prosa es ahora la única regla. 14/9/2026 (T2, "La voz cercana y la espera visible"): la sección 6 deja de mandar copiar la franja y el saludo de `TURNO ACTUAL` —causaba saludos por hora en mitad de una conversación— y pasa a "saludas UNA sola vez, con un hola neutro, solo cuando el sufijo lo pida, nunca por la hora"; el sufijo `needsGreeting` ("le escribes de SBK Motorcycles") es el único lugar que decide el saludo y sigue pasando `revealsIdentity`. T3 (14/9/2026): nueva sección exportada `TONE_RULES` ("6 BIS. CÓMO SUENAS", interpolada entre la sección 6 y `MEDIA_RULES` para no correr la numeración 5.x/7 que citan otros módulos y tests): tutear, reconocer el pedido antes de responder, decir por qué y qué va a pasar al escalar, nunca contestar seco, agradecer, un emoji como mucho (🏍️/👍/🙌), disculpa antes que solución, nada de fórmulas de correo ("estimado", "le informamos"…). `TurnContext` suma `customerName?`: sufijo nuevo ("El cliente se llama X: úsalo con naturalidad…") solo cuando `customerFirstName` devolvió algo. `OFF_TOPIC_REPLY` y el sufijo `missingCatalog` reescritos cálidos. T7 (14/9/2026, Decisión 8): 5.5 responde el horario o si la tienda está abierta con TURNO ACTUAL, sin escalar ni consultar la biblioteca ("horarios" salió de la lista de temas de biblioteca); 5.1: un agotado con pedido de aviso escala con motivo `seguimiento`, no `intencion_compra`, con resumen de repuesto y moto; sección 3: listas largas y mayoreo se toman completas, UNA sola pregunta de marca/modelo y la lista ordenada al escalar. T6 (14/9/2026): `MEDIA_RULES` avisa que al segundo adjunto sin texto el caso pasa solo a un asesor (la regla vive en código, la línea es informativa). T2 ("La voz de mostrador con nombre propio", 15/9/2026): importa `BUSINESS_NAME` de `brand.ts` (cabecera del bloque, sección 1, "con quién hablo", sufijo `needsGreeting`); la sección 1 describe la identidad que pidió el operador —"alguien sumamente amable que recibe cada mensaje, entiende qué necesita el cliente, lo orienta y le cotiza, y le pasa la conversación a un asesor de ventas" ante compra, reclamo, devolución o pedido específico— y el alcance real (orientar y cotizar con catálogo y biblioteca, pasar lo específico); la línea de prohibición suma "agente virtual, agente automatizado". T3 (15/9/2026): `greetingFor`/`dayBand` (`business-hours.ts`) vuelven a tener llamador en `buildInstructions`: el saludo por franja (buenos días/tardes/noches) regresa, pero SOLO en el sufijo `needsGreeting`, calculado una vez por conversación con la hora de Barinas (helper local `capitalizar`); `turnClockLine` sigue sin traer franja, así que el bug del 14/9 (saludar por hora en cada turno) no puede reaparecer. La sección 6 deja de prohibir "saludar por la hora" y pasa a exigir el saludo exacto que llega calculado. T2a (18/9/2026, plan "Seba atiende el mostrador"): cambia SOLO las secciones 1, 3, 4 y 5.1 (`buildInstructions`/`TurnContext`/`needsGreeting`, `MEDIA_RULES` y la sección 6 quedan para T2b, tarea aparte del mismo plan). Importa `AI_NAME` (`brand.ts`) y las cuatro constantes de `seba.ts` (`PREGUNTA_FILTRO`, `TEXTO_CONFIRMAR_INVENTARIO`, `TEXTO_SIN_STOCK`, `TEXTO_NO_IDENTIFICADO`). Sección 1: "No tienes nombre propio" se reemplaza por "Te llamas Seba… si preguntan con quién hablan, eres Seba, el asistente de SBK Motors" (interpolado con `AI_NAME`/`BUSINESS_NAME`, nunca el literal); la línea de prohibición suma "asistente virtual, asistente automatizado" y dejar "asistente" a secas permitido (espejo del cambio en `identity-guard.ts`). Sección 3: "da un paso hacia el cierre" y "pídelo directo y en una sola pregunta" se funden en la REGLA DE LA ÚNICA PREGUNTA (requisito 5: cero preguntas salvo una consulta genérica, que admite UNA de filtro, `PREGUNTA_FILTRO`); se conservan `SALES_ACCEPTANCE_RULES` y la regla de listas largas. Sección 4: el párrafo de la herramienta de escalar nombra cuándo se usa en consulta de disponibilidad (en cuanto hay resultado de catálogo, con o sin existencia, o cuando Seba no maneja la información). Sección 5.1: tres casos con texto fijo y motivo de escalada —encontrado con existencia → `TEXTO_CONFIRMAR_INVENTARIO` + `confirmar_inventario`; en cero → `TEXTO_SIN_STOCK` + `sin_stock`; nada o no identificado → `TEXTO_NO_IDENTIFICADO` + `no_identificado`, sin inventar alternativas—, conservando intactos el párrafo del agotado con aviso (`seguimiento`) y el de fuera de horario (`intencion_compra`) de la Tarea 7. La red de seguridad en CÓDIGO que hace cumplir estos tres casos (el enum de `buildEscalateTool` en `tools.ts` y el bloque nuevo en `agent.ts`) es tarea aparte (T3 del plan): acá solo vive el guion que se lo pide al modelo. T2b, mismo plan (18/9/2026): completa lo que T2a había dejado pendiente. `TurnContext.needsGreeting` se renombra `introducedThisTurn: boolean` (semántica invertida: `true` SOLO cuando Seba se presentó EN ESTE MISMO TURNO, en el mensaje aparte que `agent.ts` manda por código justo antes; `false` en cualquier otro turno de la conversación) — las DOS ramas del sufijo dicen "no saludes ni te presentes", la diferencia es solo si hace falta explicar que la presentación YA salió en un mensaje aparte. Se eliminan los imports `dayBand`/`greetingFor` y el helper local `capitalizar`: ya no calculan ninguna franja, el saludo salió del prompt por completo (lo manda `sebaGreeting`, `seba.ts`, desde `agent.ts`). Sección 6 ("CÓMO ESCRIBES") pierde el párrafo "Saludas UNA sola vez por conversación…" y pasa a "No saludas ni te presentas: Seba ya se presentó en un mensaje aparte que el sistema manda antes que el tuyo. Ni al abrir la conversación ni cuando el cliente vuelva a saludar." `MEDIA_RULES` pierde "saluda y" de la línea del sticker (queda "…pregunta en qué lo puedes ayudar", sin saludo). T5, mismo plan (18/9/2026, requisito 7, "Lecciones de Seba"): `TurnContext` suma `lessons?: TurnLessons` (`lessons.ts`); nueva `cacheablePrefix(lessons)` exportada — `SYSTEM_PROMPT` más, si las hay, `buildGlobalLessonsBlock(lessons.global)` pegado con una línea en blanco (vacío → exactamente `SYSTEM_PROMPT`, ni un carácter de más, para no romper el caché de los turnos sin ninguna lección todavía). `buildInstructions` arma el prefijo con `cacheablePrefix` en vez de `SYSTEM_PROMPT` a secas, y el sufijo suma `buildChatLessonsLine(lessons.chat)` al final del todo (después del nombre del cliente): las lecciones de ESTA conversación son la corrección más específica que existe |
| `tools.ts` | Herramientas del tool loop: catálogo, historial de pedidos, escalar; topes de resultados. `EscalationOutcome` gana `unassigned?: boolean` (anexo A1, 5/9/2026), que `buildEscalateTool` copia de `result.unassigned`: es lo que le dice a `agent.ts` si la despedida que va a mandar el modelo se quedó sin nadie detrás. B4 (5/9/2026): `ToolDeps` gana `businessHours?`/`now?` (los usa solo la herramienta de escalar) y `unassignedEscalationInstruction(status)` traduce el `businessStatus` que devuelve `escalateConversation` a la instrucción con la que el modelo redacta la despedida sin asesores — abierta → "en breve" sin prometer plazo; cerrada → "un asesor te escribe el lunes a partir de las 8:00 am"; todo cerrado → "apenas la tienda vuelva a abrir". Sigue siendo `is_auto_reply` (no apaga `awaiting_reply`); con asesor asignado la instrucción no cambia. D3 ("Lo pequeño que quedó atrás", 6/9/2026): si la consulta de catálogo o de historial devuelve `error`, `log.error` deja `herramienta_catalogo_fallo`/`herramienta_historial_fallo` con `conversationId` y `detail` antes de devolverle al modelo el "No se pudo consultar…" de siempre; antes ese error se tragaba en silencio. T2 (8/9/2026, plan "Seis frentes del buzón") le había sumado a `ToolDeps` `lastCustomerMessageAt?`/`handoffConfirmationPendingAt?`, y `buildEscalateTool`, SOLO con `motivo === "intencion_compra"`, consultaba `handoffConfirmationState` (`handoff-confirmation.ts`) antes de escalar en vez de llamar directo a `escalateConversation`. Revertido el 9/9/2026 (corrida "El pase a ventas al primer sí"): esa reconfirmación hizo bucle en producción con "ok"/"está bien"/"dale" en vez de un segundo "sí" literal, y el operador aprobó eliminarla por completo. Desde entonces `buildEscalateTool` escala con la PRIMERA llamada para los tres motivos ("devolucion", "queja", "intencion_compra") — todos caen directo en `escalateConversation`, sin reconfirmar nada —, `ToolDeps` perdió los dos campos, y `SALES_HANDOFF_PENDING_INSTRUCTION` se borró con el resto de la máquina de estados. T5 (14/9/2026): `unassignedEscalationInstruction` pasa a `escalationInstruction(status, assignedName)` con cuatro ramas (con/sin asesor × abierta/cerrada); con asesor y tienda cerrada la instrucción nombra el día y la hora de apertura y prohíbe prometer que lo atienden ahora. `EscalationOutcome` gana `businessStatus`. T3 (14/9/2026): las cuatro ramas de `escalationInstruction` ganan calidez explícita ("con calidez"/"agradécele"); antes solo la rama con asesor y tienda cerrada la traía. T7 (14/9/2026): `buildEscalateTool` suma `seguimiento` al enum de `motivo` (`EscalationMotivo` en escalate.ts ya lo admitía; el modelo no podía elegirlo) con `.describe` que explica el caso: aviso de reposición, listas largas/mayoreo, postventa. Corrección del 15/9/2026 (verificación final de "La voz de mostrador con nombre propio"): las cinco combinaciones de `escalationInstruction` terminan con la constante exportada `RECORDATORIO_SALUDO` ("si este es el primer mensaje… abre igual con el saludo exacto que te dio; si no, no saludes"): un chat nuevo que escaló por falta de stock se comía el saludo de franja porque la instrucción de escalada, la última que lee el modelo antes de redactar, no lo mencionaba. T2a (18/9/2026, plan "Seba atiende el mostrador", requisito 6 del cliente): la `description` de `buildEscalateTool` decía "pausa la IA" — pasa a "la IA sigue contestando en este chat hasta que el asesor escriba", reflejando que escalar deja de apagar `ai_enabled` en el acto (la reescritura real de `escalate.ts` para que eso sea cierto es T4, tarea aparte); el enum de `motivo` NO cambia acá (`confirmar_inventario`/`sin_stock`/`no_identificado` los suma T3). T2b, mismo plan (18/9/2026): `RECORDATORIO_SALUDO` pierde su sentido literal —el saludo salió del prompt por completo, ya no hay nada que "sobrevivir" a una escalada— pero se conserva como constante, con el sentido invertido: "No saludes ni te presentes: Seba ya se presentó en un mensaje aparte." Sigue siendo la ÚLTIMA instrucción de las cinco ramas de `escalationInstruction`, así que sigue teniendo que decir la verdad T3 (18/9/2026): `buildCatalogTool(deps, catalogOutcome)` gana un segundo parámetro (`CatalogOutcome`, exportado) que acumula `ran`/`conExistencia`/`agotados`/`sinResultados`/`generico` entre llamadas del mismo turno; cada llamada con resultado deja SIEMPRE una `instruccionParaTuRespuesta` (antes solo aparecía si había algo que avisar), en este orden de precedencia: `conExistencia` (algún stock > 0, texto `TEXTO_CONFIRMAR_INVENTARIO` y motivo `confirmar_inventario`) → `generico` (`!motoBrand && !motoModel && (quoted.length > 3 || hayMas)`: UNA pregunta de filtro, `PREGUNTA_FILTRO`, y NO escala en ese turno — suprime también `RECORTE_INSTRUCTION`, que contradiría "no listes") → todos en cero (`TEXTO_SIN_STOCK`, motivo `sin_stock`) → sin resultados, sea por `terms.length === 0` o por `quoted.length === 0` tras filtrar (`TEXTO_NO_IDENTIFICADO`, motivo `no_identificado`, marca `sinResultados`); el error de la base también marca `sinResultados`. La vieja `SIN_STOCK_INSTRUCTION` (avisaba sin obligar a escalar) se retira. `buildEscalateTool` suma `confirmar_inventario`/`sin_stock`/`no_identificado` al `z.enum` de `motivo`, con su línea de descripción cada uno; sin `consulta_generica` — ese caso no escala. T5c, mismo plan (18/9/2026, requisito 7, decisión P3, "Lecciones de Seba"): `buildCatalogTool` lee, ANTES de armar el filtro, los sinónimos activos de `ai_lessons` (`kind = 'sinonimo'`, límite `MAX_SYNONYM_LESSONS = 200`) y expande `terms` con `expandTerms` (`catalog-search.ts`) — jerga del cliente ("pastilla") que también encuentra el nombre real del catálogo ("pastillas de freno"). El `.or()` del filtro y `rankByTerms` usan los términos YA expandidos |
| `agent-tools.ts` | Interruptores por herramienta, en la base, cambiables en vivo |
| `catalog-search.ts` / `knowledge-search.ts` | La parte que se equivoca en silencio: cómo se arma la búsqueda en catálogo/biblioteca. T5c, plan "Seba atiende el mostrador" (18/9/2026, requisito 7, "Lecciones de Seba"): `catalog-search.ts` suma `expandTerms(terms, synonyms: SearchSynonym[])` — agrega, a los términos que ya salió `searchTerms`, el término real de cada sinónimo (`kind='sinonimo'` en `ai_lessons`) cuya jerga esté entre ellos; compara normalizado, no duplica (`Set`) e ignora `isActive === false` como segunda guarda (la primera es que `tools.ts` ya filtra `is_active = true` en la consulta). NO reutiliza `products.sinonimos_busqueda` (hallazgo 9 del plan) |
| `knowledge.ts` | Consulta de la biblioteca (políticas, garantías, horarios): información para REDACTAR, no respuestas. D3 (6/9/2026): recibe `conversationId` y, si la consulta devuelve `error`, deja `herramienta_biblioteca_fallo` en el log antes de responder "No se pudo consultar la biblioteca…" |
| `escalate.ts` / `claim-agent.ts` | Escalamiento a humano; reclamo atómico del asesor menos recién asignado. Desde T0.3 (5/9/2026), tras el `update` de la conversación y la nota interna, `escalateConversation` deja su propio traspaso en `conversation_handoffs`: `human`+`toId` con razón `escalada` si hubo candidato, `unassigned` con razón `escalada_sin_asesor` si no. B4 ("El reloj dice la verdad", 5/9/2026): `escalateConversation` gana `businessHours`/`now` (defaults: horario por defecto y reloj real), calcula `businessStatus` UNA vez, sufija el evento de sistema sin asesores con "(en horario)"/"(fuera de horario)" y devuelve ese estado en el resultado (`unassigned`) para que `tools.ts` arme la despedida sin recalcularlo con otro reloj. T2 (8/9/2026) había hecho que el `update` de la conversación SIEMPRE pusiera `handoff_confirmation_pending_at: null`, para cerrar una oferta de pase a ventas pendiente al escalar. Se fue el 9/9/2026 (corrida "El pase a ventas al primer sí") junto con la reconfirmación entera —ya no hay ningún sello que limpiar, `escalateConversation` nunca vuelve a escribir esa columna—; el bucle del "ok"/"está bien"/"dale" en producción fue el motivo. T5 (14/9/2026): `businessStatus` en el resultado deja de ser exclusivo del caso sin asesor: viaja siempre, para que la despedida y la instrucción de escalada con asesor también digan cuándo | T4, "Seba atiende el mostrador" (18/9/2026, D2/D3, requisito 6 del cliente): el `update` DEJA de tocar `ai_enabled` — escalar ya no apaga la IA; lo que la apaga es el trigger `handle_agent_message_silences_ai` (migración 20260917010000, un asesor mandando su primer mensaje real) o la pausa manual, nunca `escalate.ts`. Antes del `update`, una lectura nueva (`select("assigned_agent_id, ai_enabled, assigned_agent:agents!conversations_assigned_agent_id_fkey(id, display_name)").eq("id", ...).maybeSingle()`) mira si el chat YA tenía asesor (hallazgo 3 del plan: con la IA encendida, el modelo puede volver a llamar a esta función en cada consulta de inventario del mismo chat — las reglas 3/4 del prompt escalan con o sin existencia — y reclamar de nuevo reasignaría por round-robin a OTRO asesor). Si ya está asignado: NO reclama, actualiza `deal_status` solo si `motivo === "intencion_compra"`, deja una nota "IA reiteró la escalada a X…" y devuelve `{ escalated: true, assignedAgentName, alreadyAssigned: true, businessStatus }` SIN `recordHandoff` (el aviso de asignación solo dispara con la razón `escalada`, `assignment-notice.ts`). Si el `select` previo falla, cae al camino de siempre (falla abierto: trata como "no asignado" en vez de tumbar la herramienta que el modelo está esperando). El freno contra el bucle de reescalar sin asesor de noche (P1: Seba sigue vendiendo) ya no es `ai_enabled=false`: es esta misma rama más el predicado nuevo del reconciliador (ver `reconciler.ts`). T3 (18/9/2026): `EscalationMotivo` suma `confirmar_inventario`/`sin_stock`/`no_identificado` — los motivos con los que `buildCatalogTool` (tools.ts) le pide al modelo que escale tras cotizar, y con los que la red de seguridad de `agent.ts` escala en código si el modelo se queda sin pasos antes de hacerlo. Sin CHECK en la base (hallazgo 7 del plan): viajan crudos en el texto del `system_event`, sin migración |
| `human-handled.ts` | La IA no entra donde una persona atiende el chat AHORA (sin `server-only` a propósito: lo usa `data.ts`). T7 (corrida "La IA ve lo que llega", 8/9/2026): dejó de preguntar "¿alguna vez escribió un asesor?" —47 de 48 conversaciones mudas medidas el 8/9 tenían un humano que había escrito ALGUNA VEZ, caso `3b654d2c…`, un "a" del 28/8— y pasó a `humanClaimsChat(lastHumanAt, lastCustomerMessageAt, now, graceMinutes)`, función PURA que comparten `humanHasWritten` (consulta individual, `select("created_at")...order().limit(1)`) y `conversationsWrittenByHumans` (lote, `select("conversation_id, created_at").in(ids).eq("agent").gt("created_at", umbral)` con `umbral = min(now − G, min lcma)`, UNA sola consulta): bloquea si el humano escribió DESPUÉS del último mensaje del cliente (`created_at > lastCustomerMessageAt`) o si escribió hace menos de `G` minutos (`AI_HUMAN_GRACE_MINUTES`, `humanGraceMinutes()`, default 30). `lastCustomerMessageAt` null → `false` sin consultar. Sigue fallando CERRADO. H2, plan "Seba atiende el mostrador" (18/9/2026, D2 "la reapertura salta la gracia"): `humanClaimsChat` gana un quinto parámetro opcional `reopenedAt` — un mensaje de asesor de ANTES de la última reapertura por el cliente (`conversation_handoffs.reason = 'reabierta_por_cliente'`, la escribe `route.ts`) ya no cuenta para la cláusula de gracia, porque el chat reabierto arranca de cero; la cláusula de "se adelantó" no cambia. `humanHasWritten`/`conversationsWrittenByHumans` solo consultan esa reapertura cuando la gracia iba a disparar de todas formas (una consulta de lote para `conversationsWrittenByHumans`, solo con los ids que la necesitan) y fallan cerrado con `console.error` (no `@/lib/log`: server-only rompería `data.ts`) ante un error de esa consulta puntual |
| `identity-guard.ts` | Guarda de identidad (6/9/2026, módulo PURO, sin imports, sin `server-only` a propósito —comparte el rasgo con `human-handled.ts`: la reusa `mutations.ts` desde el navegador). `revealsIdentity(text)` normaliza a minúsculas sin diacríticos y devuelve `null` o `{categoria: "automatizacion" \| "persona", fragmento}`; ancla los patrones en la AUTORREFERENCIA ("soy un…", "asistente automatizado", "no soy una persona") y en términos sin otro uso en el negocio (`bot`, `chatbot`, `inteligencia artificial`, `IA` en mayúsculas sobre el texto ORIGINAL —así `guía`/`GUIA`/`AUTOASIA` no calzan—, `ChatGPT`, `OpenAI`, `GPT`, `Gemini`, `modelo de lenguaje`), nunca en `automátic*`/`digital`/`sistema` sueltos: en `products` hay 25 filas como `AUTOMATICO HORSE` o `TACOMETRO DIGITAL BERA SBR` que esos adjetivos sí nombran de verdad. `rewriteSuffix(fragmento)` arma el pedido de reescritura que se pega DESPUÉS de `SYSTEM_PROMPT` (prefijo exacto intacto, el caché no se rompe). Medido en `messages` antes de la guarda: 34/68 mensajes el 26/8/2026 y 26/151 el 27/8/2026 se describían como "asistente automatizado"; con el prompt nuevo, 0/702 el 28/8/2026 — la guarda es la red para cuando la regla del prompt falla igual. T2 (15/9/2026): suma el patrón `agente (virtual|automatizad\w*|de ia|de inteligencia artificial|conversacional)` a la autorreferencia bloqueada ("jamás dice que es un agente de IA", pedido del operador) y `rewriteSuffix` lo nombra; "agente" suelto NO se bloquea porque los asesores humanos son `agents` ("un agente de ventas te escribe por acá" pasa). T2a (18/9/2026, plan "Seba atiende el mostrador", requisito 1 del cliente): la IA pasa a llamarse Seba y a presentarse como "tu asistente" — se retira `/soy (el|la|un|una) asistente/` y se saca "asistente" de `/como (ia|inteligencia artificial|asistente)\b/` (queda `/como (ia|inteligencia artificial)\b/`); "asistente" a secas ya no está anclado, solo en combinación con "virtual"/"automatizado" (que sigue bloqueada). En `PATRONES_PERSONA`, `/me llamo/` y `/mi nombre es/` ganan un lookahead negativo `(?!seba\b)` —corre sobre el texto NORMALIZADO, en minúsculas— que excepciona SOLO ese nombre: "mi nombre es Seba" pasa, "mi nombre es Carlos" sigue bloqueado como persona. `rewriteSuffix` pide quitar "asistente virtual" en vez de "asistente" a secas, para no pedirle al modelo que se quite de encima una palabra que ahora tiene permitida |
| `send.ts` | Envío de las respuestas del turno (texto redactado o escenario con adjunto). `sendAgentText`/`sendAgentMedia`/`sendPlaybookReply` devuelven (T0.3, 5/9/2026) el `DeliveryOutcome` en vez de `Promise<void>` — `sendPlaybookReply` propaga el del TEXTO, no el del adjunto — para que `agent.ts` pueda mirar `whatsapp_status` y registrar el traspaso cuando Meta rechaza un envío. `sendAgentText` gana `opciones?: { isAutoReply?: boolean }` (anexo A1, 5/9/2026): con `isAutoReply: true` el insert lleva `is_auto_reply: true` (misma marca que la bienvenida automática, T0.1), así que el trigger `handle_new_message` no lo cuenta como respuesta real. Default `false` para los tres llamadores existentes (`agent.ts` ×2, `sendPlaybookReply`). S6 (corrida "La IA ve lo que llega", hallazgo 4, 8/9/2026): `DeliveryOutcome` suma `origenDelFallo: "meta" \| "red" \| null` — `entregar()` lo decide por el TIPO de excepción del `catch` (`MetaApiError` → `"meta"`, cualquier otra cosa como `TypeError: fetch failed` → `"red"`), no por si trae código numérico. `origenDelFallo` NO es columna de `messages`: el nuevo helper `columnasDeEntrega(entrega)` reemplaza el `...entrega` que esparcía el objeto entero en los dos inserts (`sendAgentText`/`sendAgentMedia`) por las cuatro columnas explícitas de siempre, así que sumarle campos a `DeliveryOutcome` no puede volver a colarse en la base por accidente | T4, "Seba atiende el mostrador" (18/9/2026, D2/D3): `sendPlaybookReply` gana un cuarto parámetro `opciones?: SendAgentTextOptions` que reenvía tal cual a `sendAgentText` — `runPlaybook` (`agent.ts`) lo arma con `{ isAutoReply: esperandoAsesor }` para que un escenario que calza en un chat YA asignado salga marcado desde el mismo envío, sin esperar al UPDATE posterior que solo existe para el camino `afterSend: "escalate"` (ese todavía no sabe, al mandar el texto, si va a hacer falta un asesor). T3, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D3/D4): `playbookMessageText(playbook, links)` y `sendPlaybookReply(supabase, target, playbook, links, opciones?)` ganan el parámetro `links: CatalogLink[]` (los catálogos ACTIVOS del turno) — resuelven `{{catalogo:<key>}}`/`{{catalogos}}` (`resolveCatalogMarkers`, `catalog-links.ts`) en `response_text` y, si el adjunto es `link`, en `attachment_url`; sin ningún marcador el resultado es byte a byte el de antes. Fase 0 (`matchPlaybook`) ya descartó cualquier escenario con marcador sin resolver, así que acá `missing` siempre llega vacío en un turno real |
| `precio.ts` | Formato de precios en código, no en el prompt: la aritmética no se le confía al modelo |
| `model.ts` | Selección de proveedor/modelo por `.env` (prod: OpenAI-compatible vía `OPENAI_BASE_URL`, hoy OpenRouter con `gpt-5.6-luna`; dev: Gemini); clasificador puede ir aparte. `AI_AGENT_REASONING` (S6, 8/9/2026, default `on`): `build()` solo adjunta `providerOptions.openai.reasoningEffort` si el proveedor es OpenAI Y la variable no es exactamente `"off"` — `gpt-5.6-luna` no razona (es un alias de OpenRouter) y mandarle el parámetro igual solo producía el warning del AI SDK `reasoningEffort is not supported` 3-4 veces por turno sin aplicar nada (medido 7/9/2026). Gobierna el proceso, no el nombre del modelo: es una decisión de `.env`, no una heurística sobre el id. T2 ("La respuesta llega en siete segundos", 7/9/2026): su test fija la trampa — un id de OpenRouter con prefijo de fabricante (`google/gemini-3.1-flash-lite`) NO empieza con `gemini`, así que sale por el proveedor `openai` (`OPENAI_BASE_URL`), que es lo que se quiere; si `resolveProvider` reconociera el prefijo `google/`, el caso se pone en rojo |
| `bcv.ts` / `bcv-fetch.ts` / `bcv-intermediate-ca.ts` | Tasa oficial del BCV leída de su web; con el certificado intermedio que su servidor no manda. Tests: `bcv.test.ts` (parseo contra el fixture real de `__fixtures__/bcv-home-2026-08-28.html`, con `it.todo` del defecto D1: punto decimal → tasa ×100), `bcv-cache.test.ts` (caché/refresco/respaldo `isStale`), `bcv-fetch.test.ts` (TLS/HTTP sin red) |
| `__fixtures__/` | Capturas literales de bcv.org.ve, sin editar; `.gitattributes` en la raíz las protege de la normalización CRLF/LF (las regex del parseo miden distancias en caracteres) |
| `pgrst.ts` | Entrecomillado seguro para filtros `.or()` de PostgREST (anti-inyección) |
| `fake-redis.ts` | Redis en memoria SOLO para pruebas que no dependen de la atomicidad Lua. T0 (7/9/2026): suma `zscore`, `get` y `scan` (para `defer`/`purge`) y el `CLAIM_SCRIPT` reconocido devuelve `[member, vencimiento]`, sembrando/leyendo la misma clave de vencimiento que el script real |
| `queue-continuation.test.ts` | Segundo archivo de test de `queue.ts` (T3, 7/9/2026): la continuación automática de la cola con `FakeRedis` + `vi.useFakeTimers()` — programa exactamente una, elige el menor plazo, no se solapa, `stopAgentQueue` la cancela, un error no la dispara, y la simulación de 100 conversaciones sin cron que documenta que una pasada que agota su tope sin diferir no la programa |

## `src/lib/supabase` y `src/lib/whatsapp`

| Archivo | Qué es |
|---|---|
| `supabase/admin.ts` | Cliente service_role (bypassa RLS); solo rutas de servidor sin sesión |
| `supabase/client.ts` / `server.ts` | Clientes anon para navegador / Server Components |
| `supabase/middleware.ts` | Sesión desde la cookie sin preguntarle a GoTrue en cada petición; redirecciones de acceso |
| `supabase/database.types.ts` | Tipos generados del schema (a mano cuando `supabase gen types` no está disponible; ver T0.2, 5/9/2026, que le agregó `conversations.last_reply_at`/`last_reply_sender` y `messages.is_auto_reply`; T1.7, mismo día, le agrega `contacts.search_text` solo en `Row` —generada, no se puede insertar/actualizar—, igual que ya está tipado `products.search_text`; T3.4, mismo día, le agrega a `whatsapp_channels` las cuatro columnas de salud: `quality_rating`, `messaging_limit`, `account_restrictions`, `health_updated_at`; T3.2, mismo día, le agrega `messages.payload` y `conversations.referral`, ambas `Json | null`). B3 (5/9/2026): `agent_settings.business_hours: Json` en Row/Insert/Update (migración 20260906010000). T2 (8/9/2026): `conversations.handoff_confirmation_pending_at: string \| null` en Row/Insert/Update (migración 20260909010000) — sin uso en código desde el 9/9/2026 (corrida "El pase a ventas al primer sí"), pero se conserva a propósito porque el esquema real de la base todavía tiene la columna. T3a (8/9/2026): tabla `stickers` (migración 20260909020000), insertada alfabéticamente entre `sinonimos_busqueda` y `tags`. T5 (8/9/2026): agrega la tabla `invoices` completa (migración 20260909040000) — nótese que `conversation_pins` (20260905040000) quedó SIN agregar nunca a este archivo, así que el orden alfabético de las tablas ya no es estricto. T2/T3, "La IA no vuelve a pedir lo que ya pidió" — revisión (16/9/2026, migración `20260916010000`): `conversations` suma `ai_resume_cutoff_at: string \| null` en Row/Insert/Update y `new_since_ai_resume: boolean \| null` SOLO en Row —columna generada, Postgres la calcula sola, no se escribe por Insert/Update—, reemplazando al campo `awaiting_any_reply` del diseño descartado del 15/9. T5, plan "Seba atiende el mostrador" (18/9/2026): agrega la tabla `ai_lessons` (migración 20260917020000), insertada alfabéticamente entre `agents` y `ai_playbook_tags`. M1, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026): agrega la tabla `catalog_links` (migración 20260918010000), insertada alfabéticamente entre `ai_playbooks` y `contact_tags` |
| `whatsapp/meta-client.ts` | Cliente server-only de la Graph API: texto, plantillas, multimedia, reply, descarga de media. `markWhatsappRead`/`sendTypingIndicator` (T3.1, 4/9/2026, doble check azul y "escribiendo…") NUNCA lanzan —a diferencia del resto del archivo, que sí lanza `MetaApiError`—: atrapan su propio fallo con `log.warn` y listo, porque un check o un typing que no salió no puede tumbar la bandeja ni el turno de la IA. Las dos comparten `notifyGraphApi` y el mismo "message_id" que exige Meta: el wamid del mensaje ENTRANTE al que se está respondiendo. `sendWhatsappTemplate` gana `components?` (T3.3, 5/9/2026, tipo `WhatsappTemplateComponent[]` importado de `whatsapp/template-variables.ts`): solo lo agrega al payload si viene con al menos un elemento —Meta rechaza `components: []` en una plantilla sin parámetros—. `sendWhatsappTemplate` gana `components?` (T3.3, 5/9/2026, tipo `WhatsappTemplateComponent[]` importado de `whatsapp/template-variables.ts`): solo lo agrega al payload si viene con al menos un elemento —Meta rechaza `components: []` en una plantilla sin parámetros—. T3a (8/9/2026): `MediaKind` suma `"sticker"`; su payload es `{link}` SIN `caption` —según la referencia de mensajes de la Cloud API el objeto `sticker` no admite `caption`—, mismo trato que ya tenía `audio`. Verificado contra producción el 8/9/2026: el primer sticker saliente SÍ rebotó, pero por peso (131053, animado de 973.668 bytes contra el tope de 500 KB, mensaje `642661b5…`), no por el `caption` — la forma del payload no fue el problema; ver `whatsapp/sticker-guard.ts` y la trampa "los límites de Meta son de salida" en `CLAUDE.md`. `sendWhatsappTemplate` gana `components?` (T3.3, 5/9/2026, tipo `WhatsappTemplateComponent[]` importado de `whatsapp/template-variables.ts`): solo lo agrega al payload si viene con al menos un elemento —Meta rechaza `components: []` en una plantilla sin parámetros— |
| `whatsapp/template-variables.ts` | T3.3 (5/9/2026): variables posicionales `{{1}}`, `{{2}}`... del cuerpo de una plantilla — puro y compartido a propósito entre el selector (cliente, para saber cuántos campos pedir y renderizar la vista previa) y `api/messages/send` (servidor, para armar `components` y sustituir el `content` que se guarda): `templateVariableCount`, `substituteTemplateVariables`, `buildTemplateBodyComponents` |
| `whatsapp/failure-reason.ts` | Del código de error de Meta a una frase accionable para el asesor. Tabla completa (T3.3, 5/9/2026): opt-out/bloqueo (131050), límite de ritmo por número (131056) y de cuenta/app (130429/80007/4), la plantilla misma (132012/132015/132016, además de 132000/132001 que ya estaban), y cuenta/número restringidos (131037/131031/368/133010) o con límite por mala calidad (131064). `failureAction(code)` es nuevo: la única acción con gesto propio hoy es 131047 (ventana vencida) → `"abrir_plantillas"`, que `message-bubble.tsx` usa para ofrecer el botón que abre el selector. D4 (6/9/2026): la frase de 131026 pasa de "confirma el número" a decirle al asesor qué mirar primero —el chat, por si hay un aviso de cambio de número— antes de asumir que el cliente desapareció; `failureAction(131026)` sigue en `null` porque el botón para abrir la conversación del número nuevo necesita que la burbuja conozca ese número (deuda, otra corrida) |
| `whatsapp/phone.ts` | Qué cuenta como número escribible (la falla del `+undefined`). T6 (8/9/2026): `normalizePhoneInput(raw, defaultCountryCode = "58")` convierte lo que un asesor escribe a mano ("0414-1234567", "58 414…", "0058414…", ya con `+`) a E.164, resuelve los cuatro prefijos venezolanos y termina pasando SIEMPRE por `isDeliverablePhoneNumber` — null antes que un E.164 con la forma correcta pero un número que Meta va a rechazar |
| `whatsapp/sticker-guard.ts` | NUEVO ("El sticker que no entra", 8/9/2026), `server-only`. `checkStickerBeforeSend(mediaUrl)`: baja el sticker saliente del bucket con el cliente admin y lo mide contra el límite de Meta (`isAnimatedWebp`/`isWithinStickerLimit`/`stickerRejectionMessage` de `sticker-image.ts`) ANTES de que `api/messages/send/route.ts` inserte la fila — Meta acepta el POST con 200/wamid igual y el rechazo real (131053) llega recién 3 s después por el webhook de status, un `failed` silencioso. NO confía en la columna `animated` de `stickers`: el sticker roto de producción (`642661b5…`) estaba guardado con `animated = false` siendo animado. Si el archivo no se puede leer (Storage caído, fila apuntando a nada), DEJA PASAR el envío y solo deja `log.warn("send.sticker_no_se_pudo_leer")` — una falla de disponibilidad no es evidencia de que el sticker esté roto, y `signedUrlForSending` ya es la red de seguridad real más adelante si el archivo de verdad no está |
| `media-extension.ts` | `extensionForMime(mime)`: extensión de archivo a partir del `Content-Type` que manda Meta; corta el parámetro tras `;` (las notas de voz llegan como `audio/ogg; codecs=opus`, no `audio/ogg` a secas), normaliza mayúsculas y cae a `"bin"` sin match. Módulo PURO, movido desde `EXTENSION_BY_MIME` del webhook (T6, "La voz de mostrador con nombre propio", 15/9/2026): hasta esa fecha toda nota de voz quedaba en Storage como `.bin` porque el lookup exacto nunca calzaba con el parámetro del códec; solo afectaba el nombre al descargar, el `Content-Type` del upload siempre fue el correcto. Sin backfill de los `.bin` ya guardados |

## `src/components`

**Shell:** `crm-shell.tsx` (cliente raíz de la bandeja: estado, realtime, outbox, navegación — 26K, el componente más cargado; la paginación de "Todos" es `useInboxPager` sembrado con la primera página que resuelve el servidor — el shell ya no guarda `cursorRef` ni banderas propias, una ráfaga de scroll disparaba varias consultas con el mismo cursor; `livePulse` (A.T6b, 29/8/2026) es un contador que sube dentro de `fetchInboxHead` cada vez que el refresco en vivo trae una cabecera fresca de `conversations`, y viaja como prop a `InboxSidebar` — es el eco que le avisa a las píldoras que resuelven contra la base ("Pendientes" desde el 30/8/2026, "No leídas" y "Mías") que algo cambió sin que necesiten su propio canal de realtime; la prop de siembra es `initialPendingConversations`, que el shell pasa como `initialPendingRows` a `InboxSidebar`; T1.1, 4/9/2026: el canal `messages-{id}` consulta `read-on-arrival.ts` en cada INSERT entrante — si el chat abierto no está de verdad al frente y con foco, anota `pendingRead` en vez de marcar leído, y un oyente de `visibilitychange`/`focus` del mismo efecto lo suelta una sola vez al volver, siempre que el chat siga siendo el mismo — cambiar de conversación limpia el pendiente del chat viejo al desmontar sus oyentes; T3.1, mismo día: junto a `markConversationRead` —al abrir un chat apartado/con mensajes sin leer, y en `markReadNow()`— llama también a `sendReadReceipt` (`mutations.ts`), el doble check azul hacia Meta; NO se llama desde el apartado/desapartado a mano de `markUnread`/`markRead` del menú, que no prueban que el asesor haya mirado nada; T6, 8/9/2026: `handleContactCreated(conversationId)` — llega como `onContactCreated` a `InboxSidebar` — abre el chat (`openConversation`) y llama a `refreshConversations` para pedir la cabecera de nuevo: la conversación que acaba de crear `NewContactModal` todavía no está en `conversations`, nadie la bajó todavía; 8/9/2026, "La bandeja habla español y el número nuevo queda a un clic": `handleOpenConversationByPhone` resuelve `fetchConversationIdByPhone` y llama `openConversation(id)` —funciona aunque la fila no esté en la ventana cargada de la lista, el detalle se pide por id—, devuelve `false` sin conversación o con error de red), `app-rail` (navegación entre secciones; T6, "El aviso que llega", 8/9/2026: monta `<AssignmentNotifier />` — ver el detalle en la segunda mención de este mismo párrafo, más abajo), `url-search-box` (buscador sincronizado con la URL), `sliding-pills` (píldoras de filtro), `context-menu`, `theme-toggle`, `section-skeleton`, `sbk-logo`, `crm.css` (49K, estilos del CRM).. **Shell:** `crm-shell.tsx` (cliente raíz de la bandeja: estado, realtime, outbox, navegación — 26K, el componente más cargado; la paginación de "Todos" es `useInboxPager` sembrado con la primera página que resuelve el servidor — el shell ya no guarda `cursorRef` ni banderas propias, una ráfaga de scroll disparaba varias consultas con el mismo cursor; `livePulse` (A.T6b, 29/8/2026) es un contador que sube dentro de `fetchInboxHead` cada vez que el refresco en vivo trae una cabecera fresca de `conversations`, y viaja como prop a `InboxSidebar` — es el eco que le avisa a las píldoras que resuelven contra la base ("Pendientes" desde el 30/8/2026, "No leídas" y "Mías") que algo cambió sin que necesiten su propio canal de realtime; la prop de siembra es `initialPendingConversations`, que el shell pasa como `initialPendingRows` a `InboxSidebar`; T1.1, 4/9/2026: el canal `messages-{id}` consulta `read-on-arrival.ts` en cada INSERT entrante — si el chat abierto no está de verdad al frente y con foco, anota `pendingRead` en vez de marcar leído, y un oyente de `visibilitychange`/`focus` del mismo efecto lo suelta una sola vez al volver, siempre que el chat siga siendo el mismo — cambiar de conversación limpia el pendiente del chat viejo al desmontar sus oyentes; T3.1, mismo día: junto a `markConversationRead` —al abrir un chat apartado/con mensajes sin leer, y en `markReadNow()`— llama también a `sendReadReceipt` (`mutations.ts`), el doble check azul hacia Meta; NO se llama desde el apartado/desapartado a mano de `markUnread`/`markRead` del menú, que no prueban que el asesor haya mirado nada; T1, 8/9/2026: `dayScope` — `useState<InboxDayScope>("today")`, el default con el que `app/inbox/page.tsx` ya sembró todo — vive ACÁ y no en `InboxSidebar` (que sí guarda `filter`/`sort` bajo `sbk:inbox:{agentId}`) porque el corte también gobierna `fetchInboxHead`/`fetchInboxCounts`; se persiste bajo `sbk.inbox.scope.{agentId}` (`dayScopeStorageKey`, llave propia, distinta de la del sidebar), leído UNA vez al montar con el mismo patrón try/catch que el resto de la app; `dayStart = useInboxDay(dayScope)` es la ÚNICA fuente del corte, pasada tal cual a `InboxSidebar` y sumada como `since` a `fetchInboxHead`/`refreshInboxCounts`/`allPager.fetchPage`. `allPager` nace SEMBRADO (`seed`) y `useInboxPager` bloquea para siempre su primera página tras el primer montaje (`hasSeedRef`), así que un `sessionKey` nuevo no lo haría refetchear: un efecto aparte, con `didSkipInitialDayFetchRef` para saltar la primera pasada (ya sembrada por el servidor con el corte de hoy), pide una cabecera nueva a mano cuando cambia `dayStart` y la MEZCLA con `mergeById` — asimétrico a propósito: "hoy"→"Ver todo" trae lo viejo de una vez (se agrega al fondo), "Ver todo"→"hoy" no lo saca de memoria, solo deja de pintarse (`matchesDay` filtra en cada render lo que ya hay en `conversations`); T6, "El aviso que llega", 8/9/2026: `selectedId` ahora SÍ sigue a `initialConversationId` cuando el prop cambia SIN remontar el componente —antes el `useState` lo leía una sola vez al montar, así que el clic del aviso de asignación (`router.push("/inbox?conversation=<id>")`) no abría nada si el asesor ya estaba parado en `/inbox`, solo funcionaba viniendo de otra sección—; se resuelve comparando contra el valor ANTERIOR del prop en un `useState` propio (no un `useEffect` con el prop en las dependencias, que no puede distinguir "el prop trae un id nuevo" de "sigue siendo el mismo pero el asesor ya seleccionó otro hilo a mano"; tampoco un `useRef` leído Y escrito en el render, que la regla de lint `react-hooks/refs` de este repo prohíbe) durante el render mismo, el patrón oficial de React para "estado derivado de un prop": un rerender con el mismo prop no pisa la selección manual, y un prop `null` no cierra nada), `app-rail` (navegación entre secciones; T6, "El aviso que llega", 8/9/2026: monta `<AssignmentNotifier />` GLOBAL, fuera del `<nav>`, una vez por cada `AppRail` vivo —incluida la instancia que `section-skeleton.tsx` deja unos milisegundos durante una navegación—, sin plumbear quién es el agente actual: el dedupe de `assignment-notice.ts` es lo que garantiza un solo aviso aunque haya dos instancias a la vez; 10/9/2026, "El aviso de asignación sale arriba a la derecha": `AssignmentNotifier` ya no deja ningún nodo en el DOM —devuelve `null`—, así que el resguardo de layout pasó de mirar una hoja de estilos a `app-rail.test.tsx` ["entrega un solo hijo directo (el <nav>)"], `container.childElementCount === 1`), `assignment-notifier` (T6, 8/9/2026: aviso cuando un INSERT en `conversation_handoffs` pasa `shouldShowAssignmentNotice` —`assignment-notice.ts`—; resuelve su propio agente con `fetchCurrentAgent` y, aparte, el nombre del contacto —si esa segunda consulta falla el aviso se muestra igual con el texto neutro, es un requisito duro, nunca se traga el aviso por eso—; canal `assignment-notice` filtrado por `to_kind=eq.human` en el servidor, `to_id`/`reason` decididos del lado del cliente porque `to_id` depende de quién soy y no se puede fijar en el filtro de la suscripción; sin resync al reconectar, a propósito: este aviso no guarda lista, un handoff perdido durante una caída se pierde igual que si el navegador hubiera estado cerrado. 10/9/2026, "El aviso de asignación sale arriba a la derecha": diagnóstico medido en el navegador —el toast casero SÍ se dibujaba a los 200 ms del INSERT, pero abajo a la derecha, 6 s y sin el nombre (llegaba después por una consulta aparte)— llevó a reemplazarlo por `toast()` de `@heroui/react` sobre el `Toast.Provider placement="top end"` ya montado en `layout.tsx:57`, calcado del toast de "sticker guardado"; ahora se ESPERA `fetchContactName` antes de llamar a `toast()` (ya no hay un segundo `setState` que actualice la descripción a mitad de camino), con `timeout: 10000`, `variant: "accent"` y `actionProps: { children: "Abrir", onPress }` que navega con `router.push`\`/inbox?conversation=<id>\`\` —confirmado en los tipos de HeroUI que `ButtonProps` viene de `react-aria-components/Button`, con `onPress`, no `onClick`—; el componente ya no dibuja nada propio, devuelve `null`; el dedupe de `assignment-notice.ts` —Set de MÓDULO— sigue siendo lo que evita el `toast()` doble con dos `AppRail` montados a la vez), `url-search-box` (buscador sincronizado con la URL), `sliding-pills` (píldoras de filtro), `context-menu`, `theme-toggle`, `section-skeleton`, `sbk-logo`, `crm.css` (49K, estilos del CRM; T1 suma `.crm-day-scope-btn`, 32×32 junto a `.crm-sort-btn`; `.crm-system-note-action`, 8/9/2026: el botón bajo la nota de sistema, mismo lenguaje que `.crm-msg-failure-action` pero centrado). T4 ("Los números del día", 10/9/2026): gana `initialAgentDay`/`initialAiAssignments` (opcionales, default `null`/`[]`) y el estado `agentDay`/`aiAssignments` que alimenta a `AgentHomePanel`; `agentDayStart = useInboxDay("today")` es una fuente APARTE de `dayStart` —SIEMPRE hoy, sin importar el interruptor "Ver todo"— porque el resumen del día del asesor no es una vista de la bandeja; `refreshAgentDay` (useCallback) pide `fetchAgentDaySummary`/`fetchAiAssignmentsToday` con ese corte y se llama de rebote desde `fetchInboxHead`/`refreshInboxCounts` (sin bloquear su `Promise.all`, `void refreshAgentDay()`), desde un efecto que salta su primera pasada (`didSkipInitialAgentDayFetchRef`, mismo patrón que `didSkipInitialDayFetchRef`) cuando rueda la medianoche, y desde el canal `agent-day-handoffs` (`conversation_handoffs`, filtro de servidor `to_kind=eq.human`, chequeo de `to_id === currentAgent.id` del lado del cliente porque ese valor no se puede fijar en el filtro de la suscripción — mismo motivo que documenta `assignment-notifier.tsx`) vía `useLiveRefresh` para agrupar ráfagas. T4b (plan "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026): gana `initialCatalogLinks` (opcional, default `[]`) y el estado `catalogLinks`, que baja hasta `chat-panel` → `composer` para resolver `{{catalogo:<key>}}`/`{{catalogos}}`; canal `catalog-links-changes` sobre `public.catalog_links`, MISMO patrón que el canal `quick-replies-changes` de `quick_replies` (cualquier cambio pide `fetchActiveCatalogLinks` de nuevo) — la tabla ya está publicada en Realtime desde la migración 20260918010000.

**chat/:** `chat-panel` (historial + composer; reenvía `messages` al `composer` desde T2 "La ventana de 24 h dice la verdad", 7/9/2026; cabecera gana el botón "Cerrar"/"Reabrir" T2.1, 5/9/2026 — `closeConversation`/`reopenConversation` de `mutations.ts`, misma acción que el menú contextual de la bandeja; T3.3, 5/9/2026: guarda `templateModalSignal`, un contador que sube cada vez que una burbuja pide abrir el selector de plantillas —botón "Abrir plantillas" de `message-bubble` en un envío rechazado con 131047— y se lo pasa a `composer` como `openTemplateModalSignal`; T3.2, mismo día: pinta el banner "Llegó desde el anuncio…" bajo la cabecera con `isReferralFresh(conversation.referral)` —pasadas 72 h desde `referral.receivedAt` deja de mostrarse aunque la fila se conserve—, `.crm-referral-banner` en `crm.css`; 8/9/2026, "La bandeja habla español": recibe `onOpenConversationByPhone` y le pasa a cada `MessageBubble` `contactPhone={conversation.contact.phoneNumber}` y el callback, un caso en `chat-panel.test.tsx`), `composer` (texto, adjuntos, quick replies, plantillas — 19K; T2 "La ventana de 24 h dice la verdad", 7/9/2026: recibe la prop `messages` y `withinWindow` pasa a `isComposerWindowOpen(lastCustomerMessageAt, messages)` en un `useMemo` —un saliente rechazado con 131047 sin entrante real posterior deshabilita la caja, muestra el placeholder de plantilla y esconde `WindowCountdown` aunque `lastCustomerMessageAt` diga que quedan horas [caso real `aa75ef33…`, 6/9/2026]; dos casos nuevos en `composer.test.tsx`; T3.1, 4/9/2026: "escribiendo…" hacia Meta vía `sendTypingSignal` —`mutations.ts`—, un intervalo fijo de 20 s mientras haya texto, arrancado y detenido por la PRESENCIA de texto y no por su contenido; no usa `use-debounced-callback.ts`, un debounce trailing-edge nunca dispararía mientras el asesor sigue tecleando sin pausa, que es justo lo contrario de lo que hace falta; T3.3, 5/9/2026: `openTemplateModalSignal?: number` — un pedido de abrir el selector que viene de AFUERA, de una burbuja fallida; se compara contra la referencia del valor anterior, no contra `> 0`, para tolerar cualquier valor inicial y para que dos pedidos seguidos abran el modal las dos veces; `handleSelectTemplate` pasa las `variables` a `sendTemplateMessage`; T3b, "Seis frentes del buzón", 8/9/2026: gana la prop `currentAgent: Agent` —`chat-panel.tsx` ya la tenía, era el único hueco— para atribuir un sticker creado desde cero; suma el botón `Smile` (`EmojiStickerPopover`) a la izquierda del clip, e `insertEmoji(emoji)` lee `textareaRef.current.selectionStart/End` en el momento del clic —el popover nunca tuvo el foco, no lo sabe— y llama a `insertAtCaret` (`composer-text.ts`) con `flushSync` antes de `setSelectionRange`, mismo motivo que `wrapSelection`: sin el commit forzado el cursor pierde la carrera contra el commit de React bajo CPU contendida), `emoji-sticker-popover` (T3b, 8/9/2026: botón anclado con dos pestañas, mismo patrón que `TagFilterMenu` de la bandeja —cierra con Escape o clic afuera, sin diálogo modal, sin atrapar foco—, no HeroUI `Popover`; pestaña "Emojis" carga `emoji-picker-react` con `next/dynamic(..., {ssr:false})` recién al abrirse la primera vez —`Categories`/`EmojiClickData`/`Theme` se importan solo como TIPO, nunca como valor, o el paquete se cargaría igual con el resto del compositor y el `dynamic` no serviría de nada—, categorías con `name` en español y `theme` de `useTheme().resolved`; pestaña "Stickers" pide `fetchStickers` al ACTIVARSE la pestaña, no al abrir el popover, clic en uno llama a `sendStickerMessage` y cierra el popover, deshabilitada con `!withinWindow` con el mismo aviso que el compositor; clic derecho o el botón `⋯` piden confirmación en línea antes de `deleteSticker` —si la RLS lo rechaza, toast "Solo quien lo guardó o un supervisor puede quitarlo"—; botón "Crear sticker" abre `create-sticker-modal`), `create-sticker-modal` (T3b, 8/9/2026: elegir imagen por archivo o pegada del portapapeles —mismo gesto que los adjuntos del compositor—, vista previa 512×512, nombre opcional; "Guardar" llama a `renderStickerWebp` [`sticker-canvas.ts`] y, si devuelve `null` [ni la calidad más baja entró en el límite de Meta], corta con el toast "La imagen es muy pesada, prueba una más simple" ANTES de tocar `createSticker`), `message-bubble` (T3.3, 5/9/2026: con `whatsappStatus === "failed"` y `failureAction(whatsappErrorCode) === "abrir_plantillas"` —hoy solo 131047— pinta un botón "Abrir plantillas" que llama a la prop `onOpenTemplatePicker`, opcional: sin ella la burbuja se queda con el motivo en palabras, como antes; T3.2, mismo día: `messageType === "interactive"` pinta el chip "Respondió a: {título}" —recortado del `content` que ya arma el webhook, no duplica el texto crudo— y `messageType === "order"` pinta `OrderCard` con los ítems y el total de `payload.productItems`; en los dos casos se omite el `content` en prosa para no repetir la misma frase dos veces; S3b, "El cliente que cambió de número", 6/9/2026: `messageType === "unsupported"` —D3, los tipos que Meta no entrega por la API: encuesta, foto de ver una vez, evento…, guardados con `content` null— pinta un marcador fijo en vez de una burbuja vacía, y una segunda línea atenuada con `payload.type` cuando viene; sin lógica de datos nueva, `payload` ya viajaba desde T3.2; T3a, 8/9/2026: gana la prop opcional `agent?: Agent` —`chat-panel.tsx` le pasa `currentAgent`— que reenvía a `message-context-menu` para que pueda ofrecer "Guardar sticker" con dueño; 8/9/2026, "La bandeja habla español y el número nuevo queda a un clic": la rama `system_event` gana el botón "Abrir el chat de {newPhone}" SOLO si `payload.systemType === "user_changed_number"`, hay `payload.newPhone`, es DISTINTO de la prop `contactPhone` —iguales significa que el contacto sí se movió a este chat, D1, y no hay otro a dónde ir— y llegó `onOpenConversationByPhone: (phone) => Promise<boolean>`; deshabilitado mientras la promesa viaja y, con `false`, la nota en línea "No hay conversación con ese número" (el shell no tiene toasts); los dos `useState` van ANTES del `return` temprano, junto a `menuAt`; cinco casos en `message-bubble.test.tsx`), `outbox-bubble` (en cola/fallido con reintento), `media-group` + `media-lightbox` (galerías), `formatted-text` (negritas estilo WhatsApp), `quoted-content` (cita), `message-context-menu` (T3a, 8/9/2026: con `messageType === "sticker"`, `mediaUrl` presente y un `agent` conocido —prop opcional, sin ella la opción no se ofrece— pinta "Guardar sticker", que llama a `saveStickerFromMessage` con un `createClient()` propio, no uno recibido por prop; toast "Sticker guardado" o, si falla, el `err.message` TAL CUAL —"El sticker que no entra", 8/9/2026: el `Error` que tira `saveStickerFromMessage` cuando el sticker no entra en el límite de Meta ya trae el tipo, el peso y el límite en KB; reemplazarlo por el genérico "No se pudo guardar el sticker." de antes dejaría al asesor sin saber por qué—, mismo criterio que `copiarImagen`: el fallo se dice, no se cierra el menú como si hubiera funcionado), `template-picker-modal` (reabrir fuera de 24h; T3.3, 5/9/2026: detecta `{{n}}` en `body_preview` con `templateVariableCount` —`whatsapp/template-variables.ts`—; una plantilla sin variables se envía directo, con variables abre un paso intermedio con un campo por cada una y sugerencias que lo llenan de un clic —el nombre del contacto siempre; el último repuesto cotizado en el chat si `fetchConversationQuotes` devuelve alguno, la misma consulta que ya usa `sale-items-editor.tsx`, no una nueva— y una vista previa con `substituteTemplateVariables`; una plantilla `status <> 'approved'` queda deshabilitada con un `Tooltip` que explica por qué), `quick-replies-modal` (crear/editar/borrar mensajes rápidos, "Usar" pega el `content` en el compositor; T4b, "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026: gana `catalogLinks`; botón "Insertar catálogo" junto al campo Mensaje —menú propio con `role="menu"`/`role="menuitem"`, no HeroUI `Dropdown`, mismo criterio que `emoji-sticker-popover`— con "Todos los catálogos" [pega `{{catalogos}}`] y una entrada por catálogo ACTIVO [pega `{{catalogo:<key>}}` con `catalogMarkerFor`]; `insertCatalogMarker` pega en la posición del cursor con `insertAtCaret`/`flushSync`, mismo patrón que `insertEmoji` de `composer.tsx`; `hasRawUrl(content)` pinta el aviso "Este texto lleva un enlace escrito a mano…" bajo el campo; cada fila de la lista marca "Marcador sin resolver" cuando `resolveCatalogMarkers(reply.content, catalogLinks).missing` no está vacío —D6, un catálogo INACTIVO también cuenta como sin resolver—; test nuevo `quick-replies-modal.test.tsx`: "Insertar catálogo" pega `{{catalogos}}`/`{{catalogo:<key>}}`, aviso de enlace escrito a mano, marca de marcador sin resolver), `window-countdown` (cuenta atrás de 24h; C2, "El reloj dice la verdad", 5/9/2026: el redondeo vive en `window-countdown-format.ts` —`formatWindowRemaining`—, que redondea los MINUTOS TOTALES una sola vez y de ahí deriva horas y minutos, para que el acarreo empuje a la hora siguiente en vez de mostrar "23h 60m"; tests `window-countdown-format.test.ts` [tabla de casos, sin jsdom] y `window-countdown.test.tsx` [el borde 23h 59m 40s renderizado]), `delivery-check` (palomitas; T3.2, 5/9/2026: `whatsappStatus === "played"` —nota de voz reproducida— lleva ícono y etiqueta "Reproducido" propios, distintos del doble check azul de "Leído"), `ai-status-banner`. T7 ("La voz de mostrador con nombre propio", 15/9/2026): `AudioContent` de `message-bubble` ya NO esconde Reintentar cuando `<audio>` reporta el código 4 —en Chrome ese código también sale de un 401/400 con cuerpo JSON (la URL firmada de `api/media` vence a los 60 s), no solo de un códec no soportado—: dice "No se pudo reproducir el audio." y ofrece Reintentar y Descargar; ver `docs/diagnosticos/2026-09-15-notas-de-voz-chrome.md`. T4, "Seba atiende el mostrador" (18/9/2026, D2/D3, requisito 6 del cliente): `ai-status-banner` gana `waitingForHuman: boolean` — con la escalada sin apagar la IA, un chat asignado puede seguir con `aiEnabled: true`, y el texto genérico "La IA sigue respondiendo automáticamente" sonaba a que nadie estaba mirando el caso; con `waitingForHuman` (solo aplica cuando el resto de las guardas ya dejan `respondiendo: true`) el texto pasa a "`${AI_NAME}` responde mientras el asesor no escriba; se apaga con tu primer mensaje". `chat-panel.tsx` le pasa `Boolean(conversation.assignedAgent)`. **Lecciones de Seba (T6, "Seba atiende el mostrador", 18/9/2026):** `message-context-menu` suma "Enseñar a Seba…" (`onTeach`; solo sobre un entrante o una respuesta de la IA, nunca sobre el saliente de un asesor, y solo con `agent`); `message-bubble` y `chat-panel` cablean `onTeach`/`teachingMessage` igual que `onReply`/`replyingTo`, y `chat-panel` monta el modal SOLO con un mensaje elegido; `teach-seba-modal` (nuevo — modal HeroUI por portal: cita el mensaje, nota de hasta 200 caracteres con contador o sinónimo de búsqueda [jerga → término del catálogo, `content` se arma solo], alcance "Todos los chats" por defecto o "Solo este chat"; llama `createLesson` y muestra en el propio modal el `LessonIdentityError`). Hueco conocido: `media-group` no ofrece la opción sobre una foto de una galería agrupada. T4b (plan "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026): `chat-panel`/`composer` ganan la prop opcional `catalogLinks` (default `[]`, sembrada por `crm-shell.tsx`) y la reenvían hasta `quick-replies-modal`; `handleSelectQuickReply` (`composer.tsx`) resuelve `{{catalogo:<key>}}`/`{{catalogos}}` con `resolveCatalogMarkers` (`catalog-links.ts`) ANTES de pegar el contenido del mensaje rápido en el cuadro —la fuente única es la tabla `catalog_links`, nunca la URL copiada dentro del texto del mensaje rápido (D4)— y, si algún marcador no calzó con ningún catálogo activo, avisa con `toast.warning("El catálogo «x» no está configurado")` en vez de bloquear el pegado (D6).

**inbox/:** `inbox-sidebar` (C1, "El reloj dice la verdad", 5/9/2026: `unassignedIds` es un `useMemo` con los ids de `resolvedRows` SOLO cuando la píldora activa es "Sin dueño" —null mientras esa consulta viaja— y viaja a `applyInboxFilters` para que la lista de "Sin dueño" sea la que resolvió la base y no la ventana local entera; seis píldoras Pendientes/Sin dueño/Escaladas/No leídas/Mías/Todos —"Escaladas" sube de cinco a seis el 5/9/2026, T1.5—, abre en "Pendientes" desde la tercera reforma del 30/8/2026 (medida contra producción: "No leídas" resultó subconjunto ESTRICTO de "Pendientes", 282 filas contra 51, dejando 231 chats leídos-y-sin-responder sin ninguna píldora que los alcanzara); secciones por píldora vía `inbox-sections` — "Pendientes" y "Mías" se parten por LECTURA, "No leídas"/"Todos" van en una sola sección, ninguna corta por ventana de 24h (eso sigue siendo terreno de `dashboard.ts`); "Pendientes"/"No leídas"/"Mías"/"Escaladas" se resuelven contra la base vía `pillQueryOptions` — función de módulo con `switch` exhaustivo sobre `InboxFilter` (T1.5, 5/9/2026: suma `case "escalated"` → `escalatedOnly`, mismo predicado que el `case "escalated"` de `matchesFilter` en `inbox-filters.ts`) (reforma del 30/8/2026: reemplazó al ternario de dos ramas que, al sumar `pending` al union, habría mandado la píldora nueva a la rama `assignedTo` en silencio —pidiendo la cola de un asesor en vez de la del equipo— sin que `tsc` avisara; un `switch` sin `default` sí lo caza: falta un `case` y la build no compila) que arma `activeOnly`+`awaitingReplyOnly` para "Pendientes", `unreadOnly` para "No leídas", `assignedTo` para "Mías"; gana el parámetro `sort` (T1.3, 5/9/2026, píldora "Más antiguas"): viaja en `page` como `order` para las cuatro ramas y entra también al `sessionKey` de `useInboxPager` (junto a la etiqueta activa) — con `sort === "oldest"` hasta "Todos" se resuelve en el servidor sin necesitar etiqueta, misma vía que ya usaba la etiqueta activa, porque la ventana cargada localmente solo tiene las conversaciones más recientes, nunca la más vieja de toda la base; `initialPendingRows` siembra la primera para que la bandeja abra con datos y no con "Buscando…"; contador de píldora desde la base (`counts.pending`/`counts.unread`); el buscador salta a "Todos"; paginación por cursor con `useInboxPager` (`src/lib/use-inbox-pager.ts`), el mismo paginador que usa "Todos" en el shell: `serverRows` guarda solo `{filter, rows}` y el cursor, el candado, la sesión y `reachedEnd` viven en el hook — las tres carreras del 29/8/2026 se cierran allá y se prueban allá; un fallo de la primera página (`pager.status === "error"`, A.T5) pinta "No se pudo traer la bandeja." con botón Reintentar en vez de festejar "Todo leído", sin borrar filas ya pintadas; un fallo de la página SIGUIENTE (`pager.lastPageFailed`, A.T4) cambia el botón "Cargar más conversaciones" del pie por un aviso angosto con Reintentar que llama `pager.loadMore` — no `retry`, que es solo para la primera página —, en las cuatro píldoras: "Todos" recibe la bandera como prop `lastPageFailed` desde `allPager.lastPageFailed` en `crm-shell.tsx`, mismo pager que ya presta `hasMore`/`onLoadMore`; `serverRows` era invisible para realtime (A.T6b, revisión de código del 29/8/2026): lo que hace ESTE asesor lo cubre `patchServerRows`, pero lo que hace OTRO —leer el chat, que se lo reasignen— llegaba por el canal de "Todos" en `crm-shell.tsx`, que no toca esta consulta aparte, así que una fila vieja sin leer podía quedarse pintada de más o de menos hasta reentrar a la píldora. El eco del pulso vivo del shell (prop `livePulse`) lo cierra: un efecto que, solo con la píldora de servidor activa y ya con filas cargadas, vuelve a pedir la cabecera de ESA píldora sin cursor y la reconcilia con `reconcileHead` (`inbox-paging.ts`) contra `current.rows` — con `freshIsComplete` SIEMPRE en `false`, porque esa cabecera nunca es el conjunto entero, solo el tamaño de una página, y tratarla como completa borraría las páginas profundas que el asesor bajó; descarta la respuesta si la píldora cambió mientras viajaba; no toca `serverPager`, es una consulta aparte de su candado/cursor/`reachedEnd`), `conversation-list-item` (gana T1.6, 5/9/2026: solo con `awaitingReply(conversation)` —`dashboard.ts`— pinta la píldora `.crm-thread-window` con `hoursUntilWindowCloses(lastCustomerMessageAt)` —`whatsapp-window.ts`—, "18 h" neutro / "3 h" ámbar bajo 4h [corte más generoso que los 2h de `WindowCountdown` del chat: acá se ve de reojo, sin abrir el hilo] / "cerrada" en rojo con la ventana ya vencida, con `useClock` al minuto y los colores semánticos de `crm.css` [`--lm-wait-ink`/`--lm-hot-ink`], nunca el acento; DEUDA: el `title` con `failureReason` —`whatsapp/failure-reason.ts`— que pedía la tarea para `DeliveryCheck` en mensajes `failed` queda pendiente porque la fila [`ConversationSummary`/`BoardConversation`] no trae ningún dato de motivo de fallo, ni código ni `whatsappError` —eso solo vive en `Message`—, y agregarlo es una columna nueva fuera de esta tarea), `filter-scroller`, `tag-filter-menu`, `conversation-context-menu`, `agent-home-panel` (panel de inicio; T4, "Los números del día", 10/9/2026, reescribe su forma: tres bloques —"Tu día" [asignadas/respondidas/ventas/monto de `agentDay: AgentDaySummary | null`, `null` pinta "—" en las cuatro tarjetas, NUNCA un cero, ver el comentario del componente; el monto con `Intl.NumberFormat("es-VE", {minimumFractionDigits:2, maximumFractionDigits:2})` precedido de "$ ", formateador a nivel de módulo], "Tus chats" [Tuyas/Tuyas sin leer — `counts.mineUnread`, con `data-alerta` solo > 0, mismo criterio que "Sin dueño"] y "La IA te pasó hoy · N" [`aiAssignments: AiAssignment[]`, cada fila un `Link` a `/inbox?conversation=<id>` con el nombre y `formatTime12h`; vacía pinta "Todavía nada hoy"]— más una línea compacta `.crm-agent-team` que baja Pendientes/Sin dueño/Esperando asesor/Esperando +24h [solo si > 0] de las cinco tarjetas que tenía antes: "Sin dueño" sigue siendo el KPI de la reforma "ningún lead invisible" —T1.5, 5/9/2026, mismo número que la píldora "Escaladas"— y sigue a la vista, solo que en texto en vez de tarjeta), `bcv-rate-chip`. T1.2 (4/9/2026, scroll infinito, F7): `handleListScroll` (umbral de scroll a 200px del fondo) se reemplaza por un sentinel `.crm-list-sentinel` al final de las filas, observado con `IntersectionObserver` (`root` = `.crm-list` vía `listRef`, `rootMargin: "0px 0px 150% 0px"`) que llama a `pager.loadMore()` con página y media de anticipación — mismo `pager` (local o `serverPager`) que ya usan las cuatro píldoras; el efecto se recrea entero cada vez que cambia `pager` (la primera observación de un `IntersectionObserver` siempre entrega una entrada inicial, así que recrearlo al terminar una carga reobserva el sentinel y encadena páginas solas si la lista quedó corta); el botón "Cargar más" queda de red de seguridad para navegadores sin soporte. `INBOX_PAGE_SIZE` (`src/lib/data.ts`) sube de 30 a 50. T2.2 (5/9/2026, hasta tres chats fijados por asesor — `conversation_pins`): `inbox-sidebar.tsx` fija/desfija con `pinConversation`/`unpinConversation` (mismo patrón optimista que `markUnread`/`markRead` de `crm-shell.tsx`: mueve el estado local, y si la escritura falla —el trigger del cuarto pin, típicamente— el catch relee `fetchPinnedIds` para devolver la verdad, sin abrir un canal de realtime nuevo); pasa `pinnedIds` a `applyInboxFilters` y a cada `ConversationListItem` (pinta un ícono junto al nombre, `.crm-thread-pin`); el menú gana "Fijar"/"Desfijar" en `conversation-context-menu`, deshabilitado con tooltip cuando ya hay tres fijados y esta conversación no es una de ellas. La preferencia de píldora y orden (`filter`/`sort`) se recuerda en `localStorage` bajo `sbk:inbox:{agentId}`: se lee UNA sola vez al montar (nunca en el render — el SSR de este "use client" no tiene `localStorage`) y se escribe en cada cambio, las dos veces con `try/catch` — un `didLoadPrefsRef` hace que el efecto de escritura se salte su primera pasada, para no clobbear el valor guardado con los defaults del primer render antes de que el efecto de lectura alcance a restaurarlo. T6 (8/9/2026, "Agregar contacto"): botón `UserPlus` en `crm-inbox-head`, a la derecha del título, abre `new-contact-modal` (nuevo); su `onCreated(conversationId)` sube tal cual a `onContactCreated` (prop opcional, la pone `crm-shell.tsx`) — `new-contact-modal.tsx` (nombre + teléfono, valida con `normalizePhoneInput` —`whatsapp/phone.ts`— antes de llamar a `createContactConversation` —`mutations.ts`—, toast informativo si el número ya tenía conversación [`existed: true`], toast de error sin cerrar si la mutación lanza) no toca las reglas de la ventana de 24h del compositor: un contacto sin mensajes del cliente sigue ofreciendo plantillas, que es lo correcto.. T1 del plan "Seis frentes del buzón" (8/9/2026, "habló hoy"): recibe `dayScope`/`dayStart`/`onDayScopeChange` como props CONTROLADAS (el estado vive en `crm-shell.tsx`, no acá — ver esa entrada) con default `dayScope = "all"`/`dayStart = null` cuando no vienen, para que ningún consumidor viejo que no conozca esta tarea pierda filas en silencio; pinta el interruptor "Ver todo" (icono `CalendarDays`/`History` de lucide, junto al botón de orden) SOLO si le pasan `onDayScopeChange`, mismo patrón que `onMarkUnread`/`onMarkRead`; `pillQueryOptions` gana un 6º parámetro `since` que entra a `page` (y de ahí a las cuatro ramas que resuelven en servidor) igual que `tagId`/`order`; `fetchUnassignedConversations` también lo recibe; `dayStart` entra al `sessionKey` de `useInboxPager` (junto a la etiqueta y el orden) para que tocar el interruptor —o que ruede la medianoche de Caracas— abra sesión nueva; `applyInboxFilters` recibe `dayStart` en su `InboxCriteria`.

**context-panel/:** `context-panel` (ficha del contacto junto al chat), `close-sale-modal` (cierre de venta + datos de cédula/dirección — T5, plan "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026, D9-D11: los NUEVE campos pasan a ser obligatorios —antes solo nombre, carrito y método de pago—; asterisco CSS [`.lm-required::after`, `theme.css`] en las nueve etiquetas, NUNCA texto real dentro del `<Label>`, para no romper `getByLabelText("Nombre")` de los tests existentes; `handleSubmit` corre `validateSaleDraft` [`sale-draft.ts`], pinta `<p role="alert" class="lm-field-error">` bajo cada campo inválido y enfoca el primero con un `switch` sobre un ref por campo [no un mapa `Record<SaleField, RefObject>`: la varianza de tipos entre `RefObject<HTMLInputElement>` y `RefObject<HTMLSelectElement>` lo complica sin necesidad]; "Comprobante de pago" pasa a `<fieldset aria-labelledby>` para poder enfocarlo como grupo; el botón "Guardar y cerrar venta" queda `isDisabled={isSaving}` SOLO —ya no `cart.length === 0 || !paymentMethod`—; el carrito vacío sigue avisando con un toast en vez de un error de campo, y desde la corrección R2 [revisión `code-review high`, 19/9/2026] ese toast usa `validateSaleCart` [`sale-draft.ts`] en vez de un mensaje escrito a mano, la MISMA función que corre `closeSaleWithContactInfo`. La misma corrección R2 sumó `clearFieldError(field)`: cada `onChange` (y elegir/subir el comprobante) borra SOLO el error de su propio campo, sin revalidar el resto; y como el modal no se desmonta al cerrarse [sigue vivo con `isOpen=false`], `errors` se resetea al reabrir con el patrón "Adjusting state when a prop changes" de React —comparar `isOpen` contra una copia en estado y limpiar `errors` DURANTE el render, nunca con un `setState` síncrono dentro de un `useEffect` [dispara `react-hooks/set-state-in-effect`]), `sale-items-editor` (carrito), `manage-tags-modal`.

**agent-control/:** `agent-control-view` (42K — la vista más grande del proyecto: interruptores, métricas, simulador, tarifas; B5, 5/9/2026: pinta `BusinessHoursPanel` debajo de `SpendCapPanel` con el mismo `canEdit` y `saveBusinessHours` → `updateBusinessHours`), `playbooks-panel` (escenarios; 6/9/2026: al guardar, un `PlaybookIdentityError` de `mutations.ts` muestra su mensaje en el toast en vez del genérico "No se pudo guardar el escenario."; su test `playbooks-panel.test.tsx` abre el formulario de verdad y prueba las dos ramas del `catch`, la de la guarda de identidad y la de nombre duplicado), `knowledge-panel` (biblioteca), `agent-tools-panel`, `spend-cap-panel` (tope de gasto), `business-hours-panel` (B5, "El reloj dice la verdad", 5/9/2026: siete filas lunes–domingo, cada una "Cerrado" o hasta dos franjas con `<input type="time">`; `validateDraft` local con el mensaje de error por fila —"El cierre tiene que ser después de la apertura"— porque `parseBusinessHours` solo sabe caer al default sin decir qué falló; D4, 6/9/2026: `validateDraft` también rechaza la segunda franja si empieza antes de que termine la primera —mensaje "Las franjas se solapan.", contiguas tipo `12:00`–`12:00` sí valen, y desordenadas dan el mismo error sin reordenarse solas— porque `businessMinutesBetween` sumaría dos veces los minutos en común y el tablero marcaría "Con asesor" en rojo antes de tiempo; sigue siendo deuda solo de la puerta de entrada del formulario, no de `parseBusinessHours`; vista previa con `describeSchedule`/`businessStatus` sobre el borrador, solo cuando es válido entero; `canEdit` igual que el tope de gasto, los demás lo ven en solo lectura; guarda con `updateBusinessHours` de `mutations.ts`; estilos `ac-hours-*` en `agent-control.css`; su test fija el reloj con `vi.setSystemTime` SIN `useFakeTimers()` completo, que colgaba los clics de `userEvent` con HeroUI), `channel-health-panel` (T3.4, 5/9/2026 — tarjeta "Salud del número": calidad/límite/restricciones de `fetchWhatsappChannelHealth`, "sin datos" hasta el primer webhook de Meta, puramente informativa), `agent-roster-panel` (reparto entre asesores; 10/9/2026, "Los números del día": la tarjeta "Sin asignar" que listaba uno por uno TODOS los chats abiertos sin asesor se retiró entera —en producción, con cientos de leads abiertos, ocupaba la página entera y tapaba la navegación—; solo sobrevive el número en la nota del encabezado ["N en total · M en el reparto · K sin asignar"], convertido en `<Link href="/inbox">`: `/inbox` no tiene todavía un parámetro de URL para abrir en una píldora puntual, así que enlaza a la bandeja a secas con `title="Se ven en la píldora Sin dueño de la bandeja"`), `agent-metrics-row`, `token-usage-chart`. **Lecciones de Seba (T6, 18/9/2026):** `agent-control-view` suma la pestaña "lecciones" (fetch `fetchLessons` + canal realtime de `ai_lessons`; `app/agent-control/page.tsx` la trae en el `Promise.all` inicial como `initialLessons`); `lessons-panel` (nuevo — lista `ai_lessons` con alcance, autor, fecha y extracto o `«jerga» → «término»`; activar/desactivar y borrar SOLO para el autor o supervisor/admin, el mismo predicado que la RLS usa para `update` y `delete`: un interruptor que la base va a rechazar no se muestra habilitado; 19/9/2026, T2 de "El precio se lee en bolívares": prop `onChanged` que se llama tras un `setLessonActive`/`deleteLesson` EXITOSO —nunca si la mutación falla—, con el `refresh` directo de `agent-control-view` [no `scheduleRefresh`, que pospone con la pestaña oculta]; el 19/9 se vio la base en `is_active = false` con la pantalla diciendo "Activa" porque el panel solo se ponía al día por el canal Realtime de `ai_lessons`, que calla para siempre si está caído). El texto del interruptor global ("Responde en toda conversación hasta que un asesor le escribe al cliente.") se corrigió el 19/9/2026 (T2): el viejo ("sin asesor asignado") quedó falso desde D2 de "Seba atiende el mostrador" (18/9/2026), donde la escalada dejó de apagar a Seba. **Enlaces de catálogo (T4a, plan "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026, D3/D4):** `catalog-links-panel` (nuevo — se pinta ARRIBA de los escenarios, DENTRO de "Respuestas predeterminadas": filas etiqueta/clave/URL/activo con "Copiar marcador" [`navigator.clipboard.writeText`, mismo patrón que `message-context-menu.tsx`]; formulario inline —no `Modal`, a diferencia de `playbooks-panel`: son tres campos cortos— con la clave propuesta por `slugifyKey` mientras no se toque a mano y validación por campo con `validateCatalogLinkDraft` ANTES de guardar; borrar sigue el patrón de "armar y confirmar" de `lessons-panel` [primer clic arma, el botón cambia a "¿Borrar? Lo usan N escenarios y M mensajes rápidos" contando sobre las MISMAS listas `playbooks`/`quickReplies` que ya tenía `playbooks-panel` para "Importar desde mensajes rápidos" — no se volvió a pedir nada a la base]); `playbooks-panel` monta `CatalogLinksPanel` primero y gana cinco props (`catalogLinks` + los cuatro `onCreate/onUpdate/onDelete/onToggle` que reenvía tal cual, sin mutaciones propias) — su formulario de escenario suma el selector "Insertar catálogo" (pega el marcador en el CURSOR de la Respuesta con `insertAtCaret`, el mismo módulo puro de `composer.tsx`, con el mismo `flushSync` para que `setSelectionRange` no pierda la carrera del commit de React) y el aviso `hasRawUrl` ("Este texto lleva un enlace escrito a mano…"); la lista de escenarios suma el badge "Enlace sin resolver" con `resolveCatalogMarkers` sobre `responseText`/`attachmentUrl` — visual únicamente, la guarda de verdad (que descarta el escenario de fase 0) es de `playbooks.ts`/T3. `agent-control-view` suma `catalogLinks` al estado, a `refresh` (`fetchCatalogLinks`) y al canal `agent-control-live` (tabla `catalog_links`), y cuatro handlers finos (`createCatalogLinkEntry`/`updateCatalogLinkEntry`/`deleteCatalogLinkEntry`/`toggleCatalogLinkEntry`) que llaman a la mutación y refrescan completo — como `toggleTool`/`toggleAgentActive`, no como `saveSpendCap`/`saveBusinessHours`, porque acá cambia el LARGO de la lista, no un único objeto de settings. `app/agent-control/page.tsx` sube a doce el `Promise.all` inicial con `fetchCatalogLinks` (`initialCatalogLinks`). Estilo nuevo `.ac-pb-warning` en `agent-control.css` (mismo tono ámbar que `.ac-badge[data-tone="wait"]`, como línea de texto completa en vez de etiqueta corta). Tests: `catalog-links-panel.test.tsx` (crear con la clave propuesta, fila inválida no guarda, `canEdit=false` esconde crear/editar/borrar pero deja copiar, copiar marcador, borrar avisa la cuenta de usos y sin uso pide confirmar sin contar nada); `playbooks-panel.test.tsx` suma "Insertar catálogo"/"Todos los catálogos"/sin catálogos no hay selector/aviso de enlace a mano/badge de sin resolver — y sus dos tests viejos pasan a un helper `renderPanel()` con las cinco props nuevas en blanco; `agent-control-view.test.tsx` espeja `fetchCatalogLinks`/`createCatalogLink`/`updateCatalogLink`/`deleteCatalogLink`/`setCatalogLinkActive` en los mocks y `initialCatalogLinks={[]}` en `montar()`. Corrección de la revisión `code-review high` del 19/9/2026 (puntos 4 y 5): (4) al EDITAR un catálogo, el campo "Clave del marcador" pasa a `disabled` con un hint fijo ("La clave no se puede cambiar…") — cambiarla en silencio dejaba sin resolver todos los escenarios/mensajes rápidos que ya la usaban; para "renombrar" hay que crear un catálogo nuevo. Desactivar (el switch, click a ACTIVO) gana el mismo patrón "armar y confirmar" que borrar: primer clic arma `confirmingDeactivateId` y el switch se reemplaza por un botón de texto ("¿Desactivar? Lo usan N escenarios y M mensajes rápidos", o "¿Confirmar apagar?" sin usos), el segundo clic desactiva de verdad; activar sigue siendo un solo clic, nunca pide nada porque no rompe nada. `usageOf` (ahora recibe el `CatalogLink` completo, no solo la clave) suma `referencesCatalogList` (`{{catalogos}}`) a la cuenta cuando el catálogo es el ÚLTIMO activo — apagarlo o borrarlo deja `{{catalogos}}` tan roto como `{{catalogo:<esa-clave>}}`, aunque el texto no la mencione por su nombre. (5) El selector "Insertar catálogo" de `playbooks-panel.tsx` filtraba por `catalogLinks` crudo (activos e inactivos, en el orden que llegó del servidor); ahora usa `activeCatalogLinks` (filtrado + ordenado por `sort_order`), mismo criterio que ya tenía `quick-replies-modal.tsx` — pegar la clave de un catálogo apagado dejaba el marcador sin resolver apenas se guardaba el escenario (D6). El selector también deja de mostrarse si NINGÚN catálogo está activo (antes bastaba con que existiera alguno, activo o no).

**dashboard/:** `dashboard-view` (A4, "El reloj dice la verdad", 5/9/2026: prop opcional `businessHours` con default `DEFAULT_BUSINESS_HOURS`, enhebrada a `buildJourney` y a `JourneyBoard`; la página la conecta con `fetchAgentSettings` desde B3. T2, corrida "Los números del día", 10/9/2026: `dayStart = useInboxDay("today")` —la ÚNICA fuente del corte, misma medianoche de Caracas que usa la bandeja— viaja a `buildJourney` y a `JourneyBoard`; `agentLoad` (los avatares de "Flujo de hoy") suma el mismo corte con `matchesDay` de `inbox-filters.ts`, así que el título del avatar deja de contar casos abiertos de cualquier fecha. Prop nueva `initialLeadTotal: number` → estado `leadTotal`; el `fetcher` de `useLiveConversations` pide `fetchLeadTotal` (`dashboard-data.ts`) en el MISMO `Promise.all` que `fetchDashboardConversations`, con su propio `.catch()` — un tropiezo en ese conteo no tira el refresco de conversaciones/etiquetas, conserva el número anterior. Encabezado: subtítulo pasa a "Lo que pasó hoy, de la primera línea que escribe un cliente al asesor que cierra la venta."; el pulso suma una 4ª pieza, "Total de leads" con un `<span className="dash-pulse-caption">acumulado</span>` bajo la etiqueta —la única del pulso que NO corta por hoy—, separada de las otras tres por `.dash-pulse-rule-strong` [dashboard.css: más ancha/alta que `.dash-pulse-rule`, para que el corte "hoy vs. acumulado" se note sin una segunda oración]), `journey-board` (recorrido del cliente; A4: la tarjeta pinta "espera 23 min"/"espera 3 h"/"espera 1 día y 2 h" con `waitingMinutes` —`formatWait`, local al componente— o, si es null, el `stageDetail` en gris con la misma `.dash-card-meta`; el punto rojo usa `isStalled` y lleva `title` "Umbral de esta etapa: N min" o "60 min en horario de atención" en "Con asesor"; ya no importa `minutesInStage`; test `journey-board.test.tsx` con `ResizeObserver` stubeado como en `sliding-pills.test.tsx`. T2, 10/9/2026: ganó `dayStart?: string | null` [default `null`, al final de la firma] pasado a `waitingMinutes`/`isStalled` en cada tarjeta — sin él, el punto rojo de una tarjeta de "Primer contacto" podía no coincidir con el contador de atascados de su propia columna, que ya arma `stages` con el mismo `dayStart`. Corrida "El Recorrido cuenta los números nuevos del día" (10/9/2026, misma tarde): el prop se fue entero — `stageOf` ya nunca devuelve `"first_contact"`, así que `waitingMinutes`/`isStalled` dejaron de tomar `dayStart` (ver `dashboard.ts` en este glosario) y `JourneyBoard` no tenía a quién enhebrárselo; queda un comentario en el archivo contando por qué se fue, no solo que se fue), `activity-chart`, `ticket-queue`, `ticket-stats`. Test nuevo `dashboard-view.test.tsx` (T2): mockea `@/lib/supabase/client`, `@/lib/data` (solo lo que pide `AppRail`/`AssignmentNotifier`: `fetchCurrentAgent`), `@/lib/dashboard-data`, `@/lib/use-live-conversations` (devuelve `initialConversations` tal cual) y `@/lib/use-clock` (`Date.now()`, movido con `vi.setSystemTime`) — `useInboxDay` corre REAL, sobre ese mismo reloj mockeado, para no duplicar el cálculo de medianoche en el test. Corrida "El Recorrido cuenta los números nuevos del día" (10/9/2026): el badge "N atascados" del encabezado pasa de sumar `stage.stalled` de las cinco columnas a llamar `countStalled(conversations, now, businessHours, dayStart)` (`dashboard.ts`) — desde que "Primer contacto" es una columna de cohorte que puede compartir tarjeta con otra, la suma contaba dos veces la misma conversación atascada; `countStalled` cuenta sobre conversaciones ÚNICAS. El pulso "Nuevos" gana `caption="entraron hoy"`: las tres piezas de "hoy" del pulso (Nuevos/Con la IA/Con asesor) dejaron de ser disjuntas entre sí, así que el caption avisa que "Nuevos" responde su propia pregunta, no que sea un sumando de las otras dos.

**sales/:** `sales-view` (T5, 8/9/2026: recibe `bcvRate` de la página —`getBcvRate`, null si no se pudo leer ese día, la factura se genera igual con `bcv_rate: null`—; guarda la factura de la venta abierta en el detalle por id en `invoicesBySale`, `undefined` mientras carga y `null` sin ninguna, pedida solo al abrir el detalle vía `fetchInvoicesForSale`; T6 "Los números del día", 10/9/2026: estado `dayKey` —arranca en `todayKey()`— gobierna tres tarjetas `.cli-stat` [`summarizeSalesDay`: ventas/vendido/devueltas del día elegido], la lista [`salesOnDay`, ya no todo el histórico] y un panel "Histórico · últimos 30 días con ventas" [`salesDayHistory`, filas clicables que cambian `dayKey`]; el corte es en memoria sobre `liveSales`, sin pedir nada nuevo a Supabase; `.sales-toolbar` en `sales.css` es un grid con áreas nombradas —no flex— para poder mandar el selector de día DEBAJO de las tarjetas en angosto sin reordenar el DOM), `sale-detail-modal` (T5: sección "Factura" — sin factura, botón "Generar factura" para cualquier asesor; con factura, número formateado + estado + enlace "Ver e imprimir"; "Emitir"/"Anular" solo si `currentAgent.role` es supervisor/admin, la RLS de `invoices` lo respalda), `invoice-sheet` (T5, nuevo: hoja imprimible client-side —botón `window.print()`—, encabezado del emisor con "Por definir" donde `INVOICE_ISSUER` trae `null`, ítems, subtotal/IVA/total en USD y en Bs a `bcv_rate`, `@media print` en `sales.css` esconde `.invoice-no-print`). T6, plan "Nada sin leer, un solo catálogo y la factura Saint" (18/9/2026, D9): `Sale.saintInvoiceNumber: string | null` (`types.ts`) viaja en `SALE_SELECT` como `order:orders(total_amount, currency, saint_invoice_number)` (`data.ts`, `mapSale`) — nullable porque las ventas cerradas antes del 18/9 no lo tienen, y distinto de `invoices.number` (el correlativo INTERNO). `sale-detail-modal` suma la línea "Factura Saint N.º 00123" (icono `Receipt`) después del método de pago, o "Sin número de factura Saint" en las ventas viejas; `sales-view` agrega el chip `Saint 00123` (`ac-badge` tono `muted`, misma altura fija que "Verificada"/"Cerrada") en `.sales-row-badges` SOLO cuando existe — un `ac-badge` no gana una línea de más como el bug de Inventario del 10/9, así que no desalinea la fila. **clientes/:** `clientes-view`, `cliente-ficha`, `cliente-datos-panel`, `cliente-notas`, `cliente-etiquetas`. **inventario/:** `inventario-view`, `producto-fila` (T7 "Los números del día", 10/9/2026: `.inv-row` pasa de `flex` a `grid` —`minmax(0, 1fr) 100px 150px 110px auto`, `align-items: start`— porque con `flex` + `align-items: center` la columna Precio en USD, que lleva debajo del input una línea extra `.inv-bs` ["Bs. 1234.00"] que en VES no existe, quedaba más alta y corría a Stock/Peso hacia abajo, y `flex-wrap` mandaba las cajas a una segunda línea en algunas filas sí y otras no; `producto-fila.tsx` ahora renderiza `.inv-bs` SIEMPRE en los tres campos —vacío y `aria-hidden` salvo en Precio/USD— para que los tres midan lo mismo de alto, y `inventario.css` le da `min-height: 14px` para que cuente aunque no tenga texto; verificado con `getBoundingClientRect` sobre un HTML servido con la hoja de estilos real: los tres inputs de una fila comparten `top` exacto; el `left` de las columnas Stock/Precio/Peso puede correrse unos px entre filas cuando el badge de `.inv-status` —columna `auto`, contenido variable— cambia de ancho, porque cada `<li>` es su propio grid independiente, no una tabla compartida), `inventario-css.test.ts` (nuevo, T7: lee `inventario.css` con `readFileSync`, sin jsdom —mismo patrón que "la hoja de estilos del aviso" de `assignment-notifier.test.tsx`, 9/9/2026— porque jsdom no calcula layout y ningún test que renderiza el componente puede atrapar un desalineamiento de cajas). T1, "El precio se lee en bolívares" (19/9/2026): el campo Precio deja el `<input>` — se van `priceDraft`/`commitPrice` y el caso `"precio"` de `savedField` — y pasa a texto de solo lectura (`<span className="lm-num" aria-label={...}>`, mismo `aria-label` de antes así que `getByLabelText` lo sigue encontrando); la cifra la arma `priceDisplay` de `inventory.ts` (bolívares arriba, la otra moneda en el pie `.inv-bs`, que sigue reservándose SIEMPRE como en T7). La grilla de `.inv-row`/`inventario.css` NO se tocó en esta corrida. **auth/:** `login-form`.

## `supabase/`

| Qué | Dónde |
|---|---|
| Migraciones (75, timestampeadas; recontadas el 18/9/2026 tras sumar `20260918020000` (M2) de "Nada sin leer, un solo catálogo y la factura Saint"; antes de esa, 74 tras sumar `20260918010000` (M1) de la misma corrida; antes, 73 tras sumar `20260917010000` (T0) y `20260917020000` (T1) de "Seba atiende el mostrador"; el 16/9 eran 71 tras sumar `20260916010000` de "La IA no vuelve a pedir lo que ya pidió" — revisión; el 15/9 eran 70 tras sumar `20260915010000` de "La voz de mostrador con nombre propio"; el 14/9 eran 69 con `ls supabase/migrations/*.sql \| wc -l` tras sumar `20260914010000` de "La voz cercana y la espera visible" — el 65 que decía esta línea ya estaba viejo, faltaban `20260909050000` y `20260910010000`; antes, recontadas el 8/9/2026 con `ls supabase/migrations/*.sql \| wc -l` tras sumar `20260909010000` (T2), `20260909020000` (T3a), `20260909030000` (T4) y `20260909040000` (T5) de "Seis frentes del buzón") | `migrations/` — regla: commit propio con `[migración]` en el título. El número es el CONTEO, no una secuencia: los archivos van por fecha (`20260830040000_…`), y leerlo como "la próxima es la 51" ya hizo que un plan pidiera crear `051_…`, que se habría ordenado antes que todo 2026 |
| Seed de demo (3 usuarios, 5 conversaciones) | `seed.sql` — **no va a producción** |
| Catálogo y escenarios para producción | `seeds/moto_catalog_seed.sql`, `seeds/ai_playbooks.sql` |
| Config del stack local | `config.toml` |
| Test de permisos de funciones `security definer` | `tests/permisos_funciones.sql` — falla si alguna queda ejecutable por `anon` fuera de la lista blanca (`is_agent`, `is_supervisor_or_admin`). Agrupa por a quién debe quedarle el acceso: `service_role` (cerradas también a `authenticated`), `authenticated`, y desde el anexo B1 (5/9/2026) un grupo "funciones de trigger" (`handle_new_message`, `handle_message_status_change`) que solo afirma que `anon`/`authenticated` no pueden ejecutarlas —sin exigir nada de `service_role`: Postgres no comprueba `EXECUTE` al disparar un trigger, así que ningún rol necesita el grant— |
| La invariante "ningún lead invisible", verificada contra la base | `tests/invariante_leads.sql` — proxy de Etapa 1 de la invariante (ver CLAUDE.md): cuántas conversaciones siguen esperando y quedaron sin dueño, contra `unassigned_waiting_count()`. Gemelo en TypeScript: `src/lib/invariante-leads-contrato.test.ts` (mismos casos, mismos nombres). Caso 7 (anexo A1, 5/9/2026): escalada sin asesores con la despedida de la IA guardada `is_auto_reply = true` — inserta el entrante del cliente y el saliente de la IA por separado para que sea el trigger `handle_new_message`, no un valor escrito a mano, quien deje `awaiting_reply` en `true`. Caso 8 (anexo A2, 5/9/2026): cerrada, reabierta por el cliente y CON asesor asignado — inserta un agente mínimo en `auth.users` (mismo patrón que `pins.sql`, dispara `handle_new_agent()`) para satisfacer la FK de `conversations.assigned_agent_id` y afirma que el traspaso `reabierta_por_cliente` con destino `human` NO cuenta como sin dueño. Caso 9 (anexo B2, 5/9/2026): escenario que escaló sin asesores cuyo texto nació con `is_auto_reply = false` (como sale de verdad un escenario `afterSend: "escalate"`, antes de saber si haría falta un asesor) — un bloque `do $$` propio, después de los inserts y antes de los traspasos, afirma primero que `awaiting_reply` es `false` (el bug que corrige B2), corre el mismo `update ... set is_auto_reply = true` que hace `runPlaybook`, y afirma que `awaiting_reply` vuelve a `true` — es el trigger de B1 (20260905070000) quien recalcula, no un valor escrito a mano |
| Bitácora de traspasos | `migrations/20260830040000_conversation_handoffs.sql` — tabla `conversation_handoffs` + `record_handoff()`. La razón es `text` con CHECK (no enum) porque las Etapas 2 y 3 la amplían (20260905010000 sumó `escalada`/`escalada_sin_asesor`/`rechazado_por_meta`; 20260905030000 sumó `cerrada_por_asesor`/`reabierta_por_asesor`/`reabierta_por_cliente`, T2.1); `record_handoff` sigue concedida SOLO a `service_role` — T2.1 (5/9/2026) trajo los botones de cerrar/reabrir, pero corren como `authenticated` a través de una ruta que recién abre un cliente admin para esa RPC puntual (`api/conversations/[id]/close`, `.../reopen`), no con un `grant` nuevo |
| Cerrar/reabrir una conversación (T2.1) | `migrations/20260905030000_handoff_reasons_cierre.sql` — solo amplía el CHECK de `conversation_handoffs.reason` (arriba); no toca `to_kind` (ya admitía `'closed'` desde 20260830040000) ni permisos. Código: `api/conversations/[id]/close/route.ts` y `.../reopen/route.ts` — sesión con `@/lib/supabase/server`, cliente admin (`@/lib/supabase/admin`) solo para `recordHandoff`, mismo patrón que `api/media/[...path]/route.ts`. El webhook (`webhooks/whatsapp/route.ts`) reabre sola una conversación `closed` cuando el cliente vuelve a escribir, ANTES de guardar su mensaje: evento "El cliente volvió a escribir" + `recordHandoff({reason: "reabierta_por_cliente", ...})`, cuyo destino decide en tres pasos desde el anexo A2 (5/9/2026): `ai` si la IA seguía encendida; si no, `human` + `assigned_agent_id` si el chat ya tenía asesor; solo sin ninguna de las dos, `unassigned` — antes de A2 se miraba solo `ai_enabled` y un chat con dueño y la IA apagada quedaba `unassigned` en la bitácora sin serlo de verdad. `openTurn` (`agent.ts`) hace el mismo giro: mira `assigned_agent_id` ANTES que `ai_enabled`, para que ese mismo chat registre `asignada`/`human` en vez de `pausada`/`unassigned` la próxima vez que le toque un turno. `mutations.ts` expone `closeConversation`/`reopenConversation` (fetch a esas rutas, no tocan Supabase directo); los ofrecen `conversation-context-menu.tsx` ("Cerrar conversación"/"Reabrir conversación", según `isConversationClosed`) y la cabecera de `chat-panel.tsx` (botón "Cerrar"/"Reabrir"). `crm-shell.tsx` no necesita canal nuevo: el UPDATE de `status` ya viaja por el outcome "applied" de `use-live-conversations.ts` |
| Última respuesta real, separada del último mensaje visible | `migrations/20260905010000_conversations_last_reply.sql` — T0.1 del plan "La bandeja que no pierde" (5/9/2026): suma `messages.is_auto_reply` y `conversations.last_reply_at`/`last_reply_sender`, y `awaiting_reply` (columna generada) pasa a compararse contra `last_reply_at` en vez de `last_message_at` — antes una nota interna, un evento de sistema, la bienvenida automática o un envío de la IA rechazado por Meta apagaban "esperando respuesta" sin que el cliente recibiera nada. `handle_new_message()` separa "visible" (mueve `last_message_*`) de "respuesta real" (mueve `last_reply_*`); `handle_message_status_change()` recalcula `last_reply_at`/`last_reply_sender` si un envío que la había apagado resulta rechazado por Meta después de insertarse. Recrea `conversations_free_unanswered_idx`/`conversations_pending_idx` con el mismo predicado, y suma `conversations_escalated_idx` (preparado para T1.5). La migración `20260905070000_auto_reply_recalcula` (anexo B1, 5/9/2026) amplía el trigger `on_message_status_changed` para que dispare también por `is_auto_reply` (no solo por `whatsapp_status`): marcar `is_auto_reply = true` sobre un mensaje YA insertado ahora hace recalcular `last_reply_at`/`last_reply_sender`/`awaiting_reply` igual que ya hacía un rechazo de Meta, y trae un backfill que corrige escalaciones sin asesor anteriores al anexo A1. Test: `tests/awaiting_reply.sql` (los ocho casos de T0.1 más los pasos 10 y 11 de B1, transacción con rollback) |
| Búsqueda de contactos sin acentos (F13) | `migrations/20260905020000_contacts_search_unaccent.sql` — T1.7 del plan "La bandeja que no pierde" (5/9/2026): columna generada `contacts.search_text` (`display_name`+`profile_name`+`phone_number`, sin acentos y en minúsculas) con índice trigram, mismo patrón que `products.search_text` (20260822100000). A propósito NO crea extensión ni función nuevas: `unaccent`, `pg_trgm` y el wrapper `public.immutable_unaccent(text)` ya existían desde esa migración (los tres viven en el esquema `public`, no `extensions`) y los reutiliza tal cual — es la misma función que ya usa `messages.search_text` (20260822110000). `searchConversationSummaries` (`src/lib/data.ts`) filtra `search_text.ilike.%<normalizado>%` con `normalizeForSearch` (`message-search.ts`) en vez del `ilike` directo contra las tres columnas sueltas que traía antes (ese filtro seguía sin acentos solo para lo ya cargado en memoria de la bandeja). Test: `tests/contacts_search.sql` (inserta "José", encuentra "jose"; transacción con rollback) |
| Entrantes completos: botones, listas, pedidos, anuncios, "reproducido" (T3.2) | `migrations/20260905050000_messages_interactive_payload.sql` — T3.2 del plan "La bandeja que no pierde" (Etapa 3, 5/9/2026): `messages.message_type` admite `'interactive'` (respuesta a botón/lista de un mensaje interactivo, y también el `type: "button"` de una plantilla), `'order'` (pedido del catálogo) y `'unsupported'` (lo que el CRM no sabe representar: `content` queda null y el tipo real de Meta va en `payload.type`, ya no una frase fija). `whatsapp_status` admite `'played'` (nota de voz reproducida) en `messages` Y en `conversations.last_message_status` (20260822060000, que lo recibe propagado); `whatsapp_status_rank()` (`create or replace`, no es `security definer`, no hace falta revoke) rankea `played` por encima de `read` para que `keep_whatsapp_status_moving_forward()` no dependa un "leído" tardío. Columnas nuevas `messages.payload jsonb` y `conversations.referral jsonb`. Test: `tests/whatsapp_status_forward.sql` (sent→delivered→read→played avanza, un `read` tardío no pisa `played`, `failed` sí pisa cualquiera; transacción con rollback) |
| Salud del número y estado de plantillas (T3.4) | `migrations/20260905060000_whatsapp_channel_health.sql` — T3.4 del plan "La bandeja que no pierde" (Etapa 3, 5/9/2026): suma a `whatsapp_channels` las columnas `quality_rating text`, `messaging_limit text`, `account_restrictions jsonb` y `health_updated_at timestamptz` (todo null hasta el primer webhook de calidad/cuenta). Amplía el CHECK `templates_status_check` para admitir `'paused'`/`'disabled'` además de `'approved'`/`'pending'`/`'rejected'`: `message_template_status_update` pasa una plantilla por esos dos estados cuando Meta la pausa/deshabilita por quejas repetidas, y sin ampliar el CHECK ese UPDATE se habría rechazado. Sin función `security definer` nueva: son solo columnas y un CHECK, no hace falta revoke/grant. Código: `webhooks/whatsapp/route.ts` atiende `message_template_status_update` (`templates.status` por nombre+idioma, `log.error` en `paused`/`disabled`/`rejected`), `phone_number_quality_update` y `account_update` (guardan en el canal resuelto por `resolveHealthChannel`, `log.error` cuando la calidad baja o llega una restricción). `fetchWhatsappChannelHealth` (`src/lib/data.ts`) + `ChannelHealthPanel` (`src/components/agent-control/channel-health-panel.tsx`) exponen la tarjeta "Salud del número" en Control de IA |
| El trigger de estado también recalcula por `is_auto_reply` (B1) | `migrations/20260905070000_auto_reply_recalcula.sql` — anexo "Bandeja que no pierde" B1 (5/9/2026): `handle_message_status_change()` (`create or replace`, sin revokes/grants nuevos: conserva el ACL que ya tenía desde 20260830010000) recalcula `last_reply_at`/`last_reply_sender` no solo cuando `whatsapp_status` pasa a `'failed'`, también cuando `is_auto_reply` pasa de `false` a `true` sobre un mensaje ya insertado — el caso de un escenario que escala sin asesores (B2) y de escalaciones históricas anteriores al anexo A1. El trigger `on_message_status_changed` se recrea disparando por `whatsapp_status` O `is_auto_reply`. Backfill: marca `is_auto_reply = true` en la despedida de escalaciones sin asesor, `journey_stage='assigned'`, sin asesor, abiertas, que hoy parecen atendidas (`awaiting_reply=false`, `last_reply_sender='ai'`) — tras aplicarla reaparecen en "Pendientes"/"Sin dueño", ver `docs/PRODUCCION.md` §2. Test: `tests/awaiting_reply.sql` (pasos 10 y 11) y `tests/permisos_funciones.sql` (grupo "funciones de trigger") |
| Pines de conversación (T2.2) | `migrations/20260905040000_conversation_pins.sql` — T2.2 del plan "La bandeja que no pierde" (5/9/2026): tabla `conversation_pins` (`agent_id`, `conversation_id`, `created_at`, PK compuesta) para hasta tres chats fijados por asesor. RLS `for all` con `is_agent() and agent_id = auth.uid()` (mismo patrón que el resto del esquema): cada agente lee y escribe SOLO sus propios pines — a diferencia de `conversation_handoffs`, esto es personal, no bitácora compartida. El tope de tres lo impone el trigger `conversation_pins_limit_before_insert` → `enforce_conversation_pins_limit()`, `security invoker` A PROPÓSITO (no definer): quien inserta ya puede contar sus propios pines por la misma RLS que protege la tabla, así que no hace falta saltársela; si alguien intentara fijar a nombre de otro agente, el trigger (corriendo con los privilegios de quien llama) contaría cero filas ajenas y dejaría pasar la cuenta, pero el `with check` de la política igual rechaza la fila — el trigger nunca es la única barrera, lo verifica `pins.sql`. Aun sin ser definer, lleva los DOS revokes de higiene (`from public` y `from anon, authenticated`): el EXECUTE de fábrica a PUBLIC que Postgres concede a toda función nueva no depende de si es definer o invoker, y 20260830020000 solo cambió el default de anon/authenticated, no el de PUBLIC. Sin grant: Postgres ejecuta funciones de trigger sin comprobar EXECUTE del rol que dispara la operación. Test: `tests/pins.sql` (el cuarto pin falla con el mensaje del trigger; otro agente ve 0 filas propias y no puede insertar a nombre de otro — RLS con `set local role authenticated` + `set local "request.jwt.claim.sub"`, verificado contra la base que esa forma sí alimenta `auth.uid()`) |
| Horario de la tienda para la IA y el tablero (B1, "El reloj dice la verdad") | `migrations/20260906010000_agent_settings_business_hours.sql` — B1 del plan "El reloj dice la verdad" (5/9/2026): `agent_settings.business_hours jsonb not null` con llaves `mon..sun`, cada una una lista de franjas `[inicio, fin]` en `"HH:MM"` (lista vacía = cerrado; más de una franja = horario partido); horas LOCALES de `America/Caracas` (la zona vive en `src/lib/time-zone.ts`, no acá). Default L–V 08:00–18:00, fin de semana cerrado; columna con default y SIN backfill, aplicable en caliente. Check `agent_settings_business_hours_object` exige un objeto. RLS sin cambios: `agent_settings_update` (20260820050000) sigue exigiendo supervisor/admin, lectura abierta a cualquier agente. Sin funciones `security definer`. La IA lo lee en cada turno (B3) y el tablero mide con él el atasco de "Con asesor" (A1). Test: `tests/business_hours.sql` (default con los siete días; un `update` a `'[]'::jsonb` falla por el check), cableado en el job `migraciones` de CI |
| `conversations_pending_idx` también ordena por `id` (D1, "Lo pequeño que quedó atrás") | `migrations/20260906020000_conversations_pending_idx_id.sql` — D1 del plan "Lo pequeño que quedó atrás" (`docs/planes/2026-09-06-lo-pequeno-que-quedo-atras.md`, 6/9/2026): dropea y recrea `conversations_pending_idx` como `(last_message_at desc nulls last, id desc) where awaiting_reply and status <> 'closed'` — mismo predicado de siempre, ahora calza el orden completo que pide `fetchConversations` (`src/lib/data.ts:931-932`: `.order("last_message_at")` + `.order("id")`), el mismo desempate que 20260829010000 ya le había dado a los otros tres índices de la bandeja el 29/8/2026 pero que a este, creado un día antes (20260828020000) sin cursor todavía, nadie le había sumado. Verificado contra Supabase local: `explain` con `enable_seqscan = off` da `Index Only Scan`/`Index Only Scan Backward` sobre `conversations_pending_idx` para el orden de "Pendientes" y el reverso de "Más antiguas" (`asc nulls first, id asc`), sin `Sort` ni `Incremental Sort`. Sin backfill, sin código de app: es solo el índice |
| La ventana de 24 h dice la verdad (T1) | `migrations/20260907010000_ventana_24h_dice_la_verdad.sql` — T1 de la corrida "La ventana de 24 h dice la verdad" (7/9/2026): el caso que la destapó, `aa75ef33-38e8-4ff4-8422-7e7f49615795`, mostraba la caja de texto habilitada ("quedan 11 h") mientras Meta rechazaba todo con 131047 (ventana cerrada). Dos candados sobre `last_customer_message_at` (lcma), los dos en `handle_new_message()` (`create or replace`, camino INSERT — la IA inserta sus fallos ya con `whatsapp_status='failed'`, `src/lib/ai/send.ts`) y en `handle_message_status_change()` (`create or replace`, camino UPDATE — el asesor en `api/messages/send/route.ts` y el callback de Meta en `webhooks/whatsapp/route.ts`, ambos un solo UPDATE con `whatsapp_status`+`whatsapp_error_code`): candado A, un entrante `message_type='unsupported'` deja de mover lcma/`unread_count` (sigue siendo visible: `last_message_at`/preview no cambian) — Meta tampoco lo cuenta para su propia ventana; candado B, un saliente rechazado con 131047 empuja lcma hacia atrás, `least(lcma, created_at - 24h)`, solo si `lcma is not null` (`least(null, x)` devuelve `x`, no `null` — sin esta guarda un 131047 sobre un lead sin mensajes le inventaría fecha y encendería `awaiting_reply`) y solo si `created_at > lcma` (protege contra un callback tardío que no debe retroceder un reloj que un mensaje real posterior ya adelantó). Sin `security definer` nueva, sin revokes/grants. Backfill en tres pasos con `raise notice`: (a) reclasifica a `unsupported` el texto histórico anterior a T3.2 (`content like 'El cliente envió un mensaje que el CRM todavía no sabe mostrar%'`), (b) recalcula lcma en conversaciones donde apuntaba al `created_at` de un entrante ahora `unsupported` (null si no queda ningún inbound real; `unread_count` NO se recalcula, sin forma fiable), (c) aplica el candado B retroactivo sobre fallos 131047 ya registrados. Test: `tests/ventana_24h.sql`, nueve casos (candado A visible-pero-no-cuenta, candado B por INSERT y por UPDATE, otro código no toca nada, reapertura, backfill vía `\i` con el trigger `on_message_inserted` desactivado para sembrar el estado "de producción", sin mensaje del cliente, callback tardío), cableado en el job `migraciones` de CI |
| Un turno sin contenido legible deja rastro (T1, "La IA ve lo que llega") | `migrations/20260908010000_traspaso_sin_contenido_legible.sql` — T1 de la corrida "La IA ve lo que llega" (8/9/2026): el caso que la destapó, `cea69118-5d17-4f08-84c6-925755672b87`, un audio del cliente sin texto previo — `loadHistory` descartaba toda fila sin `content`, el turno salía por historial vacío SIN escribir traspaso (viola "ningún lead invisible") y el reconciliador la reencoló 30 veces hasta que un asesor contestó a mano. Amplía el CHECK de `conversation_handoffs.reason` con `sin_contenido_legible` (mismos 24 valores vigentes desde 20260905030000, más este al final): lo escribe el código de T4 con `record_handoff(..., to_kind => 'unassigned')` cuando el historial queda vacío tras describir la media (S1/S3 de la misma corrida). Sin `security definer` nueva, sin revokes/grants. Backfill único (no en tres pasos, a diferencia de 20260907010000): limpia `journey_stage`/`active_tool` a null en las conversaciones que ese bug dejó congeladas en `classifying`/`tool_running` SIN lock vigente (`ai_turn_lock_until is null or ai_turn_lock_until < now()`, la misma condición de "libre" que usa `ai_turn_lock_acquire()`) — 17 medidas en producción el 7/9/2026, la más vieja del 27/8/2026; una con lock vigente no se toca, podría ser un turno de verdad corriendo ahora. Idempotente: reaplicada con `\i` no encuentra ya nada que limpiar. Test: `tests/traspaso_sin_contenido_legible.sql`, cuatro casos (record_handoff acepta la razón nueva, rechaza una inventada, el backfill limpia classifying sin lock y tool_running con lock vencido, no toca un classifying con lock vigente), cableado en el job `migraciones` de CI |
| La IA reconfirma antes de pasar el caso a ventas (T2, "Seis frentes del buzón") | `migrations/20260909010000_handoff_confirmation.sql` — T2 del plan "Seis frentes del buzón" (8/9/2026): hasta ahora la IA escalaba al primer "sí" del cliente; el operador pidió una segunda confirmación antes de pasar el caso a ventas. `conversations.handoff_confirmation_pending_at timestamptz` (nullable, `add column if not exists` para poder aplicarla en caliente aunque la base local estuviera apagada al escribirla) sella cuándo la IA ofreció el pase; null = sin oferta pendiente. `handoff-confirmation.ts` comparaba el sello contra el último mensaje del cliente (`handoffConfirmationState`); `buildEscalateTool` (`tools.ts`) lo usaba SOLO con `motivo: "intencion_compra"`. Sin CHECK, sin `security definer`, sin RLS nueva. Desde el 9/9/2026 (corrida "El pase a ventas al primer sí", que revirtió la reconfirmación por el bucle del "ok"/"está bien"/"dale" en producción) la columna quedó SIN USO — nadie la lee ni la escribe—; no hay migración de reversa a propósito (`drop column` es irreversible), la columna sigue existiendo y aplicada en producción con valores viejos que nadie consulta, y `database.types.ts` la conserva porque el esquema real la tiene |
| Un RPC resume el día de un asesor, y las ventas de `agent_metrics` pasan a atribuirse a quien cerró (T0, "Los números del día") | `migrations/20260910010000_resumen_del_dia_del_asesor.sql` — 10/9/2026. `agent_day_summary(p_from, p_to)` (`security definer`, guarda `is_agent()`, dos revokes + grant a `authenticated, service_role`) resume el día de UN asesor —`auth.uid()`— sin pegarle a `agent_metrics()` completo: asignadas (sobre `conversations.assigned_at`), respondidas y ventas/monto cerradas en el rango. Decisión 8 del operador, misma migración: `agent_metrics()` (`create or replace`, copia textual salvo la CTE `ventas`) deja de atribuir ventas al asesor asignado y pasa a atribuirlas a quien CERRÓ (`deal_closed_by`), igual que `sales-view` y este RPC nuevo — dos fuentes de verdad distintas para la misma pregunta no calzaban cuando un caso se reasignaba antes de cerrar. **Corrección sobre el plan original**: `conversations.assigned_at` NO es columna nueva de esta migración — existe desde `20260822080000_agent_metrics.sql` (columna, índice `conversations_assigned_at_idx` y el trigger `on_conversation_assigned`/`handle_conversation_assigned()` sellándola en `before insert or update of assigned_agent_id`); `database.types.ts` estaba desactualizado sin ese campo y llevó a una primera versión de esta migración que intentaba recrear columna/índice/trigger ya existentes (habría reventado en producción). Sin base local disponible para esta corrida (Docker apagado), verificado solo estáticamente (`permisos-funciones.test.ts`, `tsc`) |
| Los cinco valores de intención y la cortesía tras una escalada quedan admitidos (T1, "La voz cercana y la espera visible") | `migrations/20260914010000_intenciones_y_traspasos_completos.sql` — T1 del plan "La voz cercana y la espera visible" (14/9/2026): `agent_turns.intent`/`conversations.intent` nacieron con cuatro valores (20260819040000) y `classify.ts` (`INTENT_VALUES`) devuelve cinco desde que existe `fuera_de_tema` ("El reloj dice la verdad", 5/9/2026) — cada turno fuera de tema fallaba en silencio el insert de `logTurn` en `agent_turns` y el update de `conversations.intent` (la auditoría del 14/9 contó 3 turnos fuera de tema visibles y 0 en bitácora). Amplía `agent_turns_intent_check`/`conversations_intent_check` con `fuera_de_tema` y `conversation_handoffs_reason_check` (copia completa de la lista vigente en 20260908010000) con `cortesia_tras_escalada`, la razón que T4 de esta misma corrida escribe cuando el cliente cierra con una cortesía mientras espera a un asesor y el turno se calla dejando rastro. Sin backfill (los CHECK solo impiden escrituras: no hay filas rechazadas que reparar) ni funciones `security definer` nuevas. Autoverificación con `pg_get_constraintdef` contra `pg_constraint`, no contra el texto del `.sql`. Test: `tests/intenciones_y_traspasos_completos.sql`, cuatro casos (las tres tablas aceptan el valor nuevo; un valor inventado sigue rechazado en las tres), verificado contra la base local el 14/9 y cableado al job `migraciones` del CI |
| La base dice SBK Motors donde decía SBK Motorcycles (T1, "La voz de mostrador con nombre propio y el cierre de v1.1") | `migrations/20260915010000_marca_sbk_motors.sql` — 15/9/2026: "SBK Motorcycles" ya era un DATO en producción, no solo texto de código: la categoría "La tienda" de la biblioteca se sembró con ese nombre (20260825020000) y podía repetirse a mano en `knowledge_entries`/`ai_playbooks` desde el panel, ambas cosas verbatim al cliente. `replace('SBK Motorcycles', 'SBK Motors')` idempotente sobre las tres tablas, sin tocar `templates` (texto ya aprobado por Meta), sin funciones `security definer` nuevas. Autoverificación con conteo real contra las seis columnas, mismo criterio que `20260914010000`. Test: `tests/marca_sbk_motors.sql` (tres casos: cero filas viejas, "La tienda" corregida, idempotencia con `\i`), cableado al job `migraciones` del CI, que también sumó el test de `20260914010000` que faltaba cablear desde el 14/9 |
| La IA solo atiende lo que llegó después de que se la devolvieron (T1, "La IA no vuelve a pedir lo que ya pidió" — revisión del 16/9/2026) | `migrations/20260916010000_devolucion_a_la_ia.sql` — reemplaza al diseño del 15/9 (commit `56fa2df`, migración `20260915020000`, deshecho con `git reset --soft` antes de salir de esta máquina: medía `awaiting_any_reply` contra `last_message_at`, "¿salió algo después del último mensaje?", pregunta que la revisión adversarial le encontró cinco fallas — envíos fallidos, la carrera de ráfaga, la bienvenida, "Sin dueño" y el cliente que escribe mientras espera al asesor). Caso reportado: un cliente pide un asesor, la IA escala y se despide; un asesor desasigna y reactiva la IA a mano; en menos de un minuto el reconciliador reencolaba la conversación y la IA repetía la misma promesa sobre el MISMO mensaje viejo (mecanismo que el 13/9 volvió a escalar 63 casos). `set local lock_timeout = '5s'` (agregar una columna generada reescribe `conversations` entera con lock `ACCESS EXCLUSIVE`); `ai_resume_cutoff_at timestamptz` (nace `null`, sin backfill) sella el instante en que un trigger BEFORE (`handle_conversation_ai_resume()`) ve la fila ENTRAR al estado "IA encendida y sin asesor", copiando `last_customer_message_at` de ese instante —nunca `now()`, por la latencia de Meta—; `new_since_ai_resume boolean generated always as (...) stored` (sin índice propio: más angosto que `conversations_free_unanswered_idx`) dice si el último mensaje del cliente es posterior al sello. `conversation_handoffs_reason_check` suma `desasignada_por_asesor`/`mensaje_previo_a_devolucion` (`reclamado` ya vivía en el CHECK desde `20260830040000`, sin que nadie lo escribiera hasta ahora). `handle_conversation_ownership_change()` (AFTER, `security definer`) deja rastro de los TRES movimientos de dueño que `mutations.ts` hacía sin bitácora —desasignar, reactivar la IA y reclamar un caso (`assignToMe`/`intervene` tomando un chat sin que `ai_enabled` cambie a la vez, lo que excluye a la escalada)— con `to_kind` calculado una sola vez (por si más de una condición cae en el mismo UPDATE) y `created_by` por `auth.uid()`. Corrección post-revisión del mismo 16/9/2026 (`/code-review high`): `v_to_kind` mira primero si `new.status = 'closed'` (antes de mirar quién quedó asignado), para que desasignar un chat ya cerrado no lo cuente en "Sin dueño"; y la migración exige aplicarse con `psql -1 -v ON_ERROR_STOP=1` (una transacción real), porque `set local lock_timeout` fuera de transacción es un no-op y una falla a mitad de archivo dejaría el trigger AFTER leyendo una columna generada que nunca se creó. Los DOS revokes en las dos funciones (solo la segunda es `security definer`; la primera los lleva por higiene, mismo criterio que `enforce_conversation_pins_limit`), sin grant (funciones de trigger). Autoverificación con `information_schema`/`pg_trigger`/`pg_constraint`/`has_function_privilege` contra el catálogo real. Test: `tests/devolucion_a_la_ia.sql`, dieciséis casos (escalada simulada sin sellar, desasignar→reactivar y el orden inverso, un solo UPDATE con las dos condiciones, el mensaje entre escalada y devolución, un mensaje nuevo tras devolver, la latencia de Meta, un `unsupported` tras devolver, reactivar sin nada pendiente, un UPDATE ajeno que no dispara nada, una segunda devolución que mueve el sello, `created_by` con y sin sesión, los permisos, y los tres casos de `reclamado` más el de la rama `closed` de la corrección post-revisión), cableado al job `migraciones` del CI |
| Seba se presenta y la escalada no apaga la IA (T0, "Seba atiende el mostrador", 18/9/2026) | `migrations/20260917010000_seba_y_escalada_viva.sql` — prepara la base para dos tareas de la misma corrida que todavía no tienen código (T2b y T4): `welcome_sent_at` deja de significar "última vez que se mandó la plantilla de bienvenida" (nunca se usó: `WHATSAPP_WELCOME_TEMPLATE` está vacía desde siempre) y pasa a ser el sello de "Seba ya se presentó" (requisito 1 del cliente); backfill `coalesce(last_reply_at, last_message_at, created_at)` —nunca `now()`— solo en los chats con `has_reply` (vitalicio), los que nunca recibieron nada quedan en `null` a propósito. `conversation_handoffs_reason_check` suma `silenciada_por_asesor`. `handle_conversation_ownership_change()` (`create or replace`, conserva el ACL de 20260916010000: no repite revokes) gana dos cambios — (a) hallazgo 1 del plan: la rama `reclamado` exige además `auth.uid() is not null`, porque T4 va a dejar la escalada cambiando SOLO `assigned_agent_id` con `service_role` (sin sesión), y esa forma calza exacto con la condición vieja de `reclamado` (asesor cambia sin que `ai_enabled` cambie a la vez); (b) rama nueva `silenciada_por_asesor`: `old.ai_enabled and not new.ai_enabled and old.assigned_agent_id is not distinct from new.assigned_agent_id` —la guarda de `assigned_agent_id` sin cambios se agregó tras verificar contra `tests/devolucion_a_la_ia.sql`: la escalada de HOY (sin tocar todavía por T4) sigue apagando `ai_enabled` Y asignando un asesor en el mismo UPDATE, y sin esa guarda cada escalada de hoy dejaría una fila `silenciada_por_asesor` espuria (su caso 1 se puso en rojo al construir esta migración, antes de la corrección). `handle_agent_message_silences_ai()` (nueva, `security definer`, dos revokes, sin grant) + trigger `messages_agent_silences_ai_trigger AFTER INSERT ON messages FOR EACH ROW WHEN (sender_type='agent' AND NOT is_internal_note AND direction='outbound')`: requisito 6 del cliente, "en el instante en que el asesor manda su primer mensaje, la IA se calla en ese chat" — `WHERE ... AND ai_enabled` en el UPDATE evita un segundo apagado (y una fila de más) en cada mensaje siguiente del mismo asesor. Autoverificación con `pg_constraint`/`pg_trigger`/`has_function_privilege` contra el catálogo real. `set local lock_timeout = '5s'`, aplicar con `psql -1 -v ON_ERROR_STOP=1`, después de `20260916010000` y antes del código de la corrida. Test: `tests/seba_y_escalada_viva.sql`, ocho casos (escalada simulada sin sesión no deja `reclamado` ni sella el cutoff; el mismo UPDATE con sesión sí deja `reclamado`; el primer mensaje real de un asesor apaga la IA; una nota interna no apaga; un mensaje `ai`/`system` no apaga; la pausa manual sin asesor deja `silenciada_por_asesor` con `to_kind='unassigned'`; el contrato del backfill con `has_reply` true/false), cableado al job `migraciones` del CI. Corrección de acompañamiento en `tests/devolucion_a_la_ia.sql` (mismo commit): sus casos 13/14/15 de `reclamado` simulaban al asesor SIN sesión —válido hasta esta migración, roto después, por el hallazgo 1— y ganan `set local role authenticated` + `set local "request.jwt.claim.sub"` (mismo patrón que su caso 11a) para seguir representando lo que pasa de verdad |
| Lecciones de Seba: la tabla (T1, plan "Seba atiende el mostrador", 18/9/2026) | `migrations/20260917020000_ai_lessons.sql` — requisito 7 del cliente ("clic derecho sobre un mensaje para escribir una corrección que la IA lea"). Tabla `public.ai_lessons` (nombre en inglés, como `knowledge_entries`/`ai_playbooks`; la interfaz dirá "Lecciones de Seba"): `scope` ('global' default de la UI por decisión P2, o 'conversacion' — exige `conversation_id`, CHECK `ai_lessons_conversacion_requires_conversation`); `kind` ('nota' default = prosa libre para el modelo, o 'sinonimo' — decisión P3, expande `catalog-search.ts`; exige `synonym_from`/`synonym_to`, CHECK `ai_lessons_synonym_requires_terms`); `content` acotado a 200 caracteres (mismo criterio que `knowledge_entries`); `message_id`/`message_excerpt` (snapshot del mensaje citado — el mensaje puede borrarse, mismo motivo que el snapshot de `invoices`); `conversation_id`/`contact_id` de origen; `is_active` (apagar sin borrar); `created_by`. Índices parciales `(scope, created_at desc)`/`(conversation_id)` `where is_active` — las dos consultas que hará el turno de la IA. Trigger `set_ai_lessons_updated_at` reusa `public.set_updated_at()`. RLS: cualquier agente LEE (`is_agent()`); INSERT exige `created_by = auth.uid()`; UPDATE/DELETE exigen ser el autor o `is_supervisor_or_admin()` (mismo criterio que `stickers`, 20260909020000). Realtime con el patrón idempotente + autoverificación de `20260909050000` (tabla, índices, trigger, políticas y publicación verificados contra el catálogo real, no contra el texto del `.sql`). Sin funciones `security definer` nuevas: no hacen falta revokes. Test `tests/ai_lessons.sql` (ocho casos: insert propio OK; insert con `created_by` ajeno rechazado por RLS; otro agente sin ser supervisor ni autor no puede editar/borrar la lección de otro [0 filas afectadas]; un supervisor sí puede desactivarla y borrarla; `scope='conversacion'` sin `conversation_id` viola el CHECK; `content` de 201 caracteres viola el CHECK; `kind='sinonimo'` sin `synonym_to` viola el CHECK; la tabla queda publicada en `pg_publication_tables`) — mismo patrón que `devolucion_a_la_ia.sql` (transacción con rollback, `_errores`, `set local role authenticated` + `request.jwt.claim.sub`); la tabla temporal `_errores` necesita `grant insert ... to authenticated` explícito porque los casos que corren bajo esa sesión (3, 4, y la mutación del caso 2) necesitan poder anotar un error sin que la propia tabla de errores tire "permission denied". Del resto del plan, ya commiteado: leerla desde el turno (`src/lib/ai/lessons.ts`, `fetchTurnLessons`), el menú "Enseñar a Seba…" del chat (`teach-seba-modal`) y el panel de Lecciones en Control IA (`lessons-panel`) — ver sus entradas en este mismo glosario |
| Un solo catálogo: la tabla (M1, plan "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026) | `migrations/20260918010000_catalog_links.sql` — D3 del plan: los enlaces de catálogo (hoy archivos de Google Drive de un tercero) dejan de estar pegados a mano en tres escenarios y cuatro mensajes rápidos; el catálogo de cascos tuvo cuatro IDs de Drive distintos en 25 días y el 18/9/2026 circulaban dos versiones a la vez (la IA con una URL, el mensaje rápido "Catalogo general" con otra) porque la URL vivía copiada en más de un sitio. Tabla `public.catalog_links`: `key` (clave del marcador `{{catalogo:<key>}}`, CHECK `^[a-z0-9-]{1,30}$` — solo minúsculas/dígitos/guion, `unique`), `label` (1-40 caracteres, mismo tope que `ai_playbooks`/`ai_lessons`), `url` (CHECK `^https?://`, exige esquema explícito), `sort_order` (orden dentro de `{{catalogos}}`, sin unicidad), `is_active` (false = deja de ser candidato: un escenario con marcador sin resolver lo excluye fase 0 (D6), un mensaje rápido lo pega crudo con un toast de aviso), `updated_by` (quién hizo el último cambio, `on delete set null`). Índice parcial `(sort_order) where is_active` para `fetchActiveCatalogLinks`. Trigger `set_catalog_links_updated_at` reusa `public.set_updated_at()`. RLS: `catalog_links_select` (`is_agent()`, cualquier asesor lee) y `catalog_links_write` `for all` (`is_supervisor_or_admin()` en `using`/`with check`) — una sola política de escritura porque insert/update/delete comparten la misma condición. Realtime con el patrón idempotente + autoverificación de `20260909050000`/`20260917020000` (tabla, índice, trigger, políticas y publicación verificados contra el catálogo real). Sin funciones `security definer`: la RLS de la tabla alcanza. Test `tests/catalog_links.sql` (nueve casos: supervisor crea un catálogo válido; clave con mayúscula rechazada por el CHECK; clave con espacio rechazada; URL sin esquema `http(s)://` rechazada; clave repetida rechazada por la restricción `unique`; supervisor sí puede editar la URL y borrar; un asesor corriente lee pero no puede crear/editar/borrar [0 filas afectadas]; `anon` sin sesión ve 0 filas aunque el grant de tabla exista [`has_table_privilege` heredado de las default privileges, la RLS es la que bloquea de verdad]; la tabla queda publicada en `pg_publication_tables`) — mismo patrón que `ai_lessons.sql` (transacción con rollback, `_errores`, `set local role authenticated`/`set role anon` + `request.jwt.claim.sub`). Del resto del plan, ya commiteado: el módulo puro de marcadores (`src/lib/catalog-links.ts`, T2), la lectura/escritura desde `data.ts`/`mutations.ts` (T2), el consumo por la IA (`send.ts`/`playbooks.ts`/`agent.ts`, T3), el panel de Control IA (`catalog-links-panel.tsx`, T4a) y los mensajes rápidos de los asesores (`quick-replies-modal.tsx`/`composer.tsx`/`crm-shell.tsx`, T4b, que ya resuelven el marcador al usar un mensaje rápido) — ver sus entradas en este mismo glosario |
| Cada orden puede llevar su número de factura Saint (M2, plan "Nada sin leer, un solo catálogo y la factura Saint", 18/9/2026) | `migrations/20260918020000_factura_saint.sql` — D9 del plan: `orders.saint_invoice_number text` (nullable en base: las ventas cerradas antes del 18/9/2026 no tienen ese dato y no hay forma de reconstruirlo) con `orders_saint_invoice_number_check` (`is null or (= btrim(...) and char_length between 1 and 40)`) — exige el texto ya recortado y de 1 a 40 caracteres cuando no es null, mismo tope que `ai_playbooks`/`ai_lessons`/`catalog_links.label`. Ojo con la colisión de nombres: NO es `invoices.number` (el correlativo interno del CRM, "SBK-000123") — son dos numeraciones de dos sistemas distintos, Saint es el sistema administrativo del negocio. Sin restricción de unicidad a propósito: una factura Saint puede cubrir más de un chat, y un rechazo por duplicado en el mostrador confundiría más de lo que protege. El modal "Cerrar venta" (T5 del mismo plan, código aparte) lo exige como obligatorio del lado de la interfaz. Sin funciones `security definer`, sin RLS nueva (la política `orders_all` ya existente alcanza). Autoverificación con `information_schema`/`pg_constraint` contra el catálogo real, mismo criterio que `20260918010000`. Test `tests/factura_saint.sql` (cinco casos: acepta `'00123'`; rechaza cadena vacía; rechaza con espacios alrededor; rechaza 41 caracteres; una venta insertada sin la columna queda en NULL), cableado al job `migraciones` del CI justo después de `catalog_links.sql`. `database.types.ts` gana la columna en `Row`/`Insert`/`Update` de `orders`. Del resto del plan, ya commiteado: el módulo puro `sale-draft.ts`, el campo obligatorio y su mensaje por campo en `close-sale-modal.tsx`, la segunda barrera en `closeSaleWithContactInfo` (`mutations.ts`) y la factura Saint mostrada en Ventas (T5/T6) — ver sus entradas en este mismo glosario |
| `conversation_handoffs` entra a Realtime (T6, "El aviso que llega") | `migrations/20260909050000_realtime_conversation_handoffs.sql` — verificado contra producción el 8/9/2026 (`select tablename from pg_publication_tables where pubname='supabase_realtime'`): 15 tablas publicadas, `conversation_handoffs` NO estaba entre ellas pese a existir desde 20260830040000. Consecuencia anterior a esta corrida: el canal `unassigned-handoffs` de `crm-shell.tsx` —la píldora "Sin dueño"— está suscrito desde el 30/8/2026 a una tabla que no publica nada; se suscribe bien y no recibe jamás, la píldora solo se actualizaba al recargar. `do $$ ... if not exists ... alter publication supabase_realtime add table` (idempotente: se aplica a mano en el VPS antes de mergear y de nuevo en CI) más un segundo bloque que hace `raise exception` si la tabla no quedó publicada — para que un `alter publication` mal escrito rompa la migración en vez de dejar el canal mudo otra vez sin que nadie se entere. Sin política nueva: `conversation_handoffs_select using (is_agent())` (20260830040000) ya alcanza, Realtime la respeta sola; sin tocar replica identity, solo se escuchan INSERT. Habilita de rebote el aviso de asignación de esta misma ola (T5/T6): sin la tabla publicada, `assignment-notifier.tsx` tampoco recibiría nada |
| La vista previa de la bandeja habla español (T1, "La bandeja habla español y el número nuevo queda a un clic") | `migrations/20260908020000_preview_en_espanol.sql` — T1 de la corrida del 8/9/2026: la lista mostraba "Image", "Audio", "Sticker", "Unsupported" cuando el último mensaje visible no traía `content`, porque `handle_new_message()` escribía `initcap(replace(message_type, '_', ' '))` en `last_message_preview` y `conversation-list-item.tsx` lo pinta tal cual (deuda de "El cliente que cambió de número", 6/9/2026). Tres piezas: `message_preview_label(message_type)`, función SQL pura `immutable strict`, NO `security definer` (no toca tablas; el guardián de `permisos_funciones.sql` solo recorre `prosecdef = true`, así que no hacen falta revokes) que traduce `image`→"📷 Foto", `video`→"🎥 Video", `audio`→"🎤 Audio", `document`→"📄 Documento", `sticker`→"Sticker", `template`→"Plantilla", `unsupported`→"Mensaje que WhatsApp no entrega" y deja el `initcap` para cualquier otro; `handle_new_message()` (`create or replace`, copia exacta de 20260907010000 con SOLO la expresión de `last_message_preview` cambiada — candados A/B, `has_reply`, `unread_count`, `last_reply_*` intactos, verificado con diff mecánico; conserva el ACL, sin revokes/grants); y un backfill exacto e idempotente que solo toca las conversaciones cuyo preview actual es EXACTAMENTE el `initcap` del tipo de su último mensaje visible (mismo `distinct on` y predicado de visibilidad que el paso 5 de 20260905010000) — un cliente que escribió literalmente "Image" como texto queda intacto porque su tipo es `text`. `raise notice` con el conteo (1 fila en la base local con seeds, 0 la segunda vez). Test `tests/preview_en_espanol.sql`: seis casos, el 6 reaplica la migración con `\i` dos veces y usa un centinela en `updated_at` (`2020-01-01`) para que quitar el predicado del backfill se note aunque el texto recalculado coincida; cableado en el job `migraciones` de CI. Para correrlo local: `docker cp supabase/` a `/tmp/repo/supabase` del contenedor y `MSYS_NO_PATHCONV=1 docker exec -w /tmp/repo … psql -f supabase/tests/preview_en_espanol.sql` (receta en la cabecera del test) |

## `scripts/` y `docs/`

| Archivo | Qué es |
|---|---|
| `scripts/deploy.sh` | Despliegue por SSH: valida antes de tocar el servidor, copia, levanta, verifica TLS |
| `scripts/preflight.sh` | Frena config rota antes de arrancar (claves cruzadas, URLs locales, secretos de juguete) |
| `scripts/backup.sh` / `restore.sh` | Respaldo/restauración probados de verdad (solo `public`; el bucket no entra) |
| `scripts/verificar-respaldo.sh` | Verifica un `.sql.gz` (gzip íntegro, tamaño, `CREATE TABLE`) sin borrar ni mover nada — aísla el `pipefail` para que un SIGPIPE del productor no se lea como corrupción (bug del 29/8/2026) |
| `scripts/generar-iconos.mjs` | Iconos de la app |
| `scripts/exportar-historiales-clasificador.sql` | Export de producción para el comparador grande×chico (T2, 7/9/2026): un solo JSON con hasta 200 conversaciones con turno de IA en 7 días (últimos 20 mensajes c/u, ascendentes), escenarios activos con tags y `agent_settings.business_hours`; se corre por SSH contra `supabase-db` (`psql -At`); su salida `scripts/historiales-clasificador.json` trae texto de clientes y está en `.gitignore` |
| `scripts/comparar-clasificador.test.ts` | Mide acuerdo de intención (`classifyIntent`) y escenarios perdidos (`matchPlaybook`) entre el modelo grande y un candidato chico (`AI_CLASSIFIER_MODEL`, default `google/gemini-3.1-flash-lite`, configurable con `COMPARAR_CLASIFICADOR_CHICO`). Es un archivo de vitest porque `classify.ts`/`playbooks.ts`/`model.ts` importan `server-only`; `describe.skipIf` sin `COMPARAR_CLASIFICADOR`. Dos pasadas (todo el grande, luego todo el chico: la variable es global al proceso) con pool de 5 conversaciones; corre contra el fixture sintético por default o el export real con `COMPARAR_CLASIFICADOR_FIXTURE`; escribe matriz de confusión, escenarios perdidos/ganados, tiempos, tokens y eventos de log en `comparar-clasificador.reporte.md` (gitignored). El veredicto (≥95 % y cero escenarios perdidos) se imprime, no se afirma: falla solo si ninguna llamada al proveedor completó |
| `scripts/comparar-clasificador.fixture-sintetico.json` | 5 conversaciones INVENTADAS (marcadas `_sintetico`) para probar la plomería del comparador sin tocar datos reales |
| `scripts/sql/2026-09-18-catalogos-iniciales.sql` | D8 del plan "Nada sin leer, un solo catálogo y la factura Saint" (T2, 18/9/2026): carga inicial de `catalog_links` por SCRIPT REVISADO, no por migración —"el contenido es del cliente, no del repo"—, con marcadores de relleno `<<...>>` (URLs, ids de escenarios/mensajes rápidos y su texto ya con el marcador) que el implementador NO completó a propósito: el Claude del VPS los completa contra la base real, DESPUÉS del deploy del código (nunca antes: un marcador sin código que lo resuelva es peor que la URL vieja que reemplaza). En UNA transacción (`begin`/`commit`, `psql -1 -v ON_ERROR_STOP=1`): inserta los 7 catálogos de "Catálogo general" (Cascos, Resonadores, Maletas, Exploradoras y Bombillos, Defensas, Lubricantes ×2), reemplaza la URL de Drive por su marcador en 2 escenarios y 4 mensajes rápidos, y un bloque final que falla el script entero si el reemplazo no surtió efecto — antes, una guarda separada (`raise exception` si queda algún `<<...>>` sin completar) impide correrlo tal como lo entrega este commit. Dos consultas de ayuda en el encabezado (comentario, no se ejecutan solas) para encontrar los `id`/texto reales en `ai_playbooks`/`quick_replies` filtrando por `drive.google.com`. Pregunta pendiente para el cliente, anotada en el propio archivo: "Lubricantes" aparece dos veces en "Catálogo general" con dos URLs distintas —¿dos catálogos o uno viejo sin borrar?—; hasta la respuesta, carga los dos como `lubricantes`/`lubricantes-2`. Corrección de la revisión `code-review high` del 19/9/2026: **(punto 3) "Ubicación" queda FUERA del script** — la primera versión la cargaba como octavo catálogo (`ubicacion`, sort_order 99) y reemplazaba también el escenario "Ubicación"; eso colaba el Maps de la tienda DENTRO de `{{catalogos}}` (`formatCatalogList` lista TODO enlace activo, mezclando la ubicación con la lista de catálogos de repuestos) y apagar esa clave para sacarla de la lista habría dejado sin resolver al escenario "Ubicación" (D6). El Maps tampoco rota como un Drive, así que no gana nada entrando a la tabla — queda con su URL escrita a mano, tal como está hoy, y el script no la toca; de ahí que los conteos bajen a 7 catálogos y 2 escenarios (no 8/3). Si algún día hace falta distinguir un enlace que no deba entrar en `{{catalogos}}`, hace falta una columna nueva (`in_list boolean`, migración aparte). **(punto 2) el script daba "verificado" sin haber tocado nada**: los `update` de las secciones 4/5 nunca comprobaban cuántas filas afectaron — con un `id` de otra base el `update` toca CERO filas sin avisar, y el chequeo final (un `join` por ese mismo id) tampoco lo notaba, porque un `join` contra una fila inexistente no aporta nada al conteo de "pendientes" ("cero pendientes" pasaba igual sin haber escrito nada). Ahora cada `update` corre DENTRO de su propio `do $$ ... $$` para leer `get diagnostics ... = row_count` en el mismo bloque (fuera de PL/pgSQL no se puede leer el conteo de un `update` suelto) y aborta si no coincide EXACTO con el tamaño de la tabla de relleno correspondiente (2 para escenarios, 4 para mensajes rápidos — derivado de la propia tabla temporal, no un literal a mano); la verificación final pasó de "¿cuántas filas UNIDAS siguen con `drive.google.com`?" a "¿cuántas de las filas de relleno tienen HOY una fila real, sin `drive.google.com`?", que detecta tanto la URL no reemplazada como el id inexistente. Probado a mano contra la base local (`docker exec -i supabase_db_Liminal_CRM psql`): con un id inexistente el script aborta con el mensaje del `row_count`, sin dejar nada aplicado (`-1` lo revierte todo); con filas de prueba reales carga los 7 catálogos y deja los 2 escenarios + 4 mensajes rápidos con el marcador, sin `drive.google.com`. T6, plan "Seba sale sin pisar a nadie" (19/9/2026, hallazgo M2 de la inspección pre-despliegue) suma cuatro endurecimientos más sobre esta misma versión: (a) `\set ON_ERROR_STOP on` DENTRO del archivo, segunda guarda además del flag de la línea de comandos, para que una sentencia rota a mitad de archivo no termine con `rc=0` sin haber escrito nada; (b) los huecos de texto de escenarios/mensajes rápidos pasan de `'<<...>>'` a `$txt$<<...>>$txt$` (dollar-quoting) — una comilla simple sin escapar en el texto real que manda el cliente cortaba el literal a la mitad y rompía el INSERT; (c) sección 2b, aserción nueva: toda clave `{{catalogo:<key>}}` que aparezca en los textos nuevos de escenarios/mensajes rápidos tiene que existir en la tabla de relleno `_catalogo_valores` o ya estar ACTIVA en `catalog_links`, para no dar "verificado" sobre un escenario que apunta a una clave que nunca va a resolver nada; (d) `on conflict (key) do nothing` + un `NOTICE` por cada clave saltada con su URL ACTUAL (decisión D-C del operador, 19/9/2026) — si un supervisor ya creó esa clave desde el panel con una URL más nueva (los ids de Drive rotan), el script no la pisa, y una segunda corrida ya no muere con `duplicate key`; (e) aviso informativo 6b (NO aborta) que además de los dos escenarios que este script toca, barre CUALQUIER escenario/mensaje rápido de la base con `drive.google.com` residual, incluido `attachment_url` de `ai_playbooks` (columna que `response_text` no cubre y que este script nunca escribe). Corrección post-revisión (`code-review high`, 19/9/2026, hallazgos 7a y 7b sobre T6) suma dos secciones nuevas: **2c** — la aserción 2b solo mira coincidencias con la forma ESTRICTA de un marcador (`\{\{\s*cat[aá]logo\s*:\s*([a-z0-9-]+)\s*\}\}`, mismo patrón que `CATALOG_MARKER` de `src/lib/catalog-links.ts`), así que un marcador MAL ESCRITO —`{{catalogo:cascos_nuevos}}` (guion bajo), `{{catalogo: exploradoras y bombillos}}` (espacios dentro de la clave)— no calzaba esa regex y 2b ni lo veía: ese texto se habría guardado tal cual y en producción nunca resuelve nada. 2c es el espejo SQL de `LOOSE_UNRESOLVED_MARKER` (mismo archivo TS): busca cualquier resto que empiece como `{{catalogo`/`{{catalogos` y aborta el script entero si algo no calza ni la forma estricta de `{{catalogo:<key>}}` ni la de `{{catalogos}}`, nombrando el texto sospechoso (recortado a 60 caracteres, mismo tope que `missing` en TypeScript) — desvío aceptado: `{{Catálogo: cascos}}` (mayúscula, acento, espacios alrededor del `:`) NO aborta, porque `CATALOG_MARKER` es `gi` y admite `\s*`, así que si el revisor esperaba que abortara la premisa era incorrecta; **3b** — 2b (y la versión original de T6) daban por resuelta cualquier clave presente en `_catalogo_valores`, asumiendo que el INSERT de la sección 3 la iba a dejar ACTIVA, pero ese INSERT es `on conflict (key) do nothing` (D-C): si la clave YA existía INACTIVA (creada desde el panel, o de una corrida anterior), el conflicto la deja EXACTAMENTE como estaba — inactiva — y `resolveCatalogMarkers` solo lee enlaces activos, así que un texto nuevo que la referencia nunca resuelve nada aunque el script haya dado "verificado". 3b aborta DESPUÉS del INSERT si alguna clave referenciada por los textos nuevos sigue SIN estar activa en `catalog_links`, nombrando la(s) clave(s) — el script no las activa por su cuenta (D-C: eso es una decisión humana, desde el panel de Control IA). El `NOTICE` de la sección 3 también suma el estado ACTIVA/INACTIVA de cada clave saltada, para que quien lo corre sepa de antemano si 3b va a abortar |
| `docs/PRODUCCION.md` | LA guía de puesta en producción, paso a paso con verificación. 7/9/2026: suma "Rampa de los topes" (los dos escalones que el operador carga a mano en Dokploy para destopar la cola, con las señales a vigilar) y el cron pasa de cada 5 min a cada minuto. 14/9/2026: sección 10 "Lista operativa de v1.1" (O1–O8 de "La voz cercana y la espera visible", la verificación en producción a 48 h con sus consultas SQL y la etiqueta v1.1); la comprobación final recuenta las migraciones (69). 15/9/2026: la sección 10 suma "Despliegue de v1.1" (respaldo, las dos migraciones `20260914010000` y `20260915010000` ANTES del push, O7, push y qué mirar en el log), reescribe O3 (la IA ignora los escenarios que saludan) y corrige las métricas a 48 h (la de saludos se invierte: franja correcta y una sola vez); conteo de migraciones en 70. 16/9/2026: la sección 2 suma la entrega de `20260916010000_devolucion_a_la_ia.sql` (el sello de devolución) — la regla de orden (migración ANTES del código, sin excepción: sin las columnas el select del turno falla y se caen todos los turnos de IA), el `lock_timeout` corto por la columna generada, y las consultas de verificación (columnas/generada, triggers, CHECK, permisos, y la cuenta de escaladas sin sello que trae la Verificación · 5 del plan) |
| `docs/superpowers/plans`, `specs/` | Planes y specs de diseño históricos (decisiones documentadas) |
| `docs/planes/` | Planes aprobados bajo la metodología `liminalwork`, fuente de verdad de cada corrida de implementación (`2026-09-04-bandeja-que-no-pierde.md`: las cuatro etapas de la reforma de la bandeja, con el prompt del orquestador al lado; `2026-09-05-anexo-bandeja-que-no-pierde.md`: los dos hallazgos de esa corrida que cambiaban el plan y el operador aprobó corregir —la despedida de la IA al escalar sin asesores como `is_auto_reply` [A1] y la reapertura por el cliente traspasando al asesor asignado [A2]— y la parte B, que cierra la última excepción: la base recalcula `awaiting_reply` cuando un mensaje se marca `is_auto_reply` después de insertado [B1, migración 20260905070000 con backfill de escalaciones viejas] y el escenario que escala sin asesores marca su texto [B2]; `2026-09-07-ventana-24h-dice-la-verdad.md`: los dos candados sobre `last_customer_message_at` [migración 20260907010000] y la red de seguridad del composer; `2026-09-08-la-ia-ve-lo-que-llega.md`: siete tareas —el historial describe media en vez de esconderla [`history-line.ts`], el bloque `MEDIA_RULES` del prompt, el traspaso `sin_contenido_legible` [migración 20260908010000] con la limpieza de las tres puertas que dejaban `journey_stage` congelado, `AI_AGENT_REASONING` y `origenDelFallo` [red vs. Meta], `errorText` entendiendo errores de Supabase, y la guarda "escribió un humano" con gracia de `AI_HUMAN_GRACE_MINUTES` en vez de vitalicia— con el brief completo del operador y el resultado real de la corrida; `2026-09-07-la-respuesta-llega-en-siete-segundos.md`: los tres frenos de la cola destopados a la vez [S1], el comparador que descarta mudar la clasificación a un modelo chico [S2, sin cambio de `.env`], `claimDue` separando `debounceMs` de `colaMs` con las herramientas usadas [S4] y la continuación propia de la cola cuando un turno queda frenado [S3] —sin migración—, con el brief completo del operador, la tabla del comparador y la rampa en producción pendiente como T5). `2026-09-14-la-voz-cercana-y-la-espera-visible.md` + `2026-09-14-prompt-orquestador.md`: la última grieta antes de v1.1 (aprobado el 14/9/2026 con la Decisión 1, saludo neutro y único): nace de la auditoría "72 horas en el buzón" (727 conversaciones) y del reporte del cliente (la IA saluda por franja y suena tajante); ocho tareas de código (una migración, `20260914010000`), la lista operativa O1–O8 del panel y la verificación en producción a 48 h que habilita la etiqueta v1.1. `2026-09-15-la-voz-de-mostrador-con-nombre-propio.md`: aprobado el 15/9/2026 tras revisar la corrida del 14/9: identidad "alguien sumamente amable que orienta, cotiza y pasa lo específico", el nombre real SBK Motors en todo el producto (`brand.ts` + migración de datos `20260915010000`), saludo por franja UNA vez calculado por código, fase 0 ignorando los escenarios que saludan, la deuda del 14/9 cerrada (`escalationOpen`, `.bin`), el diagnóstico de audio en Chrome y el cierre de v1.1 (despliegue, O1–O8, métricas a 48 h, etiqueta). `2026-09-16-la-ia-no-vuelve-a-pedir.md`: revisión aprobada el 16/9/2026 de un plan del mismo día que se rehizo por completo — el diseño original (`awaiting_any_reply` contra `last_message_at`, commit `56fa2df`, migración `20260915020000`) se deshizo con `git reset --soft` tras la revisión adversarial encontrarle cinco fallas; el enfoque nuevo es un sello de devolución (`ai_resume_cutoff_at`, migración `20260916010000`) que la IA solo atiende si el mensaje del cliente es posterior a él, con el reconciliador y el botón de atraso filtrando por la columna generada `new_since_ai_resume` y el turno callándose con traspaso `mensaje_previo_a_devolucion` a "Sin dueño" |
| `docs/diagnosticos/2026-09-14-notas-de-voz.md` | Diagnóstico (T6b, "La voz cercana y la espera visible", 14/9/2026, sin código): las notas de voz no suenan para el asesor porque Meta las entrega como `audio/ogg; codecs=opus` y el CRM las sirve tal cual (`api/media/[...path]/route.ts` solo redirige a una URL firmada, no toca `Content-Type`); Safari/WebKit —y por lo tanto cualquier navegador en iOS— no soportó el contenedor Ogg hasta la versión 18.4 (~marzo 2025), y en iOS es una limitación de sistema (AVFoundation), no solo del navegador. `message-bubble.tsx` (`AudioContent`, desde el 21/8/2026) ya detecta el error y ofrece "Descargar". Deuda anotada (no corregida: no es la causa del síntoma): `EXTENSION_BY_MIME` del webhook no reconoce `audio/ogg; codecs=opus` y guarda esos archivos como `.bin`. Pregunta pendiente al operador: navegador y equipo de los cuatro asesores; tres salidas con costo para v1.2. 15/9/2026: el operador confirmó Android/PC con Chrome, así que la causa Safari/Ogg queda descartada; la segunda vuelta vive en `2026-09-15-notas-de-voz-chrome.md` y el `.bin` lo cerró T6 |
| `docs/diagnosticos/2026-09-15-notas-de-voz-chrome.md` | Segunda vuelta del diagnóstico de notas de voz (T7, "La voz de mostrador con nombre propio", 15/9/2026): descarta la hipótesis Safari/Ogg del 14/9 (los asesores usan Android o PC con Chrome/Brave/Edge) y confirma en local, con `curl` contra el bucket `whatsapp-media`, la mecánica de H1: una URL firmada de `api/media` vencida (TTL 60 s, `preload="metadata"` la resuelve al montar la burbuja) responde 400 con JSON en vez de audio, y eso es lo que Chrome reporta como código 4, igual que un códec no soportado; descarta H3 (`Accept-Ranges`/206 sí funcionan) y H4 (`Content-Type` no se normaliza); corrige la premisa de H2 (`proxy.ts` no corre para `/api/*`; `api/media` refresca la sesión sola). Deja escritos los pasos de Network y la consulta SQL para cerrar H1/H2/H6 en producción. Único arreglo: `message-bubble.tsx` ya no esconde Reintentar en el código 4. El TTL de `api/media` NO se tocó (decisión del operador, 15/9); las salidas con costo quedan para v1.2 |
