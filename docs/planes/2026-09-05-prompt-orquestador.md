Actúa como ORQUESTADOR bajo la metodología `liminalwork` (léela con la skill antes de nada, junto con `CLAUDE.md` y `docs/GLOSARIO.md`). El plan ya está aprobado por el operador y vive en `docs/planes/2026-09-05-el-reloj-dice-la-verdad.md`: léelo entero antes de repartir nada. Tu trabajo es ejecutarlo completo —tres frentes, doce tareas (B1, B2, B3, B4, B5, A1, A3, A4, C1, C2, C3 y la verificación final)— y dejar el repo con todo hecho, testeado y commiteado, sin hacer `push` ni tocar producción.

Reglas del orquestador:

1. No implementas. Cada tarea del plan se la asignas a UN subagente propio con `subagent_type: general-purpose`, `model: sonnet` y razonamiento alto, con contexto limpio. En el prompt del subagente pega literalmente: la sección "Reglas para todos" del plan, la cabecera "Decisiones del operador", la sección completa de SU tarea, y la instrucción de leer `CLAUDE.md`, `docs/GLOSARIO.md` y los archivos que la tarea nombra antes de escribir. Nada de resumirle la tarea: recibe el texto del plan tal cual.

2. Respeta el orden y las dependencias de la sección "Orden de ejecución": tanda 1 en paralelo {B1, B2, A3, C1, C2, C3}; tanda 2 {A1} (necesita B2 en el árbol); tanda 3 {A4} (necesita A1); tanda 4, el agente de IA de último: B3 primero y después {B4, B5} en paralelo. Los subagentes de una misma tanda corren en paralelo sobre el mismo árbol y tocan archivos disjuntos; cada tanda cierra con la suite completa en verde antes de abrir la siguiente.

3. Los subagentes NO hacen commit y NO editan `docs/GLOSARIO.md` ni `CLAUDE.md`: te entregan la línea de glosario propuesta por archivo tocado y la aplicas tú al commitear. Es la regla que evita que dos subagentes en paralelo se pisen el glosario.

4. Cada subagente termina con el reporte obligatorio (qué implementó y decidió; archivos y líneas de glosario propuestas; salida de sus tests, `tsc` y lint; desvíos o dudas). No cierras una tarea sin ese reporte Y sin correr tú mismo `rtk npx tsc --noEmit`, `rtk npm run lint` y `rtk npm run test -- --no-file-parallelism`; para B1 además reconstruir la base local desde cero (`supabase db reset`, o `docker exec` al contenedor de Postgres si la CLI falla, ver la memoria de `CLAUDE.md`) y correr los `.sql` de `supabase/tests` por `psql` contra `postgresql://postgres:postgres@127.0.0.1:54322/postgres`. Si algo no cuadra con el plan, abres otro subagente con la corrección concreta; no parcheas a mano.

5. Commits: los haces tú, uno por tarea, narrativos en español (el efecto observable, nunca `feat:`). La migración B1 va SIEMPRE en su propio commit con `[migración]` en el título, antes de cualquier código que la use (B2 no la usa: es un módulo puro; B3 y B5 sí). Mensajes largos con `git commit -F <archivo>` (sin rtk: `rtk git commit -m` se rompe con comillas simples). Cada commit lleva sus líneas de `docs/GLOSARIO.md` actualizadas; si una tarea cambia doctrina (A1 cambia qué significa "atascado"; B3 cambia la trampa de "Saber la hora no es saber el horario"), el mismo commit actualiza `CLAUDE.md` (sección Trampas / Arquitectura).

6. Supuestos del plan que puedes ajustar sin volver al operador (están listados en la cabecera del plan): horario default L–V 08:00–18:00; umbral de "Con asesor" 60 minutos de horario laboral; cliente callado tras respuesta de la IA se queda en "Consulta" en gris. Si un subagente se traba en algo que el plan no decide, decide tú por la opción más simple que respete `CLAUDE.md` y anótalo en el reporte final; si la decisión cambia el plan (una columna nueva, una migración que no estaba, tocar `inbox-filters.ts` desde A1), párate y pregúntame.

7. Al terminar la tanda 4 corre la "Verificación final" del plan: suite, build con `rtk proxy npm run build` (nunca `rtk next build`; verificar el timestamp de `.next/BUILD_ID`), base desde cero con los `.sql` de `supabase/tests`, el escenario a mano con `api/dev/simulate-message` (los saludos a las 3 pm y a las 8 pm, la despedida fuera de horario y la intención de compra fuera de horario), el conteo del tablero (1 atascado con la base del artefacto, no 2) y las dos mutaciones manuales (quitar `awaitingReply` de `waitingMinutes` y mover el borde 19:00→19:01 en `DAY_BANDS`: los tests correspondientes deben ponerse rojos; revertir). Antes del reporte de entrega me preguntas en qué commit está producción (la memoria dice `26d356d`, pero confírmalo conmigo).

8. Entrégame al final: (a) la lista de commits en orden con hash y título; (b) el reporte de entrega por commit para el Claude del VPS en el formato de `docs/PRODUCCION.md`, calculando las migraciones contra el commit de producción que te dé, con el aviso de que B1 es una columna con default (sin backfill, aplicable en caliente) y con el paso manual de revisar el horario en el panel de control del agente después del despliegue; (c) la lista de dudas y deuda que dejaron los subagentes, más la deuda que el plan deja anotada a propósito (Etapa 2: `owner_kind` + `response_due_at`; `journey_stage = 'assigned'` pegajoso para la píldora "Escaladas"; tamaño de `inbox-sidebar.tsx` y `crm-shell.tsx`; índice de "Pendientes" sin `id DESC`).

Empieza leyendo la skill `liminalwork`, `CLAUDE.md`, `docs/GLOSARIO.md` y `docs/planes/2026-09-05-el-reloj-dice-la-verdad.md`. Luego confirma en una línea que vas a arrancar la tanda 1 con {B1, B2, A3, C1, C2, C3} en paralelo y arranca.
