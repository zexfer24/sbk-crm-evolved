Actúa como ORQUESTADOR bajo la metodología `liminalwork` (léela con la skill antes de nada, junto con `CLAUDE.md` y `docs/GLOSARIO.md`). El plan ya está aprobado por el operador y vive en `docs/planes/2026-09-04-bandeja-que-no-pierde.md`: léelo entero antes de repartir nada. Tu trabajo es ejecutarlo completo —las cuatro etapas, dieciséis tareas— y dejar el repo con todo hecho, testeado y commiteado, sin hacer `push` ni tocar producción.

Reglas del orquestador:

1. No implementas. Cada tarea del plan (T0.1, T0.2, … T3.4) se la asignas a UN subagente propio con `subagent_type: general-purpose`, `model: sonnet` y razonamiento alto, con contexto limpio. En el prompt del subagente pega literalmente: la sección "Reglas para todos" del plan, la sección completa de SU tarea, y la instrucción de leer `CLAUDE.md`, `docs/GLOSARIO.md` y los archivos que la tarea nombra antes de escribir. Nada de resumirle la tarea: recibe el texto del plan tal cual.
2. Respeta el orden y las dependencias de la sección "Orden de ejecución": E0 secuencial (T0.1 → T0.2 → T0.3 → T0.4), E1 en dos tandas paralelas ({T1.1, T1.2, T1.3, T1.4, T1.7} y después {T1.5, T1.6}), E2 ({T2.1, T2.2}), E3 ({T3.1, T3.2, T3.3, T3.4}). Los subagentes de una misma tanda corren en paralelo; cada tanda cierra con la suite completa en verde antes de abrir la siguiente.
3. Cada subagente termina con el reporte obligatorio (qué implementó y decidió; archivos y líneas de glosario; resultado de los tests del plan; desvíos o dudas). No cierras una tarea sin ese reporte Y sin correr tú mismo `npx tsc --noEmit`, `npm run lint`, `npm run test -- --no-file-parallelism`, y para tareas con migración `supabase db reset` más los `.sql` de `supabase/tests` por `psql` contra `postgresql://postgres:postgres@127.0.0.1:54322/postgres`. Si algo no cuadra con el plan, abres otro subagente con la corrección concreta; no parcheas a mano.
4. Commits: los haces tú, uno por tarea, narrativos en español (efecto observable). Las migraciones van SIEMPRE en su propio commit con `[migración]` en el título, antes del commit del código que las usa (T0.1, T1.7, T2.1, T2.2, T3.2 y T3.4 tienen migración). Mensajes largos con `git commit -F <archivo>`. Cada commit lleva sus líneas de `docs/GLOSARIO.md` actualizadas.
5. T0.4 la haces tú: `supabase db reset`, las tres consultas SQL del plan sobre el seed (la consulta 1 debe dar 0), la mutación manual de T0.1 (quitar `and not new.is_internal_note` de `visible` → el paso 2 de `supabase/tests/awaiting_reply.sql` se pone rojo; revertir), y antes del reporte de entrega me preguntas en qué commit está producción.
6. Si un subagente se traba en algo que el plan no decide (por ejemplo, el wrapper `immutable` de `unaccent` en T1.7, o si `pg_trgm` no está), decide tú por la opción más simple que respete `CLAUDE.md` y anótalo en el reporte final; si la decisión cambia el plan (una columna nueva, una migración que no estaba), párate y pregúntame.
7. Al terminar E3 corre la verificación final del plan (suite, build con `rtk proxy npm run build`, escenario extremo a extremo con `api/dev/simulate-message`, y las mutaciones manuales de T1.2 y T2.1) y entrégame: (a) la lista de commits en orden con hash y título; (b) el reporte de entrega por commit para el Claude del VPS en el formato de `docs/PRODUCCION.md`, calculando las migraciones contra el commit de producción que te di, con la advertencia de aplicar T0.1 fuera de horario y con la app detenida, el aviso al cliente por el backfill, las tres consultas para medir antes/después y el paso manual de suscribir los campos de webhook en Meta (T3.4); (c) la lista de dudas y deuda que dejaron los subagentes.

Empieza leyendo la skill `liminalwork`, `CLAUDE.md`, `docs/GLOSARIO.md` y `docs/planes/2026-09-04-bandeja-que-no-pierde.md`. Luego confirma en una línea que vas a arrancar E0 con T0.1 y arranca.
